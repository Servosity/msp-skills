{
 "slug": "abnormal",
 "doc": "docs/reprint-survival.md",
 "_comment": "Connector-specific edits to GENERATED files that a cli-printing-press reprint must NOT clobber. check_handfixes.py asserts every marker below is still present (and banned anti-patterns absent) and fails CI if a reprint dropped one. Provenance: project_abnormal_print_gotchas.md (first print 2026-06-06, press 4.22.0). NOTE the big trap: a mid-build `generate --force` re-emits novel TODO stubs over implemented hand-authored novel files even without the generated header - do ALL spec repairs BEFORE implementing novels on any reprint.",
 "handfixes": [
  {
   "id": "x-pp-resource-renames",
   "summary": "Spec resources renamed to avoid reserved/collision: /search* -> email-search, /resources* -> api-resources (typed FTS table collided with framework resources_fts)",
   "why": "`search` is a reserved template command (parse-time collision); `resources` collides with the framework `resources_fts` typed-FTS table - NOT caught at parse time, surfaces as store test failures ('no column named description') and silently-wrong runtime FTS. The renames produce the email-search / api-resources commands and typed store tables. A reprint without the x-pp-resource renames reintroduces the collisions.",
   "status": "active",
   "spec_encode_followup": "These ARE spec-encoded (x-pp-resource on the affected paths in the abnormal spec); the markers below assert the rename survived into the generated CLI + store schema. Keep the x-pp-resource annotations on any spec re-copy.",
   "asserts": [
    {
     "file": "cli/internal/cli/email_search.go",
     "contains": "Use:         \"email-search\"",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/api_resources.go",
     "contains": "Use:         \"api-resources\"",
     "min_count": 1
    },
    {
     "file": "cli/internal/store/store.go",
     "contains": "\"api_resources\"",
     "min_count": 1
    },
    {
     "file": "cli/internal/store/store.go",
     "contains": "\"email_search\"",
     "min_count": 1
    }
   ]
  },
  {
   "id": "eu-base-url-documented",
   "summary": "ABNORMAL_BASE_URL (EU host override) is documented in guide.md env table + troubleshoots; the press omits the optional base-URL var from docs",
   "why": "config.go reads ABNORMAL_BASE_URL as an optional override for EU tenants (https://eu.api.abnormalplatform.com/v1); the press doc generator only documents the bearer token (ABNORMAL_API_TOKEN). Without the override documented, EU customers get empty results / 4xx with no guidance. A reprint drops the EU-host documentation.",
   "status": "active",
   "asserts": [
    {
     "file": "guide.md",
     "contains": "ABNORMAL_BASE_URL",
     "min_count": 1
    },
    {
     "file": "cli/internal/config/config.go",
     "contains": "ABNORMAL_BASE_URL",
     "min_count": 1
    }
   ]
  },
  {
   "id": "portal-url-no-trailing-backtick",
   "summary": "Vendor portal URL prose must not carry the markdown-quote trailing backtick harvested from spec prose (integrations`)",
   "why": "The vendor spec prose put a markdown backtick after 'integrations', which the press harvested verbatim into helpers/auth/doctor/mcp-tools output (a runtime artifact, not just docs). Stripped during onboarding. A reprint re-harvests the trailing backtick.",
   "status": "active",
   "asserts": [
    {
     "file": "cli/internal/cli/auth.go",
     "not_contains": "integrations`"
    }
   ]
  },
  {
   "id": "manifest-portal-url-no-fs-path",
   "summary": "manifest.json env-var description must use prose, not the literal portal URL (its /home/settings path trips the ci_guards personal-path guard)",
   "why": "The vendor portal URL https://portal.abnormalsecurity.com/home/settings/integrations contains a /home/<word>/ segment that ci_guards' personal-filesystem-path guard false-flags as a hardcoded /home path. Original onboard de-pathed it to prose; a re-vendor re-introduces the URL.",
   "status": "active",
   "asserts": [
    {
     "file": "manifest.json",
     "not_contains": "/home/settings"
    }
   ]
  },
  {
   "id": "doctor-verdict-tracks-reality",
   "summary": "doctor probes a real GET endpoint with the real credential and refuses to dial a placeholder base_url, instead of calling any HTTP response 'reachable' and never checking the credential",
   "why": "`doctor` is the command every SKILL, guide and docs page tells an operator to run to confirm the connector works, and it could not tell a working install from a broken one. It called any HTTP response to `GET /` \"reachable\" and rendered it green, so a base_url aimed at the vendor's web UI (where every API path 404s) and a token rejected with 401 on every call both produced an all-green report. On this connector family it also never probed the credential at all: it printed \"present, not verified\" and left the operator to guess. The mirror defect: a connector whose shipped base_url default is a placeholder dialled the placeholder host, and the DNS failure rendered as \"FAIL API: unreachable\" - telling a correctly-credentialled operator they were broken without naming the one variable that would fix it. Several placeholder hosts resolve through live wildcard DNS and answer real HTTP, so dialling one produced a confident green verdict against a stranger's server. Measured and gated by tools/maintainer/check_doctor_truth.py. See issue #282.",
   "status": "active",
   "asserts": [
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorProbeCredentials("
    },
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorBaseURLIsPlaceholder("
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorIsInfoKey(k)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorBaseURLIsPlaceholder(cfg.BaseURL)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorProbeCredentials(cmd.Context()"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "present, not verified. Run `%s %s`"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "for _, v := range report {"
    }
   ]
  },
  {
   "id": "install-credentials-declared-on-every-channel",
   "summary": "every operator credential this connector reads is declared on all three install channels: manifest.json (.mcpb), server.json (MCP Registry) and mcp-install.md (manual install)",
   "why": "Issue #282 ('three live-verified connectors prompt for the wrong credentials'). A connector is installed through THREE independent channels that each carry their own copy of the credential list: manifest.json server.mcp_config.env (what the .mcpb bundle hands Claude Desktop, which has no shell profile, no config.toml and no export - so an undeclared variable is a variable nobody is ever asked for), server.json environmentVariables (the MCP Registry publish channel), and mcp-install.md's copy-paste JSON blocks plus its remote launch line (the manual path, which never touches the bundle at all). The fleet-wide audit found 60 of 65 connectors under-declaring 122 operator-facing variables in the manifest, 61 of 65 mcp-install.md pages omitting 125, and hudu's server.json missing the base URL its manifest marks required. For abnormal the manifest gained: ABNORMAL_BASE_URL. server.json gained: ABNORMAL_BASE_URL. mcp-install.md's env blocks gained: ABNORMAL_BASE_URL. manifest.json, server.json and mcp-install.md are ALL regenerated by /msp-skills-publish on an UPDATE or re-onboard, so without this ledger a regeneration silently drops every declaration and CI stays green (check_env_schema.py is wired --warn). The asserts below name the specific credentials, not a count, so dropping one and adding another cannot cancel out.",
   "status": "active",
   "spec_encode_followup": "cli-printing-press should emit server.mcp_config.env, the matching user_config prompts, server.json environmentVariables and mcp-install.md's env blocks from ONE declaration of the connector's operator-facing environment, so the three channels cannot be generated out of step. Until then check_env_schema.py compares all three and this ledger pins them.",
   "asserts": [
    {
     "file": "manifest.json",
     "contains": "\"ABNORMAL_API_TOKEN\": \"${user_config.abnormal_api_token}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"ABNORMAL_BASE_URL\": \"${user_config.abnormal_base_url}\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"ABNORMAL_API_TOKEN\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"ABNORMAL_BASE_URL\"",
     "min_count": 1
    },
    {
     "file": "mcp-install.md",
     "contains": "\"ABNORMAL_API_TOKEN\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"ABNORMAL_BASE_URL\":",
     "min_count": 3
    }
   ]
  }
 ]
}
