// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAppResellerResourceAccountV1CompanyApiGetGetCmd(flags *rootFlags) *cobra.Command {
	var flagContext string
	var flagActive string
	var flagEnabled string
	var flagSearchText string
	var flagFromDate string
	var flagToDate string
	var flagSortField string
	var flagSortOrder string
	var flagPage string
	var flagSize int
	var flagVendor string
	var flagChannelAdmin string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "resource-account-v1-company-api-get-get",
		Aliases:     []string{"list"},
		Short:       "Retrieves companies depending on context: in the Reseller context",
		Example:     "  appdirect-cli app-reseller resource-account-v1-company-api-get-get --context RESELLER, RESELLER_MANAGER",
		Annotations: map[string]string{"pp:endpoint": "app-reseller.resource-account-v1-company-api-get-get", "pp:method": "GET", "pp:path": "/appReseller/v1/companies", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("context") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "context")
			}
			if cmd.Flags().Changed("context") {
				allowedContext := []string{"RESELLER, RESELLER_MANAGER"}
				validContext := false
				for _, v := range allowedContext {
					if flagContext == v {
						validContext = true
						break
					}
				}
				if !validContext {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagContext, "context", allowedContext)
				}
			}
			if cmd.Flags().Changed("sort-field") {
				allowedSortField := []string{"COMPANY_ID", "CREATED_ON"}
				validSortField := false
				for _, v := range allowedSortField {
					if flagSortField == v {
						validSortField = true
						break
					}
				}
				if !validSortField {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortField, "sort-field", allowedSortField)
				}
			}
			if cmd.Flags().Changed("vendor") {
				allowedVendor := []string{"ONLY_VENDORS, ONLY_NON_VENDORS, ONLY_RESELLERS"}
				validVendor := false
				for _, v := range allowedVendor {
					if flagVendor == v {
						validVendor = true
						break
					}
				}
				if !validVendor {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagVendor, "vendor", allowedVendor)
				}
			}
			if cmd.Flags().Changed("channel-admin") {
				allowedChannelAdmin := []string{"ONLY_CHANNEL_ADMINS, ONLY_NON_CHANNEL_ADMINS"}
				validChannelAdmin := false
				for _, v := range allowedChannelAdmin {
					if flagChannelAdmin == v {
						validChannelAdmin = true
						break
					}
				}
				if !validChannelAdmin {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagChannelAdmin, "channel-admin", allowedChannelAdmin)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/appReseller/v1/companies"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "app-reseller", path, map[string]string{
				"context":      formatCLIParamValue(flagContext),
				"active":       formatCLIParamValue(flagActive),
				"enabled":      formatCLIParamValue(flagEnabled),
				"searchText":   formatCLIParamValue(flagSearchText),
				"fromDate":     formatCLIParamValue(flagFromDate),
				"toDate":       formatCLIParamValue(flagToDate),
				"sortField":    formatCLIParamValue(flagSortField),
				"sortOrder":    formatCLIParamValue(flagSortOrder),
				"page":         formatCLIParamValue(flagPage),
				"size":         formatCLIParamValue(flagSize),
				"vendor":       formatCLIParamValue(flagVendor),
				"channelAdmin": formatCLIParamValue(flagChannelAdmin),
			}, nil, flagAll, "page", "page", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagContext, "context", "", "Supported AppReseller contexts (one of: RESELLER, RESELLER_MANAGER)")
	cmd.Flags().StringVar(&flagActive, "active", "", "User activation status")
	cmd.Flags().StringVar(&flagEnabled, "enabled", "", "User enablement status")
	cmd.Flags().StringVar(&flagSearchText, "search-text", "", "Search text")
	cmd.Flags().StringVar(&flagFromDate, "from-date", "", "From date, in timestamp format")
	cmd.Flags().StringVar(&flagToDate, "to-date", "", "To date, in timestamp format")
	cmd.Flags().StringVar(&flagSortField, "sort-field", "", "Field to sort by (one of: COMPANY_ID, CREATED_ON)")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Sort order")
	cmd.Flags().StringVar(&flagPage, "page", "", "Page number")
	cmd.Flags().IntVar(&flagSize, "size", 0, "Number of results per page")
	cmd.Flags().StringVar(&flagVendor, "vendor", "", "This enum filters companies that can sell (or not) or resell products in the marketplace. (one of: ONLY_VENDORS, ONLY_NON_VENDORS, ONLY_RESELLERS)")
	cmd.Flags().StringVar(&flagChannelAdmin, "channel-admin", "", "This enum filters companies that have (or not) permission to manage the marketplace. (one of: ONLY_CHANNEL_ADMINS, ONLY_NON_CHANNEL_ADMINS)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
