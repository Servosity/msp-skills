// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAssistedSalesReadOpportunitiesCmd(flags *rootFlags) *cobra.Command {
	var flagOwnerId string
	var flagOwnerCompanyId string
	var flagCustomerId string
	var flagCustomerCompanyId string
	var flagContext string
	var flagStatus string
	var flagPurchaseEffectiveDate string
	var flagCreatedOn string
	var flagSearchText string
	var flagNumber int
	var flagSize int
	var flagSort string

	cmd := &cobra.Command{
		Use:         "read-opportunities",
		Aliases:     []string{"list"},
		Short:       "Retrieve a list of opportunities.",
		Example:     "  appdirect-cli assisted-sales read-opportunities",
		Annotations: map[string]string{"pp:endpoint": "assisted-sales.read-opportunities", "pp:method": "GET", "pp:path": "/assistedSales/v1/opportunities", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("context") {
				allowedContext := []string{"RESELLER", "SSR"}
				validContext := false
				for _, v := range allowedContext {
					if flagContext == v {
						validContext = true
						break
					}
				}
				if !validContext {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagContext, "context", allowedContext)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"OPEN", "CLOSED", "PENDING_REVIEW"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/assistedSales/v1/opportunities"
			params := map[string]string{}
			if flagOwnerId != "" {
				params["ownerId"] = formatCLIParamValue(flagOwnerId)
			}
			if flagOwnerCompanyId != "" {
				params["ownerCompanyId"] = formatCLIParamValue(flagOwnerCompanyId)
			}
			if flagCustomerId != "" {
				params["customerId"] = formatCLIParamValue(flagCustomerId)
			}
			if flagCustomerCompanyId != "" {
				params["customerCompanyId"] = formatCLIParamValue(flagCustomerCompanyId)
			}
			if flagContext != "" {
				params["context"] = formatCLIParamValue(flagContext)
			}
			if flagStatus != "" {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			if flagPurchaseEffectiveDate != "" {
				params["purchaseEffectiveDate"] = formatCLIParamValue(flagPurchaseEffectiveDate)
			}
			if flagCreatedOn != "" {
				params["createdOn"] = formatCLIParamValue(flagCreatedOn)
			}
			if flagSearchText != "" {
				params["searchText"] = formatCLIParamValue(flagSearchText)
			}
			if flagNumber != 0 {
				params["number"] = formatCLIParamValue(flagNumber)
			}
			if flagSize != 0 {
				params["size"] = formatCLIParamValue(flagSize)
			}
			if flagSort != "" {
				params["sort"] = formatCLIParamValue(flagSort)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "assisted-sales", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagOwnerId, "owner-id", "", "Sales agent's user ID")
	cmd.Flags().StringVar(&flagOwnerCompanyId, "owner-company-id", "", "Sales agent's company ID, which defines which company owns opportunities.")
	cmd.Flags().StringVar(&flagCustomerId, "customer-id", "", "Customer's user ID")
	cmd.Flags().StringVar(&flagCustomerCompanyId, "customer-company-id", "", "Customer's company ID")
	cmd.Flags().StringVar(&flagContext, "context", "", "Optional. (one of: RESELLER, SSR)")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Opportunity status (one of: OPEN, CLOSED, PENDING_REVIEW)")
	cmd.Flags().StringVar(&flagPurchaseEffectiveDate, "purchase-effective-date", "", "Opportunity purchase effective date filter, defined by upper or lower inclusive or exclusive limits")
	cmd.Flags().StringVar(&flagCreatedOn, "created-on", "", "Opportunity creation date filter, defined by upper or lower inclusive or exclusive limits, in Unix timestamp format.")
	cmd.Flags().StringVar(&flagSearchText, "search-text", "", "General text search filter used for any ID (opportunity, sales agent, sales agent company, customer")
	cmd.Flags().IntVar(&flagNumber, "number", 1, "Page number")
	cmd.Flags().IntVar(&flagSize, "size", 10, "Number of results per page")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort field and order. For ascending sort order, use the '+[FieldName]' format.")

	return cmd
}
