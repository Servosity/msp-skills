// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBillingResourceOtherReadPaymentsGetCmd(flags *rootFlags) *cobra.Command {
	var flagCount string
	var flagFromDate float64
	var flagMethod string
	var flagResult string
	var flagSortField string
	var flagSortOrder string
	var flagStart string
	var flagToDate float64

	cmd := &cobra.Command{
		Use:         "resource-other-read-payments-get",
		Short:       "List all payments matching the input filters",
		Example:     "  appdirect-cli billing resource-other-read-payments-get",
		Annotations: map[string]string{"pp:endpoint": "billing.resource-other-read-payments-get", "pp:method": "GET", "pp:path": "/billing/v1/payments", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("method") {
				allowedMethod := []string{"ADP_INVOICE", "ARIBAPAY", "BT_BILL", "CLICKANDBUY", "CLOUDFOUNDRY", "CREDIT_CARD", "CUMULA_BILL", "DTAG", "EXTERNAL_INVOICE", "KREDITKARTE", "LASTSCHRIFT_MRNEXNET", "MANO_TEO_BILL", "MANUAL", "MARKETPLACE_CREDITS", "NETS_ARVATO_INVOICE", "NEXTEL", "PAYPAL", "ROGERS_CC_DTMF", "ROGERS_CC_POS", "SNAP", "TBILL", "THISTLE", "TOICLEARING"}
				validMethod := false
				for _, v := range allowedMethod {
					if flagMethod == v {
						validMethod = true
						break
					}
				}
				if !validMethod {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagMethod, "method", allowedMethod)
				}
			}
			if cmd.Flags().Changed("result") {
				allowedResult := []string{"FAILED", "GATEWAY_NOT_AVAILABLE", "MANUAL", "SUCCESSFUL"}
				validResult := false
				for _, v := range allowedResult {
					if flagResult == v {
						validResult = true
						break
					}
				}
				if !validResult {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagResult, "result", allowedResult)
				}
			}
			if cmd.Flags().Changed("sort-field") {
				allowedSortField := []string{"AMOUNT", "DATE", "PAYMENT_ID"}
				validSortField := false
				for _, v := range allowedSortField {
					if flagSortField == v {
						validSortField = true
						break
					}
				}
				if !validSortField {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortField, "sort-field", allowedSortField)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"ASC", "DESC"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortOrder, "sort-order", allowedSortOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/billing/v1/payments"
			params := map[string]string{}
			if flagCount != "" {
				params["count"] = formatCLIParamValue(flagCount)
			}
			if flagFromDate != 0.0 {
				params["fromDate"] = formatCLIParamValue(flagFromDate)
			}
			if flagMethod != "" {
				params["method"] = formatCLIParamValue(flagMethod)
			}
			if flagResult != "" {
				params["result"] = formatCLIParamValue(flagResult)
			}
			if flagSortField != "" {
				params["sortField"] = formatCLIParamValue(flagSortField)
			}
			if flagSortOrder != "" {
				params["sortOrder"] = formatCLIParamValue(flagSortOrder)
			}
			if flagStart != "" {
				params["start"] = formatCLIParamValue(flagStart)
			}
			if flagToDate != 0.0 {
				params["toDate"] = formatCLIParamValue(flagToDate)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "billing", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCount, "count", "250", "Number of results to fetch. Used for paging.")
	cmd.Flags().Float64Var(&flagFromDate, "from-date", 0.0, "From date")
	cmd.Flags().StringVar(&flagMethod, "method", "", "Payment method (one of: ADP_INVOICE, ARIBAPAY, BT_BILL, CLICKANDBUY, CLOUDFOUNDRY, CREDIT_CARD, CUMULA_BILL, DTAG, EXTERNAL_INVOICE, KREDITKARTE, LASTSCHRIFT_MRNEXNET, MANO_TEO_BILL, MANUAL, MARKETPLACE_CREDITS, NETS_ARVATO_INVOICE, NEXTEL, PAYPAL, ROGERS_CC_DTMF, ROGERS_CC_POS, SNAP, TBILL, THISTLE, TOICLEARING)")
	cmd.Flags().StringVar(&flagResult, "result", "", "Payment result (one of: FAILED, GATEWAY_NOT_AVAILABLE, MANUAL, SUCCESSFUL)")
	cmd.Flags().StringVar(&flagSortField, "sort-field", "PAYMENT_ID", "Sort field (one of: AMOUNT, DATE, PAYMENT_ID)")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "ASC", "Sort order (one of: ASC, DESC)")
	cmd.Flags().StringVar(&flagStart, "start", "0", "First result index. Used for paging.")
	cmd.Flags().Float64Var(&flagToDate, "to-date", 0.0, "To date")

	return cmd
}
