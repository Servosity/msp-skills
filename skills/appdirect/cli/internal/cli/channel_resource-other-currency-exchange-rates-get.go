// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newChannelResourceOtherCurrencyExchangeRatesGetCmd(flags *rootFlags) *cobra.Command {
	var flagBaseCurrency string
	var flagCounterCurrency string
	var flagPage string
	var flagSize int
	var flagSortField string
	var flagSortOrder string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "resource-other-currency-exchange-rates-get",
		Short:       "Retrieve all of the current and historical exchange rates set on the marketplace.",
		Example:     "  appdirect-cli channel resource-other-currency-exchange-rates-get",
		Annotations: map[string]string{"pp:endpoint": "channel.resource-other-currency-exchange-rates-get", "pp:method": "GET", "pp:path": "/channel/v1/exchangeRates", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("base-currency") {
				allowedBaseCurrency := []string{"USD", "CAD", "EUR", "JPY", "GBP", "KRW", "CHF", "SEK", "SGD", "MYR", "AUD", "MXN", "INR", "BRL", "DKK", "NZD", "NOK", "ZAR", "PHP", "CNY", "SAR", "GTQ"}
				validBaseCurrency := false
				for _, v := range allowedBaseCurrency {
					if flagBaseCurrency == v {
						validBaseCurrency = true
						break
					}
				}
				if !validBaseCurrency {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagBaseCurrency, "base-currency", allowedBaseCurrency)
				}
			}
			if cmd.Flags().Changed("counter-currency") {
				allowedCounterCurrency := []string{"USD", "CAD", "EUR", "JPY", "GBP", "KRW", "CHF", "SEK", "SGD", "MYR", "AUD", "MXN", "INR", "BRL", "DKK", "NZD", "NOK", "ZAR", "PHP", "CNY", "SAR", "GTQ"}
				validCounterCurrency := false
				for _, v := range allowedCounterCurrency {
					if flagCounterCurrency == v {
						validCounterCurrency = true
						break
					}
				}
				if !validCounterCurrency {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagCounterCurrency, "counter-currency", allowedCounterCurrency)
				}
			}
			if cmd.Flags().Changed("sort-field") {
				allowedSortField := []string{"BASE_CURRENCY", "COUNTER_CURRENCY", "VALID_FROM"}
				validSortField := false
				for _, v := range allowedSortField {
					if flagSortField == v {
						validSortField = true
						break
					}
				}
				if !validSortField {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortField, "sort-field", allowedSortField)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"ASC", "DESC"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortOrder, "sort-order", allowedSortOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/channel/v1/exchangeRates"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "channel", path, map[string]string{
				"baseCurrency":    formatCLIParamValue(flagBaseCurrency),
				"counterCurrency": formatCLIParamValue(flagCounterCurrency),
				"page":            formatCLIParamValue(flagPage),
				"size":            formatCLIParamValue(flagSize),
				"sortField":       formatCLIParamValue(flagSortField),
				"sortOrder":       formatCLIParamValue(flagSortOrder),
			}, nil, flagAll, "page", "page", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagBaseCurrency, "base-currency", "", "The base currency filter. Optional. (one of: USD, CAD, EUR, JPY, GBP, KRW, CHF, SEK, SGD, MYR, AUD, MXN, INR, BRL, DKK, NZD, NOK, ZAR, PHP, CNY, SAR, GTQ)")
	cmd.Flags().StringVar(&flagCounterCurrency, "counter-currency", "", "The counter currency filter. Optional. (one of: USD, CAD, EUR, JPY, GBP, KRW, CHF, SEK, SGD, MYR, AUD, MXN, INR, BRL, DKK, NZD, NOK, ZAR, PHP, CNY, SAR, GTQ)")
	cmd.Flags().StringVar(&flagPage, "page", "0", "The page number. Optional.")
	cmd.Flags().IntVar(&flagSize, "size", 20, "The number of application returned by page. Optional.")
	cmd.Flags().StringVar(&flagSortField, "sort-field", "VALID_FROM", "The order field. Optional. (one of: BASE_CURRENCY, COUNTER_CURRENCY, VALID_FROM)")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "DESC", "The sort direction. Optional. (one of: ASC, DESC)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
