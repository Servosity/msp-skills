// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newChannelResourceProductReadStagingCatalogGetCmd(flags *rootFlags) *cobra.Command {
	var flagAppStatus string
	var flagPage string
	var flagProductType string
	var flagSearchText string
	var flagVendorUuid string
	var flagSize string
	var flagSortOrder string
	var flagSortProperty string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "resource-product-read-staging-catalog-get",
		Short:       "This call lists all products in the Staging Catalog of your marketplace.",
		Example:     "  appdirect-cli channel resource-product-read-staging-catalog-get",
		Annotations: map[string]string{"pp:endpoint": "channel.resource-product-read-staging-catalog-get", "pp:method": "GET", "pp:path": "/channel/v1/stagingCatalog", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("app-status") {
				allowedAppStatus := []string{"DEVELOPMENT", "PENDING", "PRODUCTION"}
				validAppStatus := false
				for _, v := range allowedAppStatus {
					if flagAppStatus == v {
						validAppStatus = true
						break
					}
				}
				if !validAppStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagAppStatus, "app-status", allowedAppStatus)
				}
			}
			if cmd.Flags().Changed("product-type") {
				allowedProductType := []string{"APPDIRECT_SERVICE", "BUNDLE", "CLOUDFOUNDRY_DEPLOYABLE", "CLOUD_SERVICES", "DESIGN_ELEMENT", "DOMAIN_REGISTRATION", "DOMAIN_RESELLER", "DOWNLOAD", "DOWNLOADABLE_BUNDLE", "DOWNLOAD_WITH_FULL_PROFILE", "EMBEDDED_VIDEO", "EXTERNAL", "MODULE", "OPEN_SOURCE", "RACKSPACE_IMAGE", "STANDING_CLOUD", "STATIC", "SUPPORT", "TEMPLATE", "WEB_APP", "WEB_APP_MANUAL_SETUP", "WEB_APP_STACKED"}
				validProductType := false
				for _, v := range allowedProductType {
					if flagProductType == v {
						validProductType = true
						break
					}
				}
				if !validProductType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagProductType, "product-type", allowedProductType)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"ASC", "DESC"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortOrder, "sort-order", allowedSortOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/channel/v1/stagingCatalog"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "channel", path, map[string]string{
				"appStatus":    formatCLIParamValue(flagAppStatus),
				"page":         formatCLIParamValue(flagPage),
				"productType":  formatCLIParamValue(flagProductType),
				"searchText":   formatCLIParamValue(flagSearchText),
				"vendorUuid":   formatCLIParamValue(flagVendorUuid),
				"size":         formatCLIParamValue(flagSize),
				"sortOrder":    formatCLIParamValue(flagSortOrder),
				"sortProperty": formatCLIParamValue(flagSortProperty),
			}, nil, flagAll, "page", "page", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAppStatus, "app-status", "", "The status of the application, optional. (one of: DEVELOPMENT, PENDING, PRODUCTION)")
	cmd.Flags().StringVar(&flagPage, "page", "0", "The page number. Optional, defaults to 0.")
	cmd.Flags().StringVar(&flagProductType, "product-type", "", "The product type, optional. (one of: APPDIRECT_SERVICE, BUNDLE, CLOUDFOUNDRY_DEPLOYABLE, CLOUD_SERVICES, DESIGN_ELEMENT, DOMAIN_REGISTRATION, DOMAIN_RESELLER, DOWNLOAD, DOWNLOADABLE_BUNDLE, DOWNLOAD_WITH_FULL_PROFILE, EMBEDDED_VIDEO, EXTERNAL, MODULE, OPEN_SOURCE, RACKSPACE_IMAGE, STANDING_CLOUD, STATIC, SUPPORT, TEMPLATE, WEB_APP, WEB_APP_MANUAL_SETUP, WEB_APP_STACKED)")
	cmd.Flags().StringVar(&flagSearchText, "search-text", "", "The search text, optional.")
	cmd.Flags().StringVar(&flagVendorUuid, "vendor-uuid", "", "(Optional) ID of the vendor company")
	cmd.Flags().StringVar(&flagSize, "size", "20", "The number of application returned. Optional, defaults to 20.")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "ASC", "The order. Optional, default to ascending. (one of: ASC, DESC)")
	cmd.Flags().StringVar(&flagSortProperty, "sort-property", "createdOn", "The sort field. Optional, defaults to the creation date of the application.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
