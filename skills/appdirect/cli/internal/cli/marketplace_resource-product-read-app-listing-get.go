// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMarketplaceResourceProductReadAppListingGetCmd(flags *rootFlags) *cobra.Command {
	var flagA string
	var flagApprovalAfter float64
	var flagApprovalBefore float64
	var flagPublishedAfter float64
	var flagPublishedBefore float64
	var flagC string
	var flagCount string
	var flagFilter string
	var flagS string
	var flagOrder string
	var flagPl string
	var flagPlatform string
	var flagQ string
	var flagStart int
	var flagType string
	var flagVendor string
	var flagLocale string

	cmd := &cobra.Command{
		Use:         "resource-product-read-app-listing-get",
		Short:       "This call lists products based on specific parameters such as attribute, category, date, or type.",
		Example:     "  appdirect-cli marketplace resource-product-read-app-listing-get",
		Annotations: map[string]string{"pp:endpoint": "marketplace.resource-product-read-app-listing-get", "pp:method": "GET", "pp:path": "/marketplace/v1/listing", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("order") {
				allowedOrder := []string{"ALPHABETICAL", "NEWEST_FIRST", "NEWEST_PUBLISHED", "POPULARITY", "RATING"}
				validOrder := false
				for _, v := range allowedOrder {
					if flagOrder == v {
						validOrder = true
						break
					}
				}
				if !validOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrder, "order", allowedOrder)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/marketplace/v1/listing"
			params := map[string]string{}
			if flagA != "" {
				params["a"] = formatCLIParamValue(flagA)
			}
			if flagApprovalAfter != 0.0 {
				params["approvalAfter"] = formatCLIParamValue(flagApprovalAfter)
			}
			if flagApprovalBefore != 0.0 {
				params["approvalBefore"] = formatCLIParamValue(flagApprovalBefore)
			}
			if flagPublishedAfter != 0.0 {
				params["publishedAfter"] = formatCLIParamValue(flagPublishedAfter)
			}
			if flagPublishedBefore != 0.0 {
				params["publishedBefore"] = formatCLIParamValue(flagPublishedBefore)
			}
			if flagC != "" {
				params["c"] = formatCLIParamValue(flagC)
			}
			if flagCount != "" {
				params["count"] = formatCLIParamValue(flagCount)
			}
			if flagFilter != "" {
				params["filter"] = formatCLIParamValue(flagFilter)
			}
			if flagS != "" {
				params["s"] = formatCLIParamValue(flagS)
			}
			if flagOrder != "" {
				params["order"] = formatCLIParamValue(flagOrder)
			}
			if flagPl != "" {
				params["pl"] = formatCLIParamValue(flagPl)
			}
			if flagPlatform != "" {
				params["platform"] = formatCLIParamValue(flagPlatform)
			}
			if flagQ != "" {
				params["q"] = formatCLIParamValue(flagQ)
			}
			if flagStart != 0 {
				params["start"] = formatCLIParamValue(flagStart)
			}
			if flagType != "" {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagVendor != "" {
				params["vendor"] = formatCLIParamValue(flagVendor)
			}
			if flagLocale != "" {
				params["locale"] = formatCLIParamValue(flagLocale)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "marketplace", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagA, "a", "", "Only return products for these attribute ids")
	cmd.Flags().Float64Var(&flagApprovalAfter, "approval-after", 0.0, "Search products published after this date in milliseconds")
	cmd.Flags().Float64Var(&flagApprovalBefore, "approval-before", 0.0, "Search products published before this date in milliseconds")
	cmd.Flags().Float64Var(&flagPublishedAfter, "published-after", 0.0, "Filter products last modified after this date (epoch milliseconds). Use with publishedBefore to define a date range.")
	cmd.Flags().Float64Var(&flagPublishedBefore, "published-before", 0.0, "Filter products last modified before this date (epoch milliseconds). Use with publishedAfter to define a date range.")
	cmd.Flags().StringVar(&flagC, "c", "", "Only return products for these categories")
	cmd.Flags().StringVar(&flagCount, "count", "500", "Limit number of products returned")
	cmd.Flags().StringVar(&flagFilter, "filter", "", "Only return products for these characteristics")
	cmd.Flags().StringVar(&flagS, "s", "", "Only return products for these category ids")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Ordering type (one of: ALPHABETICAL, NEWEST_FIRST, NEWEST_PUBLISHED, POPULARITY, RATING)")
	cmd.Flags().StringVar(&flagPl, "pl", "", "Pl")
	cmd.Flags().StringVar(&flagPlatform, "platform", "", "Platform")
	cmd.Flags().StringVar(&flagQ, "q", "", "Search term used to search on different fields of an product")
	cmd.Flags().IntVar(&flagStart, "start", 0, "Start to return product after this offset")
	cmd.Flags().StringVar(&flagType, "type", "", "Only return product for these product types")
	cmd.Flags().StringVar(&flagVendor, "vendor", "", "Only return products for the specified vendor IDs")
	cmd.Flags().StringVar(&flagLocale, "locale", "", "Specify the locale context")

	return cmd
}
