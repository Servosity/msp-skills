// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"time"

	"autotask-pp-cli/internal/store"
	"github.com/spf13/cobra"
)

func newWorkflowCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "workflow",
		Short:       "Compound workflows that combine multiple API operations",
		Annotations: map[string]string{"mcp:read-only": "true"},
		RunE:        parentNoSubcommandRunE(flags),
	}
	cmd.AddCommand(newWorkflowArchiveCmd(flags))
	cmd.AddCommand(newWorkflowStatusCmd(flags))

	return cmd
}
func newWorkflowArchiveCmd(flags *rootFlags) *cobra.Command {
	var dbPath string
	var full bool

	cmd := &cobra.Command{
		Use:   "archive",
		Short: "Sync all resources to local store for offline access and search",
		Long: `Archive fetches all syncable resources from the API and stores them in a
local SQLite database. Supports incremental sync (only new data since last run)
and full resync. After archiving, use 'search' for instant full-text search.`,
		Example: `  # Archive all resources
  autotask-cli workflow archive

  # Full re-archive (ignore previous sync state)
  autotask-cli workflow archive --full`,
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			c.NoCache = true

			if dbPath == "" {
				dbPath = defaultDBPath("autotask-cli")
			}
			s, err := store.OpenWithContext(cmd.Context(), dbPath)
			if err != nil {
				return fmt.Errorf("opening store: %w", err)
			}
			defer s.Close()

			resources := []string{"appointments", "appointments-entity-information-fields", "appointments-entity-information-user-defined-fields", "billing-codes", "billing-codes-entity-information-fields", "billing-codes-entity-information-user-defined-fields", "companies", "companies-entity-information-fields", "companies-entity-information-user-defined-fields", "company-notes", "company-notes-entity-information-fields", "company-notes-entity-information-user-defined-fields", "configuration-items", "configuration-items-entity-information-fields", "configuration-items-entity-information-user-defined-fields", "contact-groups", "contact-groups-entity-information-fields", "contact-groups-entity-information-user-defined-fields", "contacts", "contacts-entity-information-fields", "contacts-entity-information-user-defined-fields", "contract-blocks", "contract-blocks-entity-information-fields", "contract-blocks-entity-information-user-defined-fields", "contract-notes", "contract-notes-entity-information-fields", "contract-notes-entity-information-user-defined-fields", "contract-rates", "contract-rates-entity-information-fields", "contract-rates-entity-information-user-defined-fields", "contract-service-units", "contract-service-units-entity-information-fields", "contract-service-units-entity-information-user-defined-fields", "contract-services", "contract-services-entity-information-fields", "contract-services-entity-information-user-defined-fields", "contracts", "contracts-entity-information-fields", "contracts-entity-information-user-defined-fields", "countries", "countries-entity-information-fields", "countries-entity-information-user-defined-fields", "currencies", "currencies-entity-information-fields", "currencies-entity-information-user-defined-fields", "departments", "departments-entity-information-fields", "departments-entity-information-user-defined-fields", "holidays", "holidays-entity-information-fields", "holidays-entity-information-user-defined-fields", "invoices", "invoices-entity-information-fields", "invoices-entity-information-user-defined-fields", "opportunities", "opportunities-entity-information-fields", "opportunities-entity-information-user-defined-fields", "phases", "phases-entity-information-fields", "phases-entity-information-user-defined-fields", "products", "products-entity-information-fields", "products-entity-information-user-defined-fields", "project-charges", "project-charges-entity-information-fields", "project-charges-entity-information-user-defined-fields", "project-notes", "project-notes-entity-information-fields", "project-notes-entity-information-user-defined-fields", "projects", "projects-entity-information-fields", "projects-entity-information-user-defined-fields", "purchase-orders", "purchase-orders-entity-information-fields", "purchase-orders-entity-information-user-defined-fields", "quotes", "quotes-entity-information-fields", "quotes-entity-information-user-defined-fields", "resource-roles", "resource-roles-entity-information-fields", "resource-roles-entity-information-user-defined-fields", "resources", "resources-entity-information-fields", "resources-entity-information-user-defined-fields", "roles", "roles-entity-information-fields", "roles-entity-information-user-defined-fields", "service-bundles", "service-bundles-entity-information-fields", "service-bundles-entity-information-user-defined-fields", "service-calls", "service-calls-entity-information-fields", "service-calls-entity-information-user-defined-fields", "services", "services-entity-information-fields", "services-entity-information-user-defined-fields", "skills", "skills-entity-information-fields", "skills-entity-information-user-defined-fields", "subscriptions", "subscriptions-entity-information-fields", "subscriptions-entity-information-user-defined-fields", "task-notes", "task-notes-entity-information-fields", "task-notes-entity-information-user-defined-fields", "tasks", "tasks-entity-information-fields", "tasks-entity-information-user-defined-fields", "ticket-categories", "ticket-categories-entity-information-fields", "ticket-categories-entity-information-user-defined-fields", "ticket-charges", "ticket-charges-entity-information-fields", "ticket-charges-entity-information-user-defined-fields", "ticket-history", "ticket-history-entity-information-fields", "ticket-history-entity-information-user-defined-fields", "ticket-notes", "ticket-notes-entity-information-fields", "ticket-notes-entity-information-user-defined-fields", "ticket-secondary-resources", "ticket-secondary-resources-entity-information-fields", "ticket-secondary-resources-entity-information-user-defined-fields", "tickets", "tickets-entity-information-fields", "tickets-entity-information-user-defined-fields", "time-entries", "time-entries-entity-information-fields", "time-entries-entity-information-user-defined-fields"}
			totalSynced := 0
			syncEventWriter := cmd.OutOrStdout()
			if flags.asJSON {
				syncEventWriter = cmd.ErrOrStderr()
			}

			// --full clears the cursor here because syncResource reads
			// existingCursor unconditionally; its full param only gates the
			// since filter, not cursor reset. Mirrors newSyncCmd's pattern.
			if full {
				for _, resource := range resources {
					_ = s.SaveSyncState(resource, "", 0)
				}
			}

			for _, resource := range resources {
				res := syncResource(cmd.Context(), c, s, resource, "", full, 100, false, nil, syncEventWriter)
				if res.Err != nil {
					fmt.Fprintf(cmd.ErrOrStderr(), "  %s: error: %v\n", resource, res.Err)
					continue
				}
				if res.Warn != nil {
					fmt.Fprintf(cmd.ErrOrStderr(), "  %s: warning: %v\n", resource, res.Warn)
					continue
				}
				totalSynced += res.Count
				fmt.Fprintf(cmd.ErrOrStderr(), "  %s: %d synced\n", resource, res.Count)
			}

			if flags.asJSON {
				enc := json.NewEncoder(cmd.OutOrStdout())
				enc.SetIndent("", "  ")
				return enc.Encode(map[string]any{
					"resources_synced": len(resources),
					"total_items":      totalSynced,
					"store_path":       dbPath,
					"timestamp":        time.Now().UTC().Format(time.RFC3339),
				})
			}

			fmt.Fprintf(cmd.OutOrStdout(), "Archived %d items across %d resources to %s\n", totalSynced, len(resources), dbPath)
			return nil
		},
	}

	cmd.Flags().StringVar(&dbPath, "db", "", "Database path (default: ~/.local/share/autotask-cli/data.db)")
	cmd.Flags().BoolVar(&full, "full", false, "Full re-archive (ignore previous sync state)")

	return cmd
}

func newWorkflowStatusCmd(flags *rootFlags) *cobra.Command {
	var dbPath string

	cmd := &cobra.Command{
		Use:         "status",
		Short:       "Show local archive status and sync state for all resources",
		Annotations: map[string]string{"mcp:read-only": "true"},
		Example: `  # Show archive status
  autotask-cli workflow status

  # Show status as JSON
  autotask-cli workflow status --json`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if dbPath == "" {
				dbPath = defaultDBPath("autotask-cli")
			}
			s, err := store.OpenWithContext(cmd.Context(), dbPath)
			if err != nil {
				return fmt.Errorf("opening store: %w", err)
			}
			defer s.Close()

			status, err := s.Status()
			if err != nil {
				return err
			}

			if flags.asJSON {
				enc := json.NewEncoder(cmd.OutOrStdout())
				enc.SetIndent("", "  ")
				return enc.Encode(status)
			}

			if len(status) == 0 {
				fmt.Fprintln(cmd.OutOrStdout(), "No archived data. Run 'workflow archive' to sync.")
				return nil
			}

			fmt.Fprintln(cmd.OutOrStdout(), "Archive Status:")
			total := 0
			for resource, count := range status {
				fmt.Fprintf(cmd.OutOrStdout(), "  %-30s %d items\n", resource, count)
				total += count
			}
			fmt.Fprintf(cmd.OutOrStdout(), "\n  Total: %d items\n", total)
			fmt.Fprintf(cmd.OutOrStdout(), "  Store: %s\n", dbPath)
			return nil
		},
	}

	cmd.Flags().StringVar(&dbPath, "db", "", "Database path")

	return cmd
}

// defaultDBPath is defined in helpers.go
