// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newContractsUpdateEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyBillToCompanyContactID string
	var bodyBillToCompanyID string
	var bodyBillingPreference int
	var bodyCompanyID string
	var bodyContactID string
	var bodyContactName string
	var bodyContractCategory int
	var bodyContractExclusionSetID string
	var bodyContractName string
	var bodyContractNumber string
	var bodyContractPeriodType int
	var bodyContractType int
	var bodyDescription string
	var bodyEndDate string
	var bodyEstimatedCost float64
	var bodyEstimatedHours float64
	var bodyEstimatedRevenue float64
	var bodyExclusionContractID string
	var bodyId string
	var bodyInternalCurrencyOverageBillingRate float64
	var bodyInternalCurrencySetupFee float64
	var bodyIsCompliant bool
	var bodyIsDefaultContract bool
	var bodyOpportunityID string
	var bodyOrganizationalLevelAssociationID string
	var bodyOverageBillingRate float64
	var bodyPurchaseOrderNumber string
	var bodyRenewedContractID string
	var bodyServiceLevelAgreementID string
	var bodySetupFee float64
	var bodySetupFeeBillingCodeID string
	var bodyStartDate string
	var bodyStatus int
	var bodyTimeReportingRequiresStartAndStopTimes int
	var bodyUserDefinedFields string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-entity",
		Short:       "Update entity",
		Example:     "  autotask-cli contracts update-entity",
		Annotations: map[string]string{"pp:endpoint": "contracts.update-entity", "pp:method": "PUT", "pp:path": "/Contracts"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Contracts"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBillToCompanyContactID != "" {
					body["billToCompanyContactID"] = bodyBillToCompanyContactID
				}
				if bodyBillToCompanyID != "" {
					body["billToCompanyID"] = bodyBillToCompanyID
				}
				if bodyBillingPreference != 0 {
					body["billingPreference"] = bodyBillingPreference
				}
				if bodyCompanyID != "" {
					body["companyID"] = bodyCompanyID
				}
				if bodyContactID != "" {
					body["contactID"] = bodyContactID
				}
				if bodyContactName != "" {
					body["contactName"] = bodyContactName
				}
				if bodyContractCategory != 0 {
					body["contractCategory"] = bodyContractCategory
				}
				if bodyContractExclusionSetID != "" {
					body["contractExclusionSetID"] = bodyContractExclusionSetID
				}
				if bodyContractName != "" {
					body["contractName"] = bodyContractName
				}
				if bodyContractNumber != "" {
					body["contractNumber"] = bodyContractNumber
				}
				if bodyContractPeriodType != 0 {
					body["contractPeriodType"] = bodyContractPeriodType
				}
				if bodyContractType != 0 {
					body["contractType"] = bodyContractType
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEndDate != "" {
					body["endDate"] = bodyEndDate
				}
				if bodyEstimatedCost != 0.0 {
					body["estimatedCost"] = bodyEstimatedCost
				}
				if bodyEstimatedHours != 0.0 {
					body["estimatedHours"] = bodyEstimatedHours
				}
				if bodyEstimatedRevenue != 0.0 {
					body["estimatedRevenue"] = bodyEstimatedRevenue
				}
				if bodyExclusionContractID != "" {
					body["exclusionContractID"] = bodyExclusionContractID
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyInternalCurrencyOverageBillingRate != 0.0 {
					body["internalCurrencyOverageBillingRate"] = bodyInternalCurrencyOverageBillingRate
				}
				if bodyInternalCurrencySetupFee != 0.0 {
					body["internalCurrencySetupFee"] = bodyInternalCurrencySetupFee
				}
				if cmd.Flags().Changed("is-compliant") {
					body["isCompliant"] = bodyIsCompliant
				}
				if cmd.Flags().Changed("is-default-contract") {
					body["isDefaultContract"] = bodyIsDefaultContract
				}
				if bodyOpportunityID != "" {
					body["opportunityID"] = bodyOpportunityID
				}
				if bodyOrganizationalLevelAssociationID != "" {
					body["organizationalLevelAssociationID"] = bodyOrganizationalLevelAssociationID
				}
				if bodyOverageBillingRate != 0.0 {
					body["overageBillingRate"] = bodyOverageBillingRate
				}
				if bodyPurchaseOrderNumber != "" {
					body["purchaseOrderNumber"] = bodyPurchaseOrderNumber
				}
				if bodyRenewedContractID != "" {
					body["renewedContractID"] = bodyRenewedContractID
				}
				if bodyServiceLevelAgreementID != "" {
					body["serviceLevelAgreementID"] = bodyServiceLevelAgreementID
				}
				if bodySetupFee != 0.0 {
					body["setupFee"] = bodySetupFee
				}
				if bodySetupFeeBillingCodeID != "" {
					body["setupFeeBillingCodeID"] = bodySetupFeeBillingCodeID
				}
				if bodyStartDate != "" {
					body["startDate"] = bodyStartDate
				}
				if bodyStatus != 0 {
					body["status"] = bodyStatus
				}
				if bodyTimeReportingRequiresStartAndStopTimes != 0 {
					body["timeReportingRequiresStartAndStopTimes"] = bodyTimeReportingRequiresStartAndStopTimes
				}
				if bodyUserDefinedFields != "" {
					var parsedUserDefinedFields any
					if err := json.Unmarshal([]byte(bodyUserDefinedFields), &parsedUserDefinedFields); err != nil {
						return fmt.Errorf("parsing --user-defined-fields JSON: %w", err)
					}
					body["userDefinedFields"] = parsedUserDefinedFields
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "contracts", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "contracts", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contracts", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contracts", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contracts", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "contracts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contracts", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contracts", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyBillToCompanyContactID, "bill-to-company-contact-id", "", "Bill to company contact id")
	cmd.Flags().StringVar(&bodyBillToCompanyID, "bill-to-company-id", "", "Bill to company id")
	cmd.Flags().IntVar(&bodyBillingPreference, "billing-preference", 0, "Billing preference")
	cmd.Flags().StringVar(&bodyCompanyID, "company-id", "", "Company id")
	cmd.Flags().StringVar(&bodyContactID, "contact-id", "", "Contact id")
	cmd.Flags().StringVar(&bodyContactName, "contact-name", "", "Contact name")
	cmd.Flags().IntVar(&bodyContractCategory, "contract-category", 0, "Contract category")
	cmd.Flags().StringVar(&bodyContractExclusionSetID, "contract-exclusion-set-id", "", "Contract exclusion set id")
	cmd.Flags().StringVar(&bodyContractName, "contract-name", "", "Contract name")
	cmd.Flags().StringVar(&bodyContractNumber, "contract-number", "", "Contract number")
	cmd.Flags().IntVar(&bodyContractPeriodType, "contract-period-type", 0, "Contract period type")
	cmd.Flags().IntVar(&bodyContractType, "contract-type", 0, "Contract type")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyEndDate, "end-date", "", "End date")
	cmd.Flags().Float64Var(&bodyEstimatedCost, "estimated-cost", 0.0, "Estimated cost")
	cmd.Flags().Float64Var(&bodyEstimatedHours, "estimated-hours", 0.0, "Estimated hours")
	cmd.Flags().Float64Var(&bodyEstimatedRevenue, "estimated-revenue", 0.0, "Estimated revenue")
	cmd.Flags().StringVar(&bodyExclusionContractID, "exclusion-contract-id", "", "Exclusion contract id")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().Float64Var(&bodyInternalCurrencyOverageBillingRate, "internal-currency-overage-billing-rate", 0.0, "Internal currency overage billing rate")
	cmd.Flags().Float64Var(&bodyInternalCurrencySetupFee, "internal-currency-setup-fee", 0.0, "Internal currency setup fee")
	cmd.Flags().BoolVar(&bodyIsCompliant, "is-compliant", false, "Is compliant")
	cmd.Flags().BoolVar(&bodyIsDefaultContract, "is-default-contract", false, "Is default contract")
	cmd.Flags().StringVar(&bodyOpportunityID, "opportunity-id", "", "Opportunity id")
	cmd.Flags().StringVar(&bodyOrganizationalLevelAssociationID, "organizational-level-association-id", "", "Organizational level association id")
	cmd.Flags().Float64Var(&bodyOverageBillingRate, "overage-billing-rate", 0.0, "Overage billing rate")
	cmd.Flags().StringVar(&bodyPurchaseOrderNumber, "purchase-order-number", "", "Purchase order number")
	cmd.Flags().StringVar(&bodyRenewedContractID, "renewed-contract-id", "", "Renewed contract id")
	cmd.Flags().StringVar(&bodyServiceLevelAgreementID, "service-level-agreement-id", "", "Service level agreement id")
	cmd.Flags().Float64Var(&bodySetupFee, "setup-fee", 0.0, "Setup fee")
	cmd.Flags().StringVar(&bodySetupFeeBillingCodeID, "setup-fee-billing-code-id", "", "Setup fee billing code id")
	cmd.Flags().StringVar(&bodyStartDate, "start-date", "", "Start date")
	cmd.Flags().IntVar(&bodyStatus, "status", 0, "Status")
	cmd.Flags().IntVar(&bodyTimeReportingRequiresStartAndStopTimes, "time-reporting-requires-start-and-stop-times", 0, "Time reporting requires start and stop times")
	cmd.Flags().StringVar(&bodyUserDefinedFields, "user-defined-fields", "", "User defined fields")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
