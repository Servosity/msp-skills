// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsTasksChildUpdateEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyAssignedResourceID string
	var bodyAssignedResourceRoleID string
	var bodyBillingCodeID string
	var bodyCanClientPortalUserCompleteTask bool
	var bodyCompanyLocationID string
	var bodyCompletedByResourceID string
	var bodyCompletedByType int
	var bodyCompletedDateTime string
	var bodyCreateDateTime string
	var bodyCreatorResourceID string
	var bodyCreatorType int
	var bodyDepartmentID string
	var bodyDescription string
	var bodyEndDateTime string
	var bodyEstimatedHours float64
	var bodyExternalID string
	var bodyHoursToBeScheduled float64
	var bodyId string
	var bodyIsTaskBillable bool
	var bodyIsVisibleInClientPortal bool
	var bodyLastActivityDateTime string
	var bodyLastActivityPersonType int
	var bodyLastActivityResourceID string
	var bodyPhaseID string
	var bodyPriority int
	var bodyPriorityLabel int
	var bodyProjectID string
	var bodyPurchaseOrderNumber string
	var bodyRemainingHours float64
	var bodySoapParentPropertyIdBodyCanReduce bool
	var bodySoapParentPropertyIdBodyNodeType string
	var bodySoapParentPropertyIdBodyType string
	var bodySoapParentPropertyIdCanReduce bool
	var bodySoapParentPropertyIdName string
	var bodySoapParentPropertyIdNodeType string
	var bodySoapParentPropertyIdParameters string
	var bodySoapParentPropertyIdReturnType string
	var bodySoapParentPropertyIdTailCall bool
	var bodySoapParentPropertyIdType string
	var bodyStartDateTime string
	var bodyStatus int
	var bodyTaskCategoryID string
	var bodyTaskNumber string
	var bodyTaskType int
	var bodyTitle string
	var bodyUserDefinedFields string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "child-update-entity <parentId>",
		Short:       "Child update entity",
		Example:     "  autotask-cli projects tasks child-update-entity 42",
		Annotations: map[string]string{"pp:endpoint": "tasks.child-update-entity", "pp:method": "PUT", "pp:path": "/Projects/{parentId}/Tasks"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Projects/{parentId}/Tasks"
			path = replacePathParam(path, "parentId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAssignedResourceID != "" {
					body["assignedResourceID"] = bodyAssignedResourceID
				}
				if bodyAssignedResourceRoleID != "" {
					body["assignedResourceRoleID"] = bodyAssignedResourceRoleID
				}
				if bodyBillingCodeID != "" {
					body["billingCodeID"] = bodyBillingCodeID
				}
				if cmd.Flags().Changed("can-client-portal-user-complete-task") {
					body["canClientPortalUserCompleteTask"] = bodyCanClientPortalUserCompleteTask
				}
				if bodyCompanyLocationID != "" {
					body["companyLocationID"] = bodyCompanyLocationID
				}
				if bodyCompletedByResourceID != "" {
					body["completedByResourceID"] = bodyCompletedByResourceID
				}
				if bodyCompletedByType != 0 {
					body["completedByType"] = bodyCompletedByType
				}
				if bodyCompletedDateTime != "" {
					body["completedDateTime"] = bodyCompletedDateTime
				}
				if bodyCreateDateTime != "" {
					body["createDateTime"] = bodyCreateDateTime
				}
				if bodyCreatorResourceID != "" {
					body["creatorResourceID"] = bodyCreatorResourceID
				}
				if bodyCreatorType != 0 {
					body["creatorType"] = bodyCreatorType
				}
				if bodyDepartmentID != "" {
					body["departmentID"] = bodyDepartmentID
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEndDateTime != "" {
					body["endDateTime"] = bodyEndDateTime
				}
				if bodyEstimatedHours != 0.0 {
					body["estimatedHours"] = bodyEstimatedHours
				}
				if bodyExternalID != "" {
					body["externalID"] = bodyExternalID
				}
				if bodyHoursToBeScheduled != 0.0 {
					body["hoursToBeScheduled"] = bodyHoursToBeScheduled
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if cmd.Flags().Changed("is-task-billable") {
					body["isTaskBillable"] = bodyIsTaskBillable
				}
				if cmd.Flags().Changed("is-visible-in-client-portal") {
					body["isVisibleInClientPortal"] = bodyIsVisibleInClientPortal
				}
				if bodyLastActivityDateTime != "" {
					body["lastActivityDateTime"] = bodyLastActivityDateTime
				}
				if bodyLastActivityPersonType != 0 {
					body["lastActivityPersonType"] = bodyLastActivityPersonType
				}
				if bodyLastActivityResourceID != "" {
					body["lastActivityResourceID"] = bodyLastActivityResourceID
				}
				if bodyPhaseID != "" {
					body["phaseID"] = bodyPhaseID
				}
				if bodyPriority != 0 {
					body["priority"] = bodyPriority
				}
				if bodyPriorityLabel != 0 {
					body["priorityLabel"] = bodyPriorityLabel
				}
				if bodyProjectID != "" {
					body["projectID"] = bodyProjectID
				}
				if bodyPurchaseOrderNumber != "" {
					body["purchaseOrderNumber"] = bodyPurchaseOrderNumber
				}
				if bodyRemainingHours != 0.0 {
					body["remainingHours"] = bodyRemainingHours
				}
				{
					nestedSoapParentPropertyId := map[string]any{}
					{
						nestedSoapParentPropertyIdBody := map[string]any{}
						if cmd.Flags().Changed("soap-parent-property-id-body-can-reduce") {
							nestedSoapParentPropertyIdBody["canReduce"] = bodySoapParentPropertyIdBodyCanReduce
						}
						if bodySoapParentPropertyIdBodyNodeType != "" {
							nestedSoapParentPropertyIdBody["nodeType"] = bodySoapParentPropertyIdBodyNodeType
						}
						if bodySoapParentPropertyIdBodyType != "" {
							nestedSoapParentPropertyIdBody["type"] = bodySoapParentPropertyIdBodyType
						}
						if len(nestedSoapParentPropertyIdBody) > 0 {
							nestedSoapParentPropertyId["body"] = nestedSoapParentPropertyIdBody
						}
					}
					if cmd.Flags().Changed("soap-parent-property-id-can-reduce") {
						nestedSoapParentPropertyId["canReduce"] = bodySoapParentPropertyIdCanReduce
					}
					if bodySoapParentPropertyIdName != "" {
						nestedSoapParentPropertyId["name"] = bodySoapParentPropertyIdName
					}
					if bodySoapParentPropertyIdNodeType != "" {
						nestedSoapParentPropertyId["nodeType"] = bodySoapParentPropertyIdNodeType
					}
					if bodySoapParentPropertyIdParameters != "" {
						var parsedSoapParentPropertyIdParameters any
						if err := json.Unmarshal([]byte(bodySoapParentPropertyIdParameters), &parsedSoapParentPropertyIdParameters); err != nil {
							return fmt.Errorf("parsing --soap-parent-property-id-parameters JSON: %w", err)
						}
						nestedSoapParentPropertyId["parameters"] = parsedSoapParentPropertyIdParameters
					}
					if bodySoapParentPropertyIdReturnType != "" {
						nestedSoapParentPropertyId["returnType"] = bodySoapParentPropertyIdReturnType
					}
					if cmd.Flags().Changed("soap-parent-property-id-tail-call") {
						nestedSoapParentPropertyId["tailCall"] = bodySoapParentPropertyIdTailCall
					}
					if bodySoapParentPropertyIdType != "" {
						nestedSoapParentPropertyId["type"] = bodySoapParentPropertyIdType
					}
					if len(nestedSoapParentPropertyId) > 0 {
						body["soapParentPropertyId"] = nestedSoapParentPropertyId
					}
				}
				if bodyStartDateTime != "" {
					body["startDateTime"] = bodyStartDateTime
				}
				if bodyStatus != 0 {
					body["status"] = bodyStatus
				}
				if bodyTaskCategoryID != "" {
					body["taskCategoryID"] = bodyTaskCategoryID
				}
				if bodyTaskNumber != "" {
					body["taskNumber"] = bodyTaskNumber
				}
				if bodyTaskType != 0 {
					body["taskType"] = bodyTaskType
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyUserDefinedFields != "" {
					var parsedUserDefinedFields any
					if err := json.Unmarshal([]byte(bodyUserDefinedFields), &parsedUserDefinedFields); err != nil {
						return fmt.Errorf("parsing --user-defined-fields JSON: %w", err)
					}
					body["userDefinedFields"] = parsedUserDefinedFields
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "tasks", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "tasks", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "tasks", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "tasks", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "tasks", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "tasks",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "tasks", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "tasks", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAssignedResourceID, "assigned-resource-id", "", "Assigned resource id")
	cmd.Flags().StringVar(&bodyAssignedResourceRoleID, "assigned-resource-role-id", "", "Assigned resource role id")
	cmd.Flags().StringVar(&bodyBillingCodeID, "billing-code-id", "", "Billing code id")
	cmd.Flags().BoolVar(&bodyCanClientPortalUserCompleteTask, "can-client-portal-user-complete-task", false, "Can client portal user complete task")
	cmd.Flags().StringVar(&bodyCompanyLocationID, "company-location-id", "", "Company location id")
	cmd.Flags().StringVar(&bodyCompletedByResourceID, "completed-by-resource-id", "", "Completed by resource id")
	cmd.Flags().IntVar(&bodyCompletedByType, "completed-by-type", 0, "Completed by type")
	cmd.Flags().StringVar(&bodyCompletedDateTime, "completed-date-time", "", "Completed date time")
	cmd.Flags().StringVar(&bodyCreateDateTime, "create-date-time", "", "Create date time")
	cmd.Flags().StringVar(&bodyCreatorResourceID, "creator-resource-id", "", "Creator resource id")
	cmd.Flags().IntVar(&bodyCreatorType, "creator-type", 0, "Creator type")
	cmd.Flags().StringVar(&bodyDepartmentID, "department-id", "", "Department id")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyEndDateTime, "end-date-time", "", "End date time")
	cmd.Flags().Float64Var(&bodyEstimatedHours, "estimated-hours", 0.0, "Estimated hours")
	cmd.Flags().StringVar(&bodyExternalID, "external-id", "", "External id")
	cmd.Flags().Float64Var(&bodyHoursToBeScheduled, "hours-to-be-scheduled", 0.0, "Hours to be scheduled")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyIsTaskBillable, "is-task-billable", false, "Is task billable")
	cmd.Flags().BoolVar(&bodyIsVisibleInClientPortal, "is-visible-in-client-portal", false, "Is visible in client portal")
	cmd.Flags().StringVar(&bodyLastActivityDateTime, "last-activity-date-time", "", "Last activity date time")
	cmd.Flags().IntVar(&bodyLastActivityPersonType, "last-activity-person-type", 0, "Last activity person type")
	cmd.Flags().StringVar(&bodyLastActivityResourceID, "last-activity-resource-id", "", "Last activity resource id")
	cmd.Flags().StringVar(&bodyPhaseID, "phase-id", "", "Phase id")
	cmd.Flags().IntVar(&bodyPriority, "priority", 0, "Priority")
	cmd.Flags().IntVar(&bodyPriorityLabel, "priority-label", 0, "Priority label")
	cmd.Flags().StringVar(&bodyProjectID, "project-id", "", "Project id")
	cmd.Flags().StringVar(&bodyPurchaseOrderNumber, "purchase-order-number", "", "Purchase order number")
	cmd.Flags().Float64Var(&bodyRemainingHours, "remaining-hours", 0.0, "Remaining hours")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdBodyCanReduce, "soap-parent-property-id-body-can-reduce", false, "Can reduce")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdBodyNodeType, "soap-parent-property-id-body-node-type", "", "Node type")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdBodyType, "soap-parent-property-id-body-type", "", "Type")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdCanReduce, "soap-parent-property-id-can-reduce", false, "Can reduce")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdName, "soap-parent-property-id-name", "", "Name")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdNodeType, "soap-parent-property-id-node-type", "", "Node type")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdParameters, "soap-parent-property-id-parameters", "", "Parameters")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdReturnType, "soap-parent-property-id-return-type", "", "Return type")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdTailCall, "soap-parent-property-id-tail-call", false, "Tail call")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdType, "soap-parent-property-id-type", "", "Type")
	cmd.Flags().StringVar(&bodyStartDateTime, "start-date-time", "", "Start date time")
	cmd.Flags().IntVar(&bodyStatus, "status", 0, "Status")
	cmd.Flags().StringVar(&bodyTaskCategoryID, "task-category-id", "", "Task category id")
	cmd.Flags().StringVar(&bodyTaskNumber, "task-number", "", "Task number")
	cmd.Flags().IntVar(&bodyTaskType, "task-type", 0, "Task type")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyUserDefinedFields, "user-defined-fields", "", "User defined fields")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
