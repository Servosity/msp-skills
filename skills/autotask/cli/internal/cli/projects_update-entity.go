// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProjectsUpdateEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyActualBilledHours float64
	var bodyActualHours float64
	var bodyChangeOrdersBudget float64
	var bodyChangeOrdersRevenue float64
	var bodyCompanyID string
	var bodyCompanyOwnerResourceID string
	var bodyCompletedDateTime string
	var bodyCompletedPercentage int
	var bodyContractID string
	var bodyCreateDateTime string
	var bodyCreatorResourceID string
	var bodyDepartment int
	var bodyDescription string
	var bodyDuration int
	var bodyEndDateTime string
	var bodyEstimatedSalesCost float64
	var bodyEstimatedTime float64
	var bodyExtProjectNumber string
	var bodyExtProjectType int
	var bodyId string
	var bodyImpersonatorCreatorResourceID string
	var bodyLaborEstimatedCosts float64
	var bodyLaborEstimatedMarginPercentage float64
	var bodyLaborEstimatedRevenue float64
	var bodyLastActivityDateTime string
	var bodyLastActivityPersonType int
	var bodyLastActivityResourceID string
	var bodyOpportunityID string
	var bodyOrganizationalLevelAssociationID string
	var bodyOriginalEstimatedRevenue float64
	var bodyProjectCostEstimatedMarginPercentage float64
	var bodyProjectCostsBudget float64
	var bodyProjectCostsRevenue float64
	var bodyProjectLeadResourceID string
	var bodyProjectName string
	var bodyProjectNumber string
	var bodyProjectType int
	var bodyPurchaseOrderNumber string
	var bodySgda float64
	var bodyStartDateTime string
	var bodyStatus int
	var bodyStatusDateTime string
	var bodyStatusDetail string
	var bodyUserDefinedFields string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-entity",
		Short:       "Update entity",
		Example:     "  autotask-cli projects update-entity",
		Annotations: map[string]string{"pp:endpoint": "projects.update-entity", "pp:method": "PUT", "pp:path": "/Projects"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Projects"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyActualBilledHours != 0.0 {
					body["actualBilledHours"] = bodyActualBilledHours
				}
				if bodyActualHours != 0.0 {
					body["actualHours"] = bodyActualHours
				}
				if bodyChangeOrdersBudget != 0.0 {
					body["changeOrdersBudget"] = bodyChangeOrdersBudget
				}
				if bodyChangeOrdersRevenue != 0.0 {
					body["changeOrdersRevenue"] = bodyChangeOrdersRevenue
				}
				if bodyCompanyID != "" {
					body["companyID"] = bodyCompanyID
				}
				if bodyCompanyOwnerResourceID != "" {
					body["companyOwnerResourceID"] = bodyCompanyOwnerResourceID
				}
				if bodyCompletedDateTime != "" {
					body["completedDateTime"] = bodyCompletedDateTime
				}
				if bodyCompletedPercentage != 0 {
					body["completedPercentage"] = bodyCompletedPercentage
				}
				if bodyContractID != "" {
					body["contractID"] = bodyContractID
				}
				if bodyCreateDateTime != "" {
					body["createDateTime"] = bodyCreateDateTime
				}
				if bodyCreatorResourceID != "" {
					body["creatorResourceID"] = bodyCreatorResourceID
				}
				if bodyDepartment != 0 {
					body["department"] = bodyDepartment
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDuration != 0 {
					body["duration"] = bodyDuration
				}
				if bodyEndDateTime != "" {
					body["endDateTime"] = bodyEndDateTime
				}
				if bodyEstimatedSalesCost != 0.0 {
					body["estimatedSalesCost"] = bodyEstimatedSalesCost
				}
				if bodyEstimatedTime != 0.0 {
					body["estimatedTime"] = bodyEstimatedTime
				}
				if bodyExtProjectNumber != "" {
					body["extProjectNumber"] = bodyExtProjectNumber
				}
				if bodyExtProjectType != 0 {
					body["extProjectType"] = bodyExtProjectType
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyImpersonatorCreatorResourceID != "" {
					body["impersonatorCreatorResourceID"] = bodyImpersonatorCreatorResourceID
				}
				if bodyLaborEstimatedCosts != 0.0 {
					body["laborEstimatedCosts"] = bodyLaborEstimatedCosts
				}
				if bodyLaborEstimatedMarginPercentage != 0.0 {
					body["laborEstimatedMarginPercentage"] = bodyLaborEstimatedMarginPercentage
				}
				if bodyLaborEstimatedRevenue != 0.0 {
					body["laborEstimatedRevenue"] = bodyLaborEstimatedRevenue
				}
				if bodyLastActivityDateTime != "" {
					body["lastActivityDateTime"] = bodyLastActivityDateTime
				}
				if bodyLastActivityPersonType != 0 {
					body["lastActivityPersonType"] = bodyLastActivityPersonType
				}
				if bodyLastActivityResourceID != "" {
					body["lastActivityResourceID"] = bodyLastActivityResourceID
				}
				if bodyOpportunityID != "" {
					body["opportunityID"] = bodyOpportunityID
				}
				if bodyOrganizationalLevelAssociationID != "" {
					body["organizationalLevelAssociationID"] = bodyOrganizationalLevelAssociationID
				}
				if bodyOriginalEstimatedRevenue != 0.0 {
					body["originalEstimatedRevenue"] = bodyOriginalEstimatedRevenue
				}
				if bodyProjectCostEstimatedMarginPercentage != 0.0 {
					body["projectCostEstimatedMarginPercentage"] = bodyProjectCostEstimatedMarginPercentage
				}
				if bodyProjectCostsBudget != 0.0 {
					body["projectCostsBudget"] = bodyProjectCostsBudget
				}
				if bodyProjectCostsRevenue != 0.0 {
					body["projectCostsRevenue"] = bodyProjectCostsRevenue
				}
				if bodyProjectLeadResourceID != "" {
					body["projectLeadResourceID"] = bodyProjectLeadResourceID
				}
				if bodyProjectName != "" {
					body["projectName"] = bodyProjectName
				}
				if bodyProjectNumber != "" {
					body["projectNumber"] = bodyProjectNumber
				}
				if bodyProjectType != 0 {
					body["projectType"] = bodyProjectType
				}
				if bodyPurchaseOrderNumber != "" {
					body["purchaseOrderNumber"] = bodyPurchaseOrderNumber
				}
				if bodySgda != 0.0 {
					body["sgda"] = bodySgda
				}
				if bodyStartDateTime != "" {
					body["startDateTime"] = bodyStartDateTime
				}
				if bodyStatus != 0 {
					body["status"] = bodyStatus
				}
				if bodyStatusDateTime != "" {
					body["statusDateTime"] = bodyStatusDateTime
				}
				if bodyStatusDetail != "" {
					body["statusDetail"] = bodyStatusDetail
				}
				if bodyUserDefinedFields != "" {
					var parsedUserDefinedFields any
					if err := json.Unmarshal([]byte(bodyUserDefinedFields), &parsedUserDefinedFields); err != nil {
						return fmt.Errorf("parsing --user-defined-fields JSON: %w", err)
					}
					body["userDefinedFields"] = parsedUserDefinedFields
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "projects", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "projects", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "projects",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().Float64Var(&bodyActualBilledHours, "actual-billed-hours", 0.0, "Actual billed hours")
	cmd.Flags().Float64Var(&bodyActualHours, "actual-hours", 0.0, "Actual hours")
	cmd.Flags().Float64Var(&bodyChangeOrdersBudget, "change-orders-budget", 0.0, "Change orders budget")
	cmd.Flags().Float64Var(&bodyChangeOrdersRevenue, "change-orders-revenue", 0.0, "Change orders revenue")
	cmd.Flags().StringVar(&bodyCompanyID, "company-id", "", "Company id")
	cmd.Flags().StringVar(&bodyCompanyOwnerResourceID, "company-owner-resource-id", "", "Company owner resource id")
	cmd.Flags().StringVar(&bodyCompletedDateTime, "completed-date-time", "", "Completed date time")
	cmd.Flags().IntVar(&bodyCompletedPercentage, "completed-percentage", 0, "Completed percentage")
	cmd.Flags().StringVar(&bodyContractID, "contract-id", "", "Contract id")
	cmd.Flags().StringVar(&bodyCreateDateTime, "create-date-time", "", "Create date time")
	cmd.Flags().StringVar(&bodyCreatorResourceID, "creator-resource-id", "", "Creator resource id")
	cmd.Flags().IntVar(&bodyDepartment, "department", 0, "Department")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().IntVar(&bodyDuration, "duration", 0, "Duration")
	cmd.Flags().StringVar(&bodyEndDateTime, "end-date-time", "", "End date time")
	cmd.Flags().Float64Var(&bodyEstimatedSalesCost, "estimated-sales-cost", 0.0, "Estimated sales cost")
	cmd.Flags().Float64Var(&bodyEstimatedTime, "estimated-time", 0.0, "Estimated time")
	cmd.Flags().StringVar(&bodyExtProjectNumber, "ext-project-number", "", "Ext project number")
	cmd.Flags().IntVar(&bodyExtProjectType, "ext-project-type", 0, "Ext project type")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyImpersonatorCreatorResourceID, "impersonator-creator-resource-id", "", "Impersonator creator resource id")
	cmd.Flags().Float64Var(&bodyLaborEstimatedCosts, "labor-estimated-costs", 0.0, "Labor estimated costs")
	cmd.Flags().Float64Var(&bodyLaborEstimatedMarginPercentage, "labor-estimated-margin-percentage", 0.0, "Labor estimated margin percentage")
	cmd.Flags().Float64Var(&bodyLaborEstimatedRevenue, "labor-estimated-revenue", 0.0, "Labor estimated revenue")
	cmd.Flags().StringVar(&bodyLastActivityDateTime, "last-activity-date-time", "", "Last activity date time")
	cmd.Flags().IntVar(&bodyLastActivityPersonType, "last-activity-person-type", 0, "Last activity person type")
	cmd.Flags().StringVar(&bodyLastActivityResourceID, "last-activity-resource-id", "", "Last activity resource id")
	cmd.Flags().StringVar(&bodyOpportunityID, "opportunity-id", "", "Opportunity id")
	cmd.Flags().StringVar(&bodyOrganizationalLevelAssociationID, "organizational-level-association-id", "", "Organizational level association id")
	cmd.Flags().Float64Var(&bodyOriginalEstimatedRevenue, "original-estimated-revenue", 0.0, "Original estimated revenue")
	cmd.Flags().Float64Var(&bodyProjectCostEstimatedMarginPercentage, "project-cost-estimated-margin-percentage", 0.0, "Project cost estimated margin percentage")
	cmd.Flags().Float64Var(&bodyProjectCostsBudget, "project-costs-budget", 0.0, "Project costs budget")
	cmd.Flags().Float64Var(&bodyProjectCostsRevenue, "project-costs-revenue", 0.0, "Project costs revenue")
	cmd.Flags().StringVar(&bodyProjectLeadResourceID, "project-lead-resource-id", "", "Project lead resource id")
	cmd.Flags().StringVar(&bodyProjectName, "project-name", "", "Project name")
	cmd.Flags().StringVar(&bodyProjectNumber, "project-number", "", "Project number")
	cmd.Flags().IntVar(&bodyProjectType, "project-type", 0, "Project type")
	cmd.Flags().StringVar(&bodyPurchaseOrderNumber, "purchase-order-number", "", "Purchase order number")
	cmd.Flags().Float64Var(&bodySgda, "sgda", 0.0, "Sgda")
	cmd.Flags().StringVar(&bodyStartDateTime, "start-date-time", "", "Start date time")
	cmd.Flags().IntVar(&bodyStatus, "status", 0, "Status")
	cmd.Flags().StringVar(&bodyStatusDateTime, "status-date-time", "", "Status date time")
	cmd.Flags().StringVar(&bodyStatusDetail, "status-detail", "", "Status detail")
	cmd.Flags().StringVar(&bodyUserDefinedFields, "user-defined-fields", "", "User defined fields")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
