// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newQuotesItemsQuoteChildCreateEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyAverageCost float64
	var bodyChargeID string
	var bodyDescription string
	var bodyExpenseID string
	var bodyHighestCost float64
	var bodyId string
	var bodyInternalCurrencyLineDiscount float64
	var bodyInternalCurrencyUnitDiscount float64
	var bodyInternalCurrencyUnitPrice float64
	var bodyIsOptional bool
	var bodyIsTaxable bool
	var bodyLaborID string
	var bodyLineDiscount float64
	var bodyMarkupRate float64
	var bodyName string
	var bodyPercentageDiscount float64
	var bodyPeriodType int
	var bodyProductID string
	var bodyQuantity float64
	var bodyQuoteID string
	var bodyQuoteItemType int
	var bodyServiceBundleID string
	var bodyServiceID string
	var bodyShippingID string
	var bodySoapParentPropertyIdBodyCanReduce bool
	var bodySoapParentPropertyIdBodyNodeType string
	var bodySoapParentPropertyIdBodyType string
	var bodySoapParentPropertyIdCanReduce bool
	var bodySoapParentPropertyIdName string
	var bodySoapParentPropertyIdNodeType string
	var bodySoapParentPropertyIdParameters string
	var bodySoapParentPropertyIdReturnType string
	var bodySoapParentPropertyIdTailCall bool
	var bodySoapParentPropertyIdType string
	var bodySortOrderID string
	var bodyTaxCategoryID string
	var bodyTotalEffectiveTax float64
	var bodyUnitCost float64
	var bodyUnitDiscount float64
	var bodyUnitPrice float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "quote-child-create-entity <parentId>",
		Aliases:     []string{"create"},
		Short:       "Quote child create entity",
		Example:     "  autotask-cli quotes items quote-child-create-entity 42",
		Annotations: map[string]string{"pp:endpoint": "items.quote-child-create-entity", "pp:method": "POST", "pp:path": "/Quotes/{parentId}/Items"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Quotes/{parentId}/Items"
			path = replacePathParam(path, "parentId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAverageCost != 0.0 {
					body["averageCost"] = bodyAverageCost
				}
				if bodyChargeID != "" {
					body["chargeID"] = bodyChargeID
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyExpenseID != "" {
					body["expenseID"] = bodyExpenseID
				}
				if bodyHighestCost != 0.0 {
					body["highestCost"] = bodyHighestCost
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyInternalCurrencyLineDiscount != 0.0 {
					body["internalCurrencyLineDiscount"] = bodyInternalCurrencyLineDiscount
				}
				if bodyInternalCurrencyUnitDiscount != 0.0 {
					body["internalCurrencyUnitDiscount"] = bodyInternalCurrencyUnitDiscount
				}
				if bodyInternalCurrencyUnitPrice != 0.0 {
					body["internalCurrencyUnitPrice"] = bodyInternalCurrencyUnitPrice
				}
				if cmd.Flags().Changed("is-optional") {
					body["isOptional"] = bodyIsOptional
				}
				if cmd.Flags().Changed("is-taxable") {
					body["isTaxable"] = bodyIsTaxable
				}
				if bodyLaborID != "" {
					body["laborID"] = bodyLaborID
				}
				if bodyLineDiscount != 0.0 {
					body["lineDiscount"] = bodyLineDiscount
				}
				if bodyMarkupRate != 0.0 {
					body["markupRate"] = bodyMarkupRate
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyPercentageDiscount != 0.0 {
					body["percentageDiscount"] = bodyPercentageDiscount
				}
				if bodyPeriodType != 0 {
					body["periodType"] = bodyPeriodType
				}
				if bodyProductID != "" {
					body["productID"] = bodyProductID
				}
				if bodyQuantity != 0.0 {
					body["quantity"] = bodyQuantity
				}
				if bodyQuoteID != "" {
					body["quoteID"] = bodyQuoteID
				}
				if bodyQuoteItemType != 0 {
					body["quoteItemType"] = bodyQuoteItemType
				}
				if bodyServiceBundleID != "" {
					body["serviceBundleID"] = bodyServiceBundleID
				}
				if bodyServiceID != "" {
					body["serviceID"] = bodyServiceID
				}
				if bodyShippingID != "" {
					body["shippingID"] = bodyShippingID
				}
				{
					nestedSoapParentPropertyId := map[string]any{}
					{
						nestedSoapParentPropertyIdBody := map[string]any{}
						if cmd.Flags().Changed("soap-parent-property-id-body-can-reduce") {
							nestedSoapParentPropertyIdBody["canReduce"] = bodySoapParentPropertyIdBodyCanReduce
						}
						if bodySoapParentPropertyIdBodyNodeType != "" {
							nestedSoapParentPropertyIdBody["nodeType"] = bodySoapParentPropertyIdBodyNodeType
						}
						if bodySoapParentPropertyIdBodyType != "" {
							nestedSoapParentPropertyIdBody["type"] = bodySoapParentPropertyIdBodyType
						}
						if len(nestedSoapParentPropertyIdBody) > 0 {
							nestedSoapParentPropertyId["body"] = nestedSoapParentPropertyIdBody
						}
					}
					if cmd.Flags().Changed("soap-parent-property-id-can-reduce") {
						nestedSoapParentPropertyId["canReduce"] = bodySoapParentPropertyIdCanReduce
					}
					if bodySoapParentPropertyIdName != "" {
						nestedSoapParentPropertyId["name"] = bodySoapParentPropertyIdName
					}
					if bodySoapParentPropertyIdNodeType != "" {
						nestedSoapParentPropertyId["nodeType"] = bodySoapParentPropertyIdNodeType
					}
					if bodySoapParentPropertyIdParameters != "" {
						var parsedSoapParentPropertyIdParameters any
						if err := json.Unmarshal([]byte(bodySoapParentPropertyIdParameters), &parsedSoapParentPropertyIdParameters); err != nil {
							return fmt.Errorf("parsing --soap-parent-property-id-parameters JSON: %w", err)
						}
						nestedSoapParentPropertyId["parameters"] = parsedSoapParentPropertyIdParameters
					}
					if bodySoapParentPropertyIdReturnType != "" {
						nestedSoapParentPropertyId["returnType"] = bodySoapParentPropertyIdReturnType
					}
					if cmd.Flags().Changed("soap-parent-property-id-tail-call") {
						nestedSoapParentPropertyId["tailCall"] = bodySoapParentPropertyIdTailCall
					}
					if bodySoapParentPropertyIdType != "" {
						nestedSoapParentPropertyId["type"] = bodySoapParentPropertyIdType
					}
					if len(nestedSoapParentPropertyId) > 0 {
						body["soapParentPropertyId"] = nestedSoapParentPropertyId
					}
				}
				if bodySortOrderID != "" {
					body["sortOrderID"] = bodySortOrderID
				}
				if bodyTaxCategoryID != "" {
					body["taxCategoryID"] = bodyTaxCategoryID
				}
				if bodyTotalEffectiveTax != 0.0 {
					body["totalEffectiveTax"] = bodyTotalEffectiveTax
				}
				if bodyUnitCost != 0.0 {
					body["unitCost"] = bodyUnitCost
				}
				if bodyUnitDiscount != 0.0 {
					body["unitDiscount"] = bodyUnitDiscount
				}
				if bodyUnitPrice != 0.0 {
					body["unitPrice"] = bodyUnitPrice
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "items", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "items", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "items", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "items", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "items", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "items",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "items", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "items", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().Float64Var(&bodyAverageCost, "average-cost", 0.0, "Average cost")
	cmd.Flags().StringVar(&bodyChargeID, "charge-id", "", "Charge id")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyExpenseID, "expense-id", "", "Expense id")
	cmd.Flags().Float64Var(&bodyHighestCost, "highest-cost", 0.0, "Highest cost")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().Float64Var(&bodyInternalCurrencyLineDiscount, "internal-currency-line-discount", 0.0, "Internal currency line discount")
	cmd.Flags().Float64Var(&bodyInternalCurrencyUnitDiscount, "internal-currency-unit-discount", 0.0, "Internal currency unit discount")
	cmd.Flags().Float64Var(&bodyInternalCurrencyUnitPrice, "internal-currency-unit-price", 0.0, "Internal currency unit price")
	cmd.Flags().BoolVar(&bodyIsOptional, "is-optional", false, "Is optional")
	cmd.Flags().BoolVar(&bodyIsTaxable, "is-taxable", false, "Is taxable")
	cmd.Flags().StringVar(&bodyLaborID, "labor-id", "", "Labor id")
	cmd.Flags().Float64Var(&bodyLineDiscount, "line-discount", 0.0, "Line discount")
	cmd.Flags().Float64Var(&bodyMarkupRate, "markup-rate", 0.0, "Markup rate")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().Float64Var(&bodyPercentageDiscount, "percentage-discount", 0.0, "Percentage discount")
	cmd.Flags().IntVar(&bodyPeriodType, "period-type", 0, "Period type")
	cmd.Flags().StringVar(&bodyProductID, "product-id", "", "Product id")
	cmd.Flags().Float64Var(&bodyQuantity, "quantity", 0.0, "Quantity")
	cmd.Flags().StringVar(&bodyQuoteID, "quote-id", "", "Quote id")
	cmd.Flags().IntVar(&bodyQuoteItemType, "quote-item-type", 0, "Quote item type")
	cmd.Flags().StringVar(&bodyServiceBundleID, "service-bundle-id", "", "Service bundle id")
	cmd.Flags().StringVar(&bodyServiceID, "service-id", "", "Service id")
	cmd.Flags().StringVar(&bodyShippingID, "shipping-id", "", "Shipping id")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdBodyCanReduce, "soap-parent-property-id-body-can-reduce", false, "Can reduce")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdBodyNodeType, "soap-parent-property-id-body-node-type", "", "Node type")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdBodyType, "soap-parent-property-id-body-type", "", "Type")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdCanReduce, "soap-parent-property-id-can-reduce", false, "Can reduce")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdName, "soap-parent-property-id-name", "", "Name")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdNodeType, "soap-parent-property-id-node-type", "", "Node type")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdParameters, "soap-parent-property-id-parameters", "", "Parameters")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdReturnType, "soap-parent-property-id-return-type", "", "Return type")
	cmd.Flags().BoolVar(&bodySoapParentPropertyIdTailCall, "soap-parent-property-id-tail-call", false, "Tail call")
	cmd.Flags().StringVar(&bodySoapParentPropertyIdType, "soap-parent-property-id-type", "", "Type")
	cmd.Flags().StringVar(&bodySortOrderID, "sort-order-id", "", "Sort order id")
	cmd.Flags().StringVar(&bodyTaxCategoryID, "tax-category-id", "", "Tax category id")
	cmd.Flags().Float64Var(&bodyTotalEffectiveTax, "total-effective-tax", 0.0, "Total effective tax")
	cmd.Flags().Float64Var(&bodyUnitCost, "unit-cost", 0.0, "Unit cost")
	cmd.Flags().Float64Var(&bodyUnitDiscount, "unit-discount", 0.0, "Unit discount")
	cmd.Flags().Float64Var(&bodyUnitPrice, "unit-price", 0.0, "Unit price")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
