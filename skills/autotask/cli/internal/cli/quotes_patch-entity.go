// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newQuotesPatchEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyApprovalStatus int
	var bodyApprovalStatusChangedByResourceID string
	var bodyApprovalStatusChangedDate string
	var bodyBillToLocationID string
	var bodyCalculateTaxSeparately bool
	var bodyComment string
	var bodyCompanyID string
	var bodyContactID string
	var bodyCreateDate string
	var bodyCreatorResourceID string
	var bodyDescription string
	var bodyEffectiveDate string
	var bodyExpirationDate string
	var bodyExtApprovalContactResponse int
	var bodyExtApprovalResponseDate string
	var bodyExtApprovalResponseSignature string
	var bodyExternalQuoteNumber string
	var bodyGroupByID string
	var bodyId string
	var bodyImpersonatorCreatorResourceID string
	var bodyIsActive bool
	var bodyLastActivityDate string
	var bodyLastModifiedBy int
	var bodyLastPublishedByResourceID string
	var bodyLastPublishedDateTime string
	var bodyName string
	var bodyOpportunityID string
	var bodyPaymentTerm int
	var bodyPaymentType int
	var bodyPrimaryQuote bool
	var bodyProposalProjectID string
	var bodyPurchaseOrderNumber string
	var bodyQuoteNumber int
	var bodyQuoteTemplateID string
	var bodyShipToLocationID string
	var bodyShippingType int
	var bodyShowEachTaxInGroup bool
	var bodyShowTaxCategory bool
	var bodySoldToLocationID string
	var bodyTaxRegionID string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "patch-entity",
		Aliases:     []string{"update"},
		Short:       "Patch entity",
		Example:     "  autotask-cli quotes patch-entity",
		Annotations: map[string]string{"pp:endpoint": "quotes.patch-entity", "pp:method": "PATCH", "pp:path": "/Quotes"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Quotes"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApprovalStatus != 0 {
					body["approvalStatus"] = bodyApprovalStatus
				}
				if bodyApprovalStatusChangedByResourceID != "" {
					body["approvalStatusChangedByResourceID"] = bodyApprovalStatusChangedByResourceID
				}
				if bodyApprovalStatusChangedDate != "" {
					body["approvalStatusChangedDate"] = bodyApprovalStatusChangedDate
				}
				if bodyBillToLocationID != "" {
					body["billToLocationID"] = bodyBillToLocationID
				}
				if cmd.Flags().Changed("calculate-tax-separately") {
					body["calculateTaxSeparately"] = bodyCalculateTaxSeparately
				}
				if bodyComment != "" {
					body["comment"] = bodyComment
				}
				if bodyCompanyID != "" {
					body["companyID"] = bodyCompanyID
				}
				if bodyContactID != "" {
					body["contactID"] = bodyContactID
				}
				if bodyCreateDate != "" {
					body["createDate"] = bodyCreateDate
				}
				if bodyCreatorResourceID != "" {
					body["creatorResourceID"] = bodyCreatorResourceID
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyEffectiveDate != "" {
					body["effectiveDate"] = bodyEffectiveDate
				}
				if bodyExpirationDate != "" {
					body["expirationDate"] = bodyExpirationDate
				}
				if bodyExtApprovalContactResponse != 0 {
					body["extApprovalContactResponse"] = bodyExtApprovalContactResponse
				}
				if bodyExtApprovalResponseDate != "" {
					body["extApprovalResponseDate"] = bodyExtApprovalResponseDate
				}
				if bodyExtApprovalResponseSignature != "" {
					body["extApprovalResponseSignature"] = bodyExtApprovalResponseSignature
				}
				if bodyExternalQuoteNumber != "" {
					body["externalQuoteNumber"] = bodyExternalQuoteNumber
				}
				if bodyGroupByID != "" {
					body["groupByID"] = bodyGroupByID
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyImpersonatorCreatorResourceID != "" {
					body["impersonatorCreatorResourceID"] = bodyImpersonatorCreatorResourceID
				}
				if cmd.Flags().Changed("is-active") {
					body["isActive"] = bodyIsActive
				}
				if bodyLastActivityDate != "" {
					body["lastActivityDate"] = bodyLastActivityDate
				}
				if bodyLastModifiedBy != 0 {
					body["lastModifiedBy"] = bodyLastModifiedBy
				}
				if bodyLastPublishedByResourceID != "" {
					body["lastPublishedByResourceID"] = bodyLastPublishedByResourceID
				}
				if bodyLastPublishedDateTime != "" {
					body["lastPublishedDateTime"] = bodyLastPublishedDateTime
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOpportunityID != "" {
					body["opportunityID"] = bodyOpportunityID
				}
				if bodyPaymentTerm != 0 {
					body["paymentTerm"] = bodyPaymentTerm
				}
				if bodyPaymentType != 0 {
					body["paymentType"] = bodyPaymentType
				}
				if cmd.Flags().Changed("primary-quote") {
					body["primaryQuote"] = bodyPrimaryQuote
				}
				if bodyProposalProjectID != "" {
					body["proposalProjectID"] = bodyProposalProjectID
				}
				if bodyPurchaseOrderNumber != "" {
					body["purchaseOrderNumber"] = bodyPurchaseOrderNumber
				}
				if bodyQuoteNumber != 0 {
					body["quoteNumber"] = bodyQuoteNumber
				}
				if bodyQuoteTemplateID != "" {
					body["quoteTemplateID"] = bodyQuoteTemplateID
				}
				if bodyShipToLocationID != "" {
					body["shipToLocationID"] = bodyShipToLocationID
				}
				if bodyShippingType != 0 {
					body["shippingType"] = bodyShippingType
				}
				if cmd.Flags().Changed("show-each-tax-in-group") {
					body["showEachTaxInGroup"] = bodyShowEachTaxInGroup
				}
				if cmd.Flags().Changed("show-tax-category") {
					body["showTaxCategory"] = bodyShowTaxCategory
				}
				if bodySoldToLocationID != "" {
					body["soldToLocationID"] = bodySoldToLocationID
				}
				if bodyTaxRegionID != "" {
					body["taxRegionID"] = bodyTaxRegionID
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "quotes", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "quotes", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "quotes", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "quotes", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "quotes", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "quotes",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "quotes", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "quotes", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().IntVar(&bodyApprovalStatus, "approval-status", 0, "Approval status")
	cmd.Flags().StringVar(&bodyApprovalStatusChangedByResourceID, "approval-status-changed-by-resource-id", "", "Approval status changed by resource id")
	cmd.Flags().StringVar(&bodyApprovalStatusChangedDate, "approval-status-changed-date", "", "Approval status changed date")
	cmd.Flags().StringVar(&bodyBillToLocationID, "bill-to-location-id", "", "Bill to location id")
	cmd.Flags().BoolVar(&bodyCalculateTaxSeparately, "calculate-tax-separately", false, "Calculate tax separately")
	cmd.Flags().StringVar(&bodyComment, "comment", "", "Comment")
	cmd.Flags().StringVar(&bodyCompanyID, "company-id", "", "Company id")
	cmd.Flags().StringVar(&bodyContactID, "contact-id", "", "Contact id")
	cmd.Flags().StringVar(&bodyCreateDate, "create-date", "", "Create date")
	cmd.Flags().StringVar(&bodyCreatorResourceID, "creator-resource-id", "", "Creator resource id")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyEffectiveDate, "effective-date", "", "Effective date")
	cmd.Flags().StringVar(&bodyExpirationDate, "expiration-date", "", "Expiration date")
	cmd.Flags().IntVar(&bodyExtApprovalContactResponse, "ext-approval-contact-response", 0, "Ext approval contact response")
	cmd.Flags().StringVar(&bodyExtApprovalResponseDate, "ext-approval-response-date", "", "Ext approval response date")
	cmd.Flags().StringVar(&bodyExtApprovalResponseSignature, "ext-approval-response-signature", "", "Ext approval response signature")
	cmd.Flags().StringVar(&bodyExternalQuoteNumber, "external-quote-number", "", "External quote number")
	cmd.Flags().StringVar(&bodyGroupByID, "group-by-id", "", "Group by id")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyImpersonatorCreatorResourceID, "impersonator-creator-resource-id", "", "Impersonator creator resource id")
	cmd.Flags().BoolVar(&bodyIsActive, "is-active", false, "Is active")
	cmd.Flags().StringVar(&bodyLastActivityDate, "last-activity-date", "", "Last activity date")
	cmd.Flags().IntVar(&bodyLastModifiedBy, "last-modified-by", 0, "Last modified by")
	cmd.Flags().StringVar(&bodyLastPublishedByResourceID, "last-published-by-resource-id", "", "Last published by resource id")
	cmd.Flags().StringVar(&bodyLastPublishedDateTime, "last-published-date-time", "", "Last published date time")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyOpportunityID, "opportunity-id", "", "Opportunity id")
	cmd.Flags().IntVar(&bodyPaymentTerm, "payment-term", 0, "Payment term")
	cmd.Flags().IntVar(&bodyPaymentType, "payment-type", 0, "Payment type")
	cmd.Flags().BoolVar(&bodyPrimaryQuote, "primary-quote", false, "Primary quote")
	cmd.Flags().StringVar(&bodyProposalProjectID, "proposal-project-id", "", "Proposal project id")
	cmd.Flags().StringVar(&bodyPurchaseOrderNumber, "purchase-order-number", "", "Purchase order number")
	cmd.Flags().IntVar(&bodyQuoteNumber, "quote-number", 0, "Quote number")
	cmd.Flags().StringVar(&bodyQuoteTemplateID, "quote-template-id", "", "Quote template id")
	cmd.Flags().StringVar(&bodyShipToLocationID, "ship-to-location-id", "", "Ship to location id")
	cmd.Flags().IntVar(&bodyShippingType, "shipping-type", 0, "Shipping type")
	cmd.Flags().BoolVar(&bodyShowEachTaxInGroup, "show-each-tax-in-group", false, "Show each tax in group")
	cmd.Flags().BoolVar(&bodyShowTaxCategory, "show-tax-category", false, "Show tax category")
	cmd.Flags().StringVar(&bodySoldToLocationID, "sold-to-location-id", "", "Sold to location id")
	cmd.Flags().StringVar(&bodyTaxRegionID, "tax-region-id", "", "Tax region id")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
