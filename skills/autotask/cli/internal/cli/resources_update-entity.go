// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newResourcesUpdateEntityCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountingReferenceID string
	var bodyDateFormat string
	var bodyDefaultServiceDeskRoleID string
	var bodyEmail string
	var bodyEmail2 string
	var bodyEmail3 string
	var bodyEmailTypeCode string
	var bodyEmailTypeCode2 string
	var bodyEmailTypeCode3 string
	var bodyFirstName string
	var bodyGender string
	var bodyGreeting int
	var bodyHireDate string
	var bodyHomePhone string
	var bodyId string
	var bodyInitials string
	var bodyInternalCost float64
	var bodyIsActive bool
	var bodyLastName string
	var bodyLicenseType int
	var bodyLocationID string
	var bodyMiddleName string
	var bodyMobilePhone string
	var bodyNumberFormat string
	var bodyOfficeExtension string
	var bodyOfficePhone string
	var bodyPayrollType int
	var bodyResourceType string
	var bodySuffix int
	var bodySurveyResourceRating float64
	var bodyTimeFormat string
	var bodyTitle string
	var bodyTravelAvailabilityPct string
	var bodyUserName string
	var bodyUserType int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-entity",
		Short:       "Update entity",
		Example:     "  autotask-cli resources update-entity",
		Annotations: map[string]string{"pp:endpoint": "resources.update-entity", "pp:method": "PUT", "pp:path": "/Resources"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Resources"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAccountingReferenceID != "" {
					body["accountingReferenceID"] = bodyAccountingReferenceID
				}
				if bodyDateFormat != "" {
					body["dateFormat"] = bodyDateFormat
				}
				if bodyDefaultServiceDeskRoleID != "" {
					body["defaultServiceDeskRoleID"] = bodyDefaultServiceDeskRoleID
				}
				if bodyEmail != "" {
					body["email"] = bodyEmail
				}
				if bodyEmail2 != "" {
					body["email2"] = bodyEmail2
				}
				if bodyEmail3 != "" {
					body["email3"] = bodyEmail3
				}
				if bodyEmailTypeCode != "" {
					body["emailTypeCode"] = bodyEmailTypeCode
				}
				if bodyEmailTypeCode2 != "" {
					body["emailTypeCode2"] = bodyEmailTypeCode2
				}
				if bodyEmailTypeCode3 != "" {
					body["emailTypeCode3"] = bodyEmailTypeCode3
				}
				if bodyFirstName != "" {
					body["firstName"] = bodyFirstName
				}
				if bodyGender != "" {
					body["gender"] = bodyGender
				}
				if bodyGreeting != 0 {
					body["greeting"] = bodyGreeting
				}
				if bodyHireDate != "" {
					body["hireDate"] = bodyHireDate
				}
				if bodyHomePhone != "" {
					body["homePhone"] = bodyHomePhone
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyInitials != "" {
					body["initials"] = bodyInitials
				}
				if bodyInternalCost != 0.0 {
					body["internalCost"] = bodyInternalCost
				}
				if cmd.Flags().Changed("is-active") {
					body["isActive"] = bodyIsActive
				}
				if bodyLastName != "" {
					body["lastName"] = bodyLastName
				}
				if bodyLicenseType != 0 {
					body["licenseType"] = bodyLicenseType
				}
				if bodyLocationID != "" {
					body["locationID"] = bodyLocationID
				}
				if bodyMiddleName != "" {
					body["middleName"] = bodyMiddleName
				}
				if bodyMobilePhone != "" {
					body["mobilePhone"] = bodyMobilePhone
				}
				if bodyNumberFormat != "" {
					body["numberFormat"] = bodyNumberFormat
				}
				if bodyOfficeExtension != "" {
					body["officeExtension"] = bodyOfficeExtension
				}
				if bodyOfficePhone != "" {
					body["officePhone"] = bodyOfficePhone
				}
				if bodyPayrollType != 0 {
					body["payrollType"] = bodyPayrollType
				}
				if bodyResourceType != "" {
					body["resourceType"] = bodyResourceType
				}
				if bodySuffix != 0 {
					body["suffix"] = bodySuffix
				}
				if bodySurveyResourceRating != 0.0 {
					body["surveyResourceRating"] = bodySurveyResourceRating
				}
				if bodyTimeFormat != "" {
					body["timeFormat"] = bodyTimeFormat
				}
				if bodyTitle != "" {
					body["title"] = bodyTitle
				}
				if bodyTravelAvailabilityPct != "" {
					body["travelAvailabilityPct"] = bodyTravelAvailabilityPct
				}
				if bodyUserName != "" {
					body["userName"] = bodyUserName
				}
				if bodyUserType != 0 {
					body["userType"] = bodyUserType
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "resources", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "resources", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "resources", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "resources", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "resources", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "resources",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "resources", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "resources", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAccountingReferenceID, "accounting-reference-id", "", "Accounting reference id")
	cmd.Flags().StringVar(&bodyDateFormat, "date-format", "", "Date format")
	cmd.Flags().StringVar(&bodyDefaultServiceDeskRoleID, "default-service-desk-role-id", "", "Default service desk role id")
	cmd.Flags().StringVar(&bodyEmail, "email", "", "Email")
	cmd.Flags().StringVar(&bodyEmail2, "email2", "", "Email2")
	cmd.Flags().StringVar(&bodyEmail3, "email3", "", "Email3")
	cmd.Flags().StringVar(&bodyEmailTypeCode, "email-type-code", "", "Email type code")
	cmd.Flags().StringVar(&bodyEmailTypeCode2, "email-type-code2", "", "Email type code2")
	cmd.Flags().StringVar(&bodyEmailTypeCode3, "email-type-code3", "", "Email type code3")
	cmd.Flags().StringVar(&bodyFirstName, "first-name", "", "First name")
	cmd.Flags().StringVar(&bodyGender, "gender", "", "Gender")
	cmd.Flags().IntVar(&bodyGreeting, "greeting", 0, "Greeting")
	cmd.Flags().StringVar(&bodyHireDate, "hire-date", "", "Hire date")
	cmd.Flags().StringVar(&bodyHomePhone, "home-phone", "", "Home phone")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyInitials, "initials", "", "Initials")
	cmd.Flags().Float64Var(&bodyInternalCost, "internal-cost", 0.0, "Internal cost")
	cmd.Flags().BoolVar(&bodyIsActive, "is-active", false, "Is active")
	cmd.Flags().StringVar(&bodyLastName, "last-name", "", "Last name")
	cmd.Flags().IntVar(&bodyLicenseType, "license-type", 0, "License type")
	cmd.Flags().StringVar(&bodyLocationID, "location-id", "", "Location id")
	cmd.Flags().StringVar(&bodyMiddleName, "middle-name", "", "Middle name")
	cmd.Flags().StringVar(&bodyMobilePhone, "mobile-phone", "", "Mobile phone")
	cmd.Flags().StringVar(&bodyNumberFormat, "number-format", "", "Number format")
	cmd.Flags().StringVar(&bodyOfficeExtension, "office-extension", "", "Office extension")
	cmd.Flags().StringVar(&bodyOfficePhone, "office-phone", "", "Office phone")
	cmd.Flags().IntVar(&bodyPayrollType, "payroll-type", 0, "Payroll type")
	cmd.Flags().StringVar(&bodyResourceType, "resource-type", "", "Resource type")
	cmd.Flags().IntVar(&bodySuffix, "suffix", 0, "Suffix")
	cmd.Flags().Float64Var(&bodySurveyResourceRating, "survey-resource-rating", 0.0, "Survey resource rating")
	cmd.Flags().StringVar(&bodyTimeFormat, "time-format", "", "Time format")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyTravelAvailabilityPct, "travel-availability-pct", "", "Travel availability pct")
	cmd.Flags().StringVar(&bodyUserName, "user-name", "", "User name")
	cmd.Flags().IntVar(&bodyUserType, "user-type", 0, "User type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
