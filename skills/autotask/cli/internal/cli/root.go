// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"bytes"
	"fmt"
	"io"
	"os"
	"strings"
	"text/tabwriter"
	"time"

	"autotask-pp-cli/internal/client"
	"autotask-pp-cli/internal/config"
	"github.com/spf13/cobra"
)

type rootFlags struct {
	asJSON        bool
	compact       bool
	csv           bool
	plain         bool
	quiet         bool
	dryRun        bool
	noCache       bool
	noInput       bool
	idempotent    bool
	ignoreMissing bool
	yes           bool
	agent         bool
	// allowPartialFailure downgrades a detected response-body partial-failure
	// (e.g. Google Ads `partialFailureError`) from a non-zero exit to a
	// stderr warning. Default false so silent partial successes surface as
	// failures by default.
	allowPartialFailure bool
	selectFields        string
	configPath          string
	profileName         string
	deliverSpec         string
	timeout             time.Duration
	rateLimit           float64
	maxAge              time.Duration
	dataSource          string
	freshnessMeta       any

	// deliverBuf captures command output when --deliver is set to a
	// non-stdout sink. Flushed to the sink after Execute returns.
	deliverBuf  *bytes.Buffer
	deliverSink DeliverSink
}

// RootCmd returns the Cobra command tree without executing it. The MCP server
// uses this to mirror every user-facing command as an agent tool.
func RootCmd() *cobra.Command {
	var flags rootFlags
	return newRootCmd(&flags)
}

// Execute runs the CLI in non-interactive mode: never prompts, all values via flags or stdin.
func Execute() error {
	var flags rootFlags
	rootCmd := newRootCmd(&flags)

	err := rootCmd.Execute()
	if err != nil && strings.Contains(err.Error(), "unknown flag") {
		msg := err.Error()
		// Extract the flag name from the error message (e.g., "unknown flag: --foob")
		if idx := strings.Index(msg, "unknown flag: "); idx >= 0 {
			flagStr := strings.TrimSpace(msg[idx+len("unknown flag: "):])
			if suggestion := suggestFlag(flagStr, rootCmd); suggestion != "" {
				// Cobra already printed `Error: unknown flag: --foob` before
				// returning; the wrap below attaches the hint to err.Error()
				// for downstream consumers and exit-code classification, but
				// would never reach stderr now that main.go no longer prints
				// err. Emit the hint explicitly so the suggestion still
				// shows up under Cobra's error line.
				fmt.Fprintf(os.Stderr, "hint: did you mean --%s?\n", suggestion)
				err = fmt.Errorf("%w\nhint: did you mean --%s?", err, suggestion)
			}
		}
	}
	if err == nil && flags.deliverBuf != nil {
		if derr := Deliver(flags.deliverSink, flags.deliverBuf.Bytes(), flags.compact); derr != nil {
			fmt.Fprintf(os.Stderr, "warning: deliver to %s:%s failed: %v\n", flags.deliverSink.Scheme, flags.deliverSink.Target, derr)
			return derr
		}
	}
	if err != nil && isCobraUsageError(err) {
		// Cobra/pflag pre-RunE errors (unknown flag, unknown command,
		// missing required, etc.) never flow through usageErr() because
		// they originate inside rootCmd.Execute() before any user RunE
		// runs. Without this wrap, ExitCode() falls through to the
		// default and emits 1 — clobbering the conventional code-2 for
		// usage errors that the helpers.go contract already promises.
		return usageErr(err)
	}
	return err
}

// isCobraUsageError reports whether err matches one of Cobra/pflag's
// pre-RunE usage-error shapes. Detection is by message prefix to match
// the same approach the unknown-flag hint path uses above; neither
// Cobra nor pflag exports typed sentinels for these.
//
// Patterns are anchored to the literal punctuation Cobra and pflag
// emit so an application's own RunE error that happens to contain the
// substring "required flag" or "invalid argument" doesn't get
// misclassified as a usage error.
//
// Patterns covered (Cobra v1.x + pflag v1.x as of 2026-05):
//   - "unknown flag: --foo"                            (pflag)
//   - "unknown shorthand flag: 'x' in -x"              (pflag)
//   - "unknown command \"foo\" for ..."                (Cobra)
//   - "required flag \"foo\" not set"                  (Cobra, single missing)
//   - "required flag(s) \"foo\" not set"               (Cobra, multiple missing)
//   - "flag needs an argument: --foo"                  (pflag, missing value)
//   - "invalid argument \"x\" for \"--y\" flag: ..."   (pflag, parse failure)
//
// Cobra emits the singular form ("required flag") when exactly one
// MarkFlagRequired flag is missing, and the plural form ("required
// flag(s)") only when multiple are missing on the same command. Both
// shapes must be anchored to avoid matching app-level errors that
// happen to mention "required flag" as prose; the trailing space + quote
// (`required flag "`) is the literal punctuation cobra emits.
//
// Returns false for nil err.
func isCobraUsageError(err error) bool {
	if err == nil {
		return false
	}
	msg := err.Error()
	return strings.HasPrefix(msg, "unknown flag") ||
		strings.HasPrefix(msg, "unknown shorthand flag") ||
		strings.HasPrefix(msg, "unknown command") ||
		strings.HasPrefix(msg, `required flag "`) ||
		strings.HasPrefix(msg, `required flag(s) "`) ||
		strings.HasPrefix(msg, "flag needs an argument:") ||
		strings.HasPrefix(msg, `invalid argument "`)
}

func newRootCmd(flags *rootFlags) *cobra.Command {
	rootCmd := &cobra.Command{
		Use:   "autotask-cli",
		Short: `Autotask CLI — Every Autotask entity at the command line, plus a local SQLite mirror that answers ticket-aging, workload, unbilled-tim…`,
		Long: `Autotask CLI — Every Autotask entity at the command line, plus a local SQLite mirror that answers ticket-aging, workload, unbilled-tim…

Highlights (not in the official API docs):
  • ticket-aging   Bucket open tickets by age so you see how stale the service desk is at a glance.
  • workload   See which technicians are overloaded by open ticket and task hours before you assign more.
  • unbilled   Surface approved time entries that haven't been invoiced yet — money on the table.
  • sla-breaches   List open tickets past their due date or overdue for first response.
  • contract-burn   Show how much of each contract's hours/blocks are consumed versus purchased.
  • stale   Find tickets or projects with no activity in N days.
  • company-360   One view of a company's tickets, contacts, contracts, config items, and opportunities.
  • project-health   Flag projects with overdue tasks or past end dates.
  • since   See what changed across tickets in the last N hours or days.
  • triage   Rank open unassigned tickets by priority and age into a workable queue.
  • reconcile   One month-end table: approved time vs invoiced, contract blocks consumed vs purchased, and the money left on the table.
  • account-brief   What changed on one account since a point in time — new tickets, contract burn movement, opportunities, stale items.
  • retainer   Block-hour contracts ranked by percent consumed, with projected run-out dates from recent burn rate.
  • data-gaps   Find tickets with no contract, time entries with no ticket, contacts and config items with no company.
  • picklist   Print the label-to-ID map for any picklist field (status, priority, queue) from cached field metadata.

Agent mode: add --agent to any command for JSON output + non-interactive mode.
Health check: run 'autotask-cli doctor' to verify auth and connectivity.
See README.md or the bundled SKILL.md for recipes.`,
		SilenceUsage: true,
		Version:      version,
	}
	rootCmd.SetVersionTemplate("autotask-cli {{ .Version }}\n")

	rootCmd.PersistentFlags().BoolVar(&flags.asJSON, "json", false, "Output as JSON")
	rootCmd.PersistentFlags().BoolVar(&flags.compact, "compact", false, "Return only key fields (id, name, status, timestamps) for minimal token usage")
	rootCmd.PersistentFlags().BoolVar(&flags.csv, "csv", false, "Output as CSV (table and array responses)")
	rootCmd.PersistentFlags().BoolVar(&flags.plain, "plain", false, "Output as plain tab-separated text")
	rootCmd.PersistentFlags().BoolVar(&flags.quiet, "quiet", false, "Bare output, one value per line")
	rootCmd.PersistentFlags().StringVar(&flags.configPath, "config", "", "Config file path")
	rootCmd.PersistentFlags().DurationVar(&flags.timeout, "timeout", 60*time.Second, "Request timeout")
	rootCmd.PersistentFlags().BoolVar(&flags.dryRun, "dry-run", false, "Show request without sending")
	rootCmd.PersistentFlags().BoolVar(&flags.noCache, "no-cache", false, "Bypass response cache")
	rootCmd.PersistentFlags().BoolVar(&flags.noInput, "no-input", false, "Disable all interactive prompts (for CI/agents)")
	rootCmd.PersistentFlags().BoolVar(&flags.idempotent, "idempotent", false, "Treat already-existing create results as a successful no-op")
	rootCmd.PersistentFlags().BoolVar(&flags.ignoreMissing, "ignore-missing", false, "Treat missing delete targets as a successful no-op")
	rootCmd.PersistentFlags().StringVar(&flags.selectFields, "select", "", "Comma-separated fields to include in output (e.g. --select id,name,status)")
	rootCmd.PersistentFlags().BoolVar(&flags.yes, "yes", false, "Skip confirmation prompts (for agents and scripts)")
	rootCmd.PersistentFlags().BoolVar(&noColor, "no-color", false, "Disable colored output")
	rootCmd.PersistentFlags().BoolVar(&humanFriendly, "human-friendly", false, "Enable colored output and rich formatting")
	rootCmd.PersistentFlags().BoolVar(&flags.agent, "agent", false, "Set all agent-friendly defaults (--json --compact --no-input --no-color --yes)")
	rootCmd.PersistentFlags().BoolVar(&flags.allowPartialFailure, "allow-partial-failure", false, "Downgrade response-body partial-failure (e.g. partialFailureError) to a warning instead of a non-zero exit")
	rootCmd.PersistentFlags().StringVar(&flags.dataSource, "data-source", "auto", "Data source for read commands: auto (live with local fallback), live (API only), local (synced data only)")
	rootCmd.PersistentFlags().DurationVar(&flags.maxAge, "max-age", 30*time.Minute, "Maximum acceptable age of local-store data before a stderr hint suggests sync; 0 disables")
	rootCmd.PersistentFlags().StringVar(&flags.profileName, "profile", "", "Apply values from a saved profile (see 'autotask-cli profile list')")
	rootCmd.PersistentFlags().StringVar(&flags.deliverSpec, "deliver", "", "Route output to a sink: stdout (default), file:<path>, webhook:<url>")
	rootCmd.PersistentFlags().Float64Var(&flags.rateLimit, "rate-limit", 0, "Max requests per second (0 to disable)")

	rootCmd.PersistentPreRunE = func(cmd *cobra.Command, args []string) error {
		if flags.deliverSpec != "" {
			sink, err := ParseDeliverSink(flags.deliverSpec)
			if err != nil {
				return err
			}
			flags.deliverSink = sink
			if sink.Scheme != "stdout" && sink.Scheme != "" {
				flags.deliverBuf = &bytes.Buffer{}
				cmd.SetOut(io.MultiWriter(os.Stdout, flags.deliverBuf))
			}
		}
		if flags.profileName != "" {
			profile, err := GetProfile(flags.profileName)
			if err != nil {
				return err
			}
			if profile == nil {
				available := ListProfileNames()
				if len(available) == 0 {
					return fmt.Errorf("profile %q not found (no profiles saved yet; run '%s profile save <name> --<flag> <value>')", flags.profileName, cmd.Root().Name())
				}
				return fmt.Errorf("profile %q not found; available: %s", flags.profileName, strings.Join(available, ", "))
			}
			if err := ApplyProfileToFlags(cmd, profile); err != nil {
				return err
			}
		}
		if flags.agent {
			if !cmd.Flags().Changed("json") {
				flags.asJSON = true
			}
			if !cmd.Flags().Changed("compact") {
				flags.compact = true
			}
			if !cmd.Flags().Changed("no-input") {
				flags.noInput = true
			}
			if !cmd.Flags().Changed("yes") {
				flags.yes = true
			}
			if !cmd.Flags().Changed("no-color") {
				noColor = true
			}
		}
		switch flags.dataSource {
		case "auto", "live", "local":
			// valid
		default:
			return fmt.Errorf("invalid --data-source value %q: must be auto, live, or local", flags.dataSource)
		}
		return nil
	}
	rootCmd.AddCommand(newAppointmentsCmd(flags))
	rootCmd.AddCommand(newBillingCodesCmd(flags))
	rootCmd.AddCommand(newCompaniesCmd(flags))
	rootCmd.AddCommand(newCompanyNotesCmd(flags))
	rootCmd.AddCommand(newConfigurationItemsCmd(flags))
	rootCmd.AddCommand(newContactGroupsCmd(flags))
	rootCmd.AddCommand(newContactsCmd(flags))
	rootCmd.AddCommand(newContractBlocksCmd(flags))
	rootCmd.AddCommand(newContractNotesCmd(flags))
	rootCmd.AddCommand(newContractRatesCmd(flags))
	rootCmd.AddCommand(newContractServiceUnitsCmd(flags))
	rootCmd.AddCommand(newContractServicesCmd(flags))
	rootCmd.AddCommand(newContractsCmd(flags))
	rootCmd.AddCommand(newCountriesCmd(flags))
	rootCmd.AddCommand(newCurrenciesCmd(flags))
	rootCmd.AddCommand(newDepartmentsCmd(flags))
	rootCmd.AddCommand(newHolidaysCmd(flags))
	rootCmd.AddCommand(newInvoicesCmd(flags))
	rootCmd.AddCommand(newOpportunitiesCmd(flags))
	rootCmd.AddCommand(newPhasesCmd(flags))
	rootCmd.AddCommand(newProductsCmd(flags))
	rootCmd.AddCommand(newProjectChargesCmd(flags))
	rootCmd.AddCommand(newProjectNotesCmd(flags))
	rootCmd.AddCommand(newProjectsCmd(flags))
	rootCmd.AddCommand(newPurchaseOrdersCmd(flags))
	rootCmd.AddCommand(newQuotesCmd(flags))
	rootCmd.AddCommand(newResourceRolesCmd(flags))
	rootCmd.AddCommand(newResourcesCmd(flags))
	rootCmd.AddCommand(newRolesCmd(flags))
	rootCmd.AddCommand(newServiceBundlesCmd(flags))
	rootCmd.AddCommand(newServiceCallsCmd(flags))
	rootCmd.AddCommand(newServicesCmd(flags))
	rootCmd.AddCommand(newSkillsCmd(flags))
	rootCmd.AddCommand(newSubscriptionsCmd(flags))
	rootCmd.AddCommand(newTaskNotesCmd(flags))
	rootCmd.AddCommand(newTasksCmd(flags))
	rootCmd.AddCommand(newTicketCategoriesCmd(flags))
	rootCmd.AddCommand(newTicketChargesCmd(flags))
	rootCmd.AddCommand(newTicketHistoryCmd(flags))
	rootCmd.AddCommand(newTicketNotesCmd(flags))
	rootCmd.AddCommand(newTicketSecondaryResourcesCmd(flags))
	rootCmd.AddCommand(newTicketsCmd(flags))
	rootCmd.AddCommand(newTimeEntriesCmd(flags))
	rootCmd.AddCommand(newDoctorCmd(flags))
	rootCmd.AddCommand(newAuthCmd(flags))
	rootCmd.AddCommand(newAgentContextCmd(rootCmd))
	rootCmd.AddCommand(newProfileCmd(flags))
	rootCmd.AddCommand(newFeedbackCmd(flags))
	rootCmd.AddCommand(newWhichCmd(flags))
	rootCmd.AddCommand(newImportCmd(flags))
	rootCmd.AddCommand(newSearchCmd(flags))
	rootCmd.AddCommand(newSyncCmd(flags))
	rootCmd.AddCommand(newTailCmd(flags))
	rootCmd.AddCommand(newAnalyticsCmd(flags))
	rootCmd.AddCommand(newWorkflowCmd(flags))
	// NOTE: the generator emits both a generic data-layer `newStaleCmd`
	// (pm_stale.go, Use:"stale", flags --days/--team) and the novel
	// `newNovelStaleCmd` (Use:"stale", flags --days/--entity). Both would
	// register with the same Use; the first-added wins and the second is a
	// dead duplicate (the pp-novel-stub-vs-handbuilt-collision trap). The
	// absorb manifest's transcendence spec is `stale [--days N] [--entity
	// tickets|projects]`, so the novel implementation is the one we keep;
	// the generic pm_stale registration is dropped (and pm_stale.go removed)
	// to avoid the dup-cobra collision.
	rootCmd.AddCommand(newOrphansCmd(flags))
	rootCmd.AddCommand(newLoadCmd(flags))
	rootCmd.AddCommand(newNovelAccountBriefCmd(flags))
	rootCmd.AddCommand(newNovelCompany360Cmd(flags))
	rootCmd.AddCommand(newNovelContractBurnCmd(flags))
	rootCmd.AddCommand(newNovelDataGapsCmd(flags))
	rootCmd.AddCommand(newNovelPicklistCmd(flags))
	rootCmd.AddCommand(newNovelProjectHealthCmd(flags))
	rootCmd.AddCommand(newNovelReconcileCmd(flags))
	rootCmd.AddCommand(newNovelRetainerCmd(flags))
	rootCmd.AddCommand(newNovelSinceCmd(flags))
	rootCmd.AddCommand(newNovelSlaBreachesCmd(flags))
	rootCmd.AddCommand(newNovelStaleCmd(flags))
	rootCmd.AddCommand(newNovelTicketAgingCmd(flags))
	rootCmd.AddCommand(newNovelTriageCmd(flags))
	rootCmd.AddCommand(newNovelUnbilledCmd(flags))
	rootCmd.AddCommand(newNovelWorkloadCmd(flags))
	rootCmd.AddCommand(newAPICmd(flags))
	rootCmd.AddCommand(newAutotaskPsaVersionPromotedCmd(flags))
	rootCmd.AddCommand(newZoneCmd(flags))
	rootCmd.AddCommand(newVersionCmd())

	return rootCmd
}

func ExitCode(err error) int {
	var codeErr *cliError
	if As(err, &codeErr) {
		return codeErr.code
	}
	return 1
}

func (f *rootFlags) newClient() (*client.Client, error) {
	cfg, err := config.Load(f.configPath)
	if err != nil {
		return nil, configErr(err)
	}
	c := client.New(cfg, f.timeout, f.rateLimit)
	c.DryRun = f.dryRun
	c.NoCache = f.noCache
	return c, nil
}

func (f *rootFlags) printJSON(w *cobra.Command, v any) error {
	return printJSONFiltered(w.OutOrStdout(), v, f)
}

func (f *rootFlags) printTable(w *cobra.Command, headers []string, rows [][]string) error {
	if f.asJSON {
		return fmt.Errorf("use printJSON for JSON output")
	}
	tw := tabwriter.NewWriter(w.OutOrStdout(), 2, 4, 2, ' ', 0)
	header := ""
	for i, h := range headers {
		if i > 0 {
			header += "\t"
		}
		header += h
	}
	fmt.Fprintln(tw, header)
	for _, row := range rows {
		line := ""
		for i, cell := range row {
			if i > 0 {
				line += "\t"
			}
			line += cell
		}
		fmt.Fprintln(tw, line)
	}
	return tw.Flush()
}
