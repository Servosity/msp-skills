// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBillingReadClientUsageCmd(flags *rootFlags) *cobra.Command {
	var flagFilterFromDate string
	var flagFilterThruDate string
	var flagTenants string

	cmd := &cobra.Command{
		Use:         "read-client-usage",
		Aliases:     []string{"list"},
		Short:       "Use the Read Client Usage API to pull a summary of a client’s (and client’s children if a multi-client)",
		Example:     "  auvik-cli billing read-client-usage --filter-from-date 2019-06-01 --filter-thru-date 2019-06-30",
		Annotations: map[string]string{"pp:endpoint": "billing.read-client-usage", "pp:method": "GET", "pp:path": "/v1/billing/usage/client", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !cmd.Flags().Changed("filter-from-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "filter-from-date")
			}
			if !cmd.Flags().Changed("filter-thru-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "filter-thru-date")
			}
			path := "/v1/billing/usage/client"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterFromDate != "" {
				params["filter[fromDate]"] = formatCLIParamValue(flagFilterFromDate)
			}
			if flagFilterThruDate != "" {
				params["filter[thruDate]"] = formatCLIParamValue(flagFilterThruDate)
			}
			if flagTenants != "" {
				params["tenants"] = formatCLIParamValue(flagTenants)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "billing", false, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"asmUserUsage": true, "billableDays": true, "clientUsage": true, "deviceUsage": true, "domainPrefix": true, "id": true, "maxMonitoredServers": true, "maxMonitoredWorkstations": true, "monitoredServers": true, "monitoredWorkstations": true, "usagePeriod": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"asmUserUsage": true, "billableDays": true, "clientUsage": true, "deviceUsage": true, "domainPrefix": true, "id": true, "maxMonitoredServers": true, "maxMonitoredWorkstations": true, "monitoredServers": true, "monitoredWorkstations": true, "usagePeriod": true})
		},
	}
	cmd.Flags().StringVar(&flagFilterFromDate, "filter-from-date", "", "Date from which you want to query")
	cmd.Flags().StringVar(&flagFilterThruDate, "filter-thru-date", "", "Date to which you want to query")
	cmd.Flags().StringVar(&flagTenants, "tenants", "", "Comma delimited list of tenant IDs to request info from.")

	return cmd
}
