// Copyright 2026 Servosity Inc. and msp-skills contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInventoryReadMultipleDeviceDetailsCmd(flags *rootFlags) *cobra.Command {
	var flagFilterManageStatus bool
	var flagFilterDiscoverySNMP string
	var flagFilterDiscoveryWMI string
	var flagFilterDiscoveryLogin string
	var flagFilterDiscoveryVMware string
	var flagFilterTrafficInsightsStatus string
	var flagTenants string
	var flagPageFirst float64
	var flagPageAfter string
	var flagPageLast float64
	var flagPageBefore string

	cmd := &cobra.Command{
		Use:         "read-multiple-device-details",
		Short:       "Use the Read Multiple Devices’ Details API to pull extra collected information about the various devices Auvik has",
		Example:     "  auvik-cli inventory read-multiple-device-details",
		Annotations: map[string]string{"pp:endpoint": "inventory.read-multiple-device-details", "pp:method": "GET", "pp:path": "/v1/inventory/device/detail", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("filter-discovery-snmp") {
				allowedFilterDiscoverySNMP := []string{"disabled", "determining", "notSupported", "notAuthorized", "authorizing", "authorized", "privileged"}
				validFilterDiscoverySNMP := false
				for _, v := range allowedFilterDiscoverySNMP {
					if flagFilterDiscoverySNMP == v {
						validFilterDiscoverySNMP = true
						break
					}
				}
				if !validFilterDiscoverySNMP {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterDiscoverySNMP, "filter-discovery-snmp", allowedFilterDiscoverySNMP)
				}
			}
			if cmd.Flags().Changed("filter-discovery-wmi") {
				allowedFilterDiscoveryWMI := []string{"disabled", "determining", "notSupported", "notAuthorized", "authorizing", "authorized", "privileged"}
				validFilterDiscoveryWMI := false
				for _, v := range allowedFilterDiscoveryWMI {
					if flagFilterDiscoveryWMI == v {
						validFilterDiscoveryWMI = true
						break
					}
				}
				if !validFilterDiscoveryWMI {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterDiscoveryWMI, "filter-discovery-wmi", allowedFilterDiscoveryWMI)
				}
			}
			if cmd.Flags().Changed("filter-discovery-login") {
				allowedFilterDiscoveryLogin := []string{"disabled", "determining", "notSupported", "notAuthorized", "authorizing", "authorized", "privileged"}
				validFilterDiscoveryLogin := false
				for _, v := range allowedFilterDiscoveryLogin {
					if flagFilterDiscoveryLogin == v {
						validFilterDiscoveryLogin = true
						break
					}
				}
				if !validFilterDiscoveryLogin {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterDiscoveryLogin, "filter-discovery-login", allowedFilterDiscoveryLogin)
				}
			}
			if cmd.Flags().Changed("filter-discovery-v-mware") {
				allowedFilterDiscoveryVMware := []string{"disabled", "determining", "notSupported", "notAuthorized", "authorizing", "authorized", "privileged"}
				validFilterDiscoveryVMware := false
				for _, v := range allowedFilterDiscoveryVMware {
					if flagFilterDiscoveryVMware == v {
						validFilterDiscoveryVMware = true
						break
					}
				}
				if !validFilterDiscoveryVMware {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterDiscoveryVMware, "filter-discovery-v-mware", allowedFilterDiscoveryVMware)
				}
			}
			if cmd.Flags().Changed("filter-traffic-insights-status") {
				allowedFilterTrafficInsightsStatus := []string{"notDetected", "detected", "notApproved", "approved", "linking", "linkingFailed", "forwarding"}
				validFilterTrafficInsightsStatus := false
				for _, v := range allowedFilterTrafficInsightsStatus {
					if flagFilterTrafficInsightsStatus == v {
						validFilterTrafficInsightsStatus = true
						break
					}
				}
				if !validFilterTrafficInsightsStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterTrafficInsightsStatus, "filter-traffic-insights-status", allowedFilterTrafficInsightsStatus)
				}
			}
			path := "/v1/inventory/device/detail"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterManageStatus != false {
				params["filter[manageStatus]"] = formatCLIParamValue(flagFilterManageStatus)
			}
			if flagFilterDiscoverySNMP != "" {
				params["filter[discoverySNMP]"] = formatCLIParamValue(flagFilterDiscoverySNMP)
			}
			if flagFilterDiscoveryWMI != "" {
				params["filter[discoveryWMI]"] = formatCLIParamValue(flagFilterDiscoveryWMI)
			}
			if flagFilterDiscoveryLogin != "" {
				params["filter[discoveryLogin]"] = formatCLIParamValue(flagFilterDiscoveryLogin)
			}
			if flagFilterDiscoveryVMware != "" {
				params["filter[discoveryVMware]"] = formatCLIParamValue(flagFilterDiscoveryVMware)
			}
			if flagFilterTrafficInsightsStatus != "" {
				params["filter[trafficInsightsStatus]"] = formatCLIParamValue(flagFilterTrafficInsightsStatus)
			}
			if flagTenants != "" {
				params["tenants"] = formatCLIParamValue(flagTenants)
			}
			if flagPageFirst != 0.0 {
				params["page[first]"] = formatCLIParamValue(flagPageFirst)
			}
			if flagPageAfter != "" {
				params["page[after]"] = formatCLIParamValue(flagPageAfter)
			}
			if flagPageLast != 0.0 {
				params["page[last]"] = formatCLIParamValue(flagPageLast)
			}
			if flagPageBefore != "" {
				params["page[before]"] = formatCLIParamValue(flagPageBefore)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "inventory", false, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"discoveryStatus": true, "id": true, "manageStatus": true, "trafficInsightsStatus": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"discoveryStatus": true, "id": true, "manageStatus": true, "trafficInsightsStatus": true})
		},
	}
	cmd.Flags().BoolVar(&flagFilterManageStatus, "filter-manage-status", false, "Filter by managed status")
	cmd.Flags().StringVar(&flagFilterDiscoverySNMP, "filter-discovery-snmp", "", "Filter by the device’s SNMP discovery status. (one of: disabled, determining, notSupported, notAuthorized, authorizing, authorized, privileged)")
	cmd.Flags().StringVar(&flagFilterDiscoveryWMI, "filter-discovery-wmi", "", "Filter by the device’s WMI discovery status. (one of: disabled, determining, notSupported, notAuthorized, authorizing, authorized, privileged)")
	cmd.Flags().StringVar(&flagFilterDiscoveryLogin, "filter-discovery-login", "", "Filter by the device’s Login discovery status. (one of: disabled, determining, notSupported, notAuthorized, authorizing, authorized, privileged)")
	cmd.Flags().StringVar(&flagFilterDiscoveryVMware, "filter-discovery-v-mware", "", "Filter by the device’s VMware discovery status. (one of: disabled, determining, notSupported, notAuthorized, authorizing, authorized, privileged)")
	cmd.Flags().StringVar(&flagFilterTrafficInsightsStatus, "filter-traffic-insights-status", "", "Filter by the device’s TrafficInsights status. (one of: notDetected, detected, notApproved, approved, linking, linkingFailed, forwarding)")
	cmd.Flags().StringVar(&flagTenants, "tenants", "", "Comma delimited list of tenant IDs to request info from.")
	cmd.Flags().Float64Var(&flagPageFirst, "page-first", 100.000000, "For paginated responses, the first N elements will be returned. Used in combination with page[after].")
	cmd.Flags().StringVar(&flagPageAfter, "page-after", "", "Cursor after which elements will be returned as a page. The page size is provided by page[first].")
	cmd.Flags().Float64Var(&flagPageLast, "page-last", 100.000000, "For paginated responses, the last N services will be returned. Used in combination with page[before].")
	cmd.Flags().StringVar(&flagPageBefore, "page-before", "", "Cursor before which elements will be returned as a page. The page size is provided by page[last].")

	return cmd
}
