// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInventoryReadMultipleDeviceLifecycleCmd(flags *rootFlags) *cobra.Command {
	var flagFilterSalesAvailability string
	var flagFilterSoftwareMaintenanceStatus string
	var flagFilterSecuritySoftwareMaintenanceStatus string
	var flagFilterLastSupportStatus string
	var flagTenants string
	var flagPageFirst float64
	var flagPageAfter string
	var flagPageLast float64
	var flagPageBefore string

	cmd := &cobra.Command{
		Use:         "read-multiple-device-lifecycle",
		Short:       "Use the Read Multiple Devices’ Lifecycle API to pull the collected lifecycle information about the various devices",
		Example:     "  auvik-cli inventory read-multiple-device-lifecycle",
		Annotations: map[string]string{"pp:endpoint": "inventory.read-multiple-device-lifecycle", "pp:method": "GET", "pp:path": "/v1/inventory/device/lifecycle", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("filter-sales-availability") {
				allowedFilterSalesAvailability := []string{"covered", "available", "expired", "securityOnly", "unpublished", "empty"}
				validFilterSalesAvailability := false
				for _, v := range allowedFilterSalesAvailability {
					if flagFilterSalesAvailability == v {
						validFilterSalesAvailability = true
						break
					}
				}
				if !validFilterSalesAvailability {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterSalesAvailability, "filter-sales-availability", allowedFilterSalesAvailability)
				}
			}
			if cmd.Flags().Changed("filter-software-maintenance-status") {
				allowedFilterSoftwareMaintenanceStatus := []string{"covered", "available", "expired", "securityOnly", "unpublished", "empty"}
				validFilterSoftwareMaintenanceStatus := false
				for _, v := range allowedFilterSoftwareMaintenanceStatus {
					if flagFilterSoftwareMaintenanceStatus == v {
						validFilterSoftwareMaintenanceStatus = true
						break
					}
				}
				if !validFilterSoftwareMaintenanceStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterSoftwareMaintenanceStatus, "filter-software-maintenance-status", allowedFilterSoftwareMaintenanceStatus)
				}
			}
			if cmd.Flags().Changed("filter-security-software-maintenance-status") {
				allowedFilterSecuritySoftwareMaintenanceStatus := []string{"covered", "available", "expired", "securityOnly", "unpublished", "empty"}
				validFilterSecuritySoftwareMaintenanceStatus := false
				for _, v := range allowedFilterSecuritySoftwareMaintenanceStatus {
					if flagFilterSecuritySoftwareMaintenanceStatus == v {
						validFilterSecuritySoftwareMaintenanceStatus = true
						break
					}
				}
				if !validFilterSecuritySoftwareMaintenanceStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterSecuritySoftwareMaintenanceStatus, "filter-security-software-maintenance-status", allowedFilterSecuritySoftwareMaintenanceStatus)
				}
			}
			if cmd.Flags().Changed("filter-last-support-status") {
				allowedFilterLastSupportStatus := []string{"covered", "available", "expired", "securityOnly", "unpublished", "empty"}
				validFilterLastSupportStatus := false
				for _, v := range allowedFilterLastSupportStatus {
					if flagFilterLastSupportStatus == v {
						validFilterLastSupportStatus = true
						break
					}
				}
				if !validFilterLastSupportStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFilterLastSupportStatus, "filter-last-support-status", allowedFilterLastSupportStatus)
				}
			}
			path := "/v1/inventory/device/lifecycle"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagFilterSalesAvailability != "" {
				params["filter[salesAvailability]"] = formatCLIParamValue(flagFilterSalesAvailability)
			}
			if flagFilterSoftwareMaintenanceStatus != "" {
				params["filter[softwareMaintenanceStatus]"] = formatCLIParamValue(flagFilterSoftwareMaintenanceStatus)
			}
			if flagFilterSecuritySoftwareMaintenanceStatus != "" {
				params["filter[securitySoftwareMaintenanceStatus]"] = formatCLIParamValue(flagFilterSecuritySoftwareMaintenanceStatus)
			}
			if flagFilterLastSupportStatus != "" {
				params["filter[lastSupportStatus]"] = formatCLIParamValue(flagFilterLastSupportStatus)
			}
			if flagTenants != "" {
				params["tenants"] = formatCLIParamValue(flagTenants)
			}
			if flagPageFirst != 0.0 {
				params["page[first]"] = formatCLIParamValue(flagPageFirst)
			}
			if flagPageAfter != "" {
				params["page[after]"] = formatCLIParamValue(flagPageAfter)
			}
			if flagPageLast != 0.0 {
				params["page[last]"] = formatCLIParamValue(flagPageLast)
			}
			if flagPageBefore != "" {
				params["page[before]"] = formatCLIParamValue(flagPageBefore)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "inventory", false, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"deviceName": true, "id": true, "lastSupportStatus": true, "salesAvailability": true, "securitySoftwareMaintenanceStatus": true, "softwareMaintenanceStatus": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"deviceName": true, "id": true, "lastSupportStatus": true, "salesAvailability": true, "securitySoftwareMaintenanceStatus": true, "softwareMaintenanceStatus": true})
		},
	}
	cmd.Flags().StringVar(&flagFilterSalesAvailability, "filter-sales-availability", "", "Filter by sales availability. (one of: covered, available, expired, securityOnly, unpublished, empty)")
	cmd.Flags().StringVar(&flagFilterSoftwareMaintenanceStatus, "filter-software-maintenance-status", "", "Filter by software maintenance status. (one of: covered, available, expired, securityOnly, unpublished, empty)")
	cmd.Flags().StringVar(&flagFilterSecuritySoftwareMaintenanceStatus, "filter-security-software-maintenance-status", "", "Filter by security software maintenance status. (one of: covered, available, expired, securityOnly, unpublished, empty)")
	cmd.Flags().StringVar(&flagFilterLastSupportStatus, "filter-last-support-status", "", "Filter by last support status. (one of: covered, available, expired, securityOnly, unpublished, empty)")
	cmd.Flags().StringVar(&flagTenants, "tenants", "", "Comma delimited list of tenant IDs to request info from.")
	cmd.Flags().Float64Var(&flagPageFirst, "page-first", 100.000000, "For paginated responses, the first N elements will be returned. Used in combination with page[after].")
	cmd.Flags().StringVar(&flagPageAfter, "page-after", "", "Cursor after which elements will be returned as a page. The page size is provided by page[first].")
	cmd.Flags().Float64Var(&flagPageLast, "page-last", 100.000000, "For paginated responses, the last N services will be returned. Used in combination with page[before].")
	cmd.Flags().StringVar(&flagPageBefore, "page-before", "", "Cursor before which elements will be returned as a page. The page size is provided by page[last].")

	return cmd
}
