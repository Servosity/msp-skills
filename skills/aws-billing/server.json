{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Servosity/aws-billing-mcp",
  "title": "Amazon Web Services MCP",
  "description": "Get a non-expert from zero to a Slack-delivered, plain-English, waste-flagged AWS bill breakdown",
  "version": "0.1.2",
  "icons": [
    {
      "src": "https://raw.githubusercontent.com/servosity/msp-skills/main/docs/assets/social/aws-billing/aws-billing-512x512.png",
      "mimeType": "image/png",
      "sizes": [
        "512x512"
      ]
    }
  ],
  "repository": {
    "url": "https://github.com/servosity/msp-skills",
    "source": "github",
    "subfolder": "skills/aws-billing"
  },
  "packages": [
    {
      "registryType": "mcpb",
      "identifier": "https://github.com/servosity/msp-skills/releases/download/aws-billing-v0.1.2/aws-billing-mcp.mcpb",
      "version": "0.1.2",
      "fileSha256": "PLACEHOLDER_SHA256_FILLED_BY_CI",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "AWS_ACCESS_KEY_ID",
          "description": "Access key ID for an IAM principal with Cost Explorer read access. Resolved by the AWS SDK's default credential chain.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "AWS_BILLING_BASE_URL",
          "description": "Base URL for the AWS Billing Intelligence API. Leave the prefilled default unless your account uses a different regional or self-hosted host.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://ce.us-east-1.amazonaws.com"
        },
        {
          "name": "AWS_BILLING_SLACK_CHANNEL",
          "description": "Slack channel or DM ID that `report --slack` posts to by default (the --slack-channel flag overrides it). Leave blank to pass the destination on every run.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "AWS_REGION",
          "description": "Region for the EC2, CloudWatch and S3 calls. Cost Explorer is always served from us-east-1 regardless of this value.",
          "isRequired": false,
          "isSecret": false,
          "default": "us-east-1"
        },
        {
          "name": "AWS_SECRET_ACCESS_KEY",
          "description": "Secret access key paired with AWS_ACCESS_KEY_ID.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "AWS_SESSION_TOKEN",
          "description": "Session token, required only when the keys above are temporary STS or SSO credentials. Leave blank for long-lived IAM user keys.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
