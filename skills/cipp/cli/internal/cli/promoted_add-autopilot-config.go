// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAddAutopilotConfigPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAssignto bool
	var bodyAutokeyboard bool
	var bodyCollectHash bool
	var bodyDeploymentMode bool
	var bodyDescription string
	var bodyDeviceNameTemplate string
	var bodyDisplayName string
	var bodyHideChangeAccount bool
	var bodyHidePrivacy bool
	var bodyHideTerms bool
	var bodyNotLocalAdmin bool
	var bodyAllowWhiteGlove bool
	var bodyLanguagesLabel string
	var bodyLanguagesValue string
	var bodySelectedTenants string

	cmd := &cobra.Command{
		Use:         "add-autopilot-config",
		Short:       "Add autopilot config",
		Long:        "Add autopilot config",
		Example:     "  cipp-cli add-autopilot-config",
		Annotations: map[string]string{"pp:endpoint": "add-autopilot-config.create", "pp:method": "POST", "pp:path": "/AddAutopilotConfig"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/AddAutopilotConfig"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if cmd.Flags().Changed("assignto") {
				body["Assignto"] = bodyAssignto
			}
			if cmd.Flags().Changed("autokeyboard") {
				body["Autokeyboard"] = bodyAutokeyboard
			}
			if cmd.Flags().Changed("collect-hash") {
				body["CollectHash"] = bodyCollectHash
			}
			if cmd.Flags().Changed("deployment-mode") {
				body["DeploymentMode"] = bodyDeploymentMode
			}
			if bodyDescription != "" {
				body["Description"] = bodyDescription
			}
			if bodyDeviceNameTemplate != "" {
				body["DeviceNameTemplate"] = bodyDeviceNameTemplate
			}
			if bodyDisplayName != "" {
				body["DisplayName"] = bodyDisplayName
			}
			if cmd.Flags().Changed("hide-change-account") {
				body["HideChangeAccount"] = bodyHideChangeAccount
			}
			if cmd.Flags().Changed("hide-privacy") {
				body["HidePrivacy"] = bodyHidePrivacy
			}
			if cmd.Flags().Changed("hide-terms") {
				body["HideTerms"] = bodyHideTerms
			}
			if cmd.Flags().Changed("not-local-admin") {
				body["NotLocalAdmin"] = bodyNotLocalAdmin
			}
			if cmd.Flags().Changed("allow-white-glove") {
				body["allowWhiteGlove"] = bodyAllowWhiteGlove
			}
			{
				nestedLanguages := map[string]any{}
				if bodyLanguagesLabel != "" {
					nestedLanguages["label"] = bodyLanguagesLabel
				}
				if bodyLanguagesValue != "" {
					nestedLanguages["value"] = bodyLanguagesValue
				}
				if len(nestedLanguages) > 0 {
					body["languages"] = nestedLanguages
				}
			}
			if bodySelectedTenants != "" {
				body["selectedTenants"] = bodySelectedTenants
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "add-autopilot-config", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAssignto, "assignto", false, "Assignto")
	cmd.Flags().BoolVar(&bodyAutokeyboard, "autokeyboard", false, "Autokeyboard")
	cmd.Flags().BoolVar(&bodyCollectHash, "collect-hash", false, "Collect hash")
	cmd.Flags().BoolVar(&bodyDeploymentMode, "deployment-mode", false, "Deployment mode")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyDeviceNameTemplate, "device-name-template", "", "Device name template")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name")
	cmd.Flags().BoolVar(&bodyHideChangeAccount, "hide-change-account", false, "Hide change account")
	cmd.Flags().BoolVar(&bodyHidePrivacy, "hide-privacy", false, "Hide privacy")
	cmd.Flags().BoolVar(&bodyHideTerms, "hide-terms", false, "Hide terms")
	cmd.Flags().BoolVar(&bodyNotLocalAdmin, "not-local-admin", false, "Not local admin")
	cmd.Flags().BoolVar(&bodyAllowWhiteGlove, "allow-white-glove", false, "Allow white glove")
	cmd.Flags().StringVar(&bodyLanguagesLabel, "languages-label", "", "Label")
	cmd.Flags().StringVar(&bodyLanguagesValue, "languages-value", "", "Value")
	cmd.Flags().StringVar(&bodySelectedTenants, "selected-tenants", "", "Selected tenants")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
