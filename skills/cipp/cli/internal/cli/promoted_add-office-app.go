// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAddOfficeAppPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAcceptLicense string
	var bodyAssignTo string
	var bodyCustomGroup string
	var bodyRemoveVersions string
	var bodySharedComputerActivation string
	var bodyArch string
	var bodyCustomXml string
	var bodyExcludedApps string
	var bodyLanguages string
	var bodySelectedTenants string
	var bodyUpdateChannel string
	var bodyUseCustomXml string

	cmd := &cobra.Command{
		Use:         "add-office-app",
		Short:       "Add office app",
		Long:        "Add office app",
		Example:     "  cipp-cli add-office-app",
		Annotations: map[string]string{"pp:endpoint": "add-office-app.create", "pp:method": "POST", "pp:path": "/AddOfficeApp"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/AddOfficeApp"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAcceptLicense != "" {
				body["AcceptLicense"] = bodyAcceptLicense
			}
			if bodyAssignTo != "" {
				body["AssignTo"] = bodyAssignTo
			}
			if bodyCustomGroup != "" {
				body["CustomGroup"] = bodyCustomGroup
			}
			if bodyRemoveVersions != "" {
				body["RemoveVersions"] = bodyRemoveVersions
			}
			if bodySharedComputerActivation != "" {
				body["SharedComputerActivation"] = bodySharedComputerActivation
			}
			if bodyArch != "" {
				body["arch"] = bodyArch
			}
			if bodyCustomXml != "" {
				body["customXml"] = bodyCustomXml
			}
			if bodyExcludedApps != "" {
				body["excludedApps"] = bodyExcludedApps
			}
			if bodyLanguages != "" {
				body["languages"] = bodyLanguages
			}
			if bodySelectedTenants != "" {
				body["selectedTenants"] = bodySelectedTenants
			}
			if bodyUpdateChannel != "" {
				body["updateChannel"] = bodyUpdateChannel
			}
			if bodyUseCustomXml != "" {
				body["useCustomXml"] = bodyUseCustomXml
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "add-office-app", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAcceptLicense, "accept-license", "", "Accept license")
	cmd.Flags().StringVar(&bodyAssignTo, "assign-to", "", "Assign to")
	cmd.Flags().StringVar(&bodyCustomGroup, "custom-group", "", "Custom group")
	cmd.Flags().StringVar(&bodyRemoveVersions, "remove-versions", "", "Remove versions")
	cmd.Flags().StringVar(&bodySharedComputerActivation, "shared-computer-activation", "", "Shared computer activation")
	cmd.Flags().StringVar(&bodyArch, "arch", "", "Arch")
	cmd.Flags().StringVar(&bodyCustomXml, "custom-xml", "", "Custom xml")
	cmd.Flags().StringVar(&bodyExcludedApps, "excluded-apps", "", "Excluded apps")
	cmd.Flags().StringVar(&bodyLanguages, "languages", "", "Languages")
	cmd.Flags().StringVar(&bodySelectedTenants, "selected-tenants", "", "Selected tenants")
	cmd.Flags().StringVar(&bodyUpdateChannel, "update-channel", "", "Update channel")
	cmd.Flags().StringVar(&bodyUseCustomXml, "use-custom-xml", "", "Use custom xml")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
