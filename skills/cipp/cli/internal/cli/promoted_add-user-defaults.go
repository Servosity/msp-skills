// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"cipp-pp-cli/internal/cliutil"
	"github.com/spf13/cobra"
)

func newAddUserDefaultsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyGUID string
	var bodyMustChangePass bool
	var bodyAddedAliases string
	var bodyCity string
	var bodyCompanyName string
	var bodyCopyFromLabel string
	var bodyCopyFromValue string
	var bodyCountry string
	var bodyDefaultForTenant string
	var bodyDepartment string
	var bodyDisplayName string
	var bodyGivenName string
	var bodyJobTitle string
	var bodyLicenses string
	var bodyMobilePhone string
	var bodyOtherMails string
	var bodyPassword string
	var bodyPostalCode string
	var bodyPrimDomainLabel string
	var bodyPrimDomainValue string
	var bodyRemoveLicenses bool
	var bodySetManagerLabel string
	var bodySetManagerValue string
	var bodySetSponsorLabel string
	var bodySetSponsorValue string
	var bodyState string
	var bodyStreetAddress string
	var bodySurname string
	var bodyTemplateName string
	var bodyTenantFilter string
	var bodyUsageLocationLabel string
	var bodyUsageLocationValue string
	var bodyUsernameFormat string

	cmd := &cobra.Command{
		Use:   "add-user-defaults",
		Short: "Add user defaults",
		Long:  "Add user defaults",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cipp-cli add-user-defaults --tenant-filter example-value",
		Annotations: map[string]string{"pp:endpoint": "add-user-defaults.create", "pp:method": "POST", "pp:path": "/AddUserDefaults"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("tenant-filter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/AddUserDefaults"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyGUID != "" {
				body["GUID"] = bodyGUID
			}
			if cmd.Flags().Changed("must-change-pass") {
				body["MustChangePass"] = bodyMustChangePass
			}
			if bodyAddedAliases != "" {
				body["addedAliases"] = bodyAddedAliases
			}
			if bodyCity != "" {
				body["city"] = bodyCity
			}
			if bodyCompanyName != "" {
				body["companyName"] = bodyCompanyName
			}
			{
				nestedCopyFrom := map[string]any{}
				if bodyCopyFromLabel != "" {
					nestedCopyFrom["label"] = bodyCopyFromLabel
				}
				if bodyCopyFromValue != "" {
					nestedCopyFrom["value"] = bodyCopyFromValue
				}
				if len(nestedCopyFrom) > 0 {
					body["copyFrom"] = nestedCopyFrom
				}
			}
			if bodyCountry != "" {
				body["country"] = bodyCountry
			}
			if bodyDefaultForTenant != "" {
				body["defaultForTenant"] = bodyDefaultForTenant
			}
			if bodyDepartment != "" {
				body["department"] = bodyDepartment
			}
			if bodyDisplayName != "" {
				body["displayName"] = bodyDisplayName
			}
			if bodyGivenName != "" {
				body["givenName"] = bodyGivenName
			}
			if bodyJobTitle != "" {
				body["jobTitle"] = bodyJobTitle
			}
			if bodyLicenses != "" {
				body["licenses"] = cliutil.SplitCSV(bodyLicenses)
			}
			if bodyMobilePhone != "" {
				body["mobilePhone"] = bodyMobilePhone
			}
			if bodyOtherMails != "" {
				body["otherMails"] = cliutil.SplitCSV(bodyOtherMails)
			}
			if bodyPassword != "" {
				body["password"] = bodyPassword
			}
			if bodyPostalCode != "" {
				body["postalCode"] = bodyPostalCode
			}
			{
				nestedPrimDomain := map[string]any{}
				if bodyPrimDomainLabel != "" {
					nestedPrimDomain["label"] = bodyPrimDomainLabel
				}
				if bodyPrimDomainValue != "" {
					nestedPrimDomain["value"] = bodyPrimDomainValue
				}
				if len(nestedPrimDomain) > 0 {
					body["primDomain"] = nestedPrimDomain
				}
			}
			if cmd.Flags().Changed("remove-licenses") {
				body["removeLicenses"] = bodyRemoveLicenses
			}
			{
				nestedSetManager := map[string]any{}
				if bodySetManagerLabel != "" {
					nestedSetManager["label"] = bodySetManagerLabel
				}
				if bodySetManagerValue != "" {
					nestedSetManager["value"] = bodySetManagerValue
				}
				if len(nestedSetManager) > 0 {
					body["setManager"] = nestedSetManager
				}
			}
			{
				nestedSetSponsor := map[string]any{}
				if bodySetSponsorLabel != "" {
					nestedSetSponsor["label"] = bodySetSponsorLabel
				}
				if bodySetSponsorValue != "" {
					nestedSetSponsor["value"] = bodySetSponsorValue
				}
				if len(nestedSetSponsor) > 0 {
					body["setSponsor"] = nestedSetSponsor
				}
			}
			if bodyState != "" {
				body["state"] = bodyState
			}
			if bodyStreetAddress != "" {
				body["streetAddress"] = bodyStreetAddress
			}
			if bodySurname != "" {
				body["surname"] = bodySurname
			}
			if bodyTemplateName != "" {
				body["templateName"] = bodyTemplateName
			}
			if bodyTenantFilter != "" {
				body["tenantFilter"] = bodyTenantFilter
			}
			{
				nestedUsageLocation := map[string]any{}
				if bodyUsageLocationLabel != "" {
					nestedUsageLocation["label"] = bodyUsageLocationLabel
				}
				if bodyUsageLocationValue != "" {
					nestedUsageLocation["value"] = bodyUsageLocationValue
				}
				if len(nestedUsageLocation) > 0 {
					body["usageLocation"] = nestedUsageLocation
				}
			}
			if bodyUsernameFormat != "" {
				body["usernameFormat"] = bodyUsernameFormat
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "add-user-defaults", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyGUID, "guid", "", "Guid")
	cmd.Flags().BoolVar(&bodyMustChangePass, "must-change-pass", false, "Must change pass")
	cmd.Flags().StringVar(&bodyAddedAliases, "added-aliases", "", "Additional SMTP aliases, newline-delimited (server splits on n)")
	cmd.Flags().StringVar(&bodyCity, "city", "", "City")
	cmd.Flags().StringVar(&bodyCompanyName, "company-name", "", "Company name")
	cmd.Flags().StringVar(&bodyCopyFromLabel, "copy-from-label", "", "Label")
	cmd.Flags().StringVar(&bodyCopyFromValue, "copy-from-value", "", "Value")
	cmd.Flags().StringVar(&bodyCountry, "country", "", "Country")
	cmd.Flags().StringVar(&bodyDefaultForTenant, "default-for-tenant", "", "Default for tenant")
	cmd.Flags().StringVar(&bodyDepartment, "department", "", "Department")
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name")
	cmd.Flags().StringVar(&bodyGivenName, "given-name", "", "Given name")
	cmd.Flags().StringVar(&bodyJobTitle, "job-title", "", "Job title")
	cmd.Flags().StringVar(&bodyLicenses, "licenses", "", "License SKU IDs to assign")
	cmd.Flags().StringVar(&bodyMobilePhone, "mobile-phone", "", "Mobile phone")
	cmd.Flags().StringVar(&bodyOtherMails, "other-mails", "", "Other mails")
	cmd.Flags().StringVar(&bodyPassword, "password", "", "Password")
	cmd.Flags().StringVar(&bodyPostalCode, "postal-code", "", "Postal code")
	cmd.Flags().StringVar(&bodyPrimDomainLabel, "prim-domain-label", "", "Label")
	cmd.Flags().StringVar(&bodyPrimDomainValue, "prim-domain-value", "", "Value")
	cmd.Flags().BoolVar(&bodyRemoveLicenses, "remove-licenses", false, "Remove licenses")
	cmd.Flags().StringVar(&bodySetManagerLabel, "set-manager-label", "", "Label")
	cmd.Flags().StringVar(&bodySetManagerValue, "set-manager-value", "", "Value")
	cmd.Flags().StringVar(&bodySetSponsorLabel, "set-sponsor-label", "", "Label")
	cmd.Flags().StringVar(&bodySetSponsorValue, "set-sponsor-value", "", "Value")
	cmd.Flags().StringVar(&bodyState, "state", "", "State")
	cmd.Flags().StringVar(&bodyStreetAddress, "street-address", "", "Street address")
	cmd.Flags().StringVar(&bodySurname, "surname", "", "Surname")
	cmd.Flags().StringVar(&bodyTemplateName, "template-name", "", "Template name")
	cmd.Flags().StringVar(&bodyTenantFilter, "tenant-filter", "", "Tenant filter")
	cmd.Flags().StringVar(&bodyUsageLocationLabel, "usage-location-label", "", "Label")
	cmd.Flags().StringVar(&bodyUsageLocationValue, "usage-location-value", "", "Value")
	cmd.Flags().StringVar(&bodyUsernameFormat, "username-format", "", "Username format")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
