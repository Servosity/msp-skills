// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"cipp-pp-cli/internal/cliutil"
	"github.com/spf13/cobra"
)

func newEditEquipmentMailboxPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDisplayName string
	var bodyAllowConflicts bool
	var bodyAllowRecurringMeetings bool
	var bodyAutomateProcessing string
	var bodyBookingWindowInDays int
	var bodyCity string
	var bodyCompany string
	var bodyCountryOrRegion string
	var bodyDepartment string
	var bodyEquipmentId string
	var bodyForwardRequestsToDelegates bool
	var bodyHiddenFromAddressListsEnabled bool
	var bodyMaximumDurationInMinutes int
	var bodyPhone string
	var bodyPostalCode string
	var bodyProcessExternalMeetingMessages bool
	var bodyScheduleOnlyDuringWorkHours bool
	var bodyStateOrProvince string
	var bodyStreetAddress string
	var bodyTags string
	var bodyTenantID string
	var bodyUserPrincipalName string
	var bodyWorkDays string
	var bodyWorkHoursEndTime string
	var bodyWorkHoursStartTime string
	var bodyWorkingHoursTimeZone string

	cmd := &cobra.Command{
		Use:         "edit-equipment-mailbox",
		Short:       "Edit equipment mailbox",
		Long:        "Edit equipment mailbox",
		Example:     "  cipp-cli edit-equipment-mailbox",
		Annotations: map[string]string{"pp:endpoint": "edit-equipment-mailbox.create", "pp:method": "POST", "pp:path": "/EditEquipmentMailbox"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/EditEquipmentMailbox"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDisplayName != "" {
				body["DisplayName"] = bodyDisplayName
			}
			if cmd.Flags().Changed("allow-conflicts") {
				body["allowConflicts"] = bodyAllowConflicts
			}
			if cmd.Flags().Changed("allow-recurring-meetings") {
				body["allowRecurringMeetings"] = bodyAllowRecurringMeetings
			}
			if bodyAutomateProcessing != "" {
				body["automateProcessing"] = bodyAutomateProcessing
			}
			if bodyBookingWindowInDays != 0 {
				body["bookingWindowInDays"] = bodyBookingWindowInDays
			}
			if bodyCity != "" {
				body["city"] = bodyCity
			}
			if bodyCompany != "" {
				body["company"] = bodyCompany
			}
			if bodyCountryOrRegion != "" {
				body["countryOrRegion"] = bodyCountryOrRegion
			}
			if bodyDepartment != "" {
				body["department"] = bodyDepartment
			}
			if bodyEquipmentId != "" {
				body["equipmentId"] = bodyEquipmentId
			}
			if cmd.Flags().Changed("forward-requests-to-delegates") {
				body["forwardRequestsToDelegates"] = bodyForwardRequestsToDelegates
			}
			if cmd.Flags().Changed("hidden-from-address-lists-enabled") {
				body["hiddenFromAddressListsEnabled"] = bodyHiddenFromAddressListsEnabled
			}
			if bodyMaximumDurationInMinutes != 0 {
				body["maximumDurationInMinutes"] = bodyMaximumDurationInMinutes
			}
			if bodyPhone != "" {
				body["phone"] = bodyPhone
			}
			if bodyPostalCode != "" {
				body["postalCode"] = bodyPostalCode
			}
			if cmd.Flags().Changed("process-external-meeting-messages") {
				body["processExternalMeetingMessages"] = bodyProcessExternalMeetingMessages
			}
			if cmd.Flags().Changed("schedule-only-during-work-hours") {
				body["scheduleOnlyDuringWorkHours"] = bodyScheduleOnlyDuringWorkHours
			}
			if bodyStateOrProvince != "" {
				body["stateOrProvince"] = bodyStateOrProvince
			}
			if bodyStreetAddress != "" {
				body["streetAddress"] = bodyStreetAddress
			}
			if bodyTags != "" {
				body["tags"] = cliutil.SplitCSV(bodyTags)
			}
			if bodyTenantID != "" {
				body["tenantID"] = bodyTenantID
			}
			if bodyUserPrincipalName != "" {
				body["userPrincipalName"] = bodyUserPrincipalName
			}
			if bodyWorkDays != "" {
				body["workDays"] = bodyWorkDays
			}
			if bodyWorkHoursEndTime != "" {
				body["workHoursEndTime"] = bodyWorkHoursEndTime
			}
			if bodyWorkHoursStartTime != "" {
				body["workHoursStartTime"] = bodyWorkHoursStartTime
			}
			if bodyWorkingHoursTimeZone != "" {
				body["workingHoursTimeZone"] = bodyWorkingHoursTimeZone
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "edit-equipment-mailbox", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDisplayName, "display-name", "", "Display name")
	cmd.Flags().BoolVar(&bodyAllowConflicts, "allow-conflicts", false, "Allow conflicts")
	cmd.Flags().BoolVar(&bodyAllowRecurringMeetings, "allow-recurring-meetings", false, "Allow recurring meetings")
	cmd.Flags().StringVar(&bodyAutomateProcessing, "automate-processing", "", "Automate processing")
	cmd.Flags().IntVar(&bodyBookingWindowInDays, "booking-window-in-days", 0, "Booking window in days")
	cmd.Flags().StringVar(&bodyCity, "city", "", "City")
	cmd.Flags().StringVar(&bodyCompany, "company", "", "Company")
	cmd.Flags().StringVar(&bodyCountryOrRegion, "country-or-region", "", "Country or region")
	cmd.Flags().StringVar(&bodyDepartment, "department", "", "Department")
	cmd.Flags().StringVar(&bodyEquipmentId, "equipment-id", "", "Equipment id")
	cmd.Flags().BoolVar(&bodyForwardRequestsToDelegates, "forward-requests-to-delegates", false, "Forward requests to delegates")
	cmd.Flags().BoolVar(&bodyHiddenFromAddressListsEnabled, "hidden-from-address-lists-enabled", false, "Hidden from address lists enabled")
	cmd.Flags().IntVar(&bodyMaximumDurationInMinutes, "maximum-duration-in-minutes", 0, "Maximum duration in minutes")
	cmd.Flags().StringVar(&bodyPhone, "phone", "", "Phone")
	cmd.Flags().StringVar(&bodyPostalCode, "postal-code", "", "Postal code")
	cmd.Flags().BoolVar(&bodyProcessExternalMeetingMessages, "process-external-meeting-messages", false, "Process external meeting messages")
	cmd.Flags().BoolVar(&bodyScheduleOnlyDuringWorkHours, "schedule-only-during-work-hours", false, "Schedule only during work hours")
	cmd.Flags().StringVar(&bodyStateOrProvince, "state-or-province", "", "State or province")
	cmd.Flags().StringVar(&bodyStreetAddress, "street-address", "", "Street address")
	cmd.Flags().StringVar(&bodyTags, "tags", "", "Tags")
	cmd.Flags().StringVar(&bodyTenantID, "tenant-id", "", "Tenant id")
	cmd.Flags().StringVar(&bodyUserPrincipalName, "user-principal-name", "", "User principal name")
	cmd.Flags().StringVar(&bodyWorkDays, "work-days", "", "Work days")
	cmd.Flags().StringVar(&bodyWorkHoursEndTime, "work-hours-end-time", "", "Work hours end time")
	cmd.Flags().StringVar(&bodyWorkHoursStartTime, "work-hours-start-time", "", "Work hours start time")
	cmd.Flags().StringVar(&bodyWorkingHoursTimeZone, "working-hours-time-zone", "", "Working hours time zone")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
