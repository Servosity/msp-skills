// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"cipp-pp-cli/internal/cliutil"
	"github.com/spf13/cobra"
)

func newEditJitadminTemplatePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyGUID string
	var bodyDefaultDomainLabel string
	var bodyDefaultDomainValue string
	var bodyDefaultDurationLabel string
	var bodyDefaultDurationValue string
	var bodyDefaultExistingUserLabel string
	var bodyDefaultExistingUserValue string
	var bodyDefaultExpireActionLabel string
	var bodyDefaultExpireActionValue string
	var bodyDefaultFirstName string
	var bodyDefaultForTenant bool
	var bodyDefaultLastName string
	var bodyDefaultNotificationActions string
	var bodyDefaultRolesLabel string
	var bodyDefaultRolesValue string
	var bodyDefaultUserAction string
	var bodyDefaultUserName string
	var bodyGenerateTAPByDefault bool
	var bodyReasonTemplate string
	var bodyTemplateName string
	var bodyTenantFilter string

	cmd := &cobra.Command{
		Use:   "edit-jitadmin-template",
		Short: "Edit jitadmin template",
		Long:  "Edit jitadmin template",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cipp-cli edit-jitadmin-template --tenant-filter example-value",
		Annotations: map[string]string{"pp:endpoint": "edit-jitadmin-template.create", "pp:method": "POST", "pp:path": "/EditJITAdminTemplate"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("tenant-filter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/EditJITAdminTemplate"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyGUID != "" {
				body["GUID"] = bodyGUID
			}
			{
				nestedDefaultDomain := map[string]any{}
				if bodyDefaultDomainLabel != "" {
					nestedDefaultDomain["label"] = bodyDefaultDomainLabel
				}
				if bodyDefaultDomainValue != "" {
					nestedDefaultDomain["value"] = bodyDefaultDomainValue
				}
				if len(nestedDefaultDomain) > 0 {
					body["defaultDomain"] = nestedDefaultDomain
				}
			}
			{
				nestedDefaultDuration := map[string]any{}
				if bodyDefaultDurationLabel != "" {
					nestedDefaultDuration["label"] = bodyDefaultDurationLabel
				}
				if bodyDefaultDurationValue != "" {
					nestedDefaultDuration["value"] = bodyDefaultDurationValue
				}
				if len(nestedDefaultDuration) > 0 {
					body["defaultDuration"] = nestedDefaultDuration
				}
			}
			{
				nestedDefaultExistingUser := map[string]any{}
				if bodyDefaultExistingUserLabel != "" {
					nestedDefaultExistingUser["label"] = bodyDefaultExistingUserLabel
				}
				if bodyDefaultExistingUserValue != "" {
					nestedDefaultExistingUser["value"] = bodyDefaultExistingUserValue
				}
				if len(nestedDefaultExistingUser) > 0 {
					body["defaultExistingUser"] = nestedDefaultExistingUser
				}
			}
			{
				nestedDefaultExpireAction := map[string]any{}
				if bodyDefaultExpireActionLabel != "" {
					nestedDefaultExpireAction["label"] = bodyDefaultExpireActionLabel
				}
				if bodyDefaultExpireActionValue != "" {
					nestedDefaultExpireAction["value"] = bodyDefaultExpireActionValue
				}
				if len(nestedDefaultExpireAction) > 0 {
					body["defaultExpireAction"] = nestedDefaultExpireAction
				}
			}
			if bodyDefaultFirstName != "" {
				body["defaultFirstName"] = bodyDefaultFirstName
			}
			if cmd.Flags().Changed("default-for-tenant") {
				body["defaultForTenant"] = bodyDefaultForTenant
			}
			if bodyDefaultLastName != "" {
				body["defaultLastName"] = bodyDefaultLastName
			}
			if bodyDefaultNotificationActions != "" {
				body["defaultNotificationActions"] = cliutil.SplitCSV(bodyDefaultNotificationActions)
			}
			{
				nestedDefaultRoles := map[string]any{}
				if bodyDefaultRolesLabel != "" {
					nestedDefaultRoles["label"] = bodyDefaultRolesLabel
				}
				if bodyDefaultRolesValue != "" {
					nestedDefaultRoles["value"] = bodyDefaultRolesValue
				}
				if len(nestedDefaultRoles) > 0 {
					body["defaultRoles"] = nestedDefaultRoles
				}
			}
			if bodyDefaultUserAction != "" {
				body["defaultUserAction"] = bodyDefaultUserAction
			}
			if bodyDefaultUserName != "" {
				body["defaultUserName"] = bodyDefaultUserName
			}
			if cmd.Flags().Changed("generate-tap-by-default") {
				body["generateTAPByDefault"] = bodyGenerateTAPByDefault
			}
			if bodyReasonTemplate != "" {
				body["reasonTemplate"] = bodyReasonTemplate
			}
			if bodyTemplateName != "" {
				body["templateName"] = bodyTemplateName
			}
			if bodyTenantFilter != "" {
				body["tenantFilter"] = bodyTenantFilter
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "edit-jitadmin-template", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyGUID, "guid", "", "Guid")
	cmd.Flags().StringVar(&bodyDefaultDomainLabel, "default-domain-label", "", "Label")
	cmd.Flags().StringVar(&bodyDefaultDomainValue, "default-domain-value", "", "Value")
	cmd.Flags().StringVar(&bodyDefaultDurationLabel, "default-duration-label", "", "Label")
	cmd.Flags().StringVar(&bodyDefaultDurationValue, "default-duration-value", "", "Value")
	cmd.Flags().StringVar(&bodyDefaultExistingUserLabel, "default-existing-user-label", "", "Label")
	cmd.Flags().StringVar(&bodyDefaultExistingUserValue, "default-existing-user-value", "", "Value")
	cmd.Flags().StringVar(&bodyDefaultExpireActionLabel, "default-expire-action-label", "", "Label")
	cmd.Flags().StringVar(&bodyDefaultExpireActionValue, "default-expire-action-value", "", "Value")
	cmd.Flags().StringVar(&bodyDefaultFirstName, "default-first-name", "", "Default first name")
	cmd.Flags().BoolVar(&bodyDefaultForTenant, "default-for-tenant", false, "Default for tenant")
	cmd.Flags().StringVar(&bodyDefaultLastName, "default-last-name", "", "Default last name")
	cmd.Flags().StringVar(&bodyDefaultNotificationActions, "default-notification-actions", "", "Default notification actions")
	cmd.Flags().StringVar(&bodyDefaultRolesLabel, "default-roles-label", "", "Label")
	cmd.Flags().StringVar(&bodyDefaultRolesValue, "default-roles-value", "", "Value")
	cmd.Flags().StringVar(&bodyDefaultUserAction, "default-user-action", "", "Default user action")
	cmd.Flags().StringVar(&bodyDefaultUserName, "default-user-name", "", "Default user name")
	cmd.Flags().BoolVar(&bodyGenerateTAPByDefault, "generate-tap-by-default", false, "Generate tapby default")
	cmd.Flags().StringVar(&bodyReasonTemplate, "reason-template", "", "Reason template")
	cmd.Flags().StringVar(&bodyTemplateName, "template-name", "", "Template name")
	cmd.Flags().StringVar(&bodyTenantFilter, "tenant-filter", "", "Tenant filter")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
