// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecCommunityRepoPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAction string
	var bodyBranch string
	var bodyDescription string
	var bodyFullName string
	var bodyGUID string
	var bodyId string
	var bodyMessage string
	var bodyPath string
	var bodyPrivate bool
	var bodyIncludeforks bool
	var bodyOrgNameLabel string
	var bodyOrgNameValue string
	var bodyPolicySourceLabel string
	var bodyPolicySourceValue string
	var bodyRepoName string
	var bodySearchTermLabel string
	var bodySearchTermValue string

	cmd := &cobra.Command{
		Use:         "exec-community-repo",
		Short:       "This function makes changes to a community repository in table storage .FUNCTIONALITY Entrypoint,AnyTenant .ROLE CIPP.",
		Long:        "This function makes changes to a community repository in table storage .FUNCTIONALITY Entrypoint,AnyTenant .ROLE CIPP.",
		Example:     "  cipp-cli exec-community-repo",
		Annotations: map[string]string{"pp:endpoint": "exec-community-repo.delete", "pp:method": "DELETE", "pp:path": "/ExecCommunityRepo"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecCommunityRepo"
			body := map[string]any{}
			if bodyAction != "" {
				body["Action"] = bodyAction
			}
			if bodyBranch != "" {
				body["Branch"] = bodyBranch
			}
			if bodyDescription != "" {
				body["Description"] = bodyDescription
			}
			if bodyFullName != "" {
				body["FullName"] = bodyFullName
			}
			if bodyGUID != "" {
				body["GUID"] = bodyGUID
			}
			if bodyId != "" {
				body["Id"] = bodyId
			}
			if bodyMessage != "" {
				body["Message"] = bodyMessage
			}
			if bodyPath != "" {
				body["Path"] = bodyPath
			}
			if cmd.Flags().Changed("private") {
				body["Private"] = bodyPrivate
			}
			if cmd.Flags().Changed("includeforks") {
				body["includeforks"] = bodyIncludeforks
			}
			{
				nestedOrgName := map[string]any{}
				if bodyOrgNameLabel != "" {
					nestedOrgName["label"] = bodyOrgNameLabel
				}
				if bodyOrgNameValue != "" {
					nestedOrgName["value"] = bodyOrgNameValue
				}
				if len(nestedOrgName) > 0 {
					body["orgName"] = nestedOrgName
				}
			}
			{
				nestedPolicySource := map[string]any{}
				if bodyPolicySourceLabel != "" {
					nestedPolicySource["label"] = bodyPolicySourceLabel
				}
				if bodyPolicySourceValue != "" {
					nestedPolicySource["value"] = bodyPolicySourceValue
				}
				if len(nestedPolicySource) > 0 {
					body["policySource"] = nestedPolicySource
				}
			}
			if bodyRepoName != "" {
				body["repoName"] = bodyRepoName
			}
			{
				nestedSearchTerm := map[string]any{}
				if bodySearchTermLabel != "" {
					nestedSearchTerm["label"] = bodySearchTermLabel
				}
				if bodySearchTermValue != "" {
					nestedSearchTerm["value"] = bodySearchTermValue
				}
				if len(nestedSearchTerm) > 0 {
					body["searchTerm"] = nestedSearchTerm
				}
			}
			data, _, err := c.DeleteWithBody(cmd.Context(), path, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAction, "action", "", "Action")
	cmd.Flags().StringVar(&bodyBranch, "branch", "", "Branch")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyFullName, "full-name", "", "Full name")
	cmd.Flags().StringVar(&bodyGUID, "guid", "", "Guid")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyMessage, "message", "", "Message")
	cmd.Flags().StringVar(&bodyPath, "path", "", "Path")
	cmd.Flags().BoolVar(&bodyPrivate, "private", false, "Private")
	cmd.Flags().BoolVar(&bodyIncludeforks, "includeforks", false, "Includeforks")
	cmd.Flags().StringVar(&bodyOrgNameLabel, "org-name-label", "", "Label")
	cmd.Flags().StringVar(&bodyOrgNameValue, "org-name-value", "", "Value")
	cmd.Flags().StringVar(&bodyPolicySourceLabel, "policy-source-label", "", "Label")
	cmd.Flags().StringVar(&bodyPolicySourceValue, "policy-source-value", "", "Value")
	cmd.Flags().StringVar(&bodyRepoName, "repo-name", "", "Repo name")
	cmd.Flags().StringVar(&bodySearchTermLabel, "search-term-label", "", "Label")
	cmd.Flags().StringVar(&bodySearchTermValue, "search-term-value", "", "Value")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
