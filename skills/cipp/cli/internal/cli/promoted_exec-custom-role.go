// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecCustomRolePromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAction string
	var bodyAction2 string
	var bodyAllowedTenants string
	var bodyBlockedEndpoints string
	var bodyBlockedTenants string
	var bodyEntraGroup string
	var bodyIpRange string
	var bodyNewRoleName string
	var bodyPermissions string
	var bodyRoleName string

	cmd := &cobra.Command{
		Use:         "exec-custom-role",
		Short:       "Exec custom role",
		Long:        "Exec custom role",
		Example:     "  cipp-cli exec-custom-role",
		Annotations: map[string]string{"pp:endpoint": "exec-custom-role.delete", "pp:method": "DELETE", "pp:path": "/ExecCustomRole"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecCustomRole"
			params := map[string]string{}
			if flagAction != "" {
				params["Action"] = formatCLIParamValue(flagAction)
			}
			body := map[string]any{}
			if bodyAction2 != "" {
				body["Action"] = bodyAction2
			}
			if bodyAllowedTenants != "" {
				body["AllowedTenants"] = bodyAllowedTenants
			}
			if bodyBlockedEndpoints != "" {
				body["BlockedEndpoints"] = bodyBlockedEndpoints
			}
			if bodyBlockedTenants != "" {
				body["BlockedTenants"] = bodyBlockedTenants
			}
			if bodyEntraGroup != "" {
				body["EntraGroup"] = bodyEntraGroup
			}
			if bodyIpRange != "" {
				body["IpRange"] = bodyIpRange
			}
			if bodyNewRoleName != "" {
				body["NewRoleName"] = bodyNewRoleName
			}
			if bodyPermissions != "" {
				body["Permissions"] = bodyPermissions
			}
			if bodyRoleName != "" {
				body["RoleName"] = bodyRoleName
			}
			data, _, err := c.DeleteWithParamsAndBody(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAction, "action", "", "Action")
	cmd.Flags().StringVar(&bodyAction2, "action-2", "", "Action")
	cmd.Flags().StringVar(&bodyAllowedTenants, "allowed-tenants", "", "Allowed tenants")
	cmd.Flags().StringVar(&bodyBlockedEndpoints, "blocked-endpoints", "", "Blocked endpoints")
	cmd.Flags().StringVar(&bodyBlockedTenants, "blocked-tenants", "", "Blocked tenants")
	cmd.Flags().StringVar(&bodyEntraGroup, "entra-group", "", "Entra group")
	cmd.Flags().StringVar(&bodyIpRange, "ip-range", "", "Ip range")
	cmd.Flags().StringVar(&bodyNewRoleName, "new-role-name", "", "New role name")
	cmd.Flags().StringVar(&bodyPermissions, "permissions", "", "Permissions")
	cmd.Flags().StringVar(&bodyRoleName, "role-name", "", "Role name")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
