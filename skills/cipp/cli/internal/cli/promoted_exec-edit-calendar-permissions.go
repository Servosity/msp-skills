// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecEditCalendarPermissionsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagCanViewPrivateItems string
	var flagFolderName string
	var flagPermissions string
	var flagRemoveAccess string
	var flagTenantFilter string
	var flagUserid string
	var flagUserToGetPermissions string
	var bodyCanViewPrivateItems2 string
	var bodyFolderName2 string
	var bodyPermissions2 string
	var bodyRemoveAccess2 string
	var bodyUserToGetPermissions2 string
	var bodyTenantFilter2 string
	var bodyUserid2 string

	cmd := &cobra.Command{
		Use:   "exec-edit-calendar-permissions",
		Short: "Exec edit calendar permissions",
		Long:  "Exec edit calendar permissions",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cipp-cli exec-edit-calendar-permissions --tenant-filter example-value --tenant-filter-2 example-value",
		Annotations: map[string]string{"pp:endpoint": "exec-edit-calendar-permissions.create", "pp:method": "POST", "pp:path": "/ExecEditCalendarPermissions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("tenant-filter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter")
			}
			if !cmd.Flags().Changed("tenant-filter-2") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter-2")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecEditCalendarPermissions"
			params := map[string]string{}
			if flagCanViewPrivateItems != "" {
				params["CanViewPrivateItems"] = formatCLIParamValue(flagCanViewPrivateItems)
			}
			if flagFolderName != "" {
				params["FolderName"] = formatCLIParamValue(flagFolderName)
			}
			if flagPermissions != "" {
				params["Permissions"] = formatCLIParamValue(flagPermissions)
			}
			if flagRemoveAccess != "" {
				params["RemoveAccess"] = formatCLIParamValue(flagRemoveAccess)
			}
			if flagTenantFilter != "" {
				params["tenantFilter"] = formatCLIParamValue(flagTenantFilter)
			}
			if flagUserid != "" {
				params["userid"] = formatCLIParamValue(flagUserid)
			}
			if flagUserToGetPermissions != "" {
				params["UserToGetPermissions"] = formatCLIParamValue(flagUserToGetPermissions)
			}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyCanViewPrivateItems2 != "" {
				body["CanViewPrivateItems"] = bodyCanViewPrivateItems2
			}
			if bodyFolderName2 != "" {
				body["FolderName"] = bodyFolderName2
			}
			if bodyPermissions2 != "" {
				body["Permissions"] = bodyPermissions2
			}
			if bodyRemoveAccess2 != "" {
				body["RemoveAccess"] = bodyRemoveAccess2
			}
			if bodyUserToGetPermissions2 != "" {
				body["UserToGetPermissions"] = bodyUserToGetPermissions2
			}
			if bodyTenantFilter2 != "" {
				body["tenantFilter"] = bodyTenantFilter2
			}
			if bodyUserid2 != "" {
				body["userid"] = bodyUserid2
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "exec-edit-calendar-permissions", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCanViewPrivateItems, "can-view-private-items", "", "Can view private items")
	cmd.Flags().StringVar(&flagFolderName, "folder-name", "", "Folder name")
	cmd.Flags().StringVar(&flagPermissions, "permissions", "", "Permissions")
	cmd.Flags().StringVar(&flagRemoveAccess, "remove-access", "", "Remove access")
	cmd.Flags().StringVar(&flagTenantFilter, "tenant-filter", "", "Target tenant domain or 'AllTenants' for multi-tenant operations.")
	cmd.Flags().StringVar(&flagUserid, "userid", "", "Userid")
	cmd.Flags().StringVar(&flagUserToGetPermissions, "user-to-get-permissions", "", "User to get permissions")
	cmd.Flags().StringVar(&bodyCanViewPrivateItems2, "can-view-private-items-2", "", "Can view private items")
	cmd.Flags().StringVar(&bodyFolderName2, "folder-name-2", "", "Folder name")
	cmd.Flags().StringVar(&bodyPermissions2, "permissions-2", "", "Permissions")
	cmd.Flags().StringVar(&bodyRemoveAccess2, "remove-access-2", "", "Remove access")
	cmd.Flags().StringVar(&bodyUserToGetPermissions2, "user-to-get-permissions-2", "", "User to get permissions")
	cmd.Flags().StringVar(&bodyTenantFilter2, "tenant-filter-2", "", "Tenant filter")
	cmd.Flags().StringVar(&bodyUserid2, "userid-2", "", "Userid")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
