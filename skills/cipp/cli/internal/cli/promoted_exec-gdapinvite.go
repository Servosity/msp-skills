// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecGdapinvitePromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAction string
	var bodyAction2 string
	var bodyInviteId string
	var bodyReference string
	var bodyGdapTemplateLabel string
	var bodyGdapTemplateValue string
	var bodyInviteCount float64
	var bodyRoleDefinitionId string
	var bodyRoleMappingsLabel string
	var bodyRoleMappingsValue string

	cmd := &cobra.Command{
		Use:         "exec-gdapinvite",
		Short:       "Exec gdapinvite",
		Long:        "Exec gdapinvite",
		Example:     "  cipp-cli exec-gdapinvite",
		Annotations: map[string]string{"pp:endpoint": "exec-gdapinvite.delete", "pp:method": "DELETE", "pp:path": "/ExecGDAPInvite"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecGDAPInvite"
			params := map[string]string{}
			if flagAction != "" {
				params["Action"] = formatCLIParamValue(flagAction)
			}
			body := map[string]any{}
			if bodyAction2 != "" {
				body["Action"] = bodyAction2
			}
			if bodyInviteId != "" {
				body["InviteId"] = bodyInviteId
			}
			if bodyReference != "" {
				body["Reference"] = bodyReference
			}
			{
				nestedGdapTemplate := map[string]any{}
				if bodyGdapTemplateLabel != "" {
					nestedGdapTemplate["label"] = bodyGdapTemplateLabel
				}
				if bodyGdapTemplateValue != "" {
					nestedGdapTemplate["value"] = bodyGdapTemplateValue
				}
				if len(nestedGdapTemplate) > 0 {
					body["gdapTemplate"] = nestedGdapTemplate
				}
			}
			if bodyInviteCount != 0.0 {
				body["inviteCount"] = bodyInviteCount
			}
			if bodyRoleDefinitionId != "" {
				body["roleDefinitionId"] = bodyRoleDefinitionId
			}
			{
				nestedRoleMappings := map[string]any{}
				if bodyRoleMappingsLabel != "" {
					nestedRoleMappings["label"] = bodyRoleMappingsLabel
				}
				if bodyRoleMappingsValue != "" {
					nestedRoleMappings["value"] = bodyRoleMappingsValue
				}
				if len(nestedRoleMappings) > 0 {
					body["roleMappings"] = nestedRoleMappings
				}
			}
			data, _, err := c.DeleteWithParamsAndBody(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAction, "action", "", "Action")
	cmd.Flags().StringVar(&bodyAction2, "action-2", "", "Action")
	cmd.Flags().StringVar(&bodyInviteId, "invite-id", "", "Invite id")
	cmd.Flags().StringVar(&bodyReference, "reference", "", "Reference")
	cmd.Flags().StringVar(&bodyGdapTemplateLabel, "gdap-template-label", "", "Label")
	cmd.Flags().StringVar(&bodyGdapTemplateValue, "gdap-template-value", "", "Value")
	cmd.Flags().Float64Var(&bodyInviteCount, "invite-count", 0.0, "Invite count")
	cmd.Flags().StringVar(&bodyRoleDefinitionId, "role-definition-id", "", "Role definition id")
	cmd.Flags().StringVar(&bodyRoleMappingsLabel, "role-mappings-label", "", "Label")
	cmd.Flags().StringVar(&bodyRoleMappingsValue, "role-mappings-value", "", "Value")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
