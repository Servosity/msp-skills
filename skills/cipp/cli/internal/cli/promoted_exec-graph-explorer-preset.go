// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecGraphExplorerPresetPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAsApp bool
	var bodyIsShared bool
	var bodyNoPagination bool
	var bodyReverseTenantLookup bool
	var bodyReverseTenantLookupProperty string
	var bodyAction string
	var bodyEndpoint string
	var bodyName string
	var bodyPreset string
	var bodyReportTemplateLabel string
	var bodyReportTemplateValue string
	var bodyVersionLabel string
	var bodyVersionValue string

	cmd := &cobra.Command{
		Use:         "exec-graph-explorer-preset",
		Short:       "Exec graph explorer preset",
		Long:        "Exec graph explorer preset",
		Example:     "  cipp-cli exec-graph-explorer-preset",
		Annotations: map[string]string{"pp:endpoint": "exec-graph-explorer-preset.delete", "pp:method": "DELETE", "pp:path": "/ExecGraphExplorerPreset"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecGraphExplorerPreset"
			body := map[string]any{}
			if cmd.Flags().Changed("as-app") {
				body["AsApp"] = bodyAsApp
			}
			if cmd.Flags().Changed("is-shared") {
				body["IsShared"] = bodyIsShared
			}
			if cmd.Flags().Changed("no-pagination") {
				body["NoPagination"] = bodyNoPagination
			}
			if cmd.Flags().Changed("reverse-tenant-lookup") {
				body["ReverseTenantLookup"] = bodyReverseTenantLookup
			}
			if bodyReverseTenantLookupProperty != "" {
				body["ReverseTenantLookupProperty"] = bodyReverseTenantLookupProperty
			}
			if bodyAction != "" {
				body["action"] = bodyAction
			}
			if bodyEndpoint != "" {
				body["endpoint"] = bodyEndpoint
			}
			if bodyName != "" {
				body["name"] = bodyName
			}
			if bodyPreset != "" {
				body["preset"] = bodyPreset
			}
			{
				nestedReportTemplate := map[string]any{}
				if bodyReportTemplateLabel != "" {
					nestedReportTemplate["label"] = bodyReportTemplateLabel
				}
				if bodyReportTemplateValue != "" {
					nestedReportTemplate["value"] = bodyReportTemplateValue
				}
				if len(nestedReportTemplate) > 0 {
					body["reportTemplate"] = nestedReportTemplate
				}
			}
			{
				nestedVersion := map[string]any{}
				if bodyVersionLabel != "" {
					nestedVersion["label"] = bodyVersionLabel
				}
				if bodyVersionValue != "" {
					nestedVersion["value"] = bodyVersionValue
				}
				if len(nestedVersion) > 0 {
					body["version"] = nestedVersion
				}
			}
			data, _, err := c.DeleteWithBody(cmd.Context(), path, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&bodyAsApp, "as-app", false, "As app")
	cmd.Flags().BoolVar(&bodyIsShared, "is-shared", false, "Is shared")
	cmd.Flags().BoolVar(&bodyNoPagination, "no-pagination", false, "No pagination")
	cmd.Flags().BoolVar(&bodyReverseTenantLookup, "reverse-tenant-lookup", false, "Reverse tenant lookup")
	cmd.Flags().StringVar(&bodyReverseTenantLookupProperty, "reverse-tenant-lookup-property", "", "Reverse tenant lookup property")
	cmd.Flags().StringVar(&bodyAction, "action", "", "Action")
	cmd.Flags().StringVar(&bodyEndpoint, "endpoint", "", "Endpoint")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyPreset, "preset", "", "Preset")
	cmd.Flags().StringVar(&bodyReportTemplateLabel, "report-template-label", "", "Label")
	cmd.Flags().StringVar(&bodyReportTemplateValue, "report-template-value", "", "Value")
	cmd.Flags().StringVar(&bodyVersionLabel, "version-label", "", "Label")
	cmd.Flags().StringVar(&bodyVersionValue, "version-value", "", "Value")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
