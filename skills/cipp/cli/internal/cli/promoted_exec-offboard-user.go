// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"cipp-pp-cli/internal/cliutil"
	"github.com/spf13/cobra"
)

func newExecOffboardUserPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAccessAutomapLabel string
	var bodyAccessAutomapValue string
	var bodyAccessNoAutomapLabel string
	var bodyAccessNoAutomapValue string
	var bodyClearImmutableId bool
	var bodyConvertToShared bool
	var bodyDeleteUser bool
	var bodyDisableSignIn bool
	var bodyHideFromGAL bool
	var bodyKeepCopy bool
	var bodyOOO string
	var bodyOnedriveAccessLabel string
	var bodyOnedriveAccessValue string
	var bodyPostExecutionEmail bool
	var bodyPostExecutionPsa bool
	var bodyPostExecutionWebhook bool
	var bodyRemoveGroups bool
	var bodyRemoveLicenses bool
	var bodyRemoveMFADevices bool
	var bodyRemoveMobile bool
	var bodyRemoveRules bool
	var bodyRemoveTeamsPhoneDID bool
	var bodyResetPass bool
	var bodyRevokeSessions bool
	var bodyScheduledEnabled bool
	var bodyScheduledDate string
	var bodyDisableForwarding bool
	var bodyForward string
	var bodyReference string
	var bodyRemoveCalendarInvites bool
	var bodyRemoveCalendarPermissions bool
	var bodyRemovePermissions bool
	var bodyTenantFilter string
	var bodyUser string

	cmd := &cobra.Command{
		Use:   "exec-offboard-user",
		Short: "Offboard a user with configurable options for mailbox conversion, access delegation, license removal, and more",
		Long:  "Offboard a user with configurable options for mailbox conversion, access delegation, license removal, and more",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cipp-cli exec-offboard-user --tenant-filter example-value",
		Annotations: map[string]string{"pp:endpoint": "exec-offboard-user.create", "pp:method": "POST", "pp:path": "/ExecOffboardUser"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("tenant-filter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecOffboardUser"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			{
				nestedAccessAutomap := map[string]any{}
				if bodyAccessAutomapLabel != "" {
					nestedAccessAutomap["label"] = bodyAccessAutomapLabel
				}
				if bodyAccessAutomapValue != "" {
					nestedAccessAutomap["value"] = bodyAccessAutomapValue
				}
				if len(nestedAccessAutomap) > 0 {
					body["AccessAutomap"] = nestedAccessAutomap
				}
			}
			{
				nestedAccessNoAutomap := map[string]any{}
				if bodyAccessNoAutomapLabel != "" {
					nestedAccessNoAutomap["label"] = bodyAccessNoAutomapLabel
				}
				if bodyAccessNoAutomapValue != "" {
					nestedAccessNoAutomap["value"] = bodyAccessNoAutomapValue
				}
				if len(nestedAccessNoAutomap) > 0 {
					body["AccessNoAutomap"] = nestedAccessNoAutomap
				}
			}
			if cmd.Flags().Changed("clear-immutable-id") {
				body["ClearImmutableId"] = bodyClearImmutableId
			}
			if cmd.Flags().Changed("convert-to-shared") {
				body["ConvertToShared"] = bodyConvertToShared
			}
			if cmd.Flags().Changed("delete-user") {
				body["DeleteUser"] = bodyDeleteUser
			}
			if cmd.Flags().Changed("disable-sign-in") {
				body["DisableSignIn"] = bodyDisableSignIn
			}
			if cmd.Flags().Changed("hide-from-gal") {
				body["HideFromGAL"] = bodyHideFromGAL
			}
			if cmd.Flags().Changed("keep-copy") {
				body["KeepCopy"] = bodyKeepCopy
			}
			if bodyOOO != "" {
				body["OOO"] = bodyOOO
			}
			{
				nestedOnedriveAccess := map[string]any{}
				if bodyOnedriveAccessLabel != "" {
					nestedOnedriveAccess["label"] = bodyOnedriveAccessLabel
				}
				if bodyOnedriveAccessValue != "" {
					nestedOnedriveAccess["value"] = bodyOnedriveAccessValue
				}
				if len(nestedOnedriveAccess) > 0 {
					body["OnedriveAccess"] = nestedOnedriveAccess
				}
			}
			{
				nestedPostExecution := map[string]any{}
				if cmd.Flags().Changed("post-execution-email") {
					nestedPostExecution["email"] = bodyPostExecutionEmail
				}
				if cmd.Flags().Changed("post-execution-psa") {
					nestedPostExecution["psa"] = bodyPostExecutionPsa
				}
				if cmd.Flags().Changed("post-execution-webhook") {
					nestedPostExecution["webhook"] = bodyPostExecutionWebhook
				}
				if len(nestedPostExecution) > 0 {
					body["PostExecution"] = nestedPostExecution
				}
			}
			if cmd.Flags().Changed("remove-groups") {
				body["RemoveGroups"] = bodyRemoveGroups
			}
			if cmd.Flags().Changed("remove-licenses") {
				body["RemoveLicenses"] = bodyRemoveLicenses
			}
			if cmd.Flags().Changed("remove-mfa-devices") {
				body["RemoveMFADevices"] = bodyRemoveMFADevices
			}
			if cmd.Flags().Changed("remove-mobile") {
				body["RemoveMobile"] = bodyRemoveMobile
			}
			if cmd.Flags().Changed("remove-rules") {
				body["RemoveRules"] = bodyRemoveRules
			}
			if cmd.Flags().Changed("remove-teams-phone-did") {
				body["RemoveTeamsPhoneDID"] = bodyRemoveTeamsPhoneDID
			}
			if cmd.Flags().Changed("reset-pass") {
				body["ResetPass"] = bodyResetPass
			}
			if cmd.Flags().Changed("revoke-sessions") {
				body["RevokeSessions"] = bodyRevokeSessions
			}
			{
				nestedScheduled := map[string]any{}
				if cmd.Flags().Changed("scheduled-enabled") {
					nestedScheduled["Enabled"] = bodyScheduledEnabled
				}
				if bodyScheduledDate != "" {
					nestedScheduled["date"] = bodyScheduledDate
				}
				if len(nestedScheduled) > 0 {
					body["Scheduled"] = nestedScheduled
				}
			}
			if cmd.Flags().Changed("disable-forwarding") {
				body["disableForwarding"] = bodyDisableForwarding
			}
			if bodyForward != "" {
				body["forward"] = bodyForward
			}
			if bodyReference != "" {
				body["reference"] = bodyReference
			}
			if cmd.Flags().Changed("remove-calendar-invites") {
				body["removeCalendarInvites"] = bodyRemoveCalendarInvites
			}
			if cmd.Flags().Changed("remove-calendar-permissions") {
				body["removeCalendarPermissions"] = bodyRemoveCalendarPermissions
			}
			if cmd.Flags().Changed("remove-permissions") {
				body["removePermissions"] = bodyRemovePermissions
			}
			if bodyTenantFilter != "" {
				body["tenantFilter"] = bodyTenantFilter
			}
			if bodyUser != "" {
				body["user"] = cliutil.SplitCSV(bodyUser)
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "exec-offboard-user", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAccessAutomapLabel, "access-automap-label", "", "Label")
	cmd.Flags().StringVar(&bodyAccessAutomapValue, "access-automap-value", "", "Value")
	cmd.Flags().StringVar(&bodyAccessNoAutomapLabel, "access-no-automap-label", "", "Label")
	cmd.Flags().StringVar(&bodyAccessNoAutomapValue, "access-no-automap-value", "", "Value")
	cmd.Flags().BoolVar(&bodyClearImmutableId, "clear-immutable-id", false, "Clear immutable id")
	cmd.Flags().BoolVar(&bodyConvertToShared, "convert-to-shared", false, "Convert to shared")
	cmd.Flags().BoolVar(&bodyDeleteUser, "delete-user", false, "Delete user")
	cmd.Flags().BoolVar(&bodyDisableSignIn, "disable-sign-in", false, "Disable sign in")
	cmd.Flags().BoolVar(&bodyHideFromGAL, "hide-from-gal", false, "Hide from gal")
	cmd.Flags().BoolVar(&bodyKeepCopy, "keep-copy", false, "Keep copy")
	cmd.Flags().StringVar(&bodyOOO, "ooo", "", "Ooo")
	cmd.Flags().StringVar(&bodyOnedriveAccessLabel, "onedrive-access-label", "", "Label")
	cmd.Flags().StringVar(&bodyOnedriveAccessValue, "onedrive-access-value", "", "Value")
	cmd.Flags().BoolVar(&bodyPostExecutionEmail, "post-execution-email", false, "Email")
	cmd.Flags().BoolVar(&bodyPostExecutionPsa, "post-execution-psa", false, "Psa")
	cmd.Flags().BoolVar(&bodyPostExecutionWebhook, "post-execution-webhook", false, "Webhook")
	cmd.Flags().BoolVar(&bodyRemoveGroups, "remove-groups", false, "Remove groups")
	cmd.Flags().BoolVar(&bodyRemoveLicenses, "remove-licenses", false, "Remove licenses")
	cmd.Flags().BoolVar(&bodyRemoveMFADevices, "remove-mfa-devices", false, "Remove mfadevices")
	cmd.Flags().BoolVar(&bodyRemoveMobile, "remove-mobile", false, "Remove mobile")
	cmd.Flags().BoolVar(&bodyRemoveRules, "remove-rules", false, "Remove rules")
	cmd.Flags().BoolVar(&bodyRemoveTeamsPhoneDID, "remove-teams-phone-did", false, "Remove teams phone did")
	cmd.Flags().BoolVar(&bodyResetPass, "reset-pass", false, "Reset pass")
	cmd.Flags().BoolVar(&bodyRevokeSessions, "revoke-sessions", false, "Revoke sessions")
	cmd.Flags().BoolVar(&bodyScheduledEnabled, "scheduled-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyScheduledDate, "scheduled-date", "", "Date")
	cmd.Flags().BoolVar(&bodyDisableForwarding, "disable-forwarding", false, "Disable forwarding")
	cmd.Flags().StringVar(&bodyForward, "forward", "", "Forward")
	cmd.Flags().StringVar(&bodyReference, "reference", "", "Reference")
	cmd.Flags().BoolVar(&bodyRemoveCalendarInvites, "remove-calendar-invites", false, "Remove calendar invites")
	cmd.Flags().BoolVar(&bodyRemoveCalendarPermissions, "remove-calendar-permissions", false, "Remove calendar permissions")
	cmd.Flags().BoolVar(&bodyRemovePermissions, "remove-permissions", false, "Remove permissions")
	cmd.Flags().StringVar(&bodyTenantFilter, "tenant-filter", "", "Tenant filter")
	cmd.Flags().StringVar(&bodyUser, "user", "", "User")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
