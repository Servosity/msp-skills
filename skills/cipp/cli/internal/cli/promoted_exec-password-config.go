// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newExecPasswordConfigPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagList string
	var bodyAppendNumber string
	var bodyAppendSpecialChar string
	var bodyCapitalizeWords string
	var bodyCharCount string
	var bodyIncludeDigits string
	var bodyIncludeLowercase string
	var bodyIncludeSpecialChars string
	var bodyIncludeUppercase string
	var bodyPasswordType string
	var bodySeparator string
	var bodySpecialCharSet string
	var bodyWordCount string

	cmd := &cobra.Command{
		Use:         "exec-password-config",
		Short:       "Exec password config",
		Long:        "Exec password config",
		Example:     "  cipp-cli exec-password-config",
		Annotations: map[string]string{"pp:endpoint": "exec-password-config.create", "pp:method": "POST", "pp:path": "/ExecPasswordConfig"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecPasswordConfig"
			params := map[string]string{}
			if flagList != "" {
				params["List"] = formatCLIParamValue(flagList)
			}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyAppendNumber != "" {
				body["appendNumber"] = bodyAppendNumber
			}
			if bodyAppendSpecialChar != "" {
				body["appendSpecialChar"] = bodyAppendSpecialChar
			}
			if bodyCapitalizeWords != "" {
				body["capitalizeWords"] = bodyCapitalizeWords
			}
			if bodyCharCount != "" {
				body["charCount"] = bodyCharCount
			}
			if bodyIncludeDigits != "" {
				body["includeDigits"] = bodyIncludeDigits
			}
			if bodyIncludeLowercase != "" {
				body["includeLowercase"] = bodyIncludeLowercase
			}
			if bodyIncludeSpecialChars != "" {
				body["includeSpecialChars"] = bodyIncludeSpecialChars
			}
			if bodyIncludeUppercase != "" {
				body["includeUppercase"] = bodyIncludeUppercase
			}
			if bodyPasswordType != "" {
				body["passwordType"] = bodyPasswordType
			}
			if bodySeparator != "" {
				body["separator"] = bodySeparator
			}
			if bodySpecialCharSet != "" {
				body["specialCharSet"] = bodySpecialCharSet
			}
			if bodyWordCount != "" {
				body["wordCount"] = bodyWordCount
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "exec-password-config", data, "Results")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagList, "list", "", "List")
	cmd.Flags().StringVar(&bodyAppendNumber, "append-number", "", "Append number")
	cmd.Flags().StringVar(&bodyAppendSpecialChar, "append-special-char", "", "Append special char")
	cmd.Flags().StringVar(&bodyCapitalizeWords, "capitalize-words", "", "Capitalize words")
	cmd.Flags().StringVar(&bodyCharCount, "char-count", "", "Char count")
	cmd.Flags().StringVar(&bodyIncludeDigits, "include-digits", "", "Include digits")
	cmd.Flags().StringVar(&bodyIncludeLowercase, "include-lowercase", "", "Include lowercase")
	cmd.Flags().StringVar(&bodyIncludeSpecialChars, "include-special-chars", "", "Include special chars")
	cmd.Flags().StringVar(&bodyIncludeUppercase, "include-uppercase", "", "Include uppercase")
	cmd.Flags().StringVar(&bodyPasswordType, "password-type", "", "Password type")
	cmd.Flags().StringVar(&bodySeparator, "separator", "", "Separator")
	cmd.Flags().StringVar(&bodySpecialCharSet, "special-char-set", "", "Special char set")
	cmd.Flags().StringVar(&bodyWordCount, "word-count", "", "Word count")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
