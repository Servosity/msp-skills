// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"cipp-pp-cli/internal/cliutil"
	"github.com/spf13/cobra"
)

func newExecTenantGroupPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAction string
	var bodyDynamicRules string
	var bodyGroupDescription string
	var bodyGroupId string
	var bodyGroupName string
	var bodyGroupType string
	var bodyMembers string
	var bodyRuleLogic string
	var bodyValue string

	cmd := &cobra.Command{
		Use:         "exec-tenant-group",
		Short:       "This function is used to manage tenant groups in CIPP .FUNCTIONALITY Entrypoint,AnyTenant .ROLE Tenant.Groups.",
		Long:        "This function is used to manage tenant groups in CIPP .FUNCTIONALITY Entrypoint,AnyTenant .ROLE Tenant.Groups.",
		Example:     "  cipp-cli exec-tenant-group",
		Annotations: map[string]string{"pp:endpoint": "exec-tenant-group.delete", "pp:method": "DELETE", "pp:path": "/ExecTenantGroup"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ExecTenantGroup"
			body := map[string]any{}
			if bodyAction != "" {
				body["Action"] = bodyAction
			}
			if bodyDynamicRules != "" {
				body["dynamicRules"] = cliutil.SplitCSV(bodyDynamicRules)
			}
			if bodyGroupDescription != "" {
				body["groupDescription"] = bodyGroupDescription
			}
			if bodyGroupId != "" {
				body["groupId"] = bodyGroupId
			}
			if bodyGroupName != "" {
				body["groupName"] = bodyGroupName
			}
			if bodyGroupType != "" {
				body["groupType"] = bodyGroupType
			}
			if bodyMembers != "" {
				body["members"] = cliutil.SplitCSV(bodyMembers)
			}
			if bodyRuleLogic != "" {
				body["ruleLogic"] = bodyRuleLogic
			}
			if bodyValue != "" {
				body["value"] = bodyValue
			}
			data, _, err := c.DeleteWithBody(cmd.Context(), path, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAction, "action", "", "Action")
	cmd.Flags().StringVar(&bodyDynamicRules, "dynamic-rules", "", "Dynamic rules")
	cmd.Flags().StringVar(&bodyGroupDescription, "group-description", "", "Group description")
	cmd.Flags().StringVar(&bodyGroupId, "group-id", "", "Group id")
	cmd.Flags().StringVar(&bodyGroupName, "group-name", "", "Group name")
	cmd.Flags().StringVar(&bodyGroupType, "group-type", "", "Group type")
	cmd.Flags().StringVar(&bodyMembers, "members", "", "Members")
	cmd.Flags().StringVar(&bodyRuleLogic, "rule-logic", "", "Rule logic")
	cmd.Flags().StringVar(&bodyValue, "value", "", "Value")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
