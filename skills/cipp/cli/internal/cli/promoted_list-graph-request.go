// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newListGraphRequestPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAsApp string
	var flagCountOnly string
	var flagEndpoint string
	var flagExpand string
	var flagGraphFilter string
	var flagIgnoreErrors string
	var flagListProperties string
	var flagManualPagination string
	var flagNextLink string
	var flagNoPagination string
	var flagQueueId string
	var flagQueueNameOverride string
	var flagReverseTenantLookup string
	var flagReverseTenantLookupProperty string
	var flagSkipCache string
	var flagSort string
	var flagTenantFilter string
	var flagVersion string
	var bodyGdapRolesLabel string
	var bodyGdapRolesValue string
	var bodyIdLabel string
	var bodyIdValue string
	var bodyIgnoreMissingRoles bool
	var bodyOooAutoReplyStateLabel string
	var bodyOooAutoReplyStateValue string
	var bodyOooEndTime string
	var bodyOooExternalMessage string
	var bodyOooInternalMessage string
	var bodyOooStartTime string
	var bodyRecipientLimitsMaxRecipients float64
	var bodyRemapRoles string
	var bodyStandardsExcludeAllTenants bool

	cmd := &cobra.Command{
		Use:   "list-graph-request",
		Short: "List graph request",
		Long:  "List graph request",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  cipp-cli list-graph-request --tenant-filter example-value",
		Annotations: map[string]string{"pp:endpoint": "list-graph-request.list", "pp:method": "GET", "pp:path": "/ListGraphRequest", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with a required flag/body prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only reads fall through so a bare call still executes; positional
			// commands keep their existing usageErr (exit 2 + JSON envelope).
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("tenant-filter") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "tenant-filter")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ListGraphRequest"
			params := map[string]string{}
			if flagAsApp != "" {
				params["AsApp"] = formatCLIParamValue(flagAsApp)
			}
			if flagCountOnly != "" {
				params["CountOnly"] = formatCLIParamValue(flagCountOnly)
			}
			if flagEndpoint != "" {
				params["Endpoint"] = formatCLIParamValue(flagEndpoint)
			}
			if flagExpand != "" {
				params["expand"] = formatCLIParamValue(flagExpand)
			}
			if flagGraphFilter != "" {
				params["graphFilter"] = formatCLIParamValue(flagGraphFilter)
			}
			if flagIgnoreErrors != "" {
				params["IgnoreErrors"] = formatCLIParamValue(flagIgnoreErrors)
			}
			if flagListProperties != "" {
				params["ListProperties"] = formatCLIParamValue(flagListProperties)
			}
			if flagManualPagination != "" {
				params["manualPagination"] = formatCLIParamValue(flagManualPagination)
			}
			if flagNextLink != "" {
				params["nextLink"] = formatCLIParamValue(flagNextLink)
			}
			if flagNoPagination != "" {
				params["NoPagination"] = formatCLIParamValue(flagNoPagination)
			}
			if flagQueueId != "" {
				params["QueueId"] = formatCLIParamValue(flagQueueId)
			}
			if flagQueueNameOverride != "" {
				params["QueueNameOverride"] = formatCLIParamValue(flagQueueNameOverride)
			}
			if flagReverseTenantLookup != "" {
				params["ReverseTenantLookup"] = formatCLIParamValue(flagReverseTenantLookup)
			}
			if flagReverseTenantLookupProperty != "" {
				params["ReverseTenantLookupProperty"] = formatCLIParamValue(flagReverseTenantLookupProperty)
			}
			if flagSkipCache != "" {
				params["SkipCache"] = formatCLIParamValue(flagSkipCache)
			}
			if flagSort != "" {
				params["Sort"] = formatCLIParamValue(flagSort)
			}
			if flagTenantFilter != "" {
				params["tenantFilter"] = formatCLIParamValue(flagTenantFilter)
			}
			if flagVersion != "" {
				params["Version"] = formatCLIParamValue(flagVersion)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "list-graph-request", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAsApp, "as-app", "", "As app")
	cmd.Flags().StringVar(&flagCountOnly, "count-only", "", "Count only")
	cmd.Flags().StringVar(&flagEndpoint, "endpoint", "", "Endpoint")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Expand")
	cmd.Flags().StringVar(&flagGraphFilter, "graph-filter", "", "Graph filter")
	cmd.Flags().StringVar(&flagIgnoreErrors, "ignore-errors", "", "Ignore errors")
	cmd.Flags().StringVar(&flagListProperties, "list-properties", "", "List properties")
	cmd.Flags().StringVar(&flagManualPagination, "manual-pagination", "", "Manual pagination")
	cmd.Flags().StringVar(&flagNextLink, "next-link", "", "Next link")
	cmd.Flags().StringVar(&flagNoPagination, "no-pagination", "", "No pagination")
	cmd.Flags().StringVar(&flagQueueId, "queue-id", "", "Queue id")
	cmd.Flags().StringVar(&flagQueueNameOverride, "queue-name-override", "", "Queue name override")
	cmd.Flags().StringVar(&flagReverseTenantLookup, "reverse-tenant-lookup", "", "Reverse tenant lookup")
	cmd.Flags().StringVar(&flagReverseTenantLookupProperty, "reverse-tenant-lookup-property", "", "Reverse tenant lookup property")
	cmd.Flags().StringVar(&flagSkipCache, "skip-cache", "", "Skip cache")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort")
	cmd.Flags().StringVar(&flagTenantFilter, "tenant-filter", "", "Target tenant domain or 'AllTenants' for multi-tenant operations.")
	cmd.Flags().StringVar(&flagVersion, "version", "", "Version")
	cmd.Flags().StringVar(&bodyGdapRolesLabel, "gdap-roles-label", "", "Label")
	cmd.Flags().StringVar(&bodyGdapRolesValue, "gdap-roles-value", "", "Value")
	cmd.Flags().StringVar(&bodyIdLabel, "id-label", "", "Label")
	cmd.Flags().StringVar(&bodyIdValue, "id-value", "", "Value")
	cmd.Flags().BoolVar(&bodyIgnoreMissingRoles, "ignore-missing-roles", false, "Ignore missing roles")
	cmd.Flags().StringVar(&bodyOooAutoReplyStateLabel, "ooo-auto-reply-state-label", "", "Label")
	cmd.Flags().StringVar(&bodyOooAutoReplyStateValue, "ooo-auto-reply-state-value", "", "Value")
	cmd.Flags().StringVar(&bodyOooEndTime, "ooo-end-time", "", "End time")
	cmd.Flags().StringVar(&bodyOooExternalMessage, "ooo-external-message", "", "External message")
	cmd.Flags().StringVar(&bodyOooInternalMessage, "ooo-internal-message", "", "Internal message")
	cmd.Flags().StringVar(&bodyOooStartTime, "ooo-start-time", "", "Start time")
	cmd.Flags().Float64Var(&bodyRecipientLimitsMaxRecipients, "recipient-limits-max-recipients", 0.0, "Max recipients")
	cmd.Flags().StringVar(&bodyRemapRoles, "remap-roles", "", "Remap roles")
	cmd.Flags().BoolVar(&bodyStandardsExcludeAllTenants, "standards-exclude-all-tenants", false, "Standards exclude all tenants")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
