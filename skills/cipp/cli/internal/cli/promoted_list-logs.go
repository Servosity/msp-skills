// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newListLogsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagAPI string
	var flagDateFilter string
	var flagEndDate string
	var flagFilter string
	var flagListLogs string
	var flagLogentryid string
	var flagScheduledTaskId string
	var flagSeverity string
	var flagStandardTemplateId string
	var flagStartDate string
	var flagTenant string
	var flagUser string

	cmd := &cobra.Command{
		Use:         "list-logs",
		Short:       "List logs",
		Long:        "List logs",
		Example:     "  cipp-cli list-logs",
		Annotations: map[string]string{"pp:endpoint": "list-logs.list", "pp:method": "GET", "pp:path": "/ListLogs", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/ListLogs"
			params := map[string]string{}
			if flagAPI != "" {
				params["API"] = formatCLIParamValue(flagAPI)
			}
			if flagDateFilter != "" {
				params["DateFilter"] = formatCLIParamValue(flagDateFilter)
			}
			if flagEndDate != "" {
				params["EndDate"] = formatCLIParamValue(flagEndDate)
			}
			if flagFilter != "" {
				params["Filter"] = formatCLIParamValue(flagFilter)
			}
			if flagListLogs != "" {
				params["ListLogs"] = formatCLIParamValue(flagListLogs)
			}
			if flagLogentryid != "" {
				params["logentryid"] = formatCLIParamValue(flagLogentryid)
			}
			if flagScheduledTaskId != "" {
				params["ScheduledTaskId"] = formatCLIParamValue(flagScheduledTaskId)
			}
			if flagSeverity != "" {
				params["Severity"] = formatCLIParamValue(flagSeverity)
			}
			if flagStandardTemplateId != "" {
				params["StandardTemplateId"] = formatCLIParamValue(flagStandardTemplateId)
			}
			if flagStartDate != "" {
				params["StartDate"] = formatCLIParamValue(flagStartDate)
			}
			if flagTenant != "" {
				params["Tenant"] = formatCLIParamValue(flagTenant)
			}
			if flagUser != "" {
				params["User"] = formatCLIParamValue(flagUser)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "list-logs", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAPI, "api", "", "Api")
	cmd.Flags().StringVar(&flagDateFilter, "date-filter", "", "Date filter")
	cmd.Flags().StringVar(&flagEndDate, "end-date", "", "End date")
	cmd.Flags().StringVar(&flagFilter, "filter", "", "Filter")
	cmd.Flags().StringVar(&flagListLogs, "list-logs", "", "List logs")
	cmd.Flags().StringVar(&flagLogentryid, "logentryid", "", "Logentryid")
	cmd.Flags().StringVar(&flagScheduledTaskId, "scheduled-task-id", "", "Scheduled task id")
	cmd.Flags().StringVar(&flagSeverity, "severity", "", "Severity")
	cmd.Flags().StringVar(&flagStandardTemplateId, "standard-template-id", "", "Standard template id")
	cmd.Flags().StringVar(&flagStartDate, "start-date", "", "Start date")
	cmd.Flags().StringVar(&flagTenant, "tenant", "", "Tenant")
	cmd.Flags().StringVar(&flagUser, "user", "", "User")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
