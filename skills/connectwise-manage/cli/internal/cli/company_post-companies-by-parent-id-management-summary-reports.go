// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCompanyPostCompaniesByParentIdManagementSummaryReportsCmd(flags *rootFlags) *cobra.Command {
	var bodyInfo string
	var bodyAgreementInfo string
	var bodyAgreementChargeFirmFlag bool
	var bodyAgreementId string
	var bodyAgreementName string
	var bodyAgreementType string
	var bodyAlertsGenerated string
	var bodyCompanyInfo string
	var bodyCompanyId string
	var bodyCompanyIdentifier string
	var bodyCompanyName string
	var bodyCpuUtilization float64
	var bodyDeviceType string
	var bodyDiskCleanups int
	var bodyDiskDefragmentations int
	var bodyDiskSpaceCleanedMb int
	var bodyFailedBackupJobs int
	var bodyFullyPatchedMachines int
	var bodyGroupIdentifier string
	var bodyId string
	var bodyInternetConnectivity float64
	var bodyManagementSolutionInfo string
	var bodyManagementSolutionId string
	var bodyManagementSolutionName string
	var bodyManagementSolutionSetupName string
	var bodyMemoryUtilization float64
	var bodyMissingMoreFivePatchesMachines int
	var bodyMissingOneTwoPatchesMachines int
	var bodyMissingSecurityPatches string
	var bodyMissingThreeFivePatchesMachines int
	var bodyMissingUnscannedPatchesMachines int
	var bodyServerAvailability int
	var bodyServersDiskSpaceLow int
	var bodyServersOffline int
	var bodySnmpMachines int
	var bodySpywareItemsRemoved int
	var bodySuccessfulBackupJobs int
	var bodyTotalManagedMachines int
	var bodyTotalNotifications int
	var bodyTotalServers int
	var bodyTotalWindowsServers int
	var bodyTotalWindowsWorkstations int
	var bodyTotalWorkstations int
	var bodyVirusesRemoved int
	var bodyWindowsPatchesInstalled int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "post-companies-by-parent-id-management-summary-reports <parentId>",
		Short: "Post CompanyManagementSummary",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  connectwise-manage-cli company post-companies-by-parent-id-management-summary-reports 42 --group-identifier example-value",
		Annotations: map[string]string{"pp:endpoint": "company.post-companies-by-parent-id-management-summary-reports", "pp:method": "POST", "pp:path": "/company/companies/{parentId}/managementSummaryReports"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("group-identifier") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "group-identifier")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/company/companies/{parentId}/managementSummaryReports"
			path = replacePathParam(path, "parentId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyInfo != "" {
					var parsedInfo any
					if err := json.Unmarshal([]byte(bodyInfo), &parsedInfo); err != nil {
						return fmt.Errorf("parsing --info JSON: %w", err)
					}
					body["_info"] = parsedInfo
				}
				{
					nestedAgreement := map[string]any{}
					if bodyAgreementInfo != "" {
						var parsedAgreementInfo any
						if err := json.Unmarshal([]byte(bodyAgreementInfo), &parsedAgreementInfo); err != nil {
							return fmt.Errorf("parsing --agreement-info JSON: %w", err)
						}
						nestedAgreement["_info"] = parsedAgreementInfo
					}
					if cmd.Flags().Changed("agreement-charge-firm-flag") {
						nestedAgreement["chargeFirmFlag"] = bodyAgreementChargeFirmFlag
					}
					if bodyAgreementId != "" {
						nestedAgreement["id"] = bodyAgreementId
					}
					if bodyAgreementName != "" {
						nestedAgreement["name"] = bodyAgreementName
					}
					if bodyAgreementType != "" {
						nestedAgreement["type"] = bodyAgreementType
					}
					if len(nestedAgreement) > 0 {
						body["agreement"] = nestedAgreement
					}
				}
				if bodyAlertsGenerated != "" {
					body["alertsGenerated"] = bodyAlertsGenerated
				}
				{
					nestedCompany := map[string]any{}
					if bodyCompanyInfo != "" {
						var parsedCompanyInfo any
						if err := json.Unmarshal([]byte(bodyCompanyInfo), &parsedCompanyInfo); err != nil {
							return fmt.Errorf("parsing --company-info JSON: %w", err)
						}
						nestedCompany["_info"] = parsedCompanyInfo
					}
					if bodyCompanyId != "" {
						nestedCompany["id"] = bodyCompanyId
					}
					if bodyCompanyIdentifier != "" {
						nestedCompany["identifier"] = bodyCompanyIdentifier
					}
					if bodyCompanyName != "" {
						nestedCompany["name"] = bodyCompanyName
					}
					if len(nestedCompany) > 0 {
						body["company"] = nestedCompany
					}
				}
				if bodyCpuUtilization != 0.0 {
					body["cpuUtilization"] = bodyCpuUtilization
				}
				if bodyDeviceType != "" {
					body["deviceType"] = bodyDeviceType
				}
				if bodyDiskCleanups != 0 {
					body["diskCleanups"] = bodyDiskCleanups
				}
				if bodyDiskDefragmentations != 0 {
					body["diskDefragmentations"] = bodyDiskDefragmentations
				}
				if bodyDiskSpaceCleanedMb != 0 {
					body["diskSpaceCleanedMb"] = bodyDiskSpaceCleanedMb
				}
				if bodyFailedBackupJobs != 0 {
					body["failedBackupJobs"] = bodyFailedBackupJobs
				}
				if bodyFullyPatchedMachines != 0 {
					body["fullyPatchedMachines"] = bodyFullyPatchedMachines
				}
				if bodyGroupIdentifier != "" {
					body["groupIdentifier"] = bodyGroupIdentifier
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyInternetConnectivity != 0.0 {
					body["internetConnectivity"] = bodyInternetConnectivity
				}
				{
					nestedManagementSolution := map[string]any{}
					if bodyManagementSolutionInfo != "" {
						var parsedManagementSolutionInfo any
						if err := json.Unmarshal([]byte(bodyManagementSolutionInfo), &parsedManagementSolutionInfo); err != nil {
							return fmt.Errorf("parsing --management-solution-info JSON: %w", err)
						}
						nestedManagementSolution["_info"] = parsedManagementSolutionInfo
					}
					if bodyManagementSolutionId != "" {
						nestedManagementSolution["id"] = bodyManagementSolutionId
					}
					if bodyManagementSolutionName != "" {
						nestedManagementSolution["name"] = bodyManagementSolutionName
					}
					if bodyManagementSolutionSetupName != "" {
						nestedManagementSolution["setupName"] = bodyManagementSolutionSetupName
					}
					if len(nestedManagementSolution) > 0 {
						body["managementSolution"] = nestedManagementSolution
					}
				}
				if bodyMemoryUtilization != 0.0 {
					body["memoryUtilization"] = bodyMemoryUtilization
				}
				if bodyMissingMoreFivePatchesMachines != 0 {
					body["missingMoreFivePatchesMachines"] = bodyMissingMoreFivePatchesMachines
				}
				if bodyMissingOneTwoPatchesMachines != 0 {
					body["missingOneTwoPatchesMachines"] = bodyMissingOneTwoPatchesMachines
				}
				if bodyMissingSecurityPatches != "" {
					body["missingSecurityPatches"] = bodyMissingSecurityPatches
				}
				if bodyMissingThreeFivePatchesMachines != 0 {
					body["missingThreeFivePatchesMachines"] = bodyMissingThreeFivePatchesMachines
				}
				if bodyMissingUnscannedPatchesMachines != 0 {
					body["missingUnscannedPatchesMachines"] = bodyMissingUnscannedPatchesMachines
				}
				if bodyServerAvailability != 0 {
					body["serverAvailability"] = bodyServerAvailability
				}
				if bodyServersDiskSpaceLow != 0 {
					body["serversDiskSpaceLow"] = bodyServersDiskSpaceLow
				}
				if bodyServersOffline != 0 {
					body["serversOffline"] = bodyServersOffline
				}
				if bodySnmpMachines != 0 {
					body["snmpMachines"] = bodySnmpMachines
				}
				if bodySpywareItemsRemoved != 0 {
					body["spywareItemsRemoved"] = bodySpywareItemsRemoved
				}
				if bodySuccessfulBackupJobs != 0 {
					body["successfulBackupJobs"] = bodySuccessfulBackupJobs
				}
				if bodyTotalManagedMachines != 0 {
					body["totalManagedMachines"] = bodyTotalManagedMachines
				}
				if bodyTotalNotifications != 0 {
					body["totalNotifications"] = bodyTotalNotifications
				}
				if bodyTotalServers != 0 {
					body["totalServers"] = bodyTotalServers
				}
				if bodyTotalWindowsServers != 0 {
					body["totalWindowsServers"] = bodyTotalWindowsServers
				}
				if bodyTotalWindowsWorkstations != 0 {
					body["totalWindowsWorkstations"] = bodyTotalWindowsWorkstations
				}
				if bodyTotalWorkstations != 0 {
					body["totalWorkstations"] = bodyTotalWorkstations
				}
				if bodyVirusesRemoved != 0 {
					body["virusesRemoved"] = bodyVirusesRemoved
				}
				if bodyWindowsPatchesInstalled != 0 {
					body["windowsPatchesInstalled"] = bodyWindowsPatchesInstalled
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "company", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "company", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "company", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "company", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "company", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "company",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "company", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "company", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyInfo, "info", "", "Info")
	cmd.Flags().StringVar(&bodyAgreementInfo, "agreement-info", "", "Info")
	cmd.Flags().BoolVar(&bodyAgreementChargeFirmFlag, "agreement-charge-firm-flag", false, "Charge firm flag")
	cmd.Flags().StringVar(&bodyAgreementId, "agreement-id", "", "Id")
	cmd.Flags().StringVar(&bodyAgreementName, "agreement-name", "", "Name")
	cmd.Flags().StringVar(&bodyAgreementType, "agreement-type", "", "Type")
	cmd.Flags().StringVar(&bodyAlertsGenerated, "alerts-generated", "", "Alerts generated")
	cmd.Flags().StringVar(&bodyCompanyInfo, "company-info", "", "Info")
	cmd.Flags().StringVar(&bodyCompanyId, "company-id", "", "Id")
	cmd.Flags().StringVar(&bodyCompanyIdentifier, "company-identifier", "", "Identifier")
	cmd.Flags().StringVar(&bodyCompanyName, "company-name", "", "Name")
	cmd.Flags().Float64Var(&bodyCpuUtilization, "cpu-utilization", 0.0, "Cpu utilization")
	cmd.Flags().StringVar(&bodyDeviceType, "device-type", "", "Gets or sets deviceType is required if the managementSolution is Legacy.")
	cmd.Flags().IntVar(&bodyDiskCleanups, "disk-cleanups", 0, "Disk cleanups")
	cmd.Flags().IntVar(&bodyDiskDefragmentations, "disk-defragmentations", 0, "Disk defragmentations")
	cmd.Flags().IntVar(&bodyDiskSpaceCleanedMb, "disk-space-cleaned-mb", 0, "Disk space cleaned mb")
	cmd.Flags().IntVar(&bodyFailedBackupJobs, "failed-backup-jobs", 0, "Failed backup jobs")
	cmd.Flags().IntVar(&bodyFullyPatchedMachines, "fully-patched-machines", 0, "Fully patched machines")
	cmd.Flags().StringVar(&bodyGroupIdentifier, "group-identifier", "", "Max length: 100;")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().Float64Var(&bodyInternetConnectivity, "internet-connectivity", 0.0, "Internet connectivity")
	cmd.Flags().StringVar(&bodyManagementSolutionInfo, "management-solution-info", "", "Info")
	cmd.Flags().StringVar(&bodyManagementSolutionId, "management-solution-id", "", "Id")
	cmd.Flags().StringVar(&bodyManagementSolutionName, "management-solution-name", "", "Name")
	cmd.Flags().StringVar(&bodyManagementSolutionSetupName, "management-solution-setup-name", "", "Setup name")
	cmd.Flags().Float64Var(&bodyMemoryUtilization, "memory-utilization", 0.0, "Memory utilization")
	cmd.Flags().IntVar(&bodyMissingMoreFivePatchesMachines, "missing-more-five-patches-machines", 0, "Missing more five patches machines")
	cmd.Flags().IntVar(&bodyMissingOneTwoPatchesMachines, "missing-one-two-patches-machines", 0, "Missing one two patches machines")
	cmd.Flags().StringVar(&bodyMissingSecurityPatches, "missing-security-patches", "", "Missing security patches")
	cmd.Flags().IntVar(&bodyMissingThreeFivePatchesMachines, "missing-three-five-patches-machines", 0, "Missing three five patches machines")
	cmd.Flags().IntVar(&bodyMissingUnscannedPatchesMachines, "missing-unscanned-patches-machines", 0, "Missing unscanned patches machines")
	cmd.Flags().IntVar(&bodyServerAvailability, "server-availability", 0, "Server availability")
	cmd.Flags().IntVar(&bodyServersDiskSpaceLow, "servers-disk-space-low", 0, "Servers disk space low")
	cmd.Flags().IntVar(&bodyServersOffline, "servers-offline", 0, "Servers offline")
	cmd.Flags().IntVar(&bodySnmpMachines, "snmp-machines", 0, "Snmp machines")
	cmd.Flags().IntVar(&bodySpywareItemsRemoved, "spyware-items-removed", 0, "Spyware items removed")
	cmd.Flags().IntVar(&bodySuccessfulBackupJobs, "successful-backup-jobs", 0, "Successful backup jobs")
	cmd.Flags().IntVar(&bodyTotalManagedMachines, "total-managed-machines", 0, "Total managed machines")
	cmd.Flags().IntVar(&bodyTotalNotifications, "total-notifications", 0, "Total notifications")
	cmd.Flags().IntVar(&bodyTotalServers, "total-servers", 0, "Total servers")
	cmd.Flags().IntVar(&bodyTotalWindowsServers, "total-windows-servers", 0, "Total windows servers")
	cmd.Flags().IntVar(&bodyTotalWindowsWorkstations, "total-windows-workstations", 0, "Total windows workstations")
	cmd.Flags().IntVar(&bodyTotalWorkstations, "total-workstations", 0, "Total workstations")
	cmd.Flags().IntVar(&bodyVirusesRemoved, "viruses-removed", 0, "Viruses removed")
	cmd.Flags().IntVar(&bodyWindowsPatchesInstalled, "windows-patches-installed", 0, "Windows patches installed")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
