// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newFinancePostInvoicesByParentIdPaymentsCmd(flags *rootFlags) *cobra.Command {
	var bodyInfo string
	var bodyARPaymentAccount string
	var bodyAmount float64
	var bodyAppliedBy string
	var bodyBalance float64
	var bodyCreditInfo string
	var bodyCreditApplyToType string
	var bodyCreditBillingType string
	var bodyCreditChargeFirmFlag bool
	var bodyCreditId string
	var bodyCreditIdentifier string
	var bodyCreditInvoiceDate string
	var bodyGlBatchID string
	var bodyId string
	var bodyInvoiceInfo string
	var bodyInvoiceApplyToType string
	var bodyInvoiceBillingType string
	var bodyInvoiceChargeFirmFlag bool
	var bodyInvoiceId string
	var bodyInvoiceIdentifier string
	var bodyInvoiceInvoiceDate string
	var bodyInvoiceBalance float64
	var bodyInvoiceTotal float64
	var bodyPaymentAccount string
	var bodyPaymentDate string
	var bodyPaymentSyncDate string
	var bodyPaymentSyncStatus string
	var bodySource string
	var bodyType string
	var bodyWisePayPaymentBatchPaymentAmount float64
	var bodyWisePayPaymentBatchPaymentWisePayHref string
	var bodyWisePayPaymentFeeInvoiceAmount float64
	var bodyWisePayPaymentFeeInvoiceId string
	var bodyWisePayPaymentFeeInvoiceInvoiceHref string
	var bodyWisePayPaymentFeeInvoiceInvoiceNumber string
	var bodyWisePayPaymentPaymentDateUtc string
	var bodyWisePayPaymentWisePayReference string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-invoices-by-parent-id-payments <parentId>",
		Short:       "Post Payment",
		Example:     "  connectwise-manage-cli finance post-invoices-by-parent-id-payments 42",
		Annotations: map[string]string{"pp:endpoint": "finance.post-invoices-by-parent-id-payments", "pp:method": "POST", "pp:path": "/finance/invoices/{parentId}/payments"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/finance/invoices/{parentId}/payments"
			path = replacePathParam(path, "parentId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyInfo != "" {
					var parsedInfo any
					if err := json.Unmarshal([]byte(bodyInfo), &parsedInfo); err != nil {
						return fmt.Errorf("parsing --info JSON: %w", err)
					}
					body["_info"] = parsedInfo
				}
				if bodyARPaymentAccount != "" {
					body["aRPaymentAccount"] = bodyARPaymentAccount
				}
				if bodyAmount != 0.0 {
					body["amount"] = bodyAmount
				}
				if bodyAppliedBy != "" {
					body["appliedBy"] = bodyAppliedBy
				}
				if bodyBalance != 0.0 {
					body["balance"] = bodyBalance
				}
				{
					nestedCredit := map[string]any{}
					if bodyCreditInfo != "" {
						var parsedCreditInfo any
						if err := json.Unmarshal([]byte(bodyCreditInfo), &parsedCreditInfo); err != nil {
							return fmt.Errorf("parsing --credit-info JSON: %w", err)
						}
						nestedCredit["_info"] = parsedCreditInfo
					}
					if bodyCreditApplyToType != "" {
						nestedCredit["applyToType"] = bodyCreditApplyToType
					}
					if bodyCreditBillingType != "" {
						nestedCredit["billingType"] = bodyCreditBillingType
					}
					if cmd.Flags().Changed("credit-charge-firm-flag") {
						nestedCredit["chargeFirmFlag"] = bodyCreditChargeFirmFlag
					}
					if bodyCreditId != "" {
						nestedCredit["id"] = bodyCreditId
					}
					if bodyCreditIdentifier != "" {
						nestedCredit["identifier"] = bodyCreditIdentifier
					}
					if bodyCreditInvoiceDate != "" {
						nestedCredit["invoiceDate"] = bodyCreditInvoiceDate
					}
					if len(nestedCredit) > 0 {
						body["credit"] = nestedCredit
					}
				}
				if bodyGlBatchID != "" {
					body["glBatchID"] = bodyGlBatchID
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				{
					nestedInvoice := map[string]any{}
					if bodyInvoiceInfo != "" {
						var parsedInvoiceInfo any
						if err := json.Unmarshal([]byte(bodyInvoiceInfo), &parsedInvoiceInfo); err != nil {
							return fmt.Errorf("parsing --invoice-info JSON: %w", err)
						}
						nestedInvoice["_info"] = parsedInvoiceInfo
					}
					if bodyInvoiceApplyToType != "" {
						nestedInvoice["applyToType"] = bodyInvoiceApplyToType
					}
					if bodyInvoiceBillingType != "" {
						nestedInvoice["billingType"] = bodyInvoiceBillingType
					}
					if cmd.Flags().Changed("invoice-charge-firm-flag") {
						nestedInvoice["chargeFirmFlag"] = bodyInvoiceChargeFirmFlag
					}
					if bodyInvoiceId != "" {
						nestedInvoice["id"] = bodyInvoiceId
					}
					if bodyInvoiceIdentifier != "" {
						nestedInvoice["identifier"] = bodyInvoiceIdentifier
					}
					if bodyInvoiceInvoiceDate != "" {
						nestedInvoice["invoiceDate"] = bodyInvoiceInvoiceDate
					}
					if len(nestedInvoice) > 0 {
						body["invoice"] = nestedInvoice
					}
				}
				if bodyInvoiceBalance != 0.0 {
					body["invoiceBalance"] = bodyInvoiceBalance
				}
				if bodyInvoiceTotal != 0.0 {
					body["invoiceTotal"] = bodyInvoiceTotal
				}
				if bodyPaymentAccount != "" {
					body["paymentAccount"] = bodyPaymentAccount
				}
				if bodyPaymentDate != "" {
					body["paymentDate"] = bodyPaymentDate
				}
				if bodyPaymentSyncDate != "" {
					body["paymentSyncDate"] = bodyPaymentSyncDate
				}
				if bodyPaymentSyncStatus != "" {
					body["paymentSyncStatus"] = bodyPaymentSyncStatus
				}
				if bodySource != "" {
					body["source"] = bodySource
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				{
					nestedWisePayPayment := map[string]any{}
					{
						nestedWisePayPaymentBatchPayment := map[string]any{}
						if bodyWisePayPaymentBatchPaymentAmount != 0.0 {
							nestedWisePayPaymentBatchPayment["amount"] = bodyWisePayPaymentBatchPaymentAmount
						}
						if bodyWisePayPaymentBatchPaymentWisePayHref != "" {
							nestedWisePayPaymentBatchPayment["wisePayHref"] = bodyWisePayPaymentBatchPaymentWisePayHref
						}
						if len(nestedWisePayPaymentBatchPayment) > 0 {
							nestedWisePayPayment["batchPayment"] = nestedWisePayPaymentBatchPayment
						}
					}
					{
						nestedWisePayPaymentFeeInvoice := map[string]any{}
						if bodyWisePayPaymentFeeInvoiceAmount != 0.0 {
							nestedWisePayPaymentFeeInvoice["amount"] = bodyWisePayPaymentFeeInvoiceAmount
						}
						if bodyWisePayPaymentFeeInvoiceId != "" {
							nestedWisePayPaymentFeeInvoice["id"] = bodyWisePayPaymentFeeInvoiceId
						}
						if bodyWisePayPaymentFeeInvoiceInvoiceHref != "" {
							nestedWisePayPaymentFeeInvoice["invoiceHref"] = bodyWisePayPaymentFeeInvoiceInvoiceHref
						}
						if bodyWisePayPaymentFeeInvoiceInvoiceNumber != "" {
							nestedWisePayPaymentFeeInvoice["invoiceNumber"] = bodyWisePayPaymentFeeInvoiceInvoiceNumber
						}
						if len(nestedWisePayPaymentFeeInvoice) > 0 {
							nestedWisePayPayment["feeInvoice"] = nestedWisePayPaymentFeeInvoice
						}
					}
					if bodyWisePayPaymentPaymentDateUtc != "" {
						nestedWisePayPayment["paymentDateUtc"] = bodyWisePayPaymentPaymentDateUtc
					}
					if bodyWisePayPaymentWisePayReference != "" {
						nestedWisePayPayment["wisePayReference"] = bodyWisePayPaymentWisePayReference
					}
					if len(nestedWisePayPayment) > 0 {
						body["wisePayPayment"] = nestedWisePayPayment
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "finance", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "finance", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "finance", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "finance", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "finance", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "finance",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "finance", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "finance", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyInfo, "info", "", "Info")
	cmd.Flags().StringVar(&bodyARPaymentAccount, "a-r-payment-account", "", "A rpayment account")
	cmd.Flags().Float64Var(&bodyAmount, "amount", 0.0, "Amount")
	cmd.Flags().StringVar(&bodyAppliedBy, "applied-by", "", "Applied by")
	cmd.Flags().Float64Var(&bodyBalance, "balance", 0.0, "Balance")
	cmd.Flags().StringVar(&bodyCreditInfo, "credit-info", "", "Info")
	cmd.Flags().StringVar(&bodyCreditApplyToType, "credit-apply-to-type", "", "Apply to type")
	cmd.Flags().StringVar(&bodyCreditBillingType, "credit-billing-type", "", "Billing type")
	cmd.Flags().BoolVar(&bodyCreditChargeFirmFlag, "credit-charge-firm-flag", false, "Charge firm flag")
	cmd.Flags().StringVar(&bodyCreditId, "credit-id", "", "Id")
	cmd.Flags().StringVar(&bodyCreditIdentifier, "credit-identifier", "", "Identifier")
	cmd.Flags().StringVar(&bodyCreditInvoiceDate, "credit-invoice-date", "", "Invoice date")
	cmd.Flags().StringVar(&bodyGlBatchID, "gl-batch-id", "", "Max length: 50;")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().StringVar(&bodyInvoiceInfo, "invoice-info", "", "Info")
	cmd.Flags().StringVar(&bodyInvoiceApplyToType, "invoice-apply-to-type", "", "Apply to type")
	cmd.Flags().StringVar(&bodyInvoiceBillingType, "invoice-billing-type", "", "Billing type")
	cmd.Flags().BoolVar(&bodyInvoiceChargeFirmFlag, "invoice-charge-firm-flag", false, "Charge firm flag")
	cmd.Flags().StringVar(&bodyInvoiceId, "invoice-id", "", "Id")
	cmd.Flags().StringVar(&bodyInvoiceIdentifier, "invoice-identifier", "", "Identifier")
	cmd.Flags().StringVar(&bodyInvoiceInvoiceDate, "invoice-invoice-date", "", "Invoice date")
	cmd.Flags().Float64Var(&bodyInvoiceBalance, "invoice-balance", 0.0, "Invoice balance")
	cmd.Flags().Float64Var(&bodyInvoiceTotal, "invoice-total", 0.0, "Invoice total")
	cmd.Flags().StringVar(&bodyPaymentAccount, "payment-account", "", "Payment account")
	cmd.Flags().StringVar(&bodyPaymentDate, "payment-date", "", "Payment date")
	cmd.Flags().StringVar(&bodyPaymentSyncDate, "payment-sync-date", "", "Payment sync date")
	cmd.Flags().StringVar(&bodyPaymentSyncStatus, "payment-sync-status", "", "Payment sync status")
	cmd.Flags().StringVar(&bodySource, "source", "", "Source")
	cmd.Flags().StringVar(&bodyType, "type", "", "Type")
	cmd.Flags().Float64Var(&bodyWisePayPaymentBatchPaymentAmount, "wise-pay-payment-batch-payment-amount", 0.0, "Amount")
	cmd.Flags().StringVar(&bodyWisePayPaymentBatchPaymentWisePayHref, "wise-pay-payment-batch-payment-wise-pay-href", "", "Wise pay href")
	cmd.Flags().Float64Var(&bodyWisePayPaymentFeeInvoiceAmount, "wise-pay-payment-fee-invoice-amount", 0.0, "Amount")
	cmd.Flags().StringVar(&bodyWisePayPaymentFeeInvoiceId, "wise-pay-payment-fee-invoice-id", "", "Id")
	cmd.Flags().StringVar(&bodyWisePayPaymentFeeInvoiceInvoiceHref, "wise-pay-payment-fee-invoice-invoice-href", "", "Invoice href")
	cmd.Flags().StringVar(&bodyWisePayPaymentFeeInvoiceInvoiceNumber, "wise-pay-payment-fee-invoice-invoice-number", "", "Invoice number")
	cmd.Flags().StringVar(&bodyWisePayPaymentPaymentDateUtc, "wise-pay-payment-payment-date-utc", "", "Payment date utc")
	cmd.Flags().StringVar(&bodyWisePayPaymentWisePayReference, "wise-pay-payment-wise-pay-reference", "", "Wise pay reference")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
