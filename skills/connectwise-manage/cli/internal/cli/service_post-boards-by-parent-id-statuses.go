// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newServicePostBoardsByParentIdStatusesCmd(flags *rootFlags) *cobra.Command {
	var bodyInfo string
	var bodyBoardInfo string
	var bodyBoardId string
	var bodyBoardName string
	var bodyClosedStatus bool
	var bodyCustomStatusIndicatorName string
	var bodyCustomerPortalDescription string
	var bodyCustomerPortalFlag bool
	var bodyDefaultFlag bool
	var bodyDisplayOnBoard bool
	var bodyEmailTemplateInfo string
	var bodyEmailTemplateId string
	var bodyEmailTemplateIdentifier string
	var bodyEmailTemplateName string
	var bodyEmailTemplateType string
	var bodyEscalationStatus string
	var bodyId string
	var bodyInactive bool
	var bodyName string
	var bodyRoundRobinCatchall bool
	var bodySaveTimeAsNote bool
	var bodySortOrder int
	var bodyStatusIndicatorInfo string
	var bodyStatusIndicatorId string
	var bodyStatusIndicatorIdentifier string
	var bodyStatusIndicatorName string
	var bodyTimeEntryNotAllowed bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-boards-by-parent-id-statuses <parentId>",
		Short:       "Post BoardStatus",
		Example:     "  connectwise-manage-cli service post-boards-by-parent-id-statuses 42 --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "service.post-boards-by-parent-id-statuses", "pp:method": "POST", "pp:path": "/service/boards/{parentId}/statuses"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/service/boards/{parentId}/statuses"
			path = replacePathParam(path, "parentId", args[0])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyInfo != "" {
					var parsedInfo any
					if err := json.Unmarshal([]byte(bodyInfo), &parsedInfo); err != nil {
						return fmt.Errorf("parsing --info JSON: %w", err)
					}
					body["_info"] = parsedInfo
				}
				{
					nestedBoard := map[string]any{}
					if bodyBoardInfo != "" {
						var parsedBoardInfo any
						if err := json.Unmarshal([]byte(bodyBoardInfo), &parsedBoardInfo); err != nil {
							return fmt.Errorf("parsing --board-info JSON: %w", err)
						}
						nestedBoard["_info"] = parsedBoardInfo
					}
					if bodyBoardId != "" {
						nestedBoard["id"] = bodyBoardId
					}
					if bodyBoardName != "" {
						nestedBoard["name"] = bodyBoardName
					}
					if len(nestedBoard) > 0 {
						body["board"] = nestedBoard
					}
				}
				if cmd.Flags().Changed("closed-status") {
					body["closedStatus"] = bodyClosedStatus
				}
				if bodyCustomStatusIndicatorName != "" {
					body["customStatusIndicatorName"] = bodyCustomStatusIndicatorName
				}
				if bodyCustomerPortalDescription != "" {
					body["customerPortalDescription"] = bodyCustomerPortalDescription
				}
				if cmd.Flags().Changed("customer-portal-flag") {
					body["customerPortalFlag"] = bodyCustomerPortalFlag
				}
				if cmd.Flags().Changed("default-flag") {
					body["defaultFlag"] = bodyDefaultFlag
				}
				if cmd.Flags().Changed("display-on-board") {
					body["displayOnBoard"] = bodyDisplayOnBoard
				}
				{
					nestedEmailTemplate := map[string]any{}
					if bodyEmailTemplateInfo != "" {
						var parsedEmailTemplateInfo any
						if err := json.Unmarshal([]byte(bodyEmailTemplateInfo), &parsedEmailTemplateInfo); err != nil {
							return fmt.Errorf("parsing --email-template-info JSON: %w", err)
						}
						nestedEmailTemplate["_info"] = parsedEmailTemplateInfo
					}
					if bodyEmailTemplateId != "" {
						nestedEmailTemplate["id"] = bodyEmailTemplateId
					}
					if bodyEmailTemplateIdentifier != "" {
						nestedEmailTemplate["identifier"] = bodyEmailTemplateIdentifier
					}
					if bodyEmailTemplateName != "" {
						nestedEmailTemplate["name"] = bodyEmailTemplateName
					}
					if bodyEmailTemplateType != "" {
						nestedEmailTemplate["type"] = bodyEmailTemplateType
					}
					if len(nestedEmailTemplate) > 0 {
						body["emailTemplate"] = nestedEmailTemplate
					}
				}
				if bodyEscalationStatus != "" {
					body["escalationStatus"] = bodyEscalationStatus
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if cmd.Flags().Changed("inactive") {
					body["inactive"] = bodyInactive
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if cmd.Flags().Changed("round-robin-catchall") {
					body["roundRobinCatchall"] = bodyRoundRobinCatchall
				}
				if cmd.Flags().Changed("save-time-as-note") {
					body["saveTimeAsNote"] = bodySaveTimeAsNote
				}
				if bodySortOrder != 0 {
					body["sortOrder"] = bodySortOrder
				}
				{
					nestedStatusIndicator := map[string]any{}
					if bodyStatusIndicatorInfo != "" {
						var parsedStatusIndicatorInfo any
						if err := json.Unmarshal([]byte(bodyStatusIndicatorInfo), &parsedStatusIndicatorInfo); err != nil {
							return fmt.Errorf("parsing --status-indicator-info JSON: %w", err)
						}
						nestedStatusIndicator["_info"] = parsedStatusIndicatorInfo
					}
					if bodyStatusIndicatorId != "" {
						nestedStatusIndicator["id"] = bodyStatusIndicatorId
					}
					if bodyStatusIndicatorIdentifier != "" {
						nestedStatusIndicator["identifier"] = bodyStatusIndicatorIdentifier
					}
					if bodyStatusIndicatorName != "" {
						nestedStatusIndicator["name"] = bodyStatusIndicatorName
					}
					if len(nestedStatusIndicator) > 0 {
						body["statusIndicator"] = nestedStatusIndicator
					}
				}
				if cmd.Flags().Changed("time-entry-not-allowed") {
					body["timeEntryNotAllowed"] = bodyTimeEntryNotAllowed
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "service", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "service", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "service", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "service", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "service", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "service",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "service", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "service", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyInfo, "info", "", "Info")
	cmd.Flags().StringVar(&bodyBoardInfo, "board-info", "", "Info")
	cmd.Flags().StringVar(&bodyBoardId, "board-id", "", "Id")
	cmd.Flags().StringVar(&bodyBoardName, "board-name", "", "Name")
	cmd.Flags().BoolVar(&bodyClosedStatus, "closed-status", false, "Closed status")
	cmd.Flags().StringVar(&bodyCustomStatusIndicatorName, "custom-status-indicator-name", "", "Max length: 30;")
	cmd.Flags().StringVar(&bodyCustomerPortalDescription, "customer-portal-description", "", "Max length: 500;")
	cmd.Flags().BoolVar(&bodyCustomerPortalFlag, "customer-portal-flag", false, "Customer portal flag")
	cmd.Flags().BoolVar(&bodyDefaultFlag, "default-flag", false, "Default flag")
	cmd.Flags().BoolVar(&bodyDisplayOnBoard, "display-on-board", false, "Display on board")
	cmd.Flags().StringVar(&bodyEmailTemplateInfo, "email-template-info", "", "Info")
	cmd.Flags().StringVar(&bodyEmailTemplateId, "email-template-id", "", "Id")
	cmd.Flags().StringVar(&bodyEmailTemplateIdentifier, "email-template-identifier", "", "Identifier")
	cmd.Flags().StringVar(&bodyEmailTemplateName, "email-template-name", "", "Name")
	cmd.Flags().StringVar(&bodyEmailTemplateType, "email-template-type", "", "Type")
	cmd.Flags().StringVar(&bodyEscalationStatus, "escalation-status", "", "Escalation status")
	cmd.Flags().StringVar(&bodyId, "id", "", "Id")
	cmd.Flags().BoolVar(&bodyInactive, "inactive", false, "Inactive")
	cmd.Flags().StringVar(&bodyName, "name", "", "Max length: 50;")
	cmd.Flags().BoolVar(&bodyRoundRobinCatchall, "round-robin-catchall", false, "Round robin catchall")
	cmd.Flags().BoolVar(&bodySaveTimeAsNote, "save-time-as-note", false, "Save time as note")
	cmd.Flags().IntVar(&bodySortOrder, "sort-order", 0, "Sort order")
	cmd.Flags().StringVar(&bodyStatusIndicatorInfo, "status-indicator-info", "", "Info")
	cmd.Flags().StringVar(&bodyStatusIndicatorId, "status-indicator-id", "", "Id")
	cmd.Flags().StringVar(&bodyStatusIndicatorIdentifier, "status-indicator-identifier", "", "Identifier")
	cmd.Flags().StringVar(&bodyStatusIndicatorName, "status-indicator-name", "", "Name")
	cmd.Flags().BoolVar(&bodyTimeEntryNotAllowed, "time-entry-not-allowed", false, "Time entry not allowed")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
