// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	"cove-pp-cli/internal/cli"
	"cove-pp-cli/internal/client"
	"cove-pp-cli/internal/config"
	"cove-pp-cli/internal/mcp/cobratree"
	"cove-pp-cli/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

const (
	mcpToolResultMaxBytes = 60000
	mcpToolResultMaxItems = 50
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("audit_list",
			mcplib.WithDescription("Enumerate audit actions in a time range (JSON-RPC EnumerateAuditActions). Optional: jsonrpc (default: 2.0), method (default: EnumerateAuditActions), id (default: 1) (plus 2 more). Returns array of AuditAction."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithString("params-action-info", mcplib.Description("Filter template (entity/operation/result), usually {}")),
			mcplib.WithNumber("params-from", mcplib.Description("Range start as Unix seconds")),
			mcplib.WithNumber("params-to", mcplib.Description("Range end as Unix seconds")),
			mcplib.WithNumber("params-count-limit", mcplib.Description("Max rows")),
			mcplib.WithBoolean("params-include-all-sub-partners", mcplib.Description("Include the whole subtree")),
			mcplib.WithBoolean("params-reverse-order", mcplib.Description("Newest first")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-action-info", WireName: "actionInfo", Location: "body", BodyPath: []string{"params", "actionInfo"}}, {PublicName: "params-from", WireName: "from", Location: "body", BodyPath: []string{"params", "from"}}, {PublicName: "params-to", WireName: "to", Location: "body", BodyPath: []string{"params", "to"}}, {PublicName: "params-count-limit", WireName: "countLimit", Location: "body", BodyPath: []string{"params", "countLimit"}}, {PublicName: "params-include-all-sub-partners", WireName: "includeAllSubPartners", Location: "body", BodyPath: []string{"params", "includeAllSubPartners"}}, {PublicName: "params-reverse-order", WireName: "reverseOrder", Location: "body", BodyPath: []string{"params", "reverseOrder"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("backup-jobs_list",
			mcplib.WithDescription("Enumerate jobs for a partner (JSON-RPC EnumerateJobs). Optional: jsonrpc (default: 2.0), method (default: EnumerateJobs), id (default: 1) (plus 2 more). Returns array of JobInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("columns_list",
			mcplib.WithDescription("Enumerate custom statistic columns visible to a partner (JSON-RPC EnumerateColumns). Optional: jsonrpc (default: 2.0), method (default: EnumerateColumns), id (default: 1) (plus 2 more). Returns array of ColumnInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("devices_get",
			mcplib.WithDescription("Get one device by id (JSON-RPC GetAccountInfoById). Optional: jsonrpc (default: 2.0), method (default: GetAccountInfoById), id (default: 1) (plus 2 more). Returns the new AccountInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-account-id", mcplib.Required(), mcplib.Description("Device (account) id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-account-id", WireName: "accountId", Location: "body", BodyPath: []string{"params", "accountId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("devices_list",
			mcplib.WithDescription("Enumerate backup devices for a partner (JSON-RPC EnumerateAccounts). Optional: jsonrpc (default: 2.0), method (default: EnumerateAccounts), id (default: 1) (plus 2 more). Returns array of AccountInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id whose devices to list")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("devices_stats",
			mcplib.WithDescription("Query device statistics by column codes (JSON-RPC EnumerateAccountStatistics; see README column-code legend). Optional: jsonrpc (default: 2.0), method (default: EnumerateAccountStatistics), id (default: 1) (plus 2 more). Returns array of AccountStatisticsRow."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-query-partner-id", mcplib.Required(), mcplib.Description("Partner id to query under")),
			mcplib.WithString("params-query-filter", mcplib.Description("Console filter expression, e.g. 'D1F00 == 2' (Files & Folders last session failed)")),
			mcplib.WithString("params-query-selection-mode", mcplib.Description("Merged | PerInstallation | Count")),
			mcplib.WithNumber("params-query-start-record-number", mcplib.Description("Paging offset")),
			mcplib.WithNumber("params-query-records-count", mcplib.Description("Page size")),
			mcplib.WithString("params-query-order-by", mcplib.Description("Column code to order by, e.g. 'I1' (device name)")),
			mcplib.WithString("params-query-columns", mcplib.Description("Column codes to return, e.g. ['I0','I1','I8','D9F00','F09']")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", false, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-query-partner-id", WireName: "PartnerId", Location: "body", BodyPath: []string{"params", "query", "PartnerId"}}, {PublicName: "params-query-filter", WireName: "Filter", Location: "body", BodyPath: []string{"params", "query", "Filter"}}, {PublicName: "params-query-selection-mode", WireName: "SelectionMode", Location: "body", BodyPath: []string{"params", "query", "SelectionMode"}}, {PublicName: "params-query-start-record-number", WireName: "StartRecordNumber", Location: "body", BodyPath: []string{"params", "query", "StartRecordNumber"}}, {PublicName: "params-query-records-count", WireName: "RecordsCount", Location: "body", BodyPath: []string{"params", "query", "RecordsCount"}}, {PublicName: "params-query-order-by", WireName: "OrderBy", Location: "body", BodyPath: []string{"params", "query", "OrderBy"}}, {PublicName: "params-query-columns", WireName: "Columns", Location: "body", BodyPath: []string{"params", "query", "Columns"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("labels_list",
			mcplib.WithDescription("Enumerate all device labels (JSON-RPC EnumerateAllLabels). Optional: jsonrpc (default: 2.0), method (default: EnumerateAllLabels), id (default: 1) (plus 1 more). Returns array of LabelInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("locations_list",
			mcplib.WithDescription("Enumerate data-center locations (JSON-RPC EnumerateLocations). Optional: jsonrpc (default: 2.0), method (default: EnumerateLocations), id (default: 1) (plus 1 more). Returns array of LocationInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("partners_get",
			mcplib.WithDescription("Get one partner by id (JSON-RPC GetPartnerInfoById). Optional: jsonrpc (default: 2.0), method (default: GetPartnerInfoById), id (default: 1) (plus 2 more). Returns the new PartnerInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("partners_list",
			mcplib.WithDescription("Enumerate partners under a parent (JSON-RPC EnumeratePartners). Optional: jsonrpc (default: 2.0), method (default: EnumeratePartners), id (default: 1) (plus 2 more). Returns array of PartnerInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-parent-partner-id", mcplib.Description("Partner id to enumerate under (your root partner id)")),
			mcplib.WithBoolean("params-fetch-recursively", mcplib.Description("Include the whole subtree, not just direct children")),
			mcplib.WithString("params-fields", mcplib.Description("PartnerField ids to return, e.g. [0,1,3,5,8,9,10,18,20]")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-parent-partner-id", WireName: "parentPartnerId", Location: "body", BodyPath: []string{"params", "parentPartnerId"}}, {PublicName: "params-fetch-recursively", WireName: "fetchRecursively", Location: "body", BodyPath: []string{"params", "fetchRecursively"}}, {PublicName: "params-fields", WireName: "fields", Location: "body", BodyPath: []string{"params", "fields"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("products_list",
			mcplib.WithDescription("Enumerate products (JSON-RPC EnumerateProducts). Optional: jsonrpc (default: 2.0), method (default: EnumerateProducts), id (default: 1) (plus 2 more). Returns array of ProductInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithBoolean("params-current-partner-only", mcplib.Description("Only products defined directly on this partner")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}, {PublicName: "params-current-partner-only", WireName: "currentPartnerOnly", Location: "body", BodyPath: []string{"params", "currentPartnerOnly"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("server_info",
			mcplib.WithDescription("Get management server info (JSON-RPC GetServerInfo). Optional: jsonrpc (default: 2.0), method (default: GetServerInfo), id (default: 1) (plus 1 more). Returns the new ServerInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", false, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_list",
			mcplib.WithDescription("Enumerate storage pools for a partner (JSON-RPC EnumerateStorages). Optional: jsonrpc (default: 2.0), method (default: EnumerateStorages), id (default: 1) (plus 2 more). Returns array of StorageInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_nodes",
			mcplib.WithDescription("Enumerate all storage nodes (JSON-RPC EnumerateAllStorageNodes). Optional: jsonrpc (default: 2.0), method (default: EnumerateAllStorageNodes), id (default: 1) (plus 1 more). Returns array of StorageNodeInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", false, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_stats",
			mcplib.WithDescription("Storage usage statistics per location (JSON-RPC EnumerateStorageStatistics). Optional: jsonrpc (default: 2.0), method (default: EnumerateStorageStatistics), id (default: 1) (plus 2 more). Returns array of StorageStatInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithNumber("params-partner-id", mcplib.Required(), mcplib.Description("Partner id")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", false, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-id", WireName: "partnerId", Location: "body", BodyPath: []string{"params", "partnerId"}}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_list",
			mcplib.WithDescription("Enumerate users for partners (JSON-RPC EnumerateUsers). Optional: jsonrpc (default: 2.0), method (default: EnumerateUsers), id (default: 1) (plus 2 more). Returns array of UserInfo."),
			mcplib.WithString("jsonrpc", mcplib.Description("JSON-RPC protocol version (leave at 2.0)")),
			mcplib.WithString("method", mcplib.Description("JSON-RPC method name (leave at default)")),
			mcplib.WithNumber("id", mcplib.Description("JSON-RPC request id")),
			mcplib.WithString("visa", mcplib.Description("Session token; pass --visa '$(cove-cli auth token)'")),
			mcplib.WithString("params-partner-ids", mcplib.Required(), mcplib.Description("Partner ids, e.g. [123456]")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/jsonapi", true, false, nil, []mcpParamBinding{{PublicName: "jsonrpc", WireName: "jsonrpc", Location: "body"}, {PublicName: "method", WireName: "method", Location: "body"}, {PublicName: "id", WireName: "id", Location: "body"}, {PublicName: "visa", WireName: "visa", Location: "body"}, {PublicName: "params-partner-ids", WireName: "partnerIds", Location: "body", BodyPath: []string{"params", "partnerIds"}}}, []string{}),
	)
	// Intent tools — higher-level compositions declared in the spec or lifted from recipes.
	RegisterIntents(s)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='items'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	BodyPath   []string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		return fmt.Sprintf("%v", v)
	}
}
func setNestedBodyArg(body map[string]any, path []string, value any) {
	if len(path) == 0 {
		return
	}
	if len(path) == 1 {
		body[path[0]] = value
		return
	}
	current := body
	for _, key := range path[:len(path)-1] {
		next, ok := current[key].(map[string]any)
		if !ok {
			next = map[string]any{}
			current[key] = next
		}
		current = next
	}
	current[path[len(path)-1]] = value
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			case "body":
				if len(binding.BodyPath) > 0 {
					setNestedBodyArg(bodyArgs, binding.BodyPath, v)
				} else {
					bodyArgs[binding.WireName] = v
				}
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'cove-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'cove-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	trimmed := strings.TrimSpace(string(data))
	if strings.EqualFold(method, "GET") && len(trimmed) > 0 && trimmed[0] == '[' {
		var items []json.RawMessage
		if json.Unmarshal(data, &items) == nil {
			return mcplib.NewToolResultText(string(mcpBoundedListEnvelope("items", items, len(data))))
		}
	}
	if len(data) <= mcpToolResultMaxBytes {
		return mcplib.NewToolResultText(string(data))
	}
	if strings.EqualFold(method, "GET") {
		if out, ok := mcpBoundedSingleArrayObject(data); ok {
			return mcplib.NewToolResultText(string(out))
		}
	}
	return mcplib.NewToolResultText(string(mcpOversizedPreviewEnvelope(data)))
}

func mcpBoundedSingleArrayObject(data json.RawMessage) ([]byte, bool) {
	var obj map[string]json.RawMessage
	if json.Unmarshal(data, &obj) != nil {
		return nil, false
	}
	arrayField := ""
	var items []json.RawMessage
	for key, raw := range obj {
		trimmed := strings.TrimSpace(string(raw))
		if len(trimmed) == 0 || trimmed[0] != '[' {
			continue
		}
		var candidate []json.RawMessage
		if json.Unmarshal(raw, &candidate) != nil {
			continue
		}
		if arrayField != "" {
			return nil, false
		}
		arrayField = key
		items = candidate
	}
	if arrayField == "" {
		return nil, false
	}
	build := func(subset []json.RawMessage) any {
		out := make(map[string]any, len(obj)+6)
		for key, raw := range obj {
			if key == arrayField {
				out[key] = subset
				continue
			}
			out[key] = raw
		}
		if len(subset) < len(items) {
			out["_pp_truncated"] = true
			out["_pp_total_count"] = len(items)
			out["_pp_returned_count"] = len(subset)
			out["_pp_original_bytes"] = len(data)
			out["_pp_max_bytes"] = mcpToolResultMaxBytes
			out["_pp_note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	out := mcpFitJSONItems(items, build)
	if len(out) > mcpToolResultMaxBytes {
		return nil, false
	}
	return out, true
}

func mcpBoundedListEnvelope(field string, items []json.RawMessage, originalBytes int) []byte {
	build := func(subset []json.RawMessage) any {
		out := map[string]any{
			"count": len(items),
			field:   subset,
		}
		if len(subset) < len(items) {
			out["truncated"] = true
			out["returned_count"] = len(subset)
			out["original_bytes"] = originalBytes
			out["max_bytes"] = mcpToolResultMaxBytes
			out["note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	return mcpFitJSONItems(items, build)
}

func mcpFitJSONItems(items []json.RawMessage, build func([]json.RawMessage) any) []byte {
	limit := len(items)
	if limit > mcpToolResultMaxItems {
		limit = mcpToolResultMaxItems
	}
	for n := limit; n >= 0; n-- {
		out, err := json.Marshal(build(items[:n]))
		if err != nil {
			continue
		}
		if len(out) <= mcpToolResultMaxBytes || n == 0 {
			return out
		}
	}
	out, _ := json.Marshal(build(items[:0]))
	return out
}

func mcpOversizedPreviewEnvelope(data json.RawMessage) []byte {
	previewBytes := data
	if len(previewBytes) > 4000 {
		previewBytes = previewBytes[:4000]
	}
	out, _ := json.Marshal(map[string]any{
		"truncated":      true,
		"original_bytes": len(data),
		"max_bytes":      mcpToolResultMaxBytes,
		"preview":        string(previewBytes),
		"note":           "Typed MCP endpoint response exceeded the tool result budget and was not a recognized list envelope. Narrow the request with filters, search/sql, or a command-mirror tool with --agent/--compact/--select.",
	})
	return out
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "cove-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "cove-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	return toolResultJSON(results)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	data, err := json.MarshalIndent(v, "", "  ")
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "cove",
		"description": "The first CLI and MCP server for Cove Data Protection — fleet-wide backup health, billing usage, and storage trends from a terminal, with the local history the vendor console doesn't keep.",
		"archetype":   "infrastructure",
		"tool_count":  16,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion cove-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "audit",
				"description": "Management console audit trail",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "backup-jobs",
				"description": "Backup/restore jobs",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "columns",
				"description": "Statistics column metadata (pairs with the README column-code legend)",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "devices",
				"description": "Backup devices (accounts) and their column-coded statistics",
				"endpoints":   []string{"get", "list", "stats"},
				"searchable":  true,
			},
			{
				"name":        "labels",
				"description": "Device labels",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "locations",
				"description": "Data-center locations",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "partners",
				"description": "Customers and resellers in the partner hierarchy",
				"endpoints":   []string{"get", "list"},
				"searchable":  true,
			},
			{
				"name":        "products",
				"description": "Backup products available to a partner",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "server",
				"description": "Management service metadata",
				"endpoints":   []string{"info"},
				"searchable":  true,
			},
			{
				"name":        "storage",
				"description": "Storage pools, statistics, and nodes",
				"endpoints":   []string{"list", "nodes", "stats"},
				"searchable":  true,
			},
			{
				"name":        "users",
				"description": "Console users",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Storage growth / drift", "command": "storage growth", "description": "See which devices and customers are growing their backup storage fastest, from timestamped local snapshots.", "rationale": "The Cove console keeps zero history — the trend line only exists because cove-cli snapshots used-storage (I14) into SQLite over time.", "via": "mcp-command-mirror"},
			{"name": "Fleet failure sweep", "command": "devices failures", "description": "Every device across all customers whose last backup session failed, aborted, errored, or never started — decoded to status names.", "rationale": "The console scopes to one partner branch at a time and shows last-session status as an opaque integer; this is one cross-tree call with the F00 enum decoded.", "via": "mcp-command-mirror"},
			{"name": "Stale-device triage", "command": "devices stale", "description": "Devices with no successful backup in N days, ranked by staleness and grouped by customer.", "rationale": "Computes age from the F09 last-successful-session timestamp across the whole partner tree — the console shows raw timestamps one branch at a time.", "via": "mcp-command-mirror"},
			{"name": "Status drift / what-changed", "command": "devices changes", "description": "Devices whose backup status flipped between the two latest snapshots — regressions and recoveries without re-sweeping the fleet.", "rationale": "Snapshot-to-snapshot F00 diff in local SQLite; impossible from the stateless vendor console.", "via": "mcp-command-mirror"},
			{"name": "Fleet health rollup", "command": "fleet health", "description": "One-screen rollup: total devices, healthy, failed, stale, never-run — with a per-customer breakdown.", "rationale": "Aggregates EnumerateAccountStatistics rows across every partner into the single-pane view the console structurally withholds.", "via": "mcp-command-mirror"},
			{"name": "Billing usage report", "command": "billing usage", "description": "Per-device SKU, used storage, and M365 seat counts with column codes decoded — the month-end billing export in one command.", "rationale": "Fleet-wide preset over the I57/I14/D19F20 billing columns; replaces per-partner PowerShell report scripts.", "via": "mcp-command-mirror"},
			{"name": "SKU change / billing delta", "command": "billing changes", "description": "Devices whose plan changed since last month — current SKU vs Cove's built-in previous-month SKU column.", "rationale": "Exploits the purpose-built I57-vs-I58 column pair; replaces the analyst's manual two-CSV diff every cycle.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Storage growth / drift", "insight": "The Cove console keeps zero history — the trend line only exists because cove-cli snapshots used-storage (I14) into SQLite over time."},
			{"topic": "Fleet failure sweep", "insight": "The console scopes to one partner branch at a time and shows last-session status as an opaque integer; this is one cross-tree call with the F00 enum decoded."},
			{"topic": "Stale-device triage", "insight": "Computes age from the F09 last-successful-session timestamp across the whole partner tree — the console shows raw timestamps one branch at a time."},
			{"topic": "Status drift / what-changed", "insight": "Snapshot-to-snapshot F00 diff in local SQLite; impossible from the stateless vendor console."},
			{"topic": "Fleet health rollup", "insight": "Aggregates EnumerateAccountStatistics rows across every partner into the single-pane view the console structurally withholds."},
			{"topic": "Billing usage report", "insight": "Fleet-wide preset over the I57/I14/D19F20 billing columns; replaces per-partner PowerShell report scripts."},
			{"topic": "SKU change / billing delta", "insight": "Exploits the purpose-built I57-vs-I58 column pair; replaces the analyst's manual two-CSV diff every cycle."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
