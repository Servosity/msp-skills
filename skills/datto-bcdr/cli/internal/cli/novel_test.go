// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import "testing"

func TestCompareVersions(t *testing.T) {
	tests := []struct {
		name string
		a, b string
		want int
	}{
		{"patch greater", "1.10.0", "1.9.9", 1},
		{"patch lesser", "1.9.9", "1.10.0", -1},
		{"minor numeric not lexical", "2.0", "1.99", 1},
		{"equal", "1.2.3", "1.2.3", 0},
		{"equal with trailing zero segment", "1.2", "1.2.0", 0},
		{"both empty", "", "", 0},
		{"empty vs zero", "", "0.0.0", 0},
		{"empty less than real", "", "1.0.0", -1},
		{"real greater than empty", "1.0.0", "", 1},
		{"longer-equal-prefix wins on extra segment", "1.0.1", "1.0", 1},
		{"shorter loses to extra segment", "1.0", "1.0.1", -1},
		{"non-numeric tail ignored", "1.2-beta", "1.2", 0},
		{"non-numeric tail compares numerically", "1.10-rc", "1.9", 1},
		{"double-digit patch", "1.0.20", "1.0.3", 1},
		{"three-way major", "3.0.0", "2.9.9", 1},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := compareVersions(tt.a, tt.b); got != tt.want {
				t.Fatalf("compareVersions(%q, %q) = %d, want %d", tt.a, tt.b, got, tt.want)
			}
			// Antisymmetry: compareVersions(b, a) must be the negation.
			if got := compareVersions(tt.b, tt.a); got != -tt.want {
				t.Fatalf("compareVersions(%q, %q) = %d, want %d (antisymmetry)", tt.b, tt.a, got, -tt.want)
			}
		})
	}
}

func TestNewestVersion(t *testing.T) {
	tests := []struct {
		name     string
		versions []string
		want     string
	}{
		{"empty slice", nil, ""},
		{"all empty strings", []string{"", "", ""}, ""},
		{"single", []string{"1.2.3"}, "1.2.3"},
		{"newest is highest patch", []string{"1.2.0", "1.10.0", "1.10.0"}, "1.10.0"},
		{"numeric not lexical", []string{"1.99", "2.0"}, "2.0"},
		{"skips empties", []string{"", "1.0.0", "", "1.2.0"}, "1.2.0"},
		{"mixed lengths", []string{"1.0", "1.0.1", "1.0.0"}, "1.0.1"},
		{"duplicates of newest", []string{"3.1.4", "3.1.4", "2.0.0"}, "3.1.4"},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := newestVersion(tt.versions); got != tt.want {
				t.Fatalf("newestVersion(%v) = %q, want %q", tt.versions, got, tt.want)
			}
		})
	}
}
