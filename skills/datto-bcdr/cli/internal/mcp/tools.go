// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"datto-bcdr-pp-cli/internal/cli"
	"datto-bcdr-pp-cli/internal/client"
	"datto-bcdr-pp-cli/internal/cliutil"
	"datto-bcdr-pp-cli/internal/config"
	"datto-bcdr-pp-cli/internal/mcp/cobratree"
	"datto-bcdr-pp-cli/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("agent_by-device",
			mcplib.WithDescription("List protected agents on a specific device. Required: serial. Optional: page (default: 1), per-page (default: 100). Returns array of Asset."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/asset/agent", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}, {PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}}, []string{"serialNumber"}),
	)
	s.AddTool(
		mcplib.NewTool("agent_list",
			mcplib.WithDescription("List every protected agent across all devices. Optional: page (default: 1), per-page (default: 100). Returns array of Asset."),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/agent", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("alert_list",
			mcplib.WithDescription("List open alerts for a device. Required: serial. Optional: page (default: 1), per-page (default: 100). Returns array of Alert."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/alert", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}, {PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}}, []string{"serialNumber"}),
	)
	s.AddTool(
		mcplib.NewTool("asset_get",
			mcplib.WithDescription("Get a single asset by its volume name. Required: serial, volume. Returns the Asset."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithString("volume", mcplib.Required(), mcplib.Description("Asset volume name (GUID or share name)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/asset/{volumeName}", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}, {PublicName: "volume", WireName: "volumeName", Location: "path"}}, []string{"serialNumber", "volumeName"}),
	)
	s.AddTool(
		mcplib.NewTool("asset_list",
			mcplib.WithDescription("List all assets (agents + shares) on a device. Required: serial. Optional: page (default: 1), per-page (default: 100). Returns array of Asset."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/asset", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}, {PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}}, []string{"serialNumber"}),
	)
	s.AddTool(
		mcplib.NewTool("device_get",
			mcplib.WithDescription("Get a single BCDR device by serial number. Required: serial. Returns the Device."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}}, []string{"serialNumber"}),
	)
	s.AddTool(
		mcplib.NewTool("device_list",
			mcplib.WithDescription("List all BCDR devices in the partner account. Optional: page (default: 1), per-page (default: 100), show-hidden (plus 1 more). Returns array of Device."),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100, max 250)")),
			mcplib.WithString("show-hidden", mcplib.Description("Include hidden devices: '1' to show, '0' to omit")),
			mcplib.WithString("show-child-reseller", mcplib.Description("Include child-reseller devices: '1' to show, '0' to omit")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}, {PublicName: "show-hidden", WireName: "showHiddenDevices", Location: "query"}, {PublicName: "show-child-reseller", WireName: "showChildResellerDevices", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("shares_list",
			mcplib.WithDescription("List protected shares on a device. Required: serial. Optional: page (default: 1), per-page (default: 100). Returns array of Asset."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithNumber("page", mcplib.Description("Page number to fetch (default 1)")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/asset/share", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}, {PublicName: "page", WireName: "_page", Location: "query"}, {PublicName: "per-page", WireName: "_perPage", Location: "query"}}, []string{"serialNumber"}),
	)
	s.AddTool(
		mcplib.NewTool("vm-restore_list",
			mcplib.WithDescription("List VM restore sessions on a device. Required: serial. Returns array of VMRestore."),
			mcplib.WithString("serial", mcplib.Required(), mcplib.Description("Device serial number")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/bcdr/device/{serialNumber}/vm-restores", true, false, nil, []mcpParamBinding{{PublicName: "serial", WireName: "serialNumber", Location: "path"}}, []string{"serialNumber"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export DATTO_BCDR_PUBLIC_KEY=<your-key>" +
					"\n      Run 'datto-bcdr-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export DATTO_BCDR_PUBLIC_KEY=<your-key>" +
					"\n      Run 'datto-bcdr-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export DATTO_BCDR_PUBLIC_KEY=<your-key>" +
					"\n      Run 'datto-bcdr-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "datto-bcdr-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "datto-bcdr-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	return toolResultJSON(results)
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	return toolResultJSON(results)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	data, err := json.MarshalIndent(v, "", "  ")
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "datto-bcdr",
		"description": "Sync your whole Datto BCDR fleet into local SQLite and answer the questions the per-appliance Partner Portal can't",
		"archetype":   "content",
		"tool_count":  9,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion datto-bcdr-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name":        "DATTO_BCDR_PUBLIC_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "DATTO_BCDR_SECRET_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "agent",
				"description": "Protected agents (machines) across the fleet or on one device",
				"endpoints":   []string{"by-device", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "alert",
				"description": "Open alerts raised by a device",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "asset",
				"description": "All assets (agents and shares) on a device, plus single-volume detail",
				"endpoints":   []string{"get", "list"},
				"searchable":  true,
			},
			{
				"name":        "device",
				"description": "Datto BCDR appliances (SIRIS / ALTO / NAS / Backup-for-Azure)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "shares",
				"description": "Network shares protected on a device",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
			{
				"name":        "vm-restore",
				"description": "Virtualization / VM restore sessions on a device",
				"endpoints":   []string{"list"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Fleet Screenshot Failures", "command": "screenshots", "description": "See every protected machine whose last backup-bootability screenshot failed, across your entire fleet", "rationale": "Only possible because the last screenshot-verification status for every agent on every device lives in one local store", "via": "mcp-command-mirror"},
			{"name": "Client Risk Roll-up", "command": "client-risk", "description": "Per-client risk scorecard that rolls up screenshot failures, stale backups, open alerts, storage pressure", "rationale": "Only the local store can group devices, agents, and alerts by client company and compute a composite score", "via": "mcp-command-mirror"},
			{"name": "Stale Backup Detector", "command": "stale-backups", "description": "Find every agent whose last local snapshot or last offsite sync is older than a threshold", "rationale": "Requires a time-window comparison of last-snapshot and last-offsite epochs joined across every device in the store — no", "via": "mcp-command-mirror"},
			{"name": "Orphaned & Paused Assets", "command": "forgotten-assets", "description": "List agents that are paused or archived and devices that haven't checked in recently, across the whole fleet", "rationale": "Needs a fleet-wide scan of isPaused and isArchived across all agents joined to device last-seen dates", "via": "mcp-command-mirror"},
			{"name": "Per-Client Recoverability Report", "command": "client-report", "description": "One QBR-ready health report for a single client: devices, agents, screenshot pass rate, stale backups", "rationale": "Requires a client-scoped join across devices, agents, screenshot status", "via": "mcp-command-mirror"},
			{"name": "Fleet Recoverability Scorecard", "command": "recoverability", "description": "One headline KPI", "rationale": "Joins screenshot-verification status AND backup recency across every agent in the local store into a single verifiable", "via": "mcp-command-mirror"},
			{"name": "Fleet Alert Triage", "command": "alert-triage", "description": "Every open alert across the whole fleet in one ranked view, grouped by client and device", "rationale": "Fans per-device alert lists into one local table and groups by client and severity — the API only serves alerts for one", "via": "mcp-command-mirror"},
			{"name": "Storage Runway Forecast", "command": "storage-runway", "description": "Rank every appliance by remaining local and offsite storage and flag the devices and clients closest to running out of", "rationale": "Requires aggregating per-device storage fields across the whole fleet and sorting by headroom — the API exposes storage", "via": "mcp-command-mirror"},
			{"name": "Outdated Agent Fleet Audit", "command": "agent-versions", "description": "Audit agent software versions across the entire fleet and flag every machine running an outdated agent", "rationale": "Requires computing the fleet's prevailing and newest agent version and joining every lagging agent back to its device", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Fleet Screenshot Failures", "insight": "Only possible because the last screenshot-verification status for every agent on every device lives in one local store; the Datto API returns this per-device only."},
			{"topic": "Client Risk Roll-up", "insight": "Only the local store can group devices, agents, and alerts by client company and compute a composite score; the API has no concept of a client rollup."},
			{"topic": "Stale Backup Detector", "insight": "Requires a time-window comparison of last-snapshot and last-offsite epochs joined across every device in the store — no single Datto call spans the fleet."},
			{"topic": "Orphaned & Paused Assets", "insight": "Needs a fleet-wide scan of isPaused and isArchived across all agents joined to device last-seen dates; the API only reveals paused state per device you already know to ask about."},
			{"topic": "Per-Client Recoverability Report", "insight": "Requires a client-scoped join across devices, agents, screenshot status, and alerts that only exists together in the local store; the API and Portal organize by appliance serial, not client."},
			{"topic": "Fleet Recoverability Scorecard", "insight": "Joins screenshot-verification status AND backup recency across every agent in the local store into a single verifiable metric — no Datto endpoint scores the fleet."},
			{"topic": "Fleet Alert Triage", "insight": "Fans per-device alert lists into one local table and groups by client and severity — the API only serves alerts for one serial number at a time."},
			{"topic": "Storage Runway Forecast", "insight": "Requires aggregating per-device storage fields across the whole fleet and sorting by headroom — the API exposes storage one device at a time with no fleet ranking."},
			{"topic": "Outdated Agent Fleet Audit", "insight": "Requires computing the fleet's prevailing and newest agent version and joining every lagging agent back to its device and client — a cross-device comparison no single endpoint performs."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
