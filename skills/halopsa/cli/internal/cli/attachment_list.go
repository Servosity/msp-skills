// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAttachmentListCmd(flags *rootFlags) *cobra.Command {
	var flagActionId string
	var flagDomotzagents bool
	var flagFiletype string
	var flagIdonly bool
	var flagIsxlsimport bool
	var flagOneAttachmentId string
	var flagOrder string
	var flagOrder2 string
	var flagOrder3 string
	var flagOrder4 string
	var flagOrder5 string
	var flagOrderdesc bool
	var flagOrderdesc2 bool
	var flagOrderdesc3 bool
	var flagOrderdesc4 bool
	var flagOrderdesc5 bool
	var flagPortal int
	var flagTicketId string
	var flagToken string
	var flagType int
	var flagUniqueId string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple Attachment. 				Requires authentication.",
		Example:     "  halopsa-cli attachment list",
		Annotations: map[string]string{"pp:endpoint": "attachment.list", "pp:method": "GET", "pp:path": "/Attachment", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Attachment"
			params := map[string]string{}
			if flagActionId != "" {
				params["action_id"] = formatCLIParamValue(flagActionId)
			}
			if flagDomotzagents != false {
				params["domotzagents"] = formatCLIParamValue(flagDomotzagents)
			}
			if flagFiletype != "" {
				params["filetype"] = formatCLIParamValue(flagFiletype)
			}
			if flagIdonly != false {
				params["idonly"] = formatCLIParamValue(flagIdonly)
			}
			if flagIsxlsimport != false {
				params["isxlsimport"] = formatCLIParamValue(flagIsxlsimport)
			}
			if flagOneAttachmentId != "" {
				params["one_attachment_id"] = formatCLIParamValue(flagOneAttachmentId)
			}
			if flagOrder != "" {
				params["order"] = formatCLIParamValue(flagOrder)
			}
			if flagOrder2 != "" {
				params["order2"] = formatCLIParamValue(flagOrder2)
			}
			if flagOrder3 != "" {
				params["order3"] = formatCLIParamValue(flagOrder3)
			}
			if flagOrder4 != "" {
				params["order4"] = formatCLIParamValue(flagOrder4)
			}
			if flagOrder5 != "" {
				params["order5"] = formatCLIParamValue(flagOrder5)
			}
			if flagOrderdesc != false {
				params["orderdesc"] = formatCLIParamValue(flagOrderdesc)
			}
			if flagOrderdesc2 != false {
				params["orderdesc2"] = formatCLIParamValue(flagOrderdesc2)
			}
			if flagOrderdesc3 != false {
				params["orderdesc3"] = formatCLIParamValue(flagOrderdesc3)
			}
			if flagOrderdesc4 != false {
				params["orderdesc4"] = formatCLIParamValue(flagOrderdesc4)
			}
			if flagOrderdesc5 != false {
				params["orderdesc5"] = formatCLIParamValue(flagOrderdesc5)
			}
			if flagPortal != 0 {
				params["portal"] = formatCLIParamValue(flagPortal)
			}
			if flagTicketId != "" {
				params["ticket_id"] = formatCLIParamValue(flagTicketId)
			}
			if flagToken != "" {
				params["token"] = formatCLIParamValue(flagToken)
			}
			if flagType != 0 {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagUniqueId != "" {
				params["unique_id"] = formatCLIParamValue(flagUniqueId)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "attachment", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagActionId, "action-id", "", "(int) Returns attachments from the action ID specified (requires ticket_id).")
	cmd.Flags().BoolVar(&flagDomotzagents, "domotzagents", false, "Domotzagents")
	cmd.Flags().StringVar(&flagFiletype, "filetype", "", "(string) Returns attachments of the specified file type.")
	cmd.Flags().BoolVar(&flagIdonly, "idonly", false, "Idonly")
	cmd.Flags().BoolVar(&flagIsxlsimport, "isxlsimport", false, "Isxlsimport")
	cmd.Flags().StringVar(&flagOneAttachmentId, "one-attachment-id", "", "One attachment id")
	cmd.Flags().StringVar(&flagOrder, "order", "", "Order")
	cmd.Flags().StringVar(&flagOrder2, "order2", "", "Order2")
	cmd.Flags().StringVar(&flagOrder3, "order3", "", "Order3")
	cmd.Flags().StringVar(&flagOrder4, "order4", "", "Order4")
	cmd.Flags().StringVar(&flagOrder5, "order5", "", "Order5")
	cmd.Flags().BoolVar(&flagOrderdesc, "orderdesc", false, "Orderdesc")
	cmd.Flags().BoolVar(&flagOrderdesc2, "orderdesc2", false, "Orderdesc2")
	cmd.Flags().BoolVar(&flagOrderdesc3, "orderdesc3", false, "Orderdesc3")
	cmd.Flags().BoolVar(&flagOrderdesc4, "orderdesc4", false, "Orderdesc4")
	cmd.Flags().BoolVar(&flagOrderdesc5, "orderdesc5", false, "Orderdesc5")
	cmd.Flags().IntVar(&flagPortal, "portal", 0, "Portal")
	cmd.Flags().StringVar(&flagTicketId, "ticket-id", "", "(int) Returns attachments from the ticket ID specified.")
	cmd.Flags().StringVar(&flagToken, "token", "", "Token")
	cmd.Flags().IntVar(&flagType, "type", 0, "(int) Returns attachments of the specified type.")
	cmd.Flags().StringVar(&flagUniqueId, "unique-id", "", "(int) Returns an attachment with the unique ID specified.")

	return cmd
}
