// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newFieldInfoListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessControlLevel int
	var flagDomain string
	var flagExcluderanges bool
	var flagExcludetables bool
	var flagExcludetableself bool
	var flagExtratype int
	var flagFieldtype int
	var flagFieldtypemultiple string
	var flagIncludecategories bool
	var flagIncludedatefields bool
	var flagIncludejirafields bool
	var flagIncluderemotefields bool
	var flagIncludevalues bool
	var flagInputtype int
	var flagIsapprovalstep bool
	var flagIsconfig bool
	var flagIscustomfieldsetup bool
	var flagSystemid string
	var flagTypeid string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple FieldInfo. 				Requires authentication.",
		Example:     "  halopsa-cli field-info list",
		Annotations: map[string]string{"pp:endpoint": "field-info.list", "pp:method": "GET", "pp:path": "/FieldInfo", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/FieldInfo"
			params := map[string]string{}
			if flagAccessControlLevel != 0 {
				params["access_control_level"] = formatCLIParamValue(flagAccessControlLevel)
			}
			if flagDomain != "" {
				params["domain"] = formatCLIParamValue(flagDomain)
			}
			if flagExcluderanges != false {
				params["excluderanges"] = formatCLIParamValue(flagExcluderanges)
			}
			if flagExcludetables != false {
				params["excludetables"] = formatCLIParamValue(flagExcludetables)
			}
			if flagExcludetableself != false {
				params["excludetableself"] = formatCLIParamValue(flagExcludetableself)
			}
			if flagExtratype != 0 {
				params["extratype"] = formatCLIParamValue(flagExtratype)
			}
			if flagFieldtype != 0 {
				params["fieldtype"] = formatCLIParamValue(flagFieldtype)
			}
			if flagFieldtypemultiple != "" {
				params["fieldtypemultiple"] = formatCLIParamValue(flagFieldtypemultiple)
			}
			if flagIncludecategories != false {
				params["includecategories"] = formatCLIParamValue(flagIncludecategories)
			}
			if flagIncludedatefields != false {
				params["includedatefields"] = formatCLIParamValue(flagIncludedatefields)
			}
			if flagIncludejirafields != false {
				params["includejirafields"] = formatCLIParamValue(flagIncludejirafields)
			}
			if flagIncluderemotefields != false {
				params["includeremotefields"] = formatCLIParamValue(flagIncluderemotefields)
			}
			if flagIncludevalues != false {
				params["includevalues"] = formatCLIParamValue(flagIncludevalues)
			}
			if flagInputtype != 0 {
				params["inputtype"] = formatCLIParamValue(flagInputtype)
			}
			if flagIsapprovalstep != false {
				params["isapprovalstep"] = formatCLIParamValue(flagIsapprovalstep)
			}
			if flagIsconfig != false {
				params["isconfig"] = formatCLIParamValue(flagIsconfig)
			}
			if flagIscustomfieldsetup != false {
				params["iscustomfieldsetup"] = formatCLIParamValue(flagIscustomfieldsetup)
			}
			if flagSystemid != "" {
				params["systemid"] = formatCLIParamValue(flagSystemid)
			}
			if flagTypeid != "" {
				params["typeid"] = formatCLIParamValue(flagTypeid)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "field-info", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAccessControlLevel, "access-control-level", 0, "Access control level")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().BoolVar(&flagExcluderanges, "excluderanges", false, "Excluderanges")
	cmd.Flags().BoolVar(&flagExcludetables, "excludetables", false, "Excludetables")
	cmd.Flags().BoolVar(&flagExcludetableself, "excludetableself", false, "Excludetableself")
	cmd.Flags().IntVar(&flagExtratype, "extratype", 0, "Extratype")
	cmd.Flags().IntVar(&flagFieldtype, "fieldtype", 0, "Fieldtype")
	cmd.Flags().StringVar(&flagFieldtypemultiple, "fieldtypemultiple", "", "Fieldtypemultiple")
	cmd.Flags().BoolVar(&flagIncludecategories, "includecategories", false, "Includecategories")
	cmd.Flags().BoolVar(&flagIncludedatefields, "includedatefields", false, "Includedatefields")
	cmd.Flags().BoolVar(&flagIncludejirafields, "includejirafields", false, "Includejirafields")
	cmd.Flags().BoolVar(&flagIncluderemotefields, "includeremotefields", false, "Includeremotefields")
	cmd.Flags().BoolVar(&flagIncludevalues, "includevalues", false, "Includevalues")
	cmd.Flags().IntVar(&flagInputtype, "inputtype", 0, "Inputtype")
	cmd.Flags().BoolVar(&flagIsapprovalstep, "isapprovalstep", false, "Isapprovalstep")
	cmd.Flags().BoolVar(&flagIsconfig, "isconfig", false, "Isconfig")
	cmd.Flags().BoolVar(&flagIscustomfieldsetup, "iscustomfieldsetup", false, "Iscustomfieldsetup")
	cmd.Flags().StringVar(&flagSystemid, "systemid", "", "Systemid")
	cmd.Flags().StringVar(&flagTypeid, "typeid", "", "Typeid")

	return cmd
}
