// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newIntegrationDataListIntegrationdata6Cmd(flags *rootFlags) *cobra.Command {
	var flagAlsoInclude string
	var flagDataType string
	var flagExcludeMapped bool
	var flagExtraFilter string
	var flagMappedOnly bool
	var flagNextUrl string
	var flagPaginate bool
	var flagParentId string
	var flagSubDataType string

	cmd := &cobra.Command{
		Use:         "list-integrationdata-6",
		Short:       ". 				Requires authentication.",
		Example:     "  halopsa-cli integration-data list-integrationdata-6",
		Annotations: map[string]string{"pp:endpoint": "integration-data.list-integrationdata-6", "pp:method": "GET", "pp:path": "/IntegrationData/Get/Auvik", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/IntegrationData/Get/Auvik"
			params := map[string]string{}
			if flagAlsoInclude != "" {
				params["alsoInclude"] = formatCLIParamValue(flagAlsoInclude)
			}
			if flagDataType != "" {
				params["dataType"] = formatCLIParamValue(flagDataType)
			}
			if flagExcludeMapped != false {
				params["excludeMapped"] = formatCLIParamValue(flagExcludeMapped)
			}
			if flagExtraFilter != "" {
				params["extraFilter"] = formatCLIParamValue(flagExtraFilter)
			}
			if flagMappedOnly != false {
				params["mappedOnly"] = formatCLIParamValue(flagMappedOnly)
			}
			if flagNextUrl != "" {
				params["next_url"] = formatCLIParamValue(flagNextUrl)
			}
			if flagPaginate != false {
				params["paginate"] = formatCLIParamValue(flagPaginate)
			}
			if flagParentId != "" {
				params["parentId"] = formatCLIParamValue(flagParentId)
			}
			if flagSubDataType != "" {
				params["subDataType"] = formatCLIParamValue(flagSubDataType)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "integration-data", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagAlsoInclude, "also-include", "", "Also include")
	cmd.Flags().StringVar(&flagDataType, "data-type", "", "Data type")
	cmd.Flags().BoolVar(&flagExcludeMapped, "exclude-mapped", false, "Exclude mapped")
	cmd.Flags().StringVar(&flagExtraFilter, "extra-filter", "", "Extra filter")
	cmd.Flags().BoolVar(&flagMappedOnly, "mapped-only", false, "Mapped only")
	cmd.Flags().StringVar(&flagNextUrl, "next-url", "", "Next url")
	cmd.Flags().BoolVar(&flagPaginate, "paginate", false, "Paginate")
	cmd.Flags().StringVar(&flagParentId, "parent-id", "", "Parent id")
	cmd.Flags().StringVar(&flagSubDataType, "sub-data-type", "", "Sub data type")

	return cmd
}
