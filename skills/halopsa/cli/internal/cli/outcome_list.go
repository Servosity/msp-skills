// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOutcomeListCmd(flags *rootFlags) *cobra.Command {
	var flagAccessControlLevel int
	var flagDebug bool
	var flagExcludesystemactions bool
	var flagQuickOnly bool
	var flagShowhidden bool
	var flagShowsystemactions string
	var flagSlastate int
	var flagStatus int
	var flagSupplierId string
	var flagSupplierStatus int
	var flagTickettypeId string
	var flagWorkflowId string
	var flagWorkflowStep int

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple TOutcome. 				Requires authentication.",
		Example:     "  halopsa-cli outcome list",
		Annotations: map[string]string{"pp:endpoint": "outcome.list", "pp:method": "GET", "pp:path": "/Outcome", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Outcome"
			params := map[string]string{}
			if flagAccessControlLevel != 0 {
				params["access_control_level"] = formatCLIParamValue(flagAccessControlLevel)
			}
			if flagDebug != false {
				params["debug"] = formatCLIParamValue(flagDebug)
			}
			if flagExcludesystemactions != false {
				params["excludesystemactions"] = formatCLIParamValue(flagExcludesystemactions)
			}
			if flagQuickOnly != false {
				params["quick_only"] = formatCLIParamValue(flagQuickOnly)
			}
			if flagShowhidden != false {
				params["showhidden"] = formatCLIParamValue(flagShowhidden)
			}
			if flagShowsystemactions != "" {
				params["showsystemactions"] = formatCLIParamValue(flagShowsystemactions)
			}
			if flagSlastate != 0 {
				params["slastate"] = formatCLIParamValue(flagSlastate)
			}
			if flagStatus != 0 {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			if flagSupplierId != "" {
				params["supplier_id"] = formatCLIParamValue(flagSupplierId)
			}
			if flagSupplierStatus != 0 {
				params["supplier_status"] = formatCLIParamValue(flagSupplierStatus)
			}
			if flagTickettypeId != "" {
				params["tickettype_id"] = formatCLIParamValue(flagTickettypeId)
			}
			if flagWorkflowId != "" {
				params["workflow_id"] = formatCLIParamValue(flagWorkflowId)
			}
			if flagWorkflowStep != 0 {
				params["workflow_step"] = formatCLIParamValue(flagWorkflowStep)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "outcome", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagAccessControlLevel, "access-control-level", 0, "Access control level")
	cmd.Flags().BoolVar(&flagDebug, "debug", false, "Debug")
	cmd.Flags().BoolVar(&flagExcludesystemactions, "excludesystemactions", false, "Excludesystemactions")
	cmd.Flags().BoolVar(&flagQuickOnly, "quick-only", false, "Quick only")
	cmd.Flags().BoolVar(&flagShowhidden, "showhidden", false, "Showhidden")
	cmd.Flags().StringVar(&flagShowsystemactions, "showsystemactions", "", "Showsystemactions")
	cmd.Flags().IntVar(&flagSlastate, "slastate", 0, "Slastate")
	cmd.Flags().IntVar(&flagStatus, "status", 0, "Status")
	cmd.Flags().StringVar(&flagSupplierId, "supplier-id", "", "Supplier id")
	cmd.Flags().IntVar(&flagSupplierStatus, "supplier-status", 0, "Supplier status")
	cmd.Flags().StringVar(&flagTickettypeId, "tickettype-id", "", "Tickettype id")
	cmd.Flags().StringVar(&flagWorkflowId, "workflow-id", "", "Workflow id")
	cmd.Flags().IntVar(&flagWorkflowStep, "workflow-step", 0, "Workflow step")

	return cmd
}
