// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPurchaseOrderListCmd(flags *rootFlags) *cobra.Command {
	var flagAwaitingApproval bool
	var flagAwaitingstock bool
	var flagClientId string
	var flagClosed bool
	var flagCount int
	var flagDeliverToUs bool
	var flagDeliverToUserSalesorderId string
	var flagMyApprovals bool
	var flagOpen bool
	var flagOrder string
	var flagOrder2 string
	var flagOrder3 string
	var flagOrder4 string
	var flagOrder5 string
	var flagOrderdesc bool
	var flagOrderdesc2 bool
	var flagOrderdesc3 bool
	var flagOrderdesc4 bool
	var flagOrderdesc5 bool
	var flagPageNo int
	var flagPageSize int
	var flagPageinate bool
	var flagPoStatus string
	var flagSalesorderId string
	var flagSearch string
	var flagSiteId string
	var flagSupplierId string
	var flagTicketId string
	var flagUnsent bool
	var flagUserId string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple SupplierOrderHeader. 				Requires authentication.",
		Example:     "  halopsa-cli purchase-order list",
		Annotations: map[string]string{"pp:endpoint": "purchase-order.list", "pp:method": "GET", "pp:path": "/PurchaseOrder", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/PurchaseOrder"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "purchase-order", path, map[string]string{
				"awaiting_approval":             formatCLIParamValue(flagAwaitingApproval),
				"awaitingstock":                 formatCLIParamValue(flagAwaitingstock),
				"client_id":                     formatCLIParamValue(flagClientId),
				"closed":                        formatCLIParamValue(flagClosed),
				"count":                         formatCLIParamValue(flagCount),
				"deliver_to_us":                 formatCLIParamValue(flagDeliverToUs),
				"deliver_to_user_salesorder_id": formatCLIParamValue(flagDeliverToUserSalesorderId),
				"my_approvals":                  formatCLIParamValue(flagMyApprovals),
				"open":                          formatCLIParamValue(flagOpen),
				"order":                         formatCLIParamValue(flagOrder),
				"order2":                        formatCLIParamValue(flagOrder2),
				"order3":                        formatCLIParamValue(flagOrder3),
				"order4":                        formatCLIParamValue(flagOrder4),
				"order5":                        formatCLIParamValue(flagOrder5),
				"orderdesc":                     formatCLIParamValue(flagOrderdesc),
				"orderdesc2":                    formatCLIParamValue(flagOrderdesc2),
				"orderdesc3":                    formatCLIParamValue(flagOrderdesc3),
				"orderdesc4":                    formatCLIParamValue(flagOrderdesc4),
				"orderdesc5":                    formatCLIParamValue(flagOrderdesc5),
				"page_no":                       formatCLIParamValue(flagPageNo),
				"page_size":                     formatCLIParamValue(flagPageSize),
				"pageinate":                     formatCLIParamValue(flagPageinate),
				"po_status":                     formatCLIParamValue(flagPoStatus),
				"salesorder_id":                 formatCLIParamValue(flagSalesorderId),
				"search":                        formatCLIParamValue(flagSearch),
				"site_id":                       formatCLIParamValue(flagSiteId),
				"supplier_id":                   formatCLIParamValue(flagSupplierId),
				"ticket_id":                     formatCLIParamValue(flagTicketId),
				"unsent":                        formatCLIParamValue(flagUnsent),
				"user_id":                       formatCLIParamValue(flagUserId),
			}, nil, flagAll, "", "offset", "page_size", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagAwaitingApproval, "awaiting-approval", false, "(int) Filter by the suppliers that are awaiting approval.")
	cmd.Flags().BoolVar(&flagAwaitingstock, "awaitingstock", false, "(bool) Filter by suppliers awaiting stock.")
	cmd.Flags().StringVar(&flagClientId, "client-id", "", "(int) Filters by the specified client.")
	cmd.Flags().BoolVar(&flagClosed, "closed", false, "(bool) Filter by closed status.")
	cmd.Flags().IntVar(&flagCount, "count", 0, "(int) Number of contracts to return in the response.")
	cmd.Flags().BoolVar(&flagDeliverToUs, "deliver-to-us", false, "(bool) Filter on delivery to us option.")
	cmd.Flags().StringVar(&flagDeliverToUserSalesorderId, "deliver-to-user-salesorder-id", "", "Deliver to user salesorder id")
	cmd.Flags().BoolVar(&flagMyApprovals, "my-approvals", false, "(int) Filter by the suppliers that are awaiting approval from you.")
	cmd.Flags().BoolVar(&flagOpen, "open", false, "(bool) Filter by open status.")
	cmd.Flags().StringVar(&flagOrder, "order", "", "(string) The name of the field to order by first.")
	cmd.Flags().StringVar(&flagOrder2, "order2", "", "(string) The name of the field to order by second.")
	cmd.Flags().StringVar(&flagOrder3, "order3", "", "(string) The name of the field to order by third.")
	cmd.Flags().StringVar(&flagOrder4, "order4", "", "(string) The name of the field to order by fourth.")
	cmd.Flags().StringVar(&flagOrder5, "order5", "", "(string) The name of the field to order by fifth.")
	cmd.Flags().BoolVar(&flagOrderdesc, "orderdesc", false, "(bool) Whether to order ascending or descending on first order.")
	cmd.Flags().BoolVar(&flagOrderdesc2, "orderdesc2", false, "(bool) Whether to order ascending or descending on second order.")
	cmd.Flags().BoolVar(&flagOrderdesc3, "orderdesc3", false, "(bool) Whether to order ascending or descending on third order.")
	cmd.Flags().BoolVar(&flagOrderdesc4, "orderdesc4", false, "(bool) Whether to order ascending or descending on fourth order.")
	cmd.Flags().BoolVar(&flagOrderdesc5, "orderdesc5", false, "(bool) Whether to order ascending or descending on fifth order.")
	cmd.Flags().IntVar(&flagPageNo, "page-no", 0, "(int) When using Pagination, the page number to return.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "(int) When using Pagination, the size of the page.")
	cmd.Flags().BoolVar(&flagPageinate, "pageinate", false, "(bool) Whether to use Pagination in the response.")
	cmd.Flags().StringVar(&flagPoStatus, "po-status", "", "(string) Filters by the specified purchase order statuses - comma seperated.")
	cmd.Flags().StringVar(&flagSalesorderId, "salesorder-id", "", "(int) Filter by the specified sales order.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "(string) Filters response based on the search string.")
	cmd.Flags().StringVar(&flagSiteId, "site-id", "", "(int) Filters by the specified site.")
	cmd.Flags().StringVar(&flagSupplierId, "supplier-id", "", "(int) Filters by the specified supplier.")
	cmd.Flags().StringVar(&flagTicketId, "ticket-id", "", "(int) Filter by the specified ticket.")
	cmd.Flags().BoolVar(&flagUnsent, "unsent", false, "(bool) Filter by sent status.")
	cmd.Flags().StringVar(&flagUserId, "user-id", "", "(int) Filters by the specified user.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
