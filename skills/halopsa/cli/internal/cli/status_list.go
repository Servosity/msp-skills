// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newStatusListCmd(flags *rootFlags) *cobra.Command {
	var flagDomain string
	var flagExcludeclosed bool
	var flagExcludepending bool
	var flagOutcomeId string
	var flagShowall bool
	var flagShowcounts bool
	var flagShowquickchangeoptions bool
	var flagSplitClosed bool
	var flagTicketId string
	var flagTicketIdFirstchild int
	var flagTicketareaId string
	var flagTickettypeGroupId string
	var flagTickettypeId string
	var flagTickettypeIds string
	var flagType string
	var flagViewId string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple TStatus. 				Requires authentication.",
		Example:     "  halopsa-cli status list",
		Annotations: map[string]string{"pp:endpoint": "status.list", "pp:method": "GET", "pp:path": "/Status", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Status"
			params := map[string]string{}
			if flagDomain != "" {
				params["domain"] = formatCLIParamValue(flagDomain)
			}
			if flagExcludeclosed != false {
				params["excludeclosed"] = formatCLIParamValue(flagExcludeclosed)
			}
			if flagExcludepending != false {
				params["excludepending"] = formatCLIParamValue(flagExcludepending)
			}
			if flagOutcomeId != "" {
				params["outcome_id"] = formatCLIParamValue(flagOutcomeId)
			}
			if flagShowall != false {
				params["showall"] = formatCLIParamValue(flagShowall)
			}
			if flagShowcounts != false {
				params["showcounts"] = formatCLIParamValue(flagShowcounts)
			}
			if flagShowquickchangeoptions != false {
				params["showquickchangeoptions"] = formatCLIParamValue(flagShowquickchangeoptions)
			}
			if flagSplitClosed != false {
				params["split_closed"] = formatCLIParamValue(flagSplitClosed)
			}
			if flagTicketId != "" {
				params["ticket_id"] = formatCLIParamValue(flagTicketId)
			}
			if flagTicketIdFirstchild != 0 {
				params["ticket_id_firstchild"] = formatCLIParamValue(flagTicketIdFirstchild)
			}
			if flagTicketareaId != "" {
				params["ticketarea_id"] = formatCLIParamValue(flagTicketareaId)
			}
			if flagTickettypeGroupId != "" {
				params["tickettype_group_id"] = formatCLIParamValue(flagTickettypeGroupId)
			}
			if flagTickettypeId != "" {
				params["tickettype_id"] = formatCLIParamValue(flagTickettypeId)
			}
			if flagTickettypeIds != "" {
				params["tickettype_ids"] = formatCLIParamValue(flagTickettypeIds)
			}
			if flagType != "" {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagViewId != "" {
				params["view_id"] = formatCLIParamValue(flagViewId)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "status", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().BoolVar(&flagExcludeclosed, "excludeclosed", false, "Excludeclosed")
	cmd.Flags().BoolVar(&flagExcludepending, "excludepending", false, "Excludepending")
	cmd.Flags().StringVar(&flagOutcomeId, "outcome-id", "", "Outcome id")
	cmd.Flags().BoolVar(&flagShowall, "showall", false, "Showall")
	cmd.Flags().BoolVar(&flagShowcounts, "showcounts", false, "Showcounts")
	cmd.Flags().BoolVar(&flagShowquickchangeoptions, "showquickchangeoptions", false, "Showquickchangeoptions")
	cmd.Flags().BoolVar(&flagSplitClosed, "split-closed", false, "Split closed")
	cmd.Flags().StringVar(&flagTicketId, "ticket-id", "", "Ticket id")
	cmd.Flags().IntVar(&flagTicketIdFirstchild, "ticket-id-firstchild", 0, "Ticket id firstchild")
	cmd.Flags().StringVar(&flagTicketareaId, "ticketarea-id", "", "Ticketarea id")
	cmd.Flags().StringVar(&flagTickettypeGroupId, "tickettype-group-id", "", "Tickettype group id")
	cmd.Flags().StringVar(&flagTickettypeId, "tickettype-id", "", "Tickettype id")
	cmd.Flags().StringVar(&flagTickettypeIds, "tickettype-ids", "", "Tickettype ids")
	cmd.Flags().StringVar(&flagType, "type", "", "Type")
	cmd.Flags().StringVar(&flagViewId, "view-id", "", "View id")

	return cmd
}
