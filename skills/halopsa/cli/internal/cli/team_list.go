// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newTeamListCmd(flags *rootFlags) *cobra.Command {
	var flagCanEditOnly bool
	var flagChatOnly bool
	var flagDepartmentId string
	var flagDomain string
	var flagIds string
	var flagIncludeManagers bool
	var flagIncludeagentsforteams string
	var flagIncludedisabled string
	var flagIncludeenabled string
	var flagIstree bool
	var flagMemberonly bool
	var flagMydeps bool
	var flagMyteamsonly bool
	var flagOrderbyseq bool
	var flagOutcomeId string
	var flagShowall bool
	var flagShowcounts bool
	var flagTicketareaId string
	var flagType string
	var flagViewId string

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Use this to return multiple SectionDetail. 				Requires authentication.",
		Example:     "  halopsa-cli team list",
		Annotations: map[string]string{"pp:endpoint": "team.list", "pp:method": "GET", "pp:path": "/Team", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/Team"
			params := map[string]string{}
			if flagCanEditOnly != false {
				params["can_edit_only"] = formatCLIParamValue(flagCanEditOnly)
			}
			if flagChatOnly != false {
				params["chat_only"] = formatCLIParamValue(flagChatOnly)
			}
			if flagDepartmentId != "" {
				params["department_id"] = formatCLIParamValue(flagDepartmentId)
			}
			if flagDomain != "" {
				params["domain"] = formatCLIParamValue(flagDomain)
			}
			if flagIds != "" {
				params["ids"] = formatCLIParamValue(flagIds)
			}
			if flagIncludeManagers != false {
				params["include_managers"] = formatCLIParamValue(flagIncludeManagers)
			}
			if flagIncludeagentsforteams != "" {
				params["includeagentsforteams"] = formatCLIParamValue(flagIncludeagentsforteams)
			}
			if flagIncludedisabled != "" {
				params["includedisabled"] = formatCLIParamValue(flagIncludedisabled)
			}
			if flagIncludeenabled != "" {
				params["includeenabled"] = formatCLIParamValue(flagIncludeenabled)
			}
			if flagIstree != false {
				params["istree"] = formatCLIParamValue(flagIstree)
			}
			if flagMemberonly != false {
				params["memberonly"] = formatCLIParamValue(flagMemberonly)
			}
			if flagMydeps != false {
				params["mydeps"] = formatCLIParamValue(flagMydeps)
			}
			if flagMyteamsonly != false {
				params["myteamsonly"] = formatCLIParamValue(flagMyteamsonly)
			}
			if flagOrderbyseq != false {
				params["orderbyseq"] = formatCLIParamValue(flagOrderbyseq)
			}
			if flagOutcomeId != "" {
				params["outcome_id"] = formatCLIParamValue(flagOutcomeId)
			}
			if flagShowall != false {
				params["showall"] = formatCLIParamValue(flagShowall)
			}
			if flagShowcounts != false {
				params["showcounts"] = formatCLIParamValue(flagShowcounts)
			}
			if flagTicketareaId != "" {
				params["ticketarea_id"] = formatCLIParamValue(flagTicketareaId)
			}
			if flagType != "" {
				params["type"] = formatCLIParamValue(flagType)
			}
			if flagViewId != "" {
				params["view_id"] = formatCLIParamValue(flagViewId)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "team", true, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().BoolVar(&flagCanEditOnly, "can-edit-only", false, "(bool) Filter on teams that you have permission to edit.")
	cmd.Flags().BoolVar(&flagChatOnly, "chat-only", false, "(bool) Filter on teams that are enabled fro live chat.")
	cmd.Flags().StringVar(&flagDepartmentId, "department-id", "", "(int) Filter Teams belonging to a particular department.")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "(string) For use with counts: reqs - tickets, opps - opportunities, prjs - projects.")
	cmd.Flags().StringVar(&flagIds, "ids", "", "(string) Filter by team ids, comma seperated.")
	cmd.Flags().BoolVar(&flagIncludeManagers, "include-managers", false, "(bool) Show the managers in the response.")
	cmd.Flags().StringVar(&flagIncludeagentsforteams, "includeagentsforteams", "", "(string) Comma separated list of team names. Agents in these teams will be returned.")
	cmd.Flags().StringVar(&flagIncludedisabled, "includedisabled", "", "(string) Include disabled Teams in the response.")
	cmd.Flags().StringVar(&flagIncludeenabled, "includeenabled", "", "(string) Include enabled Teams in the response.")
	cmd.Flags().BoolVar(&flagIstree, "istree", false, "Istree")
	cmd.Flags().BoolVar(&flagMemberonly, "memberonly", false, "(bool) Only returns sections that the current agent is a member of.")
	cmd.Flags().BoolVar(&flagMydeps, "mydeps", false, "Mydeps")
	cmd.Flags().BoolVar(&flagMyteamsonly, "myteamsonly", false, "(bool) Only returns sections that you are a member of.")
	cmd.Flags().BoolVar(&flagOrderbyseq, "orderbyseq", false, "(bool) Order by sequence number.")
	cmd.Flags().StringVar(&flagOutcomeId, "outcome-id", "", "Outcome id")
	cmd.Flags().BoolVar(&flagShowall, "showall", false, "(bool) Admin override to show all teams, overriding team permissions.")
	cmd.Flags().BoolVar(&flagShowcounts, "showcounts", false, "(bool) Show the count of tickets in the response.")
	cmd.Flags().StringVar(&flagTicketareaId, "ticketarea-id", "", "(int) Only for showcounts - ticket area filter.")
	cmd.Flags().StringVar(&flagType, "type", "", "(string) Filter by team domain: reqs - tickets, opps - opportunities, prjs - projects.")
	cmd.Flags().StringVar(&flagViewId, "view-id", "", "(int) Only for showcounts - ticket view filter.")

	return cmd
}
