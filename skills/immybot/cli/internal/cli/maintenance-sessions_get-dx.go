// Copyright 2026 Servosity Inc. and msp-skills contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMaintenanceSessionsGetDxCmd(flags *rootFlags) *cobra.Command {
	var flagLoadOptions string
	var flagComputerId string
	var flagTenantId string
	var flagPersonId string
	var flagOnboardingFilter bool
	var flagFullMaintenanceFilter bool
	var flagScheduledFilter bool
	var flagSessionType string

	cmd := &cobra.Command{
		Use:         "get-dx",
		Aliases:     []string{"list"},
		Short:       "Get dx",
		Example:     "  immybot-cli maintenance-sessions get-dx",
		Annotations: map[string]string{"pp:endpoint": "maintenance-sessions.get-dx", "pp:method": "GET", "pp:path": "/api/v1/maintenance-sessions/dx", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("session-type") {
				allowedSessionType := []string{"Cloud", "Computer", "Person"}
				validSessionType := false
				for _, v := range allowedSessionType {
					if flagSessionType == v {
						validSessionType = true
						break
					}
				}
				if !validSessionType {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSessionType, "session-type", allowedSessionType)
				}
			}
			path := "/api/v1/maintenance-sessions/dx"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagLoadOptions != "" {
				params["loadOptions"] = formatCLIParamValue(flagLoadOptions)
			}
			if flagComputerId != "" {
				params["computerId"] = formatCLIParamValue(flagComputerId)
			}
			if flagTenantId != "" {
				params["tenantId"] = formatCLIParamValue(flagTenantId)
			}
			if flagPersonId != "" {
				params["personId"] = formatCLIParamValue(flagPersonId)
			}
			if flagOnboardingFilter != false {
				params["onboardingFilter"] = formatCLIParamValue(flagOnboardingFilter)
			}
			if flagFullMaintenanceFilter != false {
				params["fullMaintenanceFilter"] = formatCLIParamValue(flagFullMaintenanceFilter)
			}
			if flagScheduledFilter != false {
				params["scheduledFilter"] = formatCLIParamValue(flagScheduledFilter)
			}
			if flagSessionType != "" {
				params["sessionType"] = formatCLIParamValue(flagSessionType)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "maintenance-sessions", false, path, params, nil, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, nil)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, nil)
		},
	}
	cmd.Flags().StringVar(&flagLoadOptions, "load-options", "", "Load options")
	cmd.Flags().StringVar(&flagComputerId, "computer-id", "", "Computer id")
	cmd.Flags().StringVar(&flagTenantId, "tenant-id", "", "Tenant id")
	cmd.Flags().StringVar(&flagPersonId, "person-id", "", "Person id")
	cmd.Flags().BoolVar(&flagOnboardingFilter, "onboarding-filter", false, "Onboarding filter")
	cmd.Flags().BoolVar(&flagFullMaintenanceFilter, "full-maintenance-filter", false, "Full maintenance filter")
	cmd.Flags().BoolVar(&flagScheduledFilter, "scheduled-filter", false, "Scheduled filter")
	cmd.Flags().StringVar(&flagSessionType, "session-type", "", "The string or integer value may be used. Cloud = 1, Computer = 2, Person = 3 (one of: Cloud, Computer, Person)")

	return cmd
}
