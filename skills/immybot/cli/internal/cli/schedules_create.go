// Copyright 2026 Abhi Saini and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newSchedulesCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowAccessToMSPResources bool
	var bodyAllowAccessToParentTenant bool
	var bodyApplyWindowsUpdates bool
	var bodyAutoConsentToReboots bool
	var bodyCustomCronExpression string
	var bodyDay int
	var bodyDisabled bool
	var bodyMaintenanceIdentifier string
	var bodyMaintenanceTime string
	var bodyMaintenanceType string
	var bodyName string
	var bodyOfflineBehavior string
	var bodyPromptTimeoutAction string
	var bodyPromptTimeoutMinutes int
	var bodyPropagateToChildTenants bool
	var bodyProviderClientGroupType string
	var bodyProviderDeviceGroupType string
	var bodyProviderLinkId int
	var bodyRebootPreference string
	var bodyScheduleExecutionAfterActiveHours bool
	var bodySendDetectionEmail bool
	var bodySendDetectionEmailWhenAllActionsAreCompliant bool
	var bodySendFollowUpEmail bool
	var bodySendFollowUpOnlyIfActionNeeded bool
	var bodyShowMaintenanceActions bool
	var bodyShowPostponeButton bool
	var bodyShowRunNowButton bool
	var bodySuppressRebootsDuringBusinessHours bool
	var bodyTarget string
	var bodyTargetCategory string
	var bodyTargetGroupFilter string
	var bodyTargetType string
	var bodyTenantId int
	var bodyTime string
	var bodyTimeZoneInfoId string
	var bodyUseComputersTimezoneForExecution bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create",
		Example:     "  immybot-cli schedules create",
		Annotations: map[string]string{"pp:endpoint": "schedules.create", "pp:method": "POST", "pp:path": "/api/v1/schedules"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			path := "/api/v1/schedules"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("allow-access-to-msp-resources") {
					bodyMap["allowAccessToMSPResources"] = bodyAllowAccessToMSPResources
				}
				if cmd.Flags().Changed("allow-access-to-parent-tenant") {
					bodyMap["allowAccessToParentTenant"] = bodyAllowAccessToParentTenant
				}
				if cmd.Flags().Changed("apply-windows-updates") {
					bodyMap["applyWindowsUpdates"] = bodyApplyWindowsUpdates
				}
				if cmd.Flags().Changed("auto-consent-to-reboots") {
					bodyMap["autoConsentToReboots"] = bodyAutoConsentToReboots
				}
				if cmd.Flags().Changed("custom-cron-expression") || bodyCustomCronExpression != "" {
					bodyMap["customCronExpression"] = bodyCustomCronExpression
				}
				if cmd.Flags().Changed("day") || bodyDay != 0 {
					bodyMap["day"] = bodyDay
				}
				if cmd.Flags().Changed("disabled") {
					bodyMap["disabled"] = bodyDisabled
				}
				if cmd.Flags().Changed("maintenance-identifier") || bodyMaintenanceIdentifier != "" {
					bodyMap["maintenanceIdentifier"] = bodyMaintenanceIdentifier
				}
				if cmd.Flags().Changed("maintenance-time") || bodyMaintenanceTime != "" {
					bodyMap["maintenanceTime"] = bodyMaintenanceTime
				}
				if cmd.Flags().Changed("maintenance-type") || bodyMaintenanceType != "" {
					bodyMap["maintenanceType"] = bodyMaintenanceType
				}
				if cmd.Flags().Changed("name") || bodyName != "" {
					bodyMap["name"] = bodyName
				}
				if cmd.Flags().Changed("offline-behavior") || bodyOfflineBehavior != "" {
					bodyMap["offlineBehavior"] = bodyOfflineBehavior
				}
				if cmd.Flags().Changed("prompt-timeout-action") || bodyPromptTimeoutAction != "" {
					bodyMap["promptTimeoutAction"] = bodyPromptTimeoutAction
				}
				if cmd.Flags().Changed("prompt-timeout-minutes") || bodyPromptTimeoutMinutes != 0 {
					bodyMap["promptTimeoutMinutes"] = bodyPromptTimeoutMinutes
				}
				if cmd.Flags().Changed("propagate-to-child-tenants") {
					bodyMap["propagateToChildTenants"] = bodyPropagateToChildTenants
				}
				if cmd.Flags().Changed("provider-client-group-type") || bodyProviderClientGroupType != "" {
					bodyMap["providerClientGroupType"] = bodyProviderClientGroupType
				}
				if cmd.Flags().Changed("provider-device-group-type") || bodyProviderDeviceGroupType != "" {
					bodyMap["providerDeviceGroupType"] = bodyProviderDeviceGroupType
				}
				if cmd.Flags().Changed("provider-link-id") || bodyProviderLinkId != 0 {
					bodyMap["providerLinkId"] = bodyProviderLinkId
				}
				if cmd.Flags().Changed("reboot-preference") || bodyRebootPreference != "" {
					bodyMap["rebootPreference"] = bodyRebootPreference
				}
				if cmd.Flags().Changed("schedule-execution-after-active-hours") {
					bodyMap["scheduleExecutionAfterActiveHours"] = bodyScheduleExecutionAfterActiveHours
				}
				if cmd.Flags().Changed("send-detection-email") {
					bodyMap["sendDetectionEmail"] = bodySendDetectionEmail
				}
				if cmd.Flags().Changed("send-detection-email-when-all-actions-are-compliant") {
					bodyMap["sendDetectionEmailWhenAllActionsAreCompliant"] = bodySendDetectionEmailWhenAllActionsAreCompliant
				}
				if cmd.Flags().Changed("send-follow-up-email") {
					bodyMap["sendFollowUpEmail"] = bodySendFollowUpEmail
				}
				if cmd.Flags().Changed("send-follow-up-only-if-action-needed") {
					bodyMap["sendFollowUpOnlyIfActionNeeded"] = bodySendFollowUpOnlyIfActionNeeded
				}
				if cmd.Flags().Changed("show-maintenance-actions") {
					bodyMap["showMaintenanceActions"] = bodyShowMaintenanceActions
				}
				if cmd.Flags().Changed("show-postpone-button") {
					bodyMap["showPostponeButton"] = bodyShowPostponeButton
				}
				if cmd.Flags().Changed("show-run-now-button") {
					bodyMap["showRunNowButton"] = bodyShowRunNowButton
				}
				if cmd.Flags().Changed("suppress-reboots-during-business-hours") {
					bodyMap["suppressRebootsDuringBusinessHours"] = bodySuppressRebootsDuringBusinessHours
				}
				if cmd.Flags().Changed("target") || bodyTarget != "" {
					bodyMap["target"] = bodyTarget
				}
				if cmd.Flags().Changed("target-category") || bodyTargetCategory != "" {
					bodyMap["targetCategory"] = bodyTargetCategory
				}
				if cmd.Flags().Changed("target-group-filter") || bodyTargetGroupFilter != "" {
					bodyMap["targetGroupFilter"] = bodyTargetGroupFilter
				}
				if cmd.Flags().Changed("target-type") || bodyTargetType != "" {
					bodyMap["targetType"] = bodyTargetType
				}
				if cmd.Flags().Changed("tenant-id") || bodyTenantId != 0 {
					bodyMap["tenantId"] = bodyTenantId
				}
				if cmd.Flags().Changed("time") || bodyTime != "" {
					bodyMap["time"] = bodyTime
				}
				if cmd.Flags().Changed("time-zone-info-id") || bodyTimeZoneInfoId != "" {
					bodyMap["timeZoneInfoId"] = bodyTimeZoneInfoId
				}
				if cmd.Flags().Changed("use-computers-timezone-for-execution") {
					bodyMap["useComputersTimezoneForExecution"] = bodyUseComputersTimezoneForExecution
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "schedules", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "schedules", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "schedules", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "schedules", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "schedules", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "schedules",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"allowAccessToMSPResources": true, "allowAccessToParentTenant": true, "applyWindowsUpdates": true, "autoConsentToReboots": true, "createdBy": true, "createdDateUTC": true, "customCronExpression": true, "day": true, "disabled": true, "id": true, "maintenanceIdentifier": true, "maintenanceTime": true, "maintenanceType": true, "name": true, "nextOccurenceDate": true, "offlineBehavior": true, "onboardingOnly": true, "promptTimeoutAction": true, "promptTimeoutMinutes": true, "propagateToChildTenants": true, "providerClientGroupType": true, "providerDeviceGroupType": true, "providerLink": true, "providerLinkId": true, "rebootPreference": true, "scheduleExecutionAfterActiveHours": true, "sendDetectionEmail": true, "sendDetectionEmailWhenAllActionsAreCompliant": true, "sendFollowUpEmail": true, "sendFollowUpOnlyIfActionNeeded": true, "showMaintenanceActions": true, "showPostponeButton": true, "showRunNowButton": true, "suppressRebootsDuringBusinessHours": true, "target": true, "targetCategory": true, "targetGroupFilter": true, "targetGroupFilterName": true, "targetMissing": true, "targetScopeName": true, "targetText": true, "targetType": true, "targetTypeDescription": true, "targetTypeMissing": true, "targetTypeName": true, "tenantId": true, "tenantMissing": true, "time": true, "timeZoneInfoId": true, "updatedBy": true, "updatedByName": true, "updatedDateUTC": true, "useComputersTimezoneForExecution": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "schedules", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "schedules", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowAccessToMSPResources, "allow-access-to-msp-resources", false, "Allow access to mspresources")
	cmd.Flags().BoolVar(&bodyAllowAccessToParentTenant, "allow-access-to-parent-tenant", false, "Allow access to parent tenant")
	cmd.Flags().BoolVar(&bodyApplyWindowsUpdates, "apply-windows-updates", false, "Apply windows updates")
	cmd.Flags().BoolVar(&bodyAutoConsentToReboots, "auto-consent-to-reboots", false, "Auto consent to reboots")
	cmd.Flags().StringVar(&bodyCustomCronExpression, "custom-cron-expression", "", "Custom cron expression")
	cmd.Flags().IntVar(&bodyDay, "day", 0, "Day")
	cmd.Flags().BoolVar(&bodyDisabled, "disabled", false, "Disabled")
	cmd.Flags().StringVar(&bodyMaintenanceIdentifier, "maintenance-identifier", "", "Maintenance identifier")
	cmd.Flags().StringVar(&bodyMaintenanceTime, "maintenance-time", "", "Maintenance time")
	cmd.Flags().StringVar(&bodyMaintenanceType, "maintenance-type", "", "The string or integer value may be used.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyOfflineBehavior, "offline-behavior", "", "The string or integer value may be used. Skip = 1, ApplyOnConnect = 2")
	cmd.Flags().StringVar(&bodyPromptTimeoutAction, "prompt-timeout-action", "", "The string or integer value may be used. Reboot = 0, Suppress = 1, FailSession = 2")
	cmd.Flags().IntVar(&bodyPromptTimeoutMinutes, "prompt-timeout-minutes", 0, "Prompt timeout minutes")
	cmd.Flags().BoolVar(&bodyPropagateToChildTenants, "propagate-to-child-tenants", false, "Propagate to child tenants")
	cmd.Flags().StringVar(&bodyProviderClientGroupType, "provider-client-group-type", "", "Provider client group type")
	cmd.Flags().StringVar(&bodyProviderDeviceGroupType, "provider-device-group-type", "", "Provider device group type")
	cmd.Flags().IntVar(&bodyProviderLinkId, "provider-link-id", 0, "Provider link id")
	cmd.Flags().StringVar(&bodyRebootPreference, "reboot-preference", "", "The string or integer value may be used. Normal = 0, Suppress = 1, Prompt = 2, Force = -1")
	cmd.Flags().BoolVar(&bodyScheduleExecutionAfterActiveHours, "schedule-execution-after-active-hours", false, "Schedule execution after active hours")
	cmd.Flags().BoolVar(&bodySendDetectionEmail, "send-detection-email", false, "Send detection email")
	cmd.Flags().BoolVar(&bodySendDetectionEmailWhenAllActionsAreCompliant, "send-detection-email-when-all-actions-are-compliant", false, "Send detection email when all actions are compliant")
	cmd.Flags().BoolVar(&bodySendFollowUpEmail, "send-follow-up-email", false, "Send follow up email")
	cmd.Flags().BoolVar(&bodySendFollowUpOnlyIfActionNeeded, "send-follow-up-only-if-action-needed", false, "Send follow up only if action needed")
	cmd.Flags().BoolVar(&bodyShowMaintenanceActions, "show-maintenance-actions", false, "Show maintenance actions")
	cmd.Flags().BoolVar(&bodyShowPostponeButton, "show-postpone-button", false, "Show postpone button")
	cmd.Flags().BoolVar(&bodyShowRunNowButton, "show-run-now-button", false, "Show run now button")
	cmd.Flags().BoolVar(&bodySuppressRebootsDuringBusinessHours, "suppress-reboots-during-business-hours", false, "Suppress reboots during business hours")
	cmd.Flags().StringVar(&bodyTarget, "target", "", "Target")
	cmd.Flags().StringVar(&bodyTargetCategory, "target-category", "", "The string or integer value may be used. Computer = 0, Tenant = 1, Person = 2")
	cmd.Flags().StringVar(&bodyTargetGroupFilter, "target-group-filter", "", "The string or integer value may be used.")
	cmd.Flags().StringVar(&bodyTargetType, "target-type", "", "The string or integer value may be used.")
	cmd.Flags().IntVar(&bodyTenantId, "tenant-id", 0, "Tenant id")
	cmd.Flags().StringVar(&bodyTime, "time", "", "Time")
	cmd.Flags().StringVar(&bodyTimeZoneInfoId, "time-zone-info-id", "", "Time zone info id")
	cmd.Flags().BoolVar(&bodyUseComputersTimezoneForExecution, "use-computers-timezone-for-execution", false, "Use computers timezone for execution")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
