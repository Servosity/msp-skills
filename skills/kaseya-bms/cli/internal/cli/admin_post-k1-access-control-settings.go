// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAdminPostK1AccessControlSettingsCmd(flags *rootFlags) *cobra.Command {
	var bodyDepartmentId string
	var bodyEmployeeRoleId string
	var bodyEmploymentTypeId string
	var bodyIsAccessGroupMappingEnabled bool
	var bodyIsDeprovisioningEnabled bool
	var bodyIsEnabled bool
	var bodyIsRequireLoginEnabled bool
	var bodyJobTitleId string
	var bodyLocationId string
	var bodyManagerId string
	var bodyRoleExceptionIds string
	var bodySecurityRoleId string
	var bodyUserExceptionIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "post-k1-access-control-settings",
		Short:       "Post k1 access control settings",
		Example:     "  kaseya-bms-cli admin post-k1-access-control-settings",
		Annotations: map[string]string{"pp:endpoint": "admin.post-k1-access-control-settings", "pp:method": "POST", "pp:path": "/v2/admin/k1/accesscontrol/settings"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("department-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "department-id")
				}
				if !cmd.Flags().Changed("employee-role-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "employee-role-id")
				}
				if !cmd.Flags().Changed("employment-type-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "employment-type-id")
				}
				if !cmd.Flags().Changed("job-title-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "job-title-id")
				}
				if !cmd.Flags().Changed("location-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "location-id")
				}
				if !cmd.Flags().Changed("manager-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "manager-id")
				}
				if !cmd.Flags().Changed("security-role-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "security-role-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/admin/k1/accesscontrol/settings"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyDepartmentId != "" {
					body["DepartmentId"] = bodyDepartmentId
				}
				if bodyEmployeeRoleId != "" {
					body["EmployeeRoleId"] = bodyEmployeeRoleId
				}
				if bodyEmploymentTypeId != "" {
					body["EmploymentTypeId"] = bodyEmploymentTypeId
				}
				if cmd.Flags().Changed("is-access-group-mapping-enabled") {
					body["IsAccessGroupMappingEnabled"] = bodyIsAccessGroupMappingEnabled
				}
				if cmd.Flags().Changed("is-deprovisioning-enabled") {
					body["IsDeprovisioningEnabled"] = bodyIsDeprovisioningEnabled
				}
				if cmd.Flags().Changed("is-enabled") {
					body["IsEnabled"] = bodyIsEnabled
				}
				if cmd.Flags().Changed("is-require-login-enabled") {
					body["IsRequireLoginEnabled"] = bodyIsRequireLoginEnabled
				}
				if bodyJobTitleId != "" {
					body["JobTitleId"] = bodyJobTitleId
				}
				if bodyLocationId != "" {
					body["LocationId"] = bodyLocationId
				}
				if bodyManagerId != "" {
					body["ManagerId"] = bodyManagerId
				}
				if bodyRoleExceptionIds != "" {
					var parsedRoleExceptionIds any
					if err := json.Unmarshal([]byte(bodyRoleExceptionIds), &parsedRoleExceptionIds); err != nil {
						return fmt.Errorf("parsing --role-exception-ids JSON: %w", err)
					}
					body["RoleExceptionIds"] = parsedRoleExceptionIds
				}
				if bodySecurityRoleId != "" {
					body["SecurityRoleId"] = bodySecurityRoleId
				}
				if bodyUserExceptionIds != "" {
					var parsedUserExceptionIds any
					if err := json.Unmarshal([]byte(bodyUserExceptionIds), &parsedUserExceptionIds); err != nil {
						return fmt.Errorf("parsing --user-exception-ids JSON: %w", err)
					}
					body["UserExceptionIds"] = parsedUserExceptionIds
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "admin", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "admin", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "admin",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDepartmentId, "department-id", "", "Department id")
	cmd.Flags().StringVar(&bodyEmployeeRoleId, "employee-role-id", "", "Employee role id")
	cmd.Flags().StringVar(&bodyEmploymentTypeId, "employment-type-id", "", "Employment type id")
	cmd.Flags().BoolVar(&bodyIsAccessGroupMappingEnabled, "is-access-group-mapping-enabled", false, "Is access group mapping enabled")
	cmd.Flags().BoolVar(&bodyIsDeprovisioningEnabled, "is-deprovisioning-enabled", false, "Is deprovisioning enabled")
	cmd.Flags().BoolVar(&bodyIsEnabled, "is-enabled", false, "Is enabled")
	cmd.Flags().BoolVar(&bodyIsRequireLoginEnabled, "is-require-login-enabled", false, "Is require login enabled")
	cmd.Flags().StringVar(&bodyJobTitleId, "job-title-id", "", "Job title id")
	cmd.Flags().StringVar(&bodyLocationId, "location-id", "", "Location id")
	cmd.Flags().StringVar(&bodyManagerId, "manager-id", "", "Manager id")
	cmd.Flags().StringVar(&bodyRoleExceptionIds, "role-exception-ids", "", "Role exception ids")
	cmd.Flags().StringVar(&bodySecurityRoleId, "security-role-id", "", "Security role id")
	cmd.Flags().StringVar(&bodyUserExceptionIds, "user-exception-ids", "", "User exception ids")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
