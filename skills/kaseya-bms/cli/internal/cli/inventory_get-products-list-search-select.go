// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInventoryGetProductsListSearchSelectCmd(flags *rootFlags) *cobra.Command {
	var flagFilterProductNumber string
	var flagFilterProductName string
	var flagFilterProductDescription string
	var flagFilterSKU string
	var flagFilterUPC string
	var flagFilterColor string
	var flagFilterProductTypeIds string
	var flagFilterCategory string
	var flagFilterBrand string
	var flagFilterProductManufacturer string
	var flagFilterSerialNumber string
	var flagFilterWarehouseIds string
	var flagFilterExcludedId string
	var flagSort string
	var flagExclude string
	var flagPageSize int
	var flagPageNumber int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-products-list-search-select",
		Short:       "Get products list search select",
		Example:     "  kaseya-bms-cli inventory get-products-list-search-select",
		Annotations: map[string]string{"pp:endpoint": "inventory.get-products-list-search-select", "pp:method": "GET", "pp:path": "/v2/inventory/products/searchselect", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v2/inventory/products/searchselect"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "inventory", path, map[string]string{
				"Filter.ProductNumber":       formatCLIParamValue(flagFilterProductNumber),
				"Filter.ProductName":         formatCLIParamValue(flagFilterProductName),
				"Filter.ProductDescription":  formatCLIParamValue(flagFilterProductDescription),
				"Filter.SKU":                 formatCLIParamValue(flagFilterSKU),
				"Filter.UPC":                 formatCLIParamValue(flagFilterUPC),
				"Filter.Color":               formatCLIParamValue(flagFilterColor),
				"Filter.ProductTypeIds":      formatCLIParamValue(flagFilterProductTypeIds),
				"Filter.Category":            formatCLIParamValue(flagFilterCategory),
				"Filter.Brand":               formatCLIParamValue(flagFilterBrand),
				"Filter.ProductManufacturer": formatCLIParamValue(flagFilterProductManufacturer),
				"Filter.SerialNumber":        formatCLIParamValue(flagFilterSerialNumber),
				"Filter.WarehouseIds":        formatCLIParamValue(flagFilterWarehouseIds),
				"Filter.ExcludedId":          formatCLIParamValue(flagFilterExcludedId),
				"Sort":                       formatCLIParamValue(flagSort),
				"Exclude":                    formatCLIParamValue(flagExclude),
				"PageSize":                   formatCLIParamValue(flagPageSize),
				"PageNumber":                 formatCLIParamValue(flagPageNumber),
			}, nil, flagAll, "PageNumber", "page", "PageSize", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFilterProductNumber, "filter-product-number", "", "Filter product number")
	cmd.Flags().StringVar(&flagFilterProductName, "filter-product-name", "", "Filter product name")
	cmd.Flags().StringVar(&flagFilterProductDescription, "filter-product-description", "", "Filter product description")
	cmd.Flags().StringVar(&flagFilterSKU, "filter-sku", "", "Filter sku")
	cmd.Flags().StringVar(&flagFilterUPC, "filter-upc", "", "Filter upc")
	cmd.Flags().StringVar(&flagFilterColor, "filter-color", "", "Filter color")
	cmd.Flags().StringVar(&flagFilterProductTypeIds, "filter-product-type-ids", "", "Filter product type ids")
	cmd.Flags().StringVar(&flagFilterCategory, "filter-category", "", "Filter category")
	cmd.Flags().StringVar(&flagFilterBrand, "filter-brand", "", "Filter brand")
	cmd.Flags().StringVar(&flagFilterProductManufacturer, "filter-product-manufacturer", "", "Filter product manufacturer")
	cmd.Flags().StringVar(&flagFilterSerialNumber, "filter-serial-number", "", "Filter serial number")
	cmd.Flags().StringVar(&flagFilterWarehouseIds, "filter-warehouse-ids", "", "Filter warehouse ids")
	cmd.Flags().StringVar(&flagFilterExcludedId, "filter-excluded-id", "", "Filter excluded id")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort")
	cmd.Flags().StringVar(&flagExclude, "exclude", "", "Exclude")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Page size")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 0, "Page number")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
