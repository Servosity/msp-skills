// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"knowbe4-pp-cli/internal/cli"
	"knowbe4-pp-cli/internal/client"
	"knowbe4-pp-cli/internal/cliutil"
	"knowbe4-pp-cli/internal/config"
	"knowbe4-pp-cli/internal/mcp/cobratree"
	"knowbe4-pp-cli/internal/store"
)

const (
	mcpToolResultMaxBytes = 60000
	mcpToolResultMaxItems = 50
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("account_info",
			mcplib.WithDescription("Get account and subscription data (name, subscription level, seats, admins, current risk score). Returns the Account."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/account", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("account_risk-score-history",
			mcplib.WithDescription("Get the org-level risk-score history time series. Optional: full. Returns array of RiskScorePoint."),
			mcplib.WithString("full", mcplib.Description("Set 'true' to return the full history (default returns the most recent points)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/account/risk_score_history", true, false, nil, []mcpParamBinding{{PublicName: "full", WireName: "full", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("groups_get",
			mcplib.WithDescription("Get a single group by id. Required: id. Returns the Group."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Group id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/groups/{group_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "group_id", Location: "path"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("groups_list",
			mcplib.WithDescription("List all groups with member counts and risk scores. Optional: page (default: 1), per-page (default: 100), status. Returns array of Group."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100, max 500)")),
			mcplib.WithString("status", mcplib.Description("Filter by status: active or archived")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/groups", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}, {PublicName: "status", WireName: "status", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("groups_members",
			mcplib.WithDescription("List the users in a group. Required: id. Optional: page (default: 1), per-page (default: 100). Returns array of User."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Group id")),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/groups/{group_id}/members", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "group_id", Location: "path"}, {PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("groups_risk-score-history",
			mcplib.WithDescription("Get a group's risk-score history time series. Required: id. Optional: full. Returns array of RiskScorePoint."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Group id")),
			mcplib.WithString("full", mcplib.Description("Set 'true' to return the full history")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/groups/{group_id}/risk_score_history", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "group_id", Location: "path"}, {PublicName: "full", WireName: "full", Location: "query"}}, []string{"group_id"}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-campaigns_get",
			mcplib.WithDescription("Get a single phishing campaign by id. Required: id. Returns the PhishingCampaign."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Phishing campaign id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/campaigns/{campaign_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "campaign_id", Location: "path"}}, []string{"campaign_id"}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-campaigns_list",
			mcplib.WithDescription("List all phishing campaigns. Optional: page (default: 1), per-page (default: 100). Returns array of PhishingCampaign."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/campaigns", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-campaigns_security-tests",
			mcplib.WithDescription("List the phishing security tests (PSTs) run by a campaign. Required: id. Returns array of PhishingSecurityTest."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Phishing campaign id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/campaigns/{campaign_id}/security_tests", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "campaign_id", Location: "path"}}, []string{"campaign_id"}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-tests_get",
			mcplib.WithDescription("Get a single phishing security test by id. Required: id. Returns the PhishingSecurityTest."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Phishing security test (PST) id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/security_tests/{pst_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "pst_id", Location: "path"}}, []string{"pst_id"}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-tests_list",
			mcplib.WithDescription("List all phishing security tests (PSTs) across every campaign. Optional: page (default: 1), per-page (default: 100). Returns array of PhishingSecurityTest."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/security_tests", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-tests_recipient",
			mcplib.WithDescription("Get a single recipient's result within a PST. Required: pst-id, recipient-id. Returns the PstRecipient."),
			mcplib.WithNumber("pst-id", mcplib.Required(), mcplib.Description("Phishing security test (PST) id")),
			mcplib.WithNumber("recipient-id", mcplib.Required(), mcplib.Description("Recipient id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/security_tests/{pst_id}/recipients/{recipient_id}", true, false, nil, []mcpParamBinding{{PublicName: "pst-id", WireName: "pst_id", Location: "path"}, {PublicName: "recipient-id", WireName: "recipient_id", Location: "path"}}, []string{"pst_id", "recipient_id"}),
	)
	s.AddTool(
		mcplib.NewTool("phishing-tests_recipients",
			mcplib.WithDescription("List per-user results for a PST (who opened, clicked, reported, entered data). Required: id. Optional: page (default: 1), per-page (default: 100). Returns array of PstRecipient."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Phishing security test (PST) id")),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/phishing/security_tests/{pst_id}/recipients", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "pst_id", Location: "path"}, {PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{"pst_id"}),
	)
	s.AddTool(
		mcplib.NewTool("policies_get",
			mcplib.WithDescription("Get a single policy by id. Required: id. Returns the Policy."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Policy id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/policies/{policy_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "policy_id", Location: "path"}}, []string{"policy_id"}),
	)
	s.AddTool(
		mcplib.NewTool("policies_list",
			mcplib.WithDescription("List all uploaded policies. Optional: page (default: 1), per-page (default: 100). Returns array of Policy."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/policies", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("store-purchases_get",
			mcplib.WithDescription("Get a single store purchase by id. Required: id. Returns the StorePurchase."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Store purchase id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/store_purchases/{store_purchase_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "store_purchase_id", Location: "path"}}, []string{"store_purchase_id"}),
	)
	s.AddTool(
		mcplib.NewTool("store-purchases_list",
			mcplib.WithDescription("List all training store purchases (licensed content/modules). Optional: page (default: 1), per-page (default: 100). Returns array of StorePurchase."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/store_purchases", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("training-campaigns_get",
			mcplib.WithDescription("Get a single training campaign by id. Required: id. Returns the TrainingCampaign."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Training campaign id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/campaigns/{campaign_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "campaign_id", Location: "path"}}, []string{"campaign_id"}),
	)
	s.AddTool(
		mcplib.NewTool("training-campaigns_list",
			mcplib.WithDescription("List all training campaigns. Optional: page (default: 1), per-page (default: 100). Returns array of TrainingCampaign."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/campaigns", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("training-enrollments_get",
			mcplib.WithDescription("Get a single training enrollment by id. Required: id. Returns the TrainingEnrollment."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("Enrollment id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/enrollments/{enrollment_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "enrollment_id", Location: "path"}}, []string{"enrollment_id"}),
	)
	s.AddTool(
		mcplib.NewTool("training-enrollments_list",
			mcplib.WithDescription("List all training enrollments with completion status and time spent. Optional: page (default: 1), per-page (default: 100), campaign-id (plus 1 more). Returns array of TrainingEnrollment."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page")),
			mcplib.WithNumber("campaign-id", mcplib.Description("Only enrollments in this training campaign")),
			mcplib.WithNumber("store-purchase-id", mcplib.Description("Only enrollments for this store purchase / module")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/training/enrollments", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}, {PublicName: "campaign-id", WireName: "campaign_id", Location: "query"}, {PublicName: "store-purchase-id", WireName: "store_purchase_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_get",
			mcplib.WithDescription("Get a single user by id. Required: id. Returns the User."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("User id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users/{user_id}", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "user_id", Location: "path"}}, []string{"user_id"}),
	)
	s.AddTool(
		mcplib.NewTool("users_list",
			mcplib.WithDescription("List all users with risk score and phish-prone percentage. Optional: page (default: 1), per-page (default: 100), status (plus 1 more). Returns array of User."),
			mcplib.WithNumber("page", mcplib.Description("1-based page number")),
			mcplib.WithNumber("per-page", mcplib.Description("Items per page (default 100, max 500)")),
			mcplib.WithString("status", mcplib.Description("Filter by status: active or archived")),
			mcplib.WithNumber("group-id", mcplib.Description("Only users in this group id")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users", true, false, nil, []mcpParamBinding{{PublicName: "page", WireName: "page", Location: "query", Default: "1"}, {PublicName: "per-page", WireName: "per_page", Location: "query", Default: "100"}, {PublicName: "status", WireName: "status", Location: "query"}, {PublicName: "group-id", WireName: "group_id", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_risk-score-history",
			mcplib.WithDescription("Get a user's risk-score history time series. Required: id. Optional: full. Returns array of RiskScorePoint."),
			mcplib.WithNumber("id", mcplib.Required(), mcplib.Description("User id")),
			mcplib.WithString("full", mcplib.Description("Set 'true' to return the full history")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/users/{user_id}/risk_score_history", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "user_id", Location: "path"}, {PublicName: "full", WireName: "full", Location: "query"}}, []string{"user_id"}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='items'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
	Default    string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		return fmt.Sprintf("%v", v)
	}
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				if binding.Default != "" {
					v = binding.Default
				} else {
					continue
				}
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, formatMCPParamValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set it with: knowbe4-cli auth set-token <token> or export KNOWBE4_API_KEY=\"your-token-here\"" +
					"\n      Get a key at: https://developer.knowbe4.com/" +
					"\n      KSAT console → Account Settings → API → enable Reporting API and copy the key. Then export KNOWBE4_API_KEY=<key>. Set KNOWBE4_REGION if your tenant is not on the US server (eu, ca, uk, de)." +
					"\n      Run 'knowbe4-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: knowbe4-cli auth set-token <token> or export KNOWBE4_API_KEY=\"your-token-here\"" +
					"\n      Get a key at: https://developer.knowbe4.com/" +
					"\n      KSAT console → Account Settings → API → enable Reporting API and copy the key. Then export KNOWBE4_API_KEY=<key>. Set KNOWBE4_REGION if your tenant is not on the US server (eu, ca, uk, de)." +
					"\n      Run 'knowbe4-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Set it with: knowbe4-cli auth set-token <token> or export KNOWBE4_API_KEY=\"your-token-here\"" +
					"\n      Get a key at: https://developer.knowbe4.com/" +
					"\n      KSAT console → Account Settings → API → enable Reporting API and copy the key. Then export KNOWBE4_API_KEY=<key>. Set KNOWBE4_REGION if your tenant is not on the US server (eu, ca, uk, de)." +
					"\n      Run 'knowbe4-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	trimmed := strings.TrimSpace(string(data))
	if strings.EqualFold(method, "GET") && len(trimmed) > 0 && trimmed[0] == '[' {
		var items []json.RawMessage
		if json.Unmarshal(data, &items) == nil {
			return mcplib.NewToolResultText(string(mcpBoundedListEnvelope("items", items, len(data))))
		}
	}
	if len(data) <= mcpToolResultMaxBytes {
		return mcplib.NewToolResultText(string(data))
	}
	if strings.EqualFold(method, "GET") {
		if out, ok := mcpBoundedSingleArrayObject(data); ok {
			return mcplib.NewToolResultText(string(out))
		}
	}
	return mcplib.NewToolResultText(string(mcpOversizedPreviewEnvelope(data)))
}

func mcpBoundedSingleArrayObject(data json.RawMessage) ([]byte, bool) {
	var obj map[string]json.RawMessage
	if json.Unmarshal(data, &obj) != nil {
		return nil, false
	}
	arrayField := ""
	var items []json.RawMessage
	for key, raw := range obj {
		trimmed := strings.TrimSpace(string(raw))
		if len(trimmed) == 0 || trimmed[0] != '[' {
			continue
		}
		var candidate []json.RawMessage
		if json.Unmarshal(raw, &candidate) != nil {
			continue
		}
		if arrayField != "" {
			return nil, false
		}
		arrayField = key
		items = candidate
	}
	if arrayField == "" {
		return nil, false
	}
	build := func(subset []json.RawMessage) any {
		out := make(map[string]any, len(obj)+6)
		for key, raw := range obj {
			if key == arrayField {
				out[key] = subset
				continue
			}
			out[key] = raw
		}
		if len(subset) < len(items) {
			out["_pp_truncated"] = true
			out["_pp_total_count"] = len(items)
			out["_pp_returned_count"] = len(subset)
			out["_pp_original_bytes"] = len(data)
			out["_pp_max_bytes"] = mcpToolResultMaxBytes
			out["_pp_note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	out := mcpFitJSONItems(items, build)
	if len(out) > mcpToolResultMaxBytes {
		return nil, false
	}
	return out, true
}

func mcpBoundedListEnvelope(field string, items []json.RawMessage, originalBytes int) []byte {
	build := func(subset []json.RawMessage) any {
		out := map[string]any{
			"count": len(items),
			field:   subset,
		}
		if len(subset) < len(items) {
			out["truncated"] = true
			out["returned_count"] = len(subset)
			out["original_bytes"] = originalBytes
			out["max_bytes"] = mcpToolResultMaxBytes
			out["note"] = "Typed MCP endpoint response exceeded the tool result budget. Narrow the request with limit, offset, filters, search/sql, or a command-mirror tool with --agent/--compact/--select."
		}
		return out
	}
	return mcpFitJSONItems(items, build)
}

func mcpFitJSONItems(items []json.RawMessage, build func([]json.RawMessage) any) []byte {
	limit := len(items)
	if limit > mcpToolResultMaxItems {
		limit = mcpToolResultMaxItems
	}
	for n := limit; n >= 0; n-- {
		out, err := json.Marshal(build(items[:n]))
		if err != nil {
			continue
		}
		if len(out) <= mcpToolResultMaxBytes || n == 0 {
			return out
		}
	}
	out, _ := json.Marshal(build(items[:0]))
	return out
}

func mcpOversizedPreviewEnvelope(data json.RawMessage) []byte {
	previewBytes := data
	if len(previewBytes) > 4000 {
		previewBytes = previewBytes[:4000]
	}
	out, _ := json.Marshal(map[string]any{
		"truncated":      true,
		"original_bytes": len(data),
		"max_bytes":      mcpToolResultMaxBytes,
		"preview":        string(previewBytes),
		"note":           "Typed MCP endpoint response exceeded the tool result budget and was not a recognized list envelope. Narrow the request with filters, search/sql, or a command-mirror tool with --agent/--compact/--select.",
	})
	return out
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "knowbe4-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "knowbe4-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	return toolResultJSON(results)
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	return toolResultJSON(results)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	data, err := json.MarshalIndent(v, "", "  ")
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "knowbe4",
		"description": "Every KnowBe4 KMSAT reporting feature, plus a local SQLite store for the questions the console cannot answer.",
		"archetype":   "crm",
		"tool_count":  24,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion knowbe4-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "KNOWBE4_API_KEY",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
			"key_url":      "https://developer.knowbe4.com/",
			"instructions": "KSAT console → Account Settings → API → enable Reporting API and copy the key. Then export KNOWBE4_API_KEY=<key>. Set KNOWBE4_REGION if your tenant is not on the US server (eu, ca, uk, de).",
		},
		"resources": []map[string]any{
			{
				"name":        "account",
				"description": "Your KnowBe4 account: subscription, seat count, admins, and org-level risk score",
				"endpoints":   []string{"info", "risk-score-history"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "groups",
				"description": "Groups/Smart Groups — membership counts and group-level risk score",
				"endpoints":   []string{"get", "list", "members", "risk-score-history"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "phishing-campaigns",
				"description": "Phishing campaigns — recurring/one-time simulated-phishing programs",
				"endpoints":   []string{"get", "list", "security-tests"},
				"syncable":    true,
			},
			{
				"name":        "phishing-tests",
				"description": "Phishing security tests (PSTs) — individual simulated-phishing sends with click/report stats",
				"endpoints":   []string{"get", "list", "recipient", "recipients"},
				"syncable":    true,
			},
			{
				"name":        "policies",
				"description": "Uploaded policies — policy documents assigned for acknowledgement",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
			},
			{
				"name":        "store-purchases",
				"description": "Store purchases — training content/modules licensed to your account",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
			},
			{
				"name":        "training-campaigns",
				"description": "Training campaigns — assigned security-awareness training programs",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
			},
			{
				"name":        "training-enrollments",
				"description": "Training enrollments — per-user module assignments with completion status",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
			},
			{
				"name":        "users",
				"description": "Users in the KnowBe4 console — phish-prone percentage, current risk score, group membership",
				"endpoints":   []string{"get", "list", "risk-score-history"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass page parameter for subsequent pages.",
			"Control page size with the per_page parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Repeat clickers across PSTs", "command": "repeat-clickers", "description": "Surface users who clicked the bait in two or more distinct phishing security tests within a window — your hardest-to-train humans.", "rationale": "Aggregates clicked_at across many synced PST-recipient sets and counts distinct pst_id per user. No single KnowBe4 API call spans multiple PSTs, so a repeat offender is invisible until you pivot CSVs by hand.", "via": "mcp-command-mirror"},
			{"name": "Risk drift over time", "command": "risk-drift", "description": "Rank users and groups by how much their risk score worsened (or improved) between a prior snapshot and now.", "rationale": "Diffs current_risk_score against locally-captured risk snapshots and the risk_score_history series. The API returns a raw series per entity, never a ranked delta across all entities.", "via": "mcp-command-mirror"},
			{"name": "Untrained clickers", "command": "untrained-clickers", "description": "Anti-join: users who clicked a phish but have no Passed training enrollment to show for it.", "rationale": "Joins PST recipients (clicked_at IS NOT NULL) against training-enrollments and keeps only users with no status='Passed' row. Phishing results and training records are separate endpoints the console never correlates.", "via": "mcp-command-mirror"},
			{"name": "Coverage gaps", "command": "coverage-gaps", "description": "Find active users enrolled in zero phishing campaigns or zero training campaigns — the people your program silently misses.", "rationale": "LEFT JOINs users against phishing/training campaign group-membership and keeps the anti-join. Campaigns are addressed by group; the console has no 'who is covered by nothing' view.", "via": "mcp-command-mirror"},
			{"name": "Phish-prone trend", "command": "phish-prone-trend", "description": "Plot a group's phish-prone percentage across sequential phishing tests to show whether training is actually working.", "rationale": "Orders phish_prone_percentage from multiple synced PSTs by started_at for a given group. A single PST is one data point; the improving/regressing arc only exists once many PSTs are stored and ordered together.", "via": "mcp-command-mirror"},
			{"name": "Risk leaderboard", "command": "risk-leaderboard", "description": "Highest-risk users ranked, each enriched with click history, report rate, and overdue-training context.", "rationale": "Joins users (current_risk_score) with PST recipients (clicks/reports) and training-enrollments (Past Due / Not Started). The API exposes the risk score alone; the why-behind-the-score requires fusing three tables.", "via": "mcp-command-mirror"},
			{"name": "Group risk contribution", "command": "group-risk-contribution", "description": "Attribute account-level risk movement to the specific groups (departments) driving it up or down.", "rationale": "Diffs each group's risk_score_history over the window, weights by member_count, and compares against the account series. The attribution of which group moved the account number is a cross-series local computation.", "via": "mcp-command-mirror"},
			{"name": "Client QBR pack", "command": "qbr", "description": "One command that assembles the full quarterly review: risk trend, phish-prone trend, training completion, and the top-risk humans.", "rationale": "Composes the local risk-drift, phish-prone-trend, training-completion, and risk-leaderboard computations into a single artifact. No KnowBe4 endpoint emits a combined QBR report.", "via": "mcp-command-mirror"},
			{"name": "Report-vs-click leaderboard", "command": "report-rate", "description": "Rank users and groups by how often they report simulated phish versus falling for them - surfacing who never uses the Phish Alert Button.", "rationale": "Requires ratioing reported vs clicked recipient rows across every synced PST in the local store; no endpoint returns report behavior across tests.", "via": "mcp-command-mirror"},
			{"name": "Sync freshness / input-readiness", "command": "freshness", "description": "See per-table sync freshness and whether the cross-entity commands have the data they need - before trusting a clicker hunt.", "rationale": "Reads per-table sync watermarks and row counts from local SQLite, including the hand-built pst_recipients fan-out that powers the headline joins.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Repeat clickers across PSTs", "insight": "Aggregates clicked_at across many synced PST-recipient sets and counts distinct pst_id per user. No single KnowBe4 API call spans multiple PSTs, so a repeat offender is invisible until you pivot CSVs by hand."},
			{"topic": "Risk drift over time", "insight": "Diffs current_risk_score against locally-captured risk snapshots and the risk_score_history series. The API returns a raw series per entity, never a ranked delta across all entities."},
			{"topic": "Untrained clickers", "insight": "Joins PST recipients (clicked_at IS NOT NULL) against training-enrollments and keeps only users with no status='Passed' row. Phishing results and training records are separate endpoints the console never correlates."},
			{"topic": "Coverage gaps", "insight": "LEFT JOINs users against phishing/training campaign group-membership and keeps the anti-join. Campaigns are addressed by group; the console has no 'who is covered by nothing' view."},
			{"topic": "Phish-prone trend", "insight": "Orders phish_prone_percentage from multiple synced PSTs by started_at for a given group. A single PST is one data point; the improving/regressing arc only exists once many PSTs are stored and ordered together."},
			{"topic": "Risk leaderboard", "insight": "Joins users (current_risk_score) with PST recipients (clicks/reports) and training-enrollments (Past Due / Not Started). The API exposes the risk score alone; the why-behind-the-score requires fusing three tables."},
			{"topic": "Group risk contribution", "insight": "Diffs each group's risk_score_history over the window, weights by member_count, and compares against the account series. The attribution of which group moved the account number is a cross-series local computation."},
			{"topic": "Client QBR pack", "insight": "Composes the local risk-drift, phish-prone-trend, training-completion, and risk-leaderboard computations into a single artifact. No KnowBe4 endpoint emits a combined QBR report."},
			{"topic": "Report-vs-click leaderboard", "insight": "Requires ratioing reported vs clicked recipient rows across every synced PST in the local store; no endpoint returns report behavior across tests."},
			{"topic": "Sync freshness / input-readiness", "insight": "Reads per-table sync watermarks and row counts from local SQLite, including the hand-built pst_recipients fan-out that powers the headline joins."},
			{"topic": "Contact lookup", "insight": "Use search for finding contacts by name/email. List endpoints return unsorted results and require pagination for large datasets."},
			{"topic": "Activity tracking", "insight": "When checking deal activity, sync first and query locally. CRM APIs often throttle activity-log endpoints heavily."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
