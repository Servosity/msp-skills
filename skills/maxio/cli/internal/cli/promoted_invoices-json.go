// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newInvoicesJsonPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagStartDate string
	var flagEndDate string
	var flagStatus string
	var flagSubscriptionId string
	var flagSubscriptionGroupUid string
	var flagConsolidationLevel string
	var flagPage string
	var flagPerPage int
	var flagDirection string
	var flagLineItems bool
	var flagDiscounts bool
	var flagTaxes bool
	var flagCredits bool
	var flagPayments bool
	var flagCustomFields bool
	var flagRefunds bool
	var flagDateField string
	var flagStartDatetime string
	var flagEndDatetime string
	var flagCustomerIds string
	var flagNumber string
	var flagProductIds string
	var flagSort string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "invoices-json",
		Short:       "By default, invoices returned on the index will only include totals, not detailed breakdowns for `line_items`",
		Long:        "By default, invoices returned on the index will only include totals, not detailed breakdowns for `line_items`",
		Example:     "  maxio-cli invoices-json",
		Annotations: map[string]string{"pp:endpoint": "invoices-json.list-invoices", "pp:method": "GET", "pp:path": "/invoices.json", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/invoices.json"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "invoices-json", path, map[string]string{
				"start_date":             formatCLIParamValue(flagStartDate),
				"end_date":               formatCLIParamValue(flagEndDate),
				"status":                 formatCLIParamValue(flagStatus),
				"subscription_id":        formatCLIParamValue(flagSubscriptionId),
				"subscription_group_uid": formatCLIParamValue(flagSubscriptionGroupUid),
				"consolidation_level":    formatCLIParamValue(flagConsolidationLevel),
				"page":                   formatCLIParamValue(flagPage),
				"per_page":               formatCLIParamValue(flagPerPage),
				"direction":              formatCLIParamValue(flagDirection),
				"line_items":             formatCLIParamValue(flagLineItems),
				"discounts":              formatCLIParamValue(flagDiscounts),
				"taxes":                  formatCLIParamValue(flagTaxes),
				"credits":                formatCLIParamValue(flagCredits),
				"payments":               formatCLIParamValue(flagPayments),
				"custom_fields":          formatCLIParamValue(flagCustomFields),
				"refunds":                formatCLIParamValue(flagRefunds),
				"date_field":             formatCLIParamValue(flagDateField),
				"start_datetime":         formatCLIParamValue(flagStartDatetime),
				"end_datetime":           formatCLIParamValue(flagEndDatetime),
				"customer_ids":           formatCLIParamValue(flagCustomerIds),
				"number":                 formatCLIParamValue(flagNumber),
				"product_ids":            formatCLIParamValue(flagProductIds),
				"sort":                   formatCLIParamValue(flagSort),
			}, nil, flagAll, "page", "page", "per_page", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagStartDate, "start-date", "", "The start date (format YYYY-MM-DD) with which to filter the date_field.")
	cmd.Flags().StringVar(&flagEndDate, "end-date", "", "The end date (format YYYY-MM-DD) with which to filter the date_field.")
	cmd.Flags().StringVar(&flagStatus, "status", "", "The current status of the invoice. Allowed Values: draft, open, paid, pending, voided")
	cmd.Flags().StringVar(&flagSubscriptionId, "subscription-id", "", "The subscription's ID.")
	cmd.Flags().StringVar(&flagSubscriptionGroupUid, "subscription-group-uid", "", "The UID of the subscription group you want to fetch consolidated invoices for.")
	cmd.Flags().StringVar(&flagConsolidationLevel, "consolidation-level", "", "The consolidation level of the invoice. Allowed Values: none, parent, child or comma-separated lists of thereof, e.g.")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Result records are organized in pages. By default, the first page of results is displayed.")
	cmd.Flags().IntVar(&flagPerPage, "per-page", 20, "This parameter indicates how many records to fetch in each request. Default value is 20.")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "The sort direction of the returned invoices.")
	cmd.Flags().BoolVar(&flagLineItems, "line-items", false, "Include line items data")
	cmd.Flags().BoolVar(&flagDiscounts, "discounts", false, "Include discounts data")
	cmd.Flags().BoolVar(&flagTaxes, "taxes", false, "Include taxes data")
	cmd.Flags().BoolVar(&flagCredits, "credits", false, "Include credits data")
	cmd.Flags().BoolVar(&flagPayments, "payments", false, "Include payments data")
	cmd.Flags().BoolVar(&flagCustomFields, "custom-fields", false, "Include custom fields data")
	cmd.Flags().BoolVar(&flagRefunds, "refunds", false, "Include refunds data")
	cmd.Flags().StringVar(&flagDateField, "date-field", "", "The type of filter you would like to apply to your search. Use in query `date_field=issue_date`.")
	cmd.Flags().StringVar(&flagStartDatetime, "start-datetime", "", "The start date and time (format YYYY-MM-DD HH:MM:SS) with which to filter the date_field.")
	cmd.Flags().StringVar(&flagEndDatetime, "end-datetime", "", "The end date and time (format YYYY-MM-DD HH:MM:SS) with which to filter the date_field.")
	cmd.Flags().StringVar(&flagCustomerIds, "customer-ids", "", "Allows fetching invoices with matching customer id based on provided values. Use in query `customer_ids=1,2,3`.")
	cmd.Flags().StringVar(&flagNumber, "number", "", "Allows fetching invoices with matching invoice number based on provided values. Use in query `number=1234,1235`.")
	cmd.Flags().StringVar(&flagProductIds, "product-ids", "", "Allows fetching invoices with matching line items product ids based on provided values.")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Allows specification of the order of the returned list. Use in query `sort=total_amount`.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
