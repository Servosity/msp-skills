{
  "manifest_version": "0.3",
  "name": "n-central-mcp",
  "display_name": "N-central",
  "version": "0.1.3",
  "description": "Every N-central REST endpoint, plus an offline SQLite mirror of your whole org tree, cross-tenant search, issue-triage rollups, and a JWT-expiry guardian no other N-central tool has.",
  "author": {
    "name": "Servosity Inc."
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/n-central-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/n-central-mcp",
      "args": [],
      "env": {
        "NCENTRAL_JWT": "${user_config.ncentral_jwt}",
        "N_CENTRAL_BASE_URL": "${user_config.n_central_base_url}",
        "N_CENTRAL_NO_CONFIG_WRITE": "${user_config.n_central_no_config_write}"
      }
    }
  },
  "user_config": {
    "ncentral_jwt": {
      "type": "string",
      "title": "NCENTRAL_JWT",
      "description": "Sets NCENTRAL_JWT for the N-central MCP server.",
      "sensitive": true,
      "required": true
    },
    "n_central_base_url": {
      "type": "string",
      "title": "N-central API base URL",
      "description": "Base URL for the N-central API. Leave the prefilled default unless your account uses a different regional or self-hosted host.",
      "required": false,
      "default": "https://ncod.n-able.com/api"
    },
    "n_central_no_config_write": {
      "type": "string",
      "title": "Keep credentials off disk",
      "description": "Set to 1 to keep credentials off disk. The token cache becomes a no-op, so the connector mints a fresh token each run instead of writing one to config.toml. Leave blank for the default cached behaviour.",
      "required": false,
      "default": ""
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  },
  "printing_press_version": "4.24.0",
  "press_provenance": {
    "printing_press_version": "4.24.0",
    "generated_at": "2026-06-13T03:59:00.699894Z",
    "spec_checksum": "sha256:eff724d99786e20e65150d2ff3ff266fbca09acd14cb3d424f330fdcaf619b9e",
    "spec_format": "internal"
  }
}
