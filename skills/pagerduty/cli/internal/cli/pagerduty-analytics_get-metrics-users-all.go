// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"pagerduty-pp-cli/internal/cliutil"
)

func newPagerdutyAnalyticsGetMetricsUsersAllCmd(flags *rootFlags) *cobra.Command {
	var bodyAggregateUnit string
	var bodyEndingBefore string
	var bodyFiltersCreatedAtEnd string
	var bodyFiltersCreatedAtStart string
	var bodyFiltersRoleIds string
	var bodyFiltersTeamIds string
	var bodyFiltersUserIds string
	var bodyLimit int
	var bodyOrder string
	var bodyOrderBy string
	var bodyStartingAfter string
	var bodyTimeZone string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "get-metrics-users-all",
		Short:       "Provides aggregated metrics across all users within their account.",
		Example:     "  pagerduty-cli pagerduty-analytics get-metrics-users-all",
		Annotations: map[string]string{"pp:endpoint": "pagerduty-analytics.get-metrics-users-all", "pp:method": "POST", "pp:path": "/analytics/metrics/users/all"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/analytics/metrics/users/all"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAggregateUnit != "" {
					body["aggregate_unit"] = bodyAggregateUnit
				}
				if bodyEndingBefore != "" {
					body["ending_before"] = bodyEndingBefore
				}
				{
					nestedFilters := map[string]any{}
					if bodyFiltersCreatedAtEnd != "" {
						nestedFilters["created_at_end"] = bodyFiltersCreatedAtEnd
					}
					if bodyFiltersCreatedAtStart != "" {
						nestedFilters["created_at_start"] = bodyFiltersCreatedAtStart
					}
					if bodyFiltersRoleIds != "" {
						nestedFilters["role_ids"] = cliutil.SplitCSV(bodyFiltersRoleIds)
					}
					if bodyFiltersTeamIds != "" {
						nestedFilters["team_ids"] = cliutil.SplitCSV(bodyFiltersTeamIds)
					}
					if bodyFiltersUserIds != "" {
						nestedFilters["user_ids"] = cliutil.SplitCSV(bodyFiltersUserIds)
					}
					if len(nestedFilters) > 0 {
						body["filters"] = nestedFilters
					}
				}
				if bodyLimit != 0 {
					body["limit"] = bodyLimit
				}
				if bodyOrder != "" {
					body["order"] = bodyOrder
				}
				if bodyOrderBy != "" {
					body["order_by"] = bodyOrderBy
				}
				if bodyStartingAfter != "" {
					body["starting_after"] = bodyStartingAfter
				}
				if bodyTimeZone != "" {
					body["time_zone"] = bodyTimeZone
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "pagerduty-analytics", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "pagerduty-analytics", data, "data")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pagerduty-analytics", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pagerduty-analytics", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pagerduty-analytics", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "pagerduty-analytics",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pagerduty-analytics", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "pagerduty-analytics", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAggregateUnit, "aggregate-unit", "", "The time unit to aggregate metrics by. If no value is provided, the metrics will be aggregated for the entire period.")
	cmd.Flags().StringVar(&bodyEndingBefore, "ending-before", "", "A cursor used for pagination. Ending before cursor provides the previous set of results in reverse pagination order.")
	cmd.Flags().StringVar(&bodyFiltersCreatedAtEnd, "filters-created-at-end", "", "The end of the date range to search")
	cmd.Flags().StringVar(&bodyFiltersCreatedAtStart, "filters-created-at-start", "", "The start of the date range to search")
	cmd.Flags().StringVar(&bodyFiltersRoleIds, "filters-role-ids", "", "An array of role IDs. Only users with these roles will be included in results.")
	cmd.Flags().StringVar(&bodyFiltersTeamIds, "filters-team-ids", "", "An array of team IDs. Only users belonging to these teams will be included in results.")
	cmd.Flags().StringVar(&bodyFiltersUserIds, "filters-user-ids", "", "An array of user IDs. Only these users will be included in results.")
	cmd.Flags().IntVar(&bodyLimit, "limit", 1000, "The maximum number of results to return per page. The default (and maximum allowed value) is 1000.")
	cmd.Flags().StringVar(&bodyOrder, "order", "desc", "The order in which the results were sorted; asc for ascending, desc for descending.")
	cmd.Flags().StringVar(&bodyOrderBy, "order-by", "user_id", "The column that was used for ordering the results.")
	cmd.Flags().StringVar(&bodyStartingAfter, "starting-after", "", "A cursor used for pagination. Starting after cursor provides the next set of results in forward pagination order.")
	cmd.Flags().StringVar(&bodyTimeZone, "time-zone", "", "The time zone to use for the results and grouping. Must be in tzdata format.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
