{
 "slug": "pagerduty",
 "doc": "docs/reprint-survival.md",
 "_comment": "Connector-specific edits to GENERATED/authored files that a cli-printing-press reprint must NOT clobber. check_handfixes.py asserts every marker below is still present (and banned anti-patterns absent) and fails CI if a reprint dropped one. Provenance: project_pagerduty_onboard_traps.md (PR #48, 2026-06-06, press 4.22.0).",
 "handfixes": [
  {
   "id": "guide-broken-vendored-links-repointed",
   "summary": "guide.md (vendored from the PagerDuty README) had ~96 ../../api-reference and ~9 openapiv3 deep-links into a non-shipped tree; repointed to developer.pagerduty.com and stripped to code text",
   "why": "PagerDuty's source README carried ~96 [API Concepts](../../api-reference/<hash>...) links into a tree msp-skills doesn't ship plus ~9 [/path](openapiv3.json/...) OpenAPI spec deep-links - check_md_links FAILs. Fixed: repoint api-concepts links to the real vendor docs (https://developer.pagerduty.com/api-reference/, verified 200; check_md_links SKIPS http(s) so a 404 would slip) and strip openapiv3 spec deep-links to backtick code text. A reprint re-vendors the broken relative/openapiv3 links.",
   "status": "active",
   "spec_encode_followup": "Fix belongs in the press README-vendoring step: rewrite or strip relative ../../ and openapiv3 deep-links that don't resolve in the shipped tree. Grep guide.md for `](../../` and `](openapiv3` after vendoring any new spec.",
   "asserts": [
    {
     "file": "guide.md",
     "contains": "developer.pagerduty.com/api-reference",
     "min_count": 1
    },
    {
     "file": "guide.md",
     "not_contains": "](../../api-reference"
    },
    {
     "file": "guide.md",
     "not_contains": "](openapiv3"
    }
   ]
  },
  {
   "id": "analytics-honest-framing",
   "summary": "Analytics differentiator framed as offline / without the paid Analytics tier / no per-question call - NOT 'no API returns it' (which is false; the binary wraps the vendor Analytics API)",
   "why": "Round-1 adversarial P1: README/SKILL/guide/page.json claimed MTTR-by-service / off-hours-pages were 'questions no single PagerDuty API call returns' - contradicted by the binary's own analytics commands that wrap the vendor Analytics API. The honest, defensible claim is 'offline / without the paid Analytics add-on / no live call per question'. A reprint of the authored marketing prose could reintroduce the false absolute claim.",
   "status": "active",
   "spec_encode_followup": "Not press-encodable - a prose/marketing review rule. The fresh-context adversarial reviewer (not the gates) is the only thing that catches it; refines feedback_outcome_prose_must_match_command_output_fields.",
   "asserts": [
    {
     "file": "README.md",
     "contains": "paid Analytics tier",
     "min_count": 1
    },
    {
     "file": "page.json",
     "contains": "paid Analytics tier",
     "min_count": 1
    }
   ]
  },
  {
   "id": "manifest-recovered-from-prior-scaffold",
   "summary": "Top-level manifest.json exists (press 4.22.0 emitted only tools-manifest.json; recovered from the prior scaffold commit so build-catalog stops failing)",
   "why": "Press run 20260606-085517 emitted only tools-manifest.json, not the mcpb/DXT manifest.json onboard copies in step 3 - build-catalog.py failed 'missing manifest.json for skill'. Recovered from the prior partial-scaffold commit. A reprint from a press that still drops manifest.json reintroduces the missing-file failure; this ledger entry makes its absence a loud failure.",
   "status": "active",
   "asserts": [
    {
     "file": "manifest.json",
     "contains": "PAGERDUTY_API_KEY",
     "min_count": 1
    }
   ]
  },
  {
   "id": "doctor-verdict-tracks-reality",
   "summary": "doctor probes a real GET endpoint with the real credential and refuses to dial a placeholder base_url, instead of calling any HTTP response 'reachable' and never checking the credential",
   "why": "`doctor` is the command every SKILL, guide and docs page tells an operator to run to confirm the connector works, and it could not tell a working install from a broken one. It called any HTTP response to `GET /` \"reachable\" and rendered it green, so a base_url aimed at the vendor's web UI (where every API path 404s) and a token rejected with 401 on every call both produced an all-green report. On this connector family it also never probed the credential at all: it printed \"present, not verified\" and left the operator to guess. The mirror defect: a connector whose shipped base_url default is a placeholder dialled the placeholder host, and the DNS failure rendered as \"FAIL API: unreachable\" - telling a correctly-credentialled operator they were broken without naming the one variable that would fix it. Several placeholder hosts resolve through live wildcard DNS and answer real HTTP, so dialling one produced a confident green verdict against a stranger's server. Measured and gated by tools/maintainer/check_doctor_truth.py. See issue #282.",
   "status": "active",
   "asserts": [
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorProbeCredentials("
    },
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorBaseURLIsPlaceholder("
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorIsInfoKey(k)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorBaseURLIsPlaceholder(cfg.BaseURL)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorUnexpectedStatus(authAPIErr.StatusCode"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "but auth was accepted"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "for _, v := range report {"
    }
   ]
  },
  {
   "id": "install-credentials-declared-on-every-channel",
   "summary": "every operator credential this connector reads is declared on all three install channels: manifest.json (.mcpb), server.json (MCP Registry) and mcp-install.md (manual install)",
   "why": "Issue #282 ('three live-verified connectors prompt for the wrong credentials'). A connector is installed through THREE independent channels that each carry their own copy of the credential list: manifest.json server.mcp_config.env (what the .mcpb bundle hands Claude Desktop, which has no shell profile, no config.toml and no export - so an undeclared variable is a variable nobody is ever asked for), server.json environmentVariables (the MCP Registry publish channel), and mcp-install.md's copy-paste JSON blocks plus its remote launch line (the manual path, which never touches the bundle at all). The fleet-wide audit found 60 of 65 connectors under-declaring 122 operator-facing variables in the manifest, 61 of 65 mcp-install.md pages omitting 125, and hudu's server.json missing the base URL its manifest marks required. For pagerduty the manifest gained: PAGERDUTY_BASE_URL. server.json gained: PAGERDUTY_BASE_URL. mcp-install.md's env blocks gained: PAGERDUTY_BASE_URL. manifest.json, server.json and mcp-install.md are ALL regenerated by /msp-skills-publish on an UPDATE or re-onboard, so without this ledger a regeneration silently drops every declaration and CI stays green (check_env_schema.py is wired --warn). The asserts below name the specific credentials, not a count, so dropping one and adding another cannot cancel out.",
   "status": "active",
   "spec_encode_followup": "cli-printing-press should emit server.mcp_config.env, the matching user_config prompts, server.json environmentVariables and mcp-install.md's env blocks from ONE declaration of the connector's operator-facing environment, so the three channels cannot be generated out of step. Until then check_env_schema.py compares all three and this ledger pins them.",
   "asserts": [
    {
     "file": "manifest.json",
     "contains": "\"PAGERDUTY_API_KEY\": \"${user_config.pagerduty_api_key}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"PAGERDUTY_BASE_URL\": \"${user_config.pagerduty_base_url}\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"PAGERDUTY_API_KEY\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"PAGERDUTY_BASE_URL\"",
     "min_count": 1
    },
    {
     "file": "mcp-install.md",
     "contains": "\"PAGERDUTY_API_KEY\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"PAGERDUTY_BASE_URL\":",
     "min_count": 3
    }
   ]
  }
 ]
}
