// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDocumentsDownloadDocumentCmd(flags *rootFlags) *cobra.Command {
	var flagWatermarkColor string
	var flagWatermarkFontSize int
	var flagWatermarkOpacity float64
	var flagWatermarkText string
	var flagSeparateFiles bool

	cmd := &cobra.Command{
		Use:         "document <id>",
		Aliases:     []string{"get"},
		Short:       "Download documents as a PDF.",
		Example:     "  pandadoc-cli documents download document BhVzRcxH9Z2LgfPPGXFUBa",
		Annotations: map[string]string{"pp:endpoint": "download.document", "pp:method": "GET", "pp:path": "/documents/{id}/download", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/documents/{id}/download"
			path = replacePathParam(path, "id", args[0])
			headerOverrides := map[string]string{
				"Accept":                           "application/pdf",
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagWatermarkColor != "" {
				params["watermark_color"] = formatCLIParamValue(flagWatermarkColor)
			}
			if flagWatermarkFontSize != 0 {
				params["watermark_font_size"] = formatCLIParamValue(flagWatermarkFontSize)
			}
			if flagWatermarkOpacity != 0.0 {
				params["watermark_opacity"] = formatCLIParamValue(flagWatermarkOpacity)
			}
			if flagWatermarkText != "" {
				params["watermark_text"] = formatCLIParamValue(flagWatermarkText)
			}
			if flagSeparateFiles != false {
				params["separate_files"] = formatCLIParamValue(flagSeparateFiles)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "download", false, path, params, headerOverrides, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagWatermarkColor, "watermark-color", "", "HEX code (for example `#FF5733`).")
	cmd.Flags().IntVar(&flagWatermarkFontSize, "watermark-font-size", 0, "Font size of the watermark.")
	cmd.Flags().Float64Var(&flagWatermarkOpacity, "watermark-opacity", 0.0, "In range 0.0-1.0")
	cmd.Flags().StringVar(&flagWatermarkText, "watermark-text", "", "Specify watermark text.")
	cmd.Flags().BoolVar(&flagSeparateFiles, "separate-files", false, "Download document bundle as a zip-archive of separate PDFs (1 file per section).")

	return cmd
}
