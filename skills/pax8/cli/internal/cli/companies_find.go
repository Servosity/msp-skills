// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newCompaniesFindCmd(flags *rootFlags) *cobra.Command {
	var flagPage string
	var flagSize float64
	var flagSort string
	var flagCity string
	var flagCountry string
	var flagStateOrProvince string
	var flagPostalCode string
	var flagSelfServiceAllowed bool
	var flagBillOnBehalfOfEnabled bool
	var flagOrderApprovalRequired bool
	var flagStatus string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "find",
		Aliases:     []string{"list"},
		Short:       "Returns a paginated list of all your companies filtered by optional parameters",
		Example:     "  pax8-cli companies list --json",
		Annotations: map[string]string{"pp:endpoint": "companies.find", "pp:method": "GET", "pp:path": "/companies", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("sort") {
				allowedSort := []string{"name", "city", "country", "stateOrProvince", "postalCode"}
				validSort := false
				for _, v := range allowedSort {
					if flagSort == v {
						validSort = true
						break
					}
				}
				if !validSort {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSort, "sort", allowedSort)
				}
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"Active", "Inactive", "Deleted"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/companies"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "companies", path, map[string]string{
				"page":                  formatCLIParamValue(flagPage),
				"size":                  formatCLIParamValue(flagSize),
				"sort":                  formatCLIParamValue(flagSort),
				"city":                  formatCLIParamValue(flagCity),
				"country":               formatCLIParamValue(flagCountry),
				"stateOrProvince":       formatCLIParamValue(flagStateOrProvince),
				"postalCode":            formatCLIParamValue(flagPostalCode),
				"selfServiceAllowed":    formatCLIParamValue(flagSelfServiceAllowed),
				"billOnBehalfOfEnabled": formatCLIParamValue(flagBillOnBehalfOfEnabled),
				"orderApprovalRequired": formatCLIParamValue(flagOrderApprovalRequired),
				"status":                formatCLIParamValue(flagStatus),
			}, nil, flagAll, "page", "page", "", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagPage, "page", "0", "The page number to request for in the companies list")
	cmd.Flags().Float64Var(&flagSize, "size", 10.000000, "Return this number of company records per page")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Return companies sorted by this field and direction. Formatted as fieldName,direction - ex. sort=name,desc (one of: name, city, country, stateOrProvince, postalCode)")
	cmd.Flags().StringVar(&flagCity, "city", "", "Return only companies matching this ```city``` value")
	cmd.Flags().StringVar(&flagCountry, "country", "", "Return only companies matching this ```country``` value")
	cmd.Flags().StringVar(&flagStateOrProvince, "state-or-province", "", "Return only companies matching this ```stateOrProvince``` value")
	cmd.Flags().StringVar(&flagPostalCode, "postal-code", "", "Return only companies matching this ```postalCode``` value")
	cmd.Flags().BoolVar(&flagSelfServiceAllowed, "self-service-allowed", false, "Return only companies matching this ```selfServiceAllowed``` value")
	cmd.Flags().BoolVar(&flagBillOnBehalfOfEnabled, "bill-on-behalf-of-enabled", false, "Return only companies matching this ```billOnBehalfOfEnabled``` value")
	cmd.Flags().BoolVar(&flagOrderApprovalRequired, "order-approval-required", false, "Return only companies matching this ```orderApprovalRequired``` value")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Return only companies matching this ```status``` value (one of: Active, Inactive, Deleted)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
