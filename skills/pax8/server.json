{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Servosity/pax8-mcp",
  "title": "Pax8 MCP",
  "description": "Every Pax8 Partner API endpoint, plus an offline store that reconciles billing, tracks MRR, and",
  "version": "0.1.2",
  "icons": [
    {
      "src": "https://raw.githubusercontent.com/servosity/msp-skills/main/docs/assets/social/pax8/pax8-512x512.png",
      "mimeType": "image/png",
      "sizes": [
        "512x512"
      ]
    }
  ],
  "repository": {
    "url": "https://github.com/servosity/msp-skills",
    "source": "github",
    "subfolder": "skills/pax8"
  },
  "packages": [
    {
      "registryType": "mcpb",
      "identifier": "https://github.com/servosity/msp-skills/releases/download/pax8-v0.1.2/pax8-mcp.mcpb",
      "version": "0.1.2",
      "fileSha256": "PLACEHOLDER_SHA256_FILLED_BY_CI",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "PAX8_AUDIENCE",
          "description": "OAuth2 audience claim requested when minting the Pax8 token. The default is the partner API audience; change it only if Pax8 told you to.",
          "isRequired": false,
          "isSecret": false,
          "default": "api://p8p.client"
        },
        {
          "name": "PAX8_BASE_URL",
          "description": "Base URL for the Pax8 API. Leave the prefilled default unless your account uses a different regional or self-hosted host.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://api.pax8.com/v1"
        },
        {
          "name": "PAX8_CLIENT_ID",
          "description": "Pax8 Partner API OAuth2 Client ID (Pax8 portal > Integrations).",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "PAX8_CLIENT_SECRET",
          "description": "Pax8 Partner API OAuth2 Client Secret (Pax8 portal > Integrations).",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "PAX8_OAUTH_SCOPE",
          "description": "Optional OAuth2 scope; omitted from the token request when unset.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "PAX8_TOKEN_URL",
          "description": "Token endpoint the client-credentials exchange posts to. Leave the prefilled default unless your tenant uses a different token endpoint.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://token-manager.pax8.com/oauth/token"
        },
        {
          "name": "PAX8_NO_CONFIG_WRITE",
          "description": "Set to 1 to keep credentials off disk. The token cache becomes a no-op, so the connector mints a fresh token each run instead of writing one to config.toml. Leave blank for the default cached behaviour.",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
