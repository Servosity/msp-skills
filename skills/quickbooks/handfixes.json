{
 "slug": "quickbooks",
 "doc": "docs/reprint-survival.md",
 "_comment": "Connector-specific edits to GENERATED files that a cli-printing-press reprint must NOT clobber. check_handfixes.py asserts every marker below is still present and fails CI if a reprint dropped one. status: active = connector-only, a reprint would clobber it; upstreamed = also fixed in the press but the back-port must still be present today.",
 "handfixes": [
  {
   "id": "qbo-query-injection",
   "summary": "sync injects `select * from <Entity>` (+ minorversion) for the /query endpoint; QBO has no plain REST list path",
   "why": "Every QuickBooks Online entity is read through /query, which REQUIRES a SQL-like `query` parameter. The generated sync path sent a bare /query with no query text, and QBO answers HTTP 200 with a `{\"Fault\":...SystemFailureError}` envelope (not a 4xx). The generic extractor finds no array, so upsertSingleObject gets the Fault envelope, extractObjectID returns \"\", and every resource fails with `missing id for <resource>` (0 rows). Fix injects a per-entity `select * from <Entity> ...` and `minorversion=75` when path==\"/query\", mapping each sync resource to its PascalCase QBO entity.",
   "status": "active",
   "spec_encode_followup": "Press change: teach cli-printing-press to inject the entity SQL + minorversion for QBO-shaped /query endpoints via a sync-hint / x-pp-query spec extension, so a reprint regenerates this by construction. File via /printing-press-retro.",
   "asserts": [
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "quickbooksEntity",
     "min_count": 2
    },
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "\"minorversion\"",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "\"JournalEntry\"",
     "min_count": 1
    }
   ]
  },
  {
   "id": "qbo-query-paging",
   "summary": "sync pages QBO /query via in-query STARTPOSITION/MAXRESULTS until a short page; without it the mirror truncates at 1000 rows/entity",
   "why": "resourceSupportsPagination() is false for every QBO entity, so the generic sync loop breaks after page 1 - capping each entity at one 1000-row page (QBO's MAXRESULTS hard cap). On a real book this silently truncated catastrophically: only the first 1000 rows of each entity, making ar-aging/ap-aging/dso/balances wrong (not just partial), and QBO returns rows oldest-first so recent invoices were missing entirely. Fix carries the next STARTPOSITION in the loop's `cursor`, sets hasMore from full-page detection (len(items) >= qboMaxResults), and exempts /query from the generic !resourceSupportsPagination and short-page breaks. Verified live against a production tenant: all 8 resources sync to their exact live count(*) across the full book.",
   "status": "active",
   "spec_encode_followup": "Same press change as qbo-query-injection: a QBO sync-hint that emits STARTPOSITION/MAXRESULTS paging by construction.",
   "asserts": [
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "qboMaxResults",
     "min_count": 2
    },
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "startposition %d maxresults %d",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "path != \"/query\"",
     "min_count": 2
    }
   ]
  },
  {
   "id": "qbo-queryresponse-envelope",
   "summary": "page-item extraction unwraps QBO's two-level `{\"QueryResponse\":{\"<Entity>\":[...]}}` envelope",
   "why": "QBO /query nests the result array one level deeper than the generic extractor handles: `QueryResponse` (object) then the PascalCase entity key (array). Without `QueryResponse` in dataEnvelopeKeys, extractPageItems cannot reach the entity array, returns 0 items, and sync falls through to single-object upsert of the whole envelope. Adding it lets the existing two-level unwrap + single-array-sibling logic pull the entity array correctly. Paired with qbo-query-injection; both are required for sync to store any rows.",
   "status": "active",
   "spec_encode_followup": "Same press change as qbo-query-injection: a QBO response-envelope hint so the generated extractor unwraps QueryResponse.<Entity> by construction.",
   "asserts": [
    {
     "file": "cli/internal/cli/sync.go",
     "contains": "\"QueryResponse\"",
     "min_count": 1
    }
   ]
  },
  {
   "id": "qbo-db-path-env",
   "summary": "defaultDBPath honors QUICKBOOKS_DB_PATH so sandbox and production keep separate local mirrors",
   "why": "The local SQLite mirror path was hardcoded to ~/.local/share/quickbooks-cli/data.db with no per-tenant separation. QBO entity IDs are per-company and collide across companies (sandbox Id \"58\" vs production Id \"58\"), so running both sandbox and production against one data.db corrupts aggregates after a mode switch. The Keychain wrapper sets QUICKBOOKS_DB_PATH per mode (.../sandbox/data.db vs .../production/data.db); this env override makes every command (sync, doctor, the aggregates) honor it.",
   "status": "active",
   "spec_encode_followup": "Press change: a generic <CLI>_DB_PATH env override on defaultDBPath would let any multi-tenant connector isolate mirrors without a hand-edit.",
   "asserts": [
    {
     "file": "cli/internal/cli/helpers.go",
     "contains": "QUICKBOOKS_DB_PATH",
     "min_count": 1
    }
   ]
  },
  {
   "id": "qbo-aging-snapshots-table",
   "summary": "aging-delta creates its aging_snapshots table on demand; no store migration defines it",
   "why": "aging_delta.go (a novel hand-authored command) reads and writes an aging_snapshots table, but nothing in the store migrations creates it. So every aging-delta run errored `no such table: aging_snapshots` on the read path (loadLatestAgingSnapshot treats only sql.ErrNoRows as a clean first-run; a missing table is a hard error) - the command was broken from first use. Fix adds ensureAgingSnapshotsTable (CREATE TABLE IF NOT EXISTS) called before the read, so a fresh or freshly reprinted DB self-heals. aging_delta.go is a novel file kept whole by regen-merge, so this survives a reprint; this entry is the cheap backstop the reprint-survival doc recommends for novel commands.",
   "status": "active",
   "spec_encode_followup": "Press change: novel commands that own a private table should declare it in the store migration set so it is created by construction.",
   "asserts": [
    {
     "file": "cli/internal/cli/aging_delta.go",
     "contains": "CREATE TABLE IF NOT EXISTS aging_snapshots",
     "min_count": 1
    }
   ]
  },
  {
   "id": "qbo-report-reports-api",
   "summary": "report pnl + report balance-sheet pull QBO financial statements (P&L, Balance Sheet) from the Reports API, which the /query-based CLI cannot reach",
   "why": "QuickBooks financial statements live behind the Reports API (/reports/ProfitAndLoss, /reports/BalanceSheet), not /query, so the generated CLI could not produce them. report.go is a novel hand-authored command group (report pnl, report balance-sheet; read-only) kept whole by regen-merge, but its registration in the generated root.go (rootCmd.AddCommand(newReportCmd(flags))) is a hand-edit a reprint would clobber. This entry records it so the command survives.",
   "status": "active",
   "spec_encode_followup": "Press change: encode the report command group in the QBO spec (or a novel-feature manifest) so root.go registers it by construction without a hand-edit.",
   "asserts": [
    {
     "file": "cli/internal/cli/root.go",
     "contains": "newReportCmd(flags)",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/report.go",
     "contains": "/reports/ProfitAndLoss",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/report.go",
     "contains": "/reports/BalanceSheet",
     "min_count": 1
    }
   ]
  },
  {
   "id": "doctor-verdict-tracks-reality",
   "summary": "doctor probes a real GET endpoint with the real credential and refuses to dial a placeholder base_url, instead of calling any HTTP response 'reachable' and never checking the credential",
   "why": "`doctor` is the command every SKILL, guide and docs page tells an operator to run to confirm the connector works, and it could not tell a working install from a broken one. It called any HTTP response to `GET /` \"reachable\" and rendered it green, so a base_url aimed at the vendor's web UI (where every API path 404s) and a token rejected with 401 on every call both produced an all-green report. On this connector family it also never probed the credential at all: it printed \"present, not verified\" and left the operator to guess. The mirror defect: a connector whose shipped base_url default is a placeholder dialled the placeholder host, and the DNS failure rendered as \"FAIL API: unreachable\" - telling a correctly-credentialled operator they were broken without naming the one variable that would fix it. Several placeholder hosts resolve through live wildcard DNS and answer real HTTP, so dialling one produced a confident green verdict against a stranger's server. Measured and gated by tools/maintainer/check_doctor_truth.py. See issue #282.",
   "status": "active",
   "asserts": [
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorProbeCredentials("
    },
    {
     "file": "cli/internal/cli/doctor_verdict.go",
     "contains": "func doctorBaseURLIsPlaceholder("
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorIsInfoKey(k)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorBaseURLIsPlaceholder(cfg.BaseURL)"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "contains": "doctorProbeCredentials(cmd.Context()"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "present, not verified. Run `%s %s`"
    },
    {
     "file": "cli/internal/cli/doctor.go",
     "not_contains": "for _, v := range report {"
    }
   ]
  },
  {
   "id": "install-credentials-declared-on-every-channel",
   "summary": "every operator credential this connector reads is declared on all three install channels: manifest.json (.mcpb), server.json (MCP Registry) and mcp-install.md (manual install)",
   "why": "Issue #282 ('three live-verified connectors prompt for the wrong credentials'). A connector is installed through THREE independent channels that each carry their own copy of the credential list: manifest.json server.mcp_config.env (what the .mcpb bundle hands Claude Desktop, which has no shell profile, no config.toml and no export - so an undeclared variable is a variable nobody is ever asked for), server.json environmentVariables (the MCP Registry publish channel), and mcp-install.md's copy-paste JSON blocks plus its remote launch line (the manual path, which never touches the bundle at all). The fleet-wide audit found 60 of 65 connectors under-declaring 122 operator-facing variables in the manifest, 61 of 65 mcp-install.md pages omitting 125, and hudu's server.json missing the base URL its manifest marks required. For quickbooks the manifest gained: QUICKBOOKS_BASE_URL, QUICKBOOKS_CLIENT_ID, QUICKBOOKS_CLIENT_SECRET, QUICKBOOKS_ENVIRONMENT, QUICKBOOKS_REALM_ID, QUICKBOOKS_REFRESH_TOKEN, QUICKBOOKS_TOKEN_URL. server.json gained: QUICKBOOKS_BASE_URL, QUICKBOOKS_CLIENT_ID, QUICKBOOKS_CLIENT_SECRET, QUICKBOOKS_ENVIRONMENT, QUICKBOOKS_REALM_ID, QUICKBOOKS_REFRESH_TOKEN, QUICKBOOKS_TOKEN_URL. mcp-install.md's env blocks gained: QUICKBOOKS_BASE_URL, QUICKBOOKS_CLIENT_ID, QUICKBOOKS_CLIENT_SECRET, QUICKBOOKS_ENVIRONMENT, QUICKBOOKS_REALM_ID, QUICKBOOKS_REFRESH_TOKEN, QUICKBOOKS_TOKEN_URL. manifest.json, server.json and mcp-install.md are ALL regenerated by /msp-skills-publish on an UPDATE or re-onboard, so without this ledger a regeneration silently drops every declaration and CI stays green (check_env_schema.py is wired --warn). The asserts below name the specific credentials, not a count, so dropping one and adding another cannot cancel out.",
   "status": "active",
   "spec_encode_followup": "cli-printing-press should emit server.mcp_config.env, the matching user_config prompts, server.json environmentVariables and mcp-install.md's env blocks from ONE declaration of the connector's operator-facing environment, so the three channels cannot be generated out of step. Until then check_env_schema.py compares all three and this ledger pins them.",
   "asserts": [
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_ACCESS_TOKEN\": \"${user_config.quickbooks_access_token}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_BASE_URL\": \"${user_config.quickbooks_base_url}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_CLIENT_ID\": \"${user_config.quickbooks_client_id}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_CLIENT_SECRET\": \"${user_config.quickbooks_client_secret}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_ENVIRONMENT\": \"${user_config.quickbooks_environment}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_REALM_ID\": \"${user_config.quickbooks_realm_id}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_REFRESH_TOKEN\": \"${user_config.quickbooks_refresh_token}\"",
     "min_count": 1
    },
    {
     "file": "manifest.json",
     "contains": "\"QUICKBOOKS_TOKEN_URL\": \"${user_config.quickbooks_token_url}\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_ACCESS_TOKEN\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_BASE_URL\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_CLIENT_ID\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_CLIENT_SECRET\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_ENVIRONMENT\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_REALM_ID\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_REFRESH_TOKEN\"",
     "min_count": 1
    },
    {
     "file": "server.json",
     "contains": "\"name\": \"QUICKBOOKS_TOKEN_URL\"",
     "min_count": 1
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_ACCESS_TOKEN\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_BASE_URL\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_CLIENT_ID\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_CLIENT_SECRET\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_ENVIRONMENT\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_REALM_ID\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_REFRESH_TOKEN\":",
     "min_count": 3
    },
    {
     "file": "mcp-install.md",
     "contains": "\"QUICKBOOKS_TOKEN_URL\":",
     "min_count": 3
    }
   ]
  },
  {
   "id": "mcp-filesystem-destination-gate",
   "summary": "The MCP shell-out layer refuses filesystem-destination flags (--db, --input, --output, --out, --home, --audit-dir, --receipt-file and friends) and any argument name carrying '=', so a tool call cannot point the local SQLite store, or any other file this account can reach, at a path of its choosing",
   "why": "The MCP server runs the companion CLI as the server account and forwarded every command-local flag straight through as a CLI argument. `sync` and the other local-store commands expose --db, and the store's migration runs `DROP TABLE IF EXISTS resources_fts` plus a resources-table rebuild against whatever file it is handed, so one tool call shaped {\"db\": \"/path/to/someone-elses.sqlite\"} rewrote an unrelated database - no credentials required, because sync opens the store before it ever reaches the API. Proven end to end: against the pre-fix binary a single MCP tools/call on `sync` changed the victim file's sha and left resources_fts with 0 rows; against the patched binary the same call returns the same sync summary and the victim file is byte-identical. The gate has three parts, all in cli/internal/mcp/cobratree/shellout.go: blockedDestinationFlags (the runtime name floor, unioned into cliArgsFromMCP alongside the pre-existing blockedRootFlags behaviour, which is unchanged); an argument-name '=' guard, because \"db=/tmp/evil\" was emitted as --db=/tmp/evil and a name-only denylist never saw it; and isFilesystemPathFlag, which classifies a flag by its USAGE TEXT rather than its NAME. The usage-text test is load bearing and must not be turned into a name rule: the generated command surface carries API body fields called --path, --seed-path, --source-paths, --exclude-paths, --filename and --working-dir whose values go to the vendor API and never touch this account's disk, so a name rule would break real API surfaces while still missing the next generated flag that does take a local path. UnblockedFilesystemPathFlags applies that rule to the live Cobra tree over exactly the command set RegisterAll turns into shell-out tools (endpoint and framework commands are out of scope, which is what keeps the rule off generated vendor body fields), and TestNoShellOutToolCanDirectAFilesystemPath fails the build when a regeneration grows a local-path flag the floor does not name. vendorAPIPathFlags is the documented exception list for flags whose usage trips the rule but which name a location in the vendor's world. Both directions are pinned by the tests: the --db argument must be dropped AND an ordinary flag must still be forwarded, because a gate that swallows the real MCP surface is as harmful as one that misses the defect.",
   "status": "active",
   "spec_encode_followup": "Press-side: the gate belongs in the generator. Two known residuals a reprint should close rather than re-inherit. (1) On the pre-4.30 cobratree the blocked flags are dropped at forward time but still appear in the tool's input schema, because toolOptionsForFlags in typemap.go advertises every flag; the caller gets silence instead of an error. The 4.30 shape (blockedStructuredArgsForCommand + allowedStructuredArgsForCommand) removes them from the schema and errors on them, which is the better behaviour. (2) isFilesystemPathFlag is only consulted by the package test here, not at request time, because the pre-4.30 shellOutToCLI receives no *cobra.Command; wiring it into the per-command blocked set (as skills/servosity does) makes a newly generated path flag refused at runtime rather than at CI time.",
   "asserts": [
    {
     "file": "cli/internal/mcp/cobratree/shellout.go",
     "contains": "var blockedDestinationFlags = map[string]bool{",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/shellout.go",
     "contains": "blockedDestinationFlags[k]",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/shellout.go",
     "contains": "strings.Contains(k, \"=\")",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/shellout.go",
     "contains": "func isFilesystemPathFlag(flag *pflag.Flag) bool {",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/shellout.go",
     "contains": "func UnblockedFilesystemPathFlags(root *cobra.Command) []string {",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/filesystem_gate_test.go",
     "contains": "func TestMCPCannotDirectTheLocalStore(",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/filesystem_gate_test.go",
     "contains": "func TestMCPCannotSmuggleAFlagThroughAnEqualsSign(",
     "min_count": 1
    },
    {
     "file": "cli/internal/mcp/cobratree/filesystem_tree_test.go",
     "contains": "func TestNoShellOutToolCanDirectAFilesystemPath(",
     "min_count": 1
    }
   ]
  },
  {
   "id": "novel-acceptance-fixtures-relative-to-today",
   "summary": "The novel-command acceptance fixtures seed every QBO date as a whole-day offset from the day the test runs, not as an absolute 2026 date, so the seeded books keep the same shape on any future date",
   "why": "seedNovelStore hard-coded absolute dates (INV-1 due 2026-04-30, INV-2 due 2026-07-15, BILL-1 due 2026-06-20) while the commands under test read the wall clock: the Cobra layer passes time.Now() into analytics.CashForecast, analytics.StaleInvoices and analytics.Aging. Those fixtures encoded a 'today' of roughly 2026-05, so the assertions only held for a 75-day window. Once the calendar passed 2026-07-15 every seeded invoice was overdue, and both TestNovelCashForecastAcceptance (overdue_inflows 800 instead of 500, an empty 8-week window, net_in_window 0) and TestNovelInvoicesStaleAcceptance (INV-2 joined the collections list) failed on the calendar rather than on a code change. This is a TEST defect, not a product one - the shipped commands correctly forecast from the real current date, and the analytics layer already takes an injected `now` that its own unit tests pin. The fix moves the fixtures onto that same footing: novelSeedDay() reads the clock exactly once and seedDate() renders integer day offsets from it, so the books are translation-invariant. The reference day is anchored in UTC even though its year/month/day come from the local clock, because AddDate over a local midnight that does not exist on a spring-forward day (America/Santiago, Asia/Beirut) normalizes back to 23:00 the previous day and would slip every derived date by one. Offsets were chosen to clear every bucket, window and overdue boundary by a wide margin, because fixture dates parse as UTC midnight while the commands floor 'today' in local time - a skew of up to 14 hours. Proven both directions across 658800 (reference-day, time-zone, hour) combinations spanning 2026-2045 over 15 zones including the DST-hostile ones: 0 failures with the relative fixtures, and the same sweep run against the old absolute fixtures fails on 7245 of 7320 reference days, so the check discriminates rather than passing by construction. The tests also got stronger on the way through: the pinned reference lets them assert overdue_outflows, the beyond-window split, net_in_window and which forward week each open item buckets into, none of which could be asserted while 'today' was unknown. The as_of assertion brackets the command with two clock reads (dayBefore before the run, dayAfter after it) and requires dayBefore <= as_of <= dayAfter, both bounds inclusive: flooring to whole days is monotonic over the ordered reads t0 <= t1 <= t2, so a midnight crossing on either side of the command is admitted while a genuinely stale or future as_of still fails.",
   "status": "active",
   "spec_encode_followup": "Press-side: the acceptance-test template should seed clock-relative dates for any connector whose novel commands take a `now`, rather than emitting absolute date literals that rot. Worth noting for the same retro: no local novel command exposes an --as-of / reference-date flag (only `report balance-sheet` has one), so a caller cannot reproduce a cash-forecast or aging run as of a past date. Adding one is a product change and is deliberately NOT part of this fix.",
   "asserts": [
    {
     "file": "cli/internal/cli/novel_acceptance_test.go",
     "contains": "func novelSeedDay() time.Time {",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/novel_acceptance_test.go",
     "contains": "func seedDate(ref time.Time, offsetDays int) string {",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/novel_acceptance_test.go",
     "contains": "seedDate(ref, inv1DueOff)",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/novel_acceptance_test.go",
     "contains": "net_in_window",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/novel_acceptance_test.go",
     "contains": "if asOf < dayBefore || asOf > dayAfter {",
     "min_count": 1
    },
    {
     "file": "cli/internal/cli/novel_prior_acceptance_test.go",
     "contains": "days_past_due",
     "min_count": 1
    }
   ]
  }
 ]
}
