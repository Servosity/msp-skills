// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newApiKeysListCmd(flags *rootFlags) *cobra.Command {
	var flagInclude string
	var flagPageNumber int
	var flagPageSize int
	var flagFilterKind string
	var flagFilterSearch string
	var flagFilterName string
	var flagFilterUserId string
	var flagFilterGroupIds string
	var flagFilterRoleId string
	var flagFilterActive bool
	var flagFilterExpired bool
	var flagFilterCreatedAtGt string
	var flagFilterCreatedAtGte string
	var flagFilterCreatedAtLt string
	var flagFilterCreatedAtLte string
	var flagFilterExpiresAtGt string
	var flagFilterExpiresAtGte string
	var flagFilterExpiresAtLt string
	var flagFilterExpiresAtLte string
	var flagFilterLastUsedAtGt string
	var flagFilterLastUsedAtGte string
	var flagFilterLastUsedAtLt string
	var flagFilterLastUsedAtLte string
	var flagSort string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List API keys for the current organization.",
		Example:     "  rootly-cli api-keys list",
		Annotations: map[string]string{"pp:endpoint": "api-keys.list", "pp:method": "GET", "pp:path": "/v1/api_keys", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/api_keys"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "api-keys", path, map[string]string{
				"include":                   formatCLIParamValue(flagInclude),
				"page[number]":              formatCLIParamValue(flagPageNumber),
				"page[size]":                formatCLIParamValue(flagPageSize),
				"filter[kind]":              formatCLIParamValue(flagFilterKind),
				"filter[search]":            formatCLIParamValue(flagFilterSearch),
				"filter[name]":              formatCLIParamValue(flagFilterName),
				"filter[user_id]":           formatCLIParamValue(flagFilterUserId),
				"filter[group_ids]":         formatCLIParamValue(flagFilterGroupIds),
				"filter[role_id]":           formatCLIParamValue(flagFilterRoleId),
				"filter[active]":            formatCLIParamValue(flagFilterActive),
				"filter[expired]":           formatCLIParamValue(flagFilterExpired),
				"filter[created_at][gt]":    formatCLIParamValue(flagFilterCreatedAtGt),
				"filter[created_at][gte]":   formatCLIParamValue(flagFilterCreatedAtGte),
				"filter[created_at][lt]":    formatCLIParamValue(flagFilterCreatedAtLt),
				"filter[created_at][lte]":   formatCLIParamValue(flagFilterCreatedAtLte),
				"filter[expires_at][gt]":    formatCLIParamValue(flagFilterExpiresAtGt),
				"filter[expires_at][gte]":   formatCLIParamValue(flagFilterExpiresAtGte),
				"filter[expires_at][lt]":    formatCLIParamValue(flagFilterExpiresAtLt),
				"filter[expires_at][lte]":   formatCLIParamValue(flagFilterExpiresAtLte),
				"filter[last_used_at][gt]":  formatCLIParamValue(flagFilterLastUsedAtGt),
				"filter[last_used_at][gte]": formatCLIParamValue(flagFilterLastUsedAtGte),
				"filter[last_used_at][lt]":  formatCLIParamValue(flagFilterLastUsedAtLt),
				"filter[last_used_at][lte]": formatCLIParamValue(flagFilterLastUsedAtLte),
				"sort":                      formatCLIParamValue(flagSort),
			}, nil, flagAll, "page[number]", "page", "page[size]", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagInclude, "include", "", "Comma-separated list of relationships to include (role, on_call_role, created_by, groups)")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 0, "Page number")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Page size")
	cmd.Flags().StringVar(&flagFilterKind, "filter-kind", "", "Filter by API key kind: personal, team, organization")
	cmd.Flags().StringVar(&flagFilterSearch, "filter-search", "", "Search by name (case-insensitive partial match)")
	cmd.Flags().StringVar(&flagFilterName, "filter-name", "", "Filter by exact name")
	cmd.Flags().StringVar(&flagFilterUserId, "filter-user-id", "", "Filter by the user ID that owns the key")
	cmd.Flags().StringVar(&flagFilterGroupIds, "filter-group-ids", "", "Filter team keys by group IDs (comma-separated)")
	cmd.Flags().StringVar(&flagFilterRoleId, "filter-role-id", "", "Filter by role ID")
	cmd.Flags().BoolVar(&flagFilterActive, "filter-active", false, "When true, return only non-expired keys")
	cmd.Flags().BoolVar(&flagFilterExpired, "filter-expired", false, "When true, return only expired keys")
	cmd.Flags().StringVar(&flagFilterCreatedAtGt, "filter-created-at-gt", "", "Created after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterCreatedAtGte, "filter-created-at-gte", "", "Created at or after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterCreatedAtLt, "filter-created-at-lt", "", "Created before (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterCreatedAtLte, "filter-created-at-lte", "", "Created at or before (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterExpiresAtGt, "filter-expires-at-gt", "", "Expires after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterExpiresAtGte, "filter-expires-at-gte", "", "Expires at or after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterExpiresAtLt, "filter-expires-at-lt", "", "Expires before (ISO 8601). Useful for finding keys approaching expiration.")
	cmd.Flags().StringVar(&flagFilterExpiresAtLte, "filter-expires-at-lte", "", "Expires at or before (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterLastUsedAtGt, "filter-last-used-at-gt", "", "Last used after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterLastUsedAtGte, "filter-last-used-at-gte", "", "Last used at or after (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterLastUsedAtLt, "filter-last-used-at-lt", "", "Last used before (ISO 8601)")
	cmd.Flags().StringVar(&flagFilterLastUsedAtLte, "filter-last-used-at-lte", "", "Last used at or before (ISO 8601)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Sort by field. Prefix with - for descending (e.g., -created_at, expires_at)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
