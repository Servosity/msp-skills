// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
	"rootly-pp-cli/internal/cliutil"
)

func newFunctionalitiesCreateFunctionalityCmd(flags *rootFlags) *cobra.Command {
	var bodyDataAttributesBackstageId string
	var bodyDataAttributesColor string
	var bodyDataAttributesCortexId string
	var bodyDataAttributesDescription string
	var bodyDataAttributesEnvironmentIds string
	var bodyDataAttributesExternalId string
	var bodyDataAttributesName string
	var bodyDataAttributesNotifyEmails string
	var bodyDataAttributesOpsgenieId string
	var bodyDataAttributesOpsgenieTeamId string
	var bodyDataAttributesOwnerGroupIds string
	var bodyDataAttributesOwnerUserIds string
	var bodyDataAttributesPagerdutyId string
	var bodyDataAttributesPosition int
	var bodyDataAttributesProperties string
	var bodyDataAttributesPublicDescription string
	var bodyDataAttributesServiceIds string
	var bodyDataAttributesServiceNowCiSysId string
	var bodyDataAttributesShowUptime bool
	var bodyDataAttributesShowUptimeLastDays int
	var bodyDataAttributesSlackAliases string
	var bodyDataAttributesSlackChannels string
	var bodyDataType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-functionality",
		Aliases:     []string{"create"},
		Short:       "Creates a new functionality from provided data",
		Example:     "  rootly-cli functionalities create-functionality",
		Annotations: map[string]string{"pp:endpoint": "functionalities.create-functionality", "pp:method": "POST", "pp:path": "/v1/functionalities"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("data-attributes-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-name")
				}
				if !cmd.Flags().Changed("data-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/functionalities"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedData := map[string]any{}
					{
						nestedDataAttributes := map[string]any{}
						if bodyDataAttributesBackstageId != "" {
							nestedDataAttributes["backstage_id"] = bodyDataAttributesBackstageId
						}
						if bodyDataAttributesColor != "" {
							nestedDataAttributes["color"] = bodyDataAttributesColor
						}
						if bodyDataAttributesCortexId != "" {
							nestedDataAttributes["cortex_id"] = bodyDataAttributesCortexId
						}
						if bodyDataAttributesDescription != "" {
							nestedDataAttributes["description"] = bodyDataAttributesDescription
						}
						if bodyDataAttributesEnvironmentIds != "" {
							nestedDataAttributes["environment_ids"] = cliutil.SplitCSV(bodyDataAttributesEnvironmentIds)
						}
						if bodyDataAttributesExternalId != "" {
							nestedDataAttributes["external_id"] = bodyDataAttributesExternalId
						}
						if bodyDataAttributesName != "" {
							nestedDataAttributes["name"] = bodyDataAttributesName
						}
						if bodyDataAttributesNotifyEmails != "" {
							nestedDataAttributes["notify_emails"] = cliutil.SplitCSV(bodyDataAttributesNotifyEmails)
						}
						if bodyDataAttributesOpsgenieId != "" {
							nestedDataAttributes["opsgenie_id"] = bodyDataAttributesOpsgenieId
						}
						if bodyDataAttributesOpsgenieTeamId != "" {
							nestedDataAttributes["opsgenie_team_id"] = bodyDataAttributesOpsgenieTeamId
						}
						if bodyDataAttributesOwnerGroupIds != "" {
							nestedDataAttributes["owner_group_ids"] = cliutil.SplitCSV(bodyDataAttributesOwnerGroupIds)
						}
						if bodyDataAttributesOwnerUserIds != "" {
							var parsedDataAttributesOwnerUserIds any
							if err := json.Unmarshal([]byte(bodyDataAttributesOwnerUserIds), &parsedDataAttributesOwnerUserIds); err != nil {
								return fmt.Errorf("parsing --data-attributes-owner-user-ids JSON: %w", err)
							}
							nestedDataAttributes["owner_user_ids"] = parsedDataAttributesOwnerUserIds
						}
						if bodyDataAttributesPagerdutyId != "" {
							nestedDataAttributes["pagerduty_id"] = bodyDataAttributesPagerdutyId
						}
						if bodyDataAttributesPosition != 0 {
							nestedDataAttributes["position"] = bodyDataAttributesPosition
						}
						if bodyDataAttributesProperties != "" {
							var parsedDataAttributesProperties any
							if err := json.Unmarshal([]byte(bodyDataAttributesProperties), &parsedDataAttributesProperties); err != nil {
								return fmt.Errorf("parsing --data-attributes-properties JSON: %w", err)
							}
							nestedDataAttributes["properties"] = parsedDataAttributesProperties
						}
						if bodyDataAttributesPublicDescription != "" {
							nestedDataAttributes["public_description"] = bodyDataAttributesPublicDescription
						}
						if bodyDataAttributesServiceIds != "" {
							nestedDataAttributes["service_ids"] = cliutil.SplitCSV(bodyDataAttributesServiceIds)
						}
						if bodyDataAttributesServiceNowCiSysId != "" {
							nestedDataAttributes["service_now_ci_sys_id"] = bodyDataAttributesServiceNowCiSysId
						}
						if cmd.Flags().Changed("data-attributes-show-uptime") {
							nestedDataAttributes["show_uptime"] = bodyDataAttributesShowUptime
						}
						if bodyDataAttributesShowUptimeLastDays != 0 {
							nestedDataAttributes["show_uptime_last_days"] = bodyDataAttributesShowUptimeLastDays
						}
						if bodyDataAttributesSlackAliases != "" {
							var parsedDataAttributesSlackAliases any
							if err := json.Unmarshal([]byte(bodyDataAttributesSlackAliases), &parsedDataAttributesSlackAliases); err != nil {
								return fmt.Errorf("parsing --data-attributes-slack-aliases JSON: %w", err)
							}
							nestedDataAttributes["slack_aliases"] = parsedDataAttributesSlackAliases
						}
						if bodyDataAttributesSlackChannels != "" {
							var parsedDataAttributesSlackChannels any
							if err := json.Unmarshal([]byte(bodyDataAttributesSlackChannels), &parsedDataAttributesSlackChannels); err != nil {
								return fmt.Errorf("parsing --data-attributes-slack-channels JSON: %w", err)
							}
							nestedDataAttributes["slack_channels"] = parsedDataAttributesSlackChannels
						}
						if len(nestedDataAttributes) > 0 {
							nestedData["attributes"] = nestedDataAttributes
						}
					}
					if bodyDataType != "" {
						nestedData["type"] = bodyDataType
					}
					if len(nestedData) > 0 {
						body["data"] = nestedData
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "functionalities", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "functionalities", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "functionalities", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "functionalities", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "functionalities", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "functionalities",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "functionalities", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "functionalities", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDataAttributesBackstageId, "data-attributes-backstage-id", "", "The Backstage entity id associated to this functionality. eg: :namespace/:kind/:entity_name")
	cmd.Flags().StringVar(&bodyDataAttributesColor, "data-attributes-color", "", "The hex color of the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesCortexId, "data-attributes-cortex-id", "", "The Cortex group id associated to this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesDescription, "data-attributes-description", "", "The description of the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesEnvironmentIds, "data-attributes-environment-ids", "", "Environments associated with this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesExternalId, "data-attributes-external-id", "", "The external id associated to this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesName, "data-attributes-name", "", "The name of the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesNotifyEmails, "data-attributes-notify-emails", "", "Emails to attach to the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesOpsgenieId, "data-attributes-opsgenie-id", "", "The Opsgenie service id associated to this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesOpsgenieTeamId, "data-attributes-opsgenie-team-id", "", "The Opsgenie team id associated to this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesOwnerGroupIds, "data-attributes-owner-group-ids", "", "Owner Teams associated with this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesOwnerUserIds, "data-attributes-owner-user-ids", "", "Owner Users associated with this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesPagerdutyId, "data-attributes-pagerduty-id", "", "The PagerDuty service id associated to this functionality")
	cmd.Flags().IntVar(&bodyDataAttributesPosition, "data-attributes-position", 0, "Position of the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesProperties, "data-attributes-properties", "", "Array of property values for this functionality.")
	cmd.Flags().StringVar(&bodyDataAttributesPublicDescription, "data-attributes-public-description", "", "The public description of the functionality")
	cmd.Flags().StringVar(&bodyDataAttributesServiceIds, "data-attributes-service-ids", "", "Services associated with this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesServiceNowCiSysId, "data-attributes-service-now-ci-sys-id", "", "The Service Now CI sys id associated to this functionality")
	cmd.Flags().BoolVar(&bodyDataAttributesShowUptime, "data-attributes-show-uptime", false, "Show uptime")
	cmd.Flags().IntVar(&bodyDataAttributesShowUptimeLastDays, "data-attributes-show-uptime-last-days", 0, "Show uptime over x days")
	cmd.Flags().StringVar(&bodyDataAttributesSlackAliases, "data-attributes-slack-aliases", "", "Slack Aliases associated with this functionality")
	cmd.Flags().StringVar(&bodyDataAttributesSlackChannels, "data-attributes-slack-channels", "", "Slack Channels associated with this functionality")
	cmd.Flags().StringVar(&bodyDataType, "data-type", "", "Type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
