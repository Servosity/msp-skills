// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLiveCallRoutersCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyDataAttributesAlertUrgencyId string
	var bodyDataAttributesCallerGreeting string
	var bodyDataAttributesCallingTreeEnabled bool
	var bodyDataAttributesCallingTreePrompt string
	var bodyDataAttributesCountryCode string
	var bodyDataAttributesEnabled bool
	var bodyDataAttributesEscalationLevelDelayInSeconds int
	var bodyDataAttributesKind string
	var bodyDataAttributesName string
	var bodyDataAttributesPagingTargets string
	var bodyDataAttributesPhoneNumber string
	var bodyDataAttributesPhoneType string
	var bodyDataAttributesSentToVoicemailDelay int
	var bodyDataAttributesShouldAutoResolveAlertOnCallEnd bool
	var bodyDataAttributesShouldRedirectToVoicemailOnNoAnswer bool
	var bodyDataAttributesVoicemailGreeting string
	var bodyDataAttributesWaitingMusicUrl string
	var bodyDataType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Creates a new Live Call Router from provided data",
		Example:     "  rootly-cli live-call-routers create",
		Annotations: map[string]string{"pp:endpoint": "live-call-routers.create", "pp:method": "POST", "pp:path": "/v1/live_call_routers"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("data-attributes-country-code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-country-code")
				}
				if !cmd.Flags().Changed("data-attributes-kind") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-kind")
				}
				if !cmd.Flags().Changed("data-attributes-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-name")
				}
				if !cmd.Flags().Changed("data-attributes-paging-targets") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-paging-targets")
				}
				if !cmd.Flags().Changed("data-attributes-phone-number") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-phone-number")
				}
				if !cmd.Flags().Changed("data-attributes-phone-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-phone-type")
				}
				if !cmd.Flags().Changed("data-attributes-voicemail-greeting") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-attributes-voicemail-greeting")
				}
				if !cmd.Flags().Changed("data-type") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "data-type")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/live_call_routers"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedData := map[string]any{}
					{
						nestedDataAttributes := map[string]any{}
						if bodyDataAttributesAlertUrgencyId != "" {
							nestedDataAttributes["alert_urgency_id"] = bodyDataAttributesAlertUrgencyId
						}
						if bodyDataAttributesCallerGreeting != "" {
							nestedDataAttributes["caller_greeting"] = bodyDataAttributesCallerGreeting
						}
						if cmd.Flags().Changed("data-attributes-calling-tree-enabled") {
							nestedDataAttributes["calling_tree_enabled"] = bodyDataAttributesCallingTreeEnabled
						}
						if bodyDataAttributesCallingTreePrompt != "" {
							nestedDataAttributes["calling_tree_prompt"] = bodyDataAttributesCallingTreePrompt
						}
						if bodyDataAttributesCountryCode != "" {
							nestedDataAttributes["country_code"] = bodyDataAttributesCountryCode
						}
						if cmd.Flags().Changed("data-attributes-enabled") {
							nestedDataAttributes["enabled"] = bodyDataAttributesEnabled
						}
						if bodyDataAttributesEscalationLevelDelayInSeconds != 0 {
							nestedDataAttributes["escalation_level_delay_in_seconds"] = bodyDataAttributesEscalationLevelDelayInSeconds
						}
						if bodyDataAttributesKind != "" {
							nestedDataAttributes["kind"] = bodyDataAttributesKind
						}
						if bodyDataAttributesName != "" {
							nestedDataAttributes["name"] = bodyDataAttributesName
						}
						if bodyDataAttributesPagingTargets != "" {
							var parsedDataAttributesPagingTargets any
							if err := json.Unmarshal([]byte(bodyDataAttributesPagingTargets), &parsedDataAttributesPagingTargets); err != nil {
								return fmt.Errorf("parsing --data-attributes-paging-targets JSON: %w", err)
							}
							nestedDataAttributes["paging_targets"] = parsedDataAttributesPagingTargets
						}
						if bodyDataAttributesPhoneNumber != "" {
							nestedDataAttributes["phone_number"] = bodyDataAttributesPhoneNumber
						}
						if bodyDataAttributesPhoneType != "" {
							nestedDataAttributes["phone_type"] = bodyDataAttributesPhoneType
						}
						if bodyDataAttributesSentToVoicemailDelay != 0 {
							nestedDataAttributes["sent_to_voicemail_delay"] = bodyDataAttributesSentToVoicemailDelay
						}
						if cmd.Flags().Changed("data-attributes-should-auto-resolve-alert-on-call-end") {
							nestedDataAttributes["should_auto_resolve_alert_on_call_end"] = bodyDataAttributesShouldAutoResolveAlertOnCallEnd
						}
						if cmd.Flags().Changed("data-attributes-should-redirect-to-voicemail-on-no-answer") {
							nestedDataAttributes["should_redirect_to_voicemail_on_no_answer"] = bodyDataAttributesShouldRedirectToVoicemailOnNoAnswer
						}
						if bodyDataAttributesVoicemailGreeting != "" {
							nestedDataAttributes["voicemail_greeting"] = bodyDataAttributesVoicemailGreeting
						}
						if bodyDataAttributesWaitingMusicUrl != "" {
							nestedDataAttributes["waiting_music_url"] = bodyDataAttributesWaitingMusicUrl
						}
						if len(nestedDataAttributes) > 0 {
							nestedData["attributes"] = nestedDataAttributes
						}
					}
					if bodyDataType != "" {
						nestedData["type"] = bodyDataType
					}
					if len(nestedData) > 0 {
						body["data"] = nestedData
					}
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "live-call-routers", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "live-call-routers", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "live-call-routers", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "live-call-routers", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "live-call-routers", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "live-call-routers",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "live-call-routers", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "live-call-routers", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyDataAttributesAlertUrgencyId, "data-attributes-alert-urgency-id", "", "This is used in escalation paths to determine who to page")
	cmd.Flags().StringVar(&bodyDataAttributesCallerGreeting, "data-attributes-caller-greeting", "", "The caller greeting message of the live_call_router")
	cmd.Flags().BoolVar(&bodyDataAttributesCallingTreeEnabled, "data-attributes-calling-tree-enabled", false, "Whether the live call router is configured as a phone tree, requiring callers to press a key before being connected")
	cmd.Flags().StringVar(&bodyDataAttributesCallingTreePrompt, "data-attributes-calling-tree-prompt", "", "The audio instructions callers will hear when they call this number")
	cmd.Flags().StringVar(&bodyDataAttributesCountryCode, "data-attributes-country-code", "", "The country code of the live_call_router")
	cmd.Flags().BoolVar(&bodyDataAttributesEnabled, "data-attributes-enabled", false, "Whether the live_call_router is enabled")
	cmd.Flags().IntVar(&bodyDataAttributesEscalationLevelDelayInSeconds, "data-attributes-escalation-level-delay-in-seconds", 0, "This overrides the delay (seconds) in escalation levels")
	cmd.Flags().StringVar(&bodyDataAttributesKind, "data-attributes-kind", "", "The kind of the live_call_router")
	cmd.Flags().StringVar(&bodyDataAttributesName, "data-attributes-name", "", "The name of the live_call_router")
	cmd.Flags().StringVar(&bodyDataAttributesPagingTargets, "data-attributes-paging-targets", "", "Paging targets that callers can select from when this live call router is configured as a phone tree.")
	cmd.Flags().StringVar(&bodyDataAttributesPhoneNumber, "data-attributes-phone-number", "", "You can select a phone number using [generate_phone_number](#//api/v1/live_call_routers/generate_phone_number)")
	cmd.Flags().StringVar(&bodyDataAttributesPhoneType, "data-attributes-phone-type", "", "The phone type of the live_call_router")
	cmd.Flags().IntVar(&bodyDataAttributesSentToVoicemailDelay, "data-attributes-sent-to-voicemail-delay", 0, "The delay (seconds) after which the caller in redirected to voicemail")
	cmd.Flags().BoolVar(&bodyDataAttributesShouldAutoResolveAlertOnCallEnd, "data-attributes-should-auto-resolve-alert-on-call-end", false, "This overrides the delay (seconds) in escalation levels")
	cmd.Flags().BoolVar(&bodyDataAttributesShouldRedirectToVoicemailOnNoAnswer, "data-attributes-should-redirect-to-voicemail-on-no-answer", false, "This prompts the caller to choose voicemail or connect live")
	cmd.Flags().StringVar(&bodyDataAttributesVoicemailGreeting, "data-attributes-voicemail-greeting", "", "The voicemail greeting of the live_call_router")
	cmd.Flags().StringVar(&bodyDataAttributesWaitingMusicUrl, "data-attributes-waiting-music-url", "", "The waiting music URL of the live_call_router")
	cmd.Flags().StringVar(&bodyDataType, "data-type", "", "Type")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
