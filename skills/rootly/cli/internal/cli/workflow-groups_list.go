// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newWorkflowGroupsListCmd(flags *rootFlags) *cobra.Command {
	var flagInclude string
	var flagPageNumber int
	var flagPageSize int
	var flagFilterSearch string
	var flagFilterName string
	var flagFilterSlug string
	var flagFilterKind string
	var flagFilterExpanded bool
	var flagFilterPosition int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "List workflow groups",
		Example:     "  rootly-cli workflow-groups list",
		Annotations: map[string]string{"pp:endpoint": "workflow-groups.list", "pp:method": "GET", "pp:path": "/v1/workflow_groups", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/workflow_groups"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "workflow-groups", path, map[string]string{
				"include":          formatCLIParamValue(flagInclude),
				"page[number]":     formatCLIParamValue(flagPageNumber),
				"page[size]":       formatCLIParamValue(flagPageSize),
				"filter[search]":   formatCLIParamValue(flagFilterSearch),
				"filter[name]":     formatCLIParamValue(flagFilterName),
				"filter[slug]":     formatCLIParamValue(flagFilterSlug),
				"filter[kind]":     formatCLIParamValue(flagFilterKind),
				"filter[expanded]": formatCLIParamValue(flagFilterExpanded),
				"filter[position]": formatCLIParamValue(flagFilterPosition),
			}, nil, flagAll, "page[number]", "page", "page[size]", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagInclude, "include", "", "Include")
	cmd.Flags().IntVar(&flagPageNumber, "page-number", 0, "Page number")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "Page size")
	cmd.Flags().StringVar(&flagFilterSearch, "filter-search", "", "Filter search")
	cmd.Flags().StringVar(&flagFilterName, "filter-name", "", "Filter name")
	cmd.Flags().StringVar(&flagFilterSlug, "filter-slug", "", "Filter slug")
	cmd.Flags().StringVar(&flagFilterKind, "filter-kind", "", "Filter kind")
	cmd.Flags().BoolVar(&flagFilterExpanded, "filter-expanded", false, "Filter expanded")
	cmd.Flags().IntVar(&flagFilterPosition, "filter-position", 0, "Filter position")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
