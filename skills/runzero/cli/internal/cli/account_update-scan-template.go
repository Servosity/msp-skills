// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newAccountUpdateScanTemplateCmd(flags *rootFlags) *cobra.Command {
	var bodyAcl string
	var bodyAgentId string
	var bodyClientId string
	var bodyCreatedAt int
	var bodyCreatedBy string
	var bodyCreatedByUserId string
	var bodyCruncherId string
	var bodyCustomIntegrationId string
	var bodyDescription string
	var bodyError string
	var bodyGlobal string
	var bodyGracePeriod string
	var bodyHidden bool
	var bodyHostedZoneId string
	var bodyId string
	var bodyLinkedTaskCount int
	var bodyName string
	var bodyOrganizationId string
	var bodyParams string
	var bodyParentId string
	var bodyRecur bool
	var bodyRecurFrequency string
	var bodyRecurLast int
	var bodyRecurLastTaskId string
	var bodyRecurNext int
	var bodySiteId string
	var bodySizeData int
	var bodySizeResults int
	var bodySizeSite int
	var bodySourceId string
	var bodyStartTime int
	var bodyStats string
	var bodyStatus string
	var bodyTemplateId string
	var bodyType string
	var bodyUpdatedAt int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update-scan-template",
		Short:       "Update scan template",
		Example:     "  runzero-cli account update-scan-template --id e77602e0-3fb8-4734-aef9-fbc6fdcb0fa8",
		Annotations: map[string]string{"pp:endpoint": "account.update-scan-template", "pp:method": "PUT", "pp:path": "/account/tasks/templates"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("acl") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "acl")
				}
				if !cmd.Flags().Changed("global") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "global")
				}
				if !cmd.Flags().Changed("id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "id")
				}
				if !cmd.Flags().Changed("organization-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "organization-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/account/tasks/templates"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAcl != "" {
					var parsedAcl any
					if err := json.Unmarshal([]byte(bodyAcl), &parsedAcl); err != nil {
						return fmt.Errorf("parsing --acl JSON: %w", err)
					}
					body["acl"] = parsedAcl
				}
				if bodyAgentId != "" {
					body["agent_id"] = bodyAgentId
				}
				if bodyClientId != "" {
					body["client_id"] = bodyClientId
				}
				if bodyCreatedAt != 0 {
					body["created_at"] = bodyCreatedAt
				}
				if bodyCreatedBy != "" {
					body["created_by"] = bodyCreatedBy
				}
				if bodyCreatedByUserId != "" {
					body["created_by_user_id"] = bodyCreatedByUserId
				}
				if bodyCruncherId != "" {
					body["cruncher_id"] = bodyCruncherId
				}
				if bodyCustomIntegrationId != "" {
					body["custom_integration_id"] = bodyCustomIntegrationId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyError != "" {
					body["error"] = bodyError
				}
				if bodyGlobal != "" {
					parsedGlobal, err := strconv.ParseBool(bodyGlobal)
					if err != nil {
						return fmt.Errorf("parsing --global as bool: %w", err)
					}
					body["global"] = parsedGlobal
				}
				if bodyGracePeriod != "" {
					body["grace_period"] = bodyGracePeriod
				}
				if cmd.Flags().Changed("hidden") {
					body["hidden"] = bodyHidden
				}
				if bodyHostedZoneId != "" {
					body["hosted_zone_id"] = bodyHostedZoneId
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyLinkedTaskCount != 0 {
					body["linked_task_count"] = bodyLinkedTaskCount
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyOrganizationId != "" {
					body["organization_id"] = bodyOrganizationId
				}
				if bodyParams != "" {
					var parsedParams any
					if err := json.Unmarshal([]byte(bodyParams), &parsedParams); err != nil {
						return fmt.Errorf("parsing --params JSON: %w", err)
					}
					body["params"] = parsedParams
				}
				if bodyParentId != "" {
					body["parent_id"] = bodyParentId
				}
				if cmd.Flags().Changed("recur") {
					body["recur"] = bodyRecur
				}
				if bodyRecurFrequency != "" {
					body["recur_frequency"] = bodyRecurFrequency
				}
				if bodyRecurLast != 0 {
					body["recur_last"] = bodyRecurLast
				}
				if bodyRecurLastTaskId != "" {
					body["recur_last_task_id"] = bodyRecurLastTaskId
				}
				if bodyRecurNext != 0 {
					body["recur_next"] = bodyRecurNext
				}
				if bodySiteId != "" {
					body["site_id"] = bodySiteId
				}
				if bodySizeData != 0 {
					body["size_data"] = bodySizeData
				}
				if bodySizeResults != 0 {
					body["size_results"] = bodySizeResults
				}
				if bodySizeSite != 0 {
					body["size_site"] = bodySizeSite
				}
				if bodySourceId != "" {
					body["source_id"] = bodySourceId
				}
				if bodyStartTime != 0 {
					body["start_time"] = bodyStartTime
				}
				if bodyStats != "" {
					var parsedStats any
					if err := json.Unmarshal([]byte(bodyStats), &parsedStats); err != nil {
						return fmt.Errorf("parsing --stats JSON: %w", err)
					}
					body["stats"] = parsedStats
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
				if bodyTemplateId != "" {
					body["template_id"] = bodyTemplateId
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
				if bodyUpdatedAt != 0 {
					body["updated_at"] = bodyUpdatedAt
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "account", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "account", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "account", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "account", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "account", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "account",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "account", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "account", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAcl, "acl", "", "A map of IDs to strings which describe how the template may be accessed.")
	cmd.Flags().StringVar(&bodyAgentId, "agent-id", "", "ID of the explorer which may execute the template.")
	cmd.Flags().StringVar(&bodyClientId, "client-id", "", "ID of the account which owns the template.")
	cmd.Flags().IntVar(&bodyCreatedAt, "created-at", 0, "Unix timestamp value indicating when the template was created.")
	cmd.Flags().StringVar(&bodyCreatedBy, "created-by", "", "The username of the account which created the template.")
	cmd.Flags().StringVar(&bodyCreatedByUserId, "created-by-user-id", "", "The ID of the account which created the template.")
	cmd.Flags().StringVar(&bodyCruncherId, "cruncher-id", "", "ID of the runZero cruncher the task is executing on.")
	cmd.Flags().StringVar(&bodyCustomIntegrationId, "custom-integration-id", "", "The ID of the custom integration source, if the last task executed with this template was an import of Asset Data.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "The description of the template.")
	cmd.Flags().StringVar(&bodyError, "error", "", "The error message, if any, of the last task using the template.")
	cmd.Flags().StringVar(&bodyGlobal, "global", "", "Whether the template is globally available to all organizations.")
	cmd.Flags().StringVar(&bodyGracePeriod, "grace-period", "", "Additional time beyond hard expiration deadline by which the task may still be allowed to execute.")
	cmd.Flags().BoolVar(&bodyHidden, "hidden", false, "A flag indicating whether the item is hidden from common view.")
	cmd.Flags().StringVar(&bodyHostedZoneId, "hosted-zone-id", "", "The ID of the hosted zone that ran the last task the template was used with.")
	cmd.Flags().StringVar(&bodyId, "id", "", "ID of the template.")
	cmd.Flags().IntVar(&bodyLinkedTaskCount, "linked-task-count", 0, "The number of tasks derived from the template.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the template.")
	cmd.Flags().StringVar(&bodyOrganizationId, "organization-id", "", "ID of the organization the template is available in.")
	cmd.Flags().StringVar(&bodyParams, "params", "", "A number of task parameter values. Currently there is no authoritative list of in-use values.")
	cmd.Flags().StringVar(&bodyParentId, "parent-id", "", "The ID of the parent entity of the task scheduled.")
	cmd.Flags().BoolVar(&bodyRecur, "recur", false, "A flag representing whether derived tasks are scheduled to repeat.")
	cmd.Flags().StringVar(&bodyRecurFrequency, "recur-frequency", "", "A string time duration value representing execution frequency, if scheduled to repeat.")
	cmd.Flags().IntVar(&bodyRecurLast, "recur-last", 0, "Unix timestamp representing the last execution if scheduled to repeat.")
	cmd.Flags().StringVar(&bodyRecurLastTaskId, "recur-last-task-id", "", "The ID of the task that last executed if scheduled to repeat.")
	cmd.Flags().IntVar(&bodyRecurNext, "recur-next", 0, "Unix timestamp representing the next execution if scheduled to repeat.")
	cmd.Flags().StringVar(&bodySiteId, "site-id", "", "ID of the site the template is being used in.")
	cmd.Flags().IntVar(&bodySizeData, "size-data", 0, "The total size of result data of the last task the template was used with.")
	cmd.Flags().IntVar(&bodySizeResults, "size-results", 0, "The number of results in the last task the template was used with.")
	cmd.Flags().IntVar(&bodySizeSite, "size-site", 0, "The size in assets of the site the last task the template was executed against.")
	cmd.Flags().StringVar(&bodySourceId, "source-id", "", "The numeric ID of the data source")
	cmd.Flags().IntVar(&bodyStartTime, "start-time", 0, "Unix timestamp representing the next execution time.")
	cmd.Flags().StringVar(&bodyStats, "stats", "", "A map of statistics about the last task executed with the template.")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "The status of the last task using the template.")
	cmd.Flags().StringVar(&bodyTemplateId, "template-id", "", "The ID of the template.")
	cmd.Flags().StringVar(&bodyType, "type", "", "The type of task the template creates.")
	cmd.Flags().IntVar(&bodyUpdatedAt, "updated-at", 0, "Unix timestamp value indicating when the template was last modified.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
