// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRunzeroExportCertificatesJsonlCmd(flags *rootFlags) *cobra.Command {
	var flagOid string
	var flagSearch string

	cmd := &cobra.Command{
		Use:         "certificates-jsonl",
		Short:       "Export the certificate inventory as JSONL line-delimited",
		Example:     "  runzero-cli runzero-export certificates-jsonl",
		Annotations: map[string]string{"pp:endpoint": "runzero-export.certificates-jsonl", "pp:method": "GET", "pp:path": "/export/org/certificates.jsonl", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/export/org/certificates.jsonl"
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagOid != "" {
				params["_oid"] = formatCLIParamValue(flagOid)
			}
			if flagSearch != "" {
				params["search"] = formatCLIParamValue(flagSearch)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "runzero-export", false, path, params, headerOverrides, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagOid, "oid", "", "The current Organization")
	cmd.Flags().StringVar(&flagSearch, "search", "", "A search query in runZero search query syntax")

	return cmd
}
