// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRunzeroExportSnowServicesCsvCmd(flags *rootFlags) *cobra.Command {
	var flagOid string

	cmd := &cobra.Command{
		Use:         "snow-services-csv",
		Short:       "Export a service inventory as CSV for ServiceNow integration",
		Example:     "  runzero-cli runzero-export snow-services-csv",
		Annotations: map[string]string{"pp:endpoint": "runzero-export.snow-services-csv", "pp:method": "GET", "pp:path": "/export/org/services.servicenow.csv", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/export/org/services.servicenow.csv"
			headerOverrides := map[string]string{
				"X-Printing-Press-Binary-Response": "true",
			}
			params := map[string]string{}
			if flagOid != "" {
				params["_oid"] = formatCLIParamValue(flagOid)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "runzero-export", false, path, params, headerOverrides, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagOid, "oid", "", "The current Organization")

	return cmd
}
