// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSingularityMarketplaceGetMarketplaceApplicationsCmd(flags *rootFlags) *cobra.Command {
	var flagSkip string
	var flagSkipCount string
	var flagAccountIds string
	var flagSiteIds string
	var flagLimit string
	var flagGroupIds string
	var flagCursor string
	var flagCreatorContains string
	var flagDisablePagination string
	var flagId string
	var flagApplicationCatalogId string
	var flagSortOrder string
	var flagQuery string
	var flagSortBy string
	var flagCountOnly string
	var flagNameContains string
	var flagScopes string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-marketplace-applications",
		Short:       "Get the installed Marketplace applications for a scope specified.",
		Example:     "  sentinelone-cli singularity-marketplace get-marketplace-applications",
		Annotations: map[string]string{"pp:endpoint": "singularity-marketplace.get-marketplace-applications", "pp:method": "GET", "pp:path": "/singularity-marketplace/applications", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/singularity-marketplace/applications"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "singularity-marketplace", path, map[string]string{
				"skip":                   formatCLIParamValue(flagSkip),
				"skipCount":              formatCLIParamValue(flagSkipCount),
				"accountIds":             formatCLIParamValue(flagAccountIds),
				"siteIds":                formatCLIParamValue(flagSiteIds),
				"limit":                  formatCLIParamValue(flagLimit),
				"groupIds":               formatCLIParamValue(flagGroupIds),
				"cursor":                 formatCLIParamValue(flagCursor),
				"creator__contains":      formatCLIParamValue(flagCreatorContains),
				"disablePagination":      formatCLIParamValue(flagDisablePagination),
				"id":                     formatCLIParamValue(flagId),
				"application_catalog_id": formatCLIParamValue(flagApplicationCatalogId),
				"sortOrder":              formatCLIParamValue(flagSortOrder),
				"query":                  formatCLIParamValue(flagQuery),
				"sortBy":                 formatCLIParamValue(flagSortBy),
				"countOnly":              formatCLIParamValue(flagCountOnly),
				"name__contains":         formatCLIParamValue(flagNameContains),
				"scopes":                 formatCLIParamValue(flagScopes),
			}, nil, flagAll, "cursor", "cursor", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagSkip, "skip", "", "Skip first number of items (0-1000). For iterating over more than a 1000 items please use 'cursor' instead.")
	cmd.Flags().StringVar(&flagSkipCount, "skip-count", "", "If true, total number of items will not be calculated, which speeds up execution time")
	cmd.Flags().StringVar(&flagAccountIds, "account-ids", "", "List of Account IDs to filter by. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagSiteIds, "site-ids", "", "List of Site IDs to filter by. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limit number of returned items (1-1000). Example: '10'.")
	cmd.Flags().StringVar(&flagGroupIds, "group-ids", "", "List of Group IDs to filter by. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Cursor position returned by the last request. Should be used for iterating over more than 1000 items.")
	cmd.Flags().StringVar(&flagCreatorContains, "creator-contains", "", "Free-text filter by application creator (supports multiple values). Example: 'Service Pack 1'.")
	cmd.Flags().StringVar(&flagDisablePagination, "disable-pagination", "", "If true, all installed applications for requested scope will be returned")
	cmd.Flags().StringVar(&flagId, "id", "", "A list of applications IDs. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagApplicationCatalogId, "application-catalog-id", "", "A list of catalog applications IDs. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Sort direction. Example: 'asc'.")
	cmd.Flags().StringVar(&flagQuery, "query", "", "Free-text filter by S1 query (supports multiple values). Example: 'Service Pack 1'.")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "", "The column to sort the results by. Example: 'id'.")
	cmd.Flags().StringVar(&flagCountOnly, "count-only", "", "If true, only total number of items will be returned, without any of the actual objects.")
	cmd.Flags().StringVar(&flagNameContains, "name-contains", "", "Free-text filter by application name (supports multiple values). Example: 'Service Pack 1'.")
	cmd.Flags().StringVar(&flagScopes, "scopes", "", "Filter results by scope. Example: 'global'.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
