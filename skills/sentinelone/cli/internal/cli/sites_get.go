// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSitesGetCmd(flags *rootFlags) *cobra.Command {
	var flagName string
	var flagStates string
	var flagFeatures string
	var flagRegistrationToken string
	var flagSkip string
	var flagSortOrder string
	var flagSiteType string
	var flagHealthStatus string
	var flagActiveLicenses string
	var flagExternalId string
	var flagAdminOnly string
	var flagTotalLicenses string
	var flagUpdatedAt string
	var flagAccountId string
	var flagSiteIds string
	var flagSortBy string
	var flagLimit string
	var flagAvailableMoveSites string
	var flagQuery string
	var flagIsDefault string
	var flagAccountIds string
	var flagCursor string
	var flagExpiration string
	var flagState string
	var flagSuite string
	var flagSkipCount string
	var flagCountOnly string
	var flagCreatedAt string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get",
		Aliases:     []string{"list"},
		Short:       "Get the Sites that match the filters. The response includes the IDs of Sites, which you can use in other commands.",
		Example:     "  sentinelone-cli sites get",
		Annotations: map[string]string{"pp:endpoint": "sites.get", "pp:method": "GET", "pp:path": "/sites", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/sites"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "sites", path, map[string]string{
				"name":               formatCLIParamValue(flagName),
				"states":             formatCLIParamValue(flagStates),
				"features":           formatCLIParamValue(flagFeatures),
				"registrationToken":  formatCLIParamValue(flagRegistrationToken),
				"skip":               formatCLIParamValue(flagSkip),
				"sortOrder":          formatCLIParamValue(flagSortOrder),
				"siteType":           formatCLIParamValue(flagSiteType),
				"healthStatus":       formatCLIParamValue(flagHealthStatus),
				"activeLicenses":     formatCLIParamValue(flagActiveLicenses),
				"externalId":         formatCLIParamValue(flagExternalId),
				"adminOnly":          formatCLIParamValue(flagAdminOnly),
				"totalLicenses":      formatCLIParamValue(flagTotalLicenses),
				"updatedAt":          formatCLIParamValue(flagUpdatedAt),
				"accountId":          formatCLIParamValue(flagAccountId),
				"siteIds":            formatCLIParamValue(flagSiteIds),
				"sortBy":             formatCLIParamValue(flagSortBy),
				"limit":              formatCLIParamValue(flagLimit),
				"availableMoveSites": formatCLIParamValue(flagAvailableMoveSites),
				"query":              formatCLIParamValue(flagQuery),
				"isDefault":          formatCLIParamValue(flagIsDefault),
				"accountIds":         formatCLIParamValue(flagAccountIds),
				"cursor":             formatCLIParamValue(flagCursor),
				"expiration":         formatCLIParamValue(flagExpiration),
				"state":              formatCLIParamValue(flagState),
				"suite":              formatCLIParamValue(flagSuite),
				"skipCount":          formatCLIParamValue(flagSkipCount),
				"countOnly":          formatCLIParamValue(flagCountOnly),
				"createdAt":          formatCLIParamValue(flagCreatedAt),
			}, nil, flagAll, "cursor", "cursor", "limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagName, "name", "", "Name. Example: 'My Site'.")
	cmd.Flags().StringVar(&flagStates, "states", "", "List of states to filter")
	cmd.Flags().StringVar(&flagFeatures, "features", "", "If sent return only sites that support this features. Example: 'firewall-control'.")
	cmd.Flags().StringVar(&flagRegistrationToken, "registration-token", "", "Registration token.")
	cmd.Flags().StringVar(&flagSkip, "skip", "", "Skip first number of items (0-1000). To iterate over more than 1000 items, use 'cursor'. Example: '150'.")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Sort direction. Example: 'asc'.")
	cmd.Flags().StringVar(&flagSiteType, "site-type", "", "Site type. Example: 'Trial'.")
	cmd.Flags().StringVar(&flagHealthStatus, "health-status", "", "Health status")
	cmd.Flags().StringVar(&flagActiveLicenses, "active-licenses", "", "Active licenses")
	cmd.Flags().StringVar(&flagExternalId, "external-id", "", "Id in a CRM external system")
	cmd.Flags().StringVar(&flagAdminOnly, "admin-only", "", "Show sites the user has Admin privileges to")
	cmd.Flags().StringVar(&flagTotalLicenses, "total-licenses", "", "Total licenses")
	cmd.Flags().StringVar(&flagUpdatedAt, "updated-at", "", "Timestamp of last update. Example: '2018-02-27T04:49:26.257525Z'.")
	cmd.Flags().StringVar(&flagAccountId, "account-id", "", "Account id. Example: '225494730938493804'.")
	cmd.Flags().StringVar(&flagSiteIds, "site-ids", "", "List of site id's to search for. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagSortBy, "sort-by", "", "The column to sort the results by. Example: 'id'.")
	cmd.Flags().StringVar(&flagLimit, "limit", "", "Limit number of returned items (1-1000). Example: '10'.")
	cmd.Flags().StringVar(&flagAvailableMoveSites, "available-move-sites", "", "Only return sites the user can move agents to")
	cmd.Flags().StringVar(&flagQuery, "query", "", "Full text search for fields: name, account_name. (Note: on single-account consoles account name will not be matched)")
	cmd.Flags().StringVar(&flagIsDefault, "is-default", "", "Is default")
	cmd.Flags().StringVar(&flagAccountIds, "account-ids", "", "List of site id's to search for. Example: '225494730938493804,225494730938493915'.")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Cursor position returned by the last request. Use to iterate over more than 1000 items.")
	cmd.Flags().StringVar(&flagExpiration, "expiration", "", "Expiration. Example: '2018-02-27T04:49:26.257525Z'.")
	cmd.Flags().StringVar(&flagState, "state", "", "Site state. Example: 'active'.")
	cmd.Flags().StringVar(&flagSuite, "suite", "", "The suite of product features active for this site. Example: 'Core'.")
	cmd.Flags().StringVar(&flagSkipCount, "skip-count", "", "If true, total number of items will not be calculated, which speeds up execution time.")
	cmd.Flags().StringVar(&flagCountOnly, "count-only", "", "If true, only total number of items will be returned, without any of the actual objects.")
	cmd.Flags().StringVar(&flagCreatedAt, "created-at", "", "Timestamp of site creation. Example: '2018-02-27T04:49:26.257525Z'.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
