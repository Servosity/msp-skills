{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Servosity/sherweb-mcp",
  "title": "Sherweb MCP",
  "description": "Every Sherweb Partner API capability, plus a local SQLite store, offline analytics, and",
  "version": "0.1.3",
  "icons": [
    {
      "src": "https://raw.githubusercontent.com/servosity/msp-skills/main/docs/assets/social/sherweb/sherweb-512x512.png",
      "mimeType": "image/png",
      "sizes": [
        "512x512"
      ]
    }
  ],
  "repository": {
    "url": "https://github.com/servosity/msp-skills",
    "source": "github",
    "subfolder": "skills/sherweb"
  },
  "packages": [
    {
      "registryType": "mcpb",
      "identifier": "https://github.com/servosity/msp-skills/releases/download/sherweb-v0.1.3/sherweb-mcp.mcpb",
      "version": "0.1.3",
      "fileSha256": "PLACEHOLDER_SHA256_FILLED_BY_CI",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "SHERWEB_BASE_URL",
          "description": "Base URL for the Sherweb API. Leave the prefilled default unless your account uses a different regional or self-hosted host.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://api.sherweb.com"
        },
        {
          "name": "SHERWEB_CLIENT_ID",
          "description": "Set the SHERWEB_CLIENT_ID credential for the Sherweb MCP server.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "SHERWEB_CLIENT_SECRET",
          "description": "Set the SHERWEB_CLIENT_SECRET credential for the Sherweb MCP server.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "SHERWEB_OAUTH_SCOPE",
          "description": "Set the SHERWEB_OAUTH_SCOPE credential for the Sherweb MCP server.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "SHERWEB_SUBSCRIPTION_KEY",
          "description": "Set the SHERWEB_SUBSCRIPTION_KEY credential for the Sherweb MCP server.",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "SHERWEB_TOKEN_URL",
          "description": "Token endpoint the client-credentials exchange posts to. Leave the prefilled default unless your tenant uses a different token endpoint.",
          "isRequired": false,
          "isSecret": false,
          "default": "https://api.sherweb.com/auth/oidc/connect/token"
        },
        {
          "name": "SHERWEB_NO_CONFIG_WRITE",
          "description": "Set to 1 to keep credentials off disk. The token cache becomes a no-op, so the connector mints a fresh token each run instead of writing one to config.toml. Leave blank for the default cached behaviour.",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
