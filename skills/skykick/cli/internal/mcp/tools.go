// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"skykick-pp-cli/internal/cli"
	"skykick-pp-cli/internal/client"
	"skykick-pp-cli/internal/cliutil"
	"skykick-pp-cli/internal/config"
	"skykick-pp-cli/internal/mcp/cobratree"
	"skykick-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("alerts_complete",
			mcplib.WithDescription("Mark a specific alert as complete. Required: alertId."),
			mcplib.WithString("alertId", mcplib.Required(), mcplib.Description("Alert GUID to mark complete")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/Alerts/{alertId}", false, false, nil, []mcpParamBinding{{PublicName: "alertId", WireName: "alertId", Location: "path"}}, []string{"alertId"}),
	)
	s.AddTool(
		mcplib.NewTool("alerts_list",
			mcplib.WithDescription("List alerts for a backup service or email migration order (max 500; the API does not support skip paging). Required: serviceId. Optional: top (default: 25)."),
			mcplib.WithString("serviceId", mcplib.Required(), mcplib.Description("Backup Service GUID or Email Migration Order GUID")),
			mcplib.WithNumber("top", mcplib.Description("Maximum alerts to return (default 25, max 500)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Alerts/{serviceId}", true, false, nil, []mcpParamBinding{{PublicName: "serviceId", WireName: "serviceId", Location: "path"}, {PublicName: "top", WireName: "$top", Location: "query"}}, []string{"serviceId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_autodiscover",
			mcplib.WithDescription("Auto-discover state (enabled/disabled) for Exchange and SharePoint. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/autodiscover", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_by-partner",
			mcplib.WithDescription("List backup subscription orders for a specific partner. Required: partnerId. Returns array of Subscription."),
			mcplib.WithString("partnerId", mcplib.Required(), mcplib.Description("Partner GUID to list subscriptions for")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{partnerId}", true, false, nil, []mcpParamBinding{{PublicName: "partnerId", WireName: "partnerId", Location: "path"}}, []string{"partnerId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_datacenters",
			mcplib.WithDescription("List the Azure data centers available for backup storage. Returns array of Datacenter."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/datacenters", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("backup_discover-mailboxes",
			mcplib.WithDescription("Trigger Exchange mailbox discovery (async; poll the returned operation with watch-operation). Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/Backup/{subscriptionId}/discovermailboxes", false, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_discover-sites",
			mcplib.WithDescription("Trigger SharePoint site discovery (async; poll the returned operation with watch-operation). Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/Backup/{subscriptionId}/discoversites", false, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_jobs",
			mcplib.WithDescription("Active backup jobs for the subscription (known upstream defect: may return Unknown error, community-reported since 2024). Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/jobs", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_last-snapshot-stats",
			mcplib.WithDescription("Last snapshot statistics for all mailboxes in the subscription. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/lastsnapshotstats", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_list",
			mcplib.WithDescription("List all placed backup subscription orders across your customers. Returns array of Subscription."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("backup_mailbox",
			mcplib.WithDescription("Details of a specific Exchange mailbox in the subscription. Required: subscriptionId, mailboxId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithString("mailboxId", mcplib.Required(), mcplib.Description("Mailbox identifier")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/mailboxes/{mailboxId}", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}, {PublicName: "mailboxId", WireName: "mailboxId", Location: "path"}}, []string{"subscriptionId", "mailboxId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_mailboxes",
			mcplib.WithDescription("Exchange mailboxes and their backup enabled/disabled status (IndividualMailboxes array). Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/mailboxes", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_retention-period",
			mcplib.WithDescription("Data retention periods for Exchange and SharePoint (response field ExchangeRentionPeriodInDays is the upstream spelling). Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/retentionperiod", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_sites",
			mcplib.WithDescription("SharePoint site URLs and their backup enabled/disabled status. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/sites", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_sku",
			mcplib.WithDescription("SKU and promotional details for a backup subscription. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/sku", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_storage-settings",
			mcplib.WithDescription("Storage settings for a backup subscription. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/storagesettings", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("backup_subscription-settings",
			mcplib.WithDescription("Subscription settings: Exchange/SharePoint backup state, enabled counts, customer info. Required: subscriptionId."),
			mcplib.WithString("subscriptionId", mcplib.Required(), mcplib.Description("Backup subscription (service) GUID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/Backup/{subscriptionId}/subscriptionsettings", true, false, nil, []mcpParamBinding{{PublicName: "subscriptionId", WireName: "subscriptionId", Location: "path"}}, []string{"subscriptionId"}),
	)
	s.AddTool(
		mcplib.NewTool("identity_whoami",
			mcplib.WithDescription("Show the identity and context of the authenticated API user."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/whoami", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("operations_status",
			mcplib.WithDescription("Poll the status of an async operation (e.g. a discovery run). Required: operationId."),
			mcplib.WithString("operationId", mcplib.Required(), mcplib.Description("Operation GUID returned by an async action")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/operation/{operationId}", true, false, nil, []mcpParamBinding{{PublicName: "operationId", WireName: "operationId", Location: "path"}}, []string{"operationId"}),
	)
	s.AddTool(
		mcplib.NewTool("operations_workqueue",
			mcplib.WithDescription("Retrieve the work queue for the authenticated account."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/workqueue", true, false, nil, []mcpParamBinding{}, []string{}),
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SKYKICK_CLIENT_ID=<your-key>" +
					"\n      Run 'skykick-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may have expired. Re-run 'skykick-cli auth login' to re-authenticate" +
					"\n      Set it with: export SKYKICK_CLIENT_ID=<your-key>" +
					"\n      Run 'skykick-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your token may lack required scopes. Re-run 'skykick-cli auth login' to re-authorize" +
					"\n      Set it with: export SKYKICK_CLIENT_ID=<your-key>" +
					"\n      Run 'skykick-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "skykick-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "skykick-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	return toolResultJSON(results)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	data, err := json.MarshalIndent(v, "", "  ")
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "skykick",
		"description": "Fleet-wide M365 backup assurance for SkyKick Cloud Backup - posture, stale snapshots",
		"archetype":   "generic",
		"tool_count":  20,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion skykick-cli binary.",
		"auth": map[string]any{
			"type": "oauth2",
			"env_vars": []map[string]any{
				{
					"name":        "SKYKICK_CLIENT_ID",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
				{
					"name":        "SKYKICK_CLIENT_SECRET",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "alerts",
				"description": "Alerts for backup services and email migration orders",
				"endpoints":   []string{"complete", "list"},
				"searchable":  true,
			},
			{
				"name":        "backup",
				"description": "Cloud Backup subscriptions - M365 Exchange and SharePoint protection per customer tenant",
				"endpoints":   []string{"autodiscover", "by-partner", "datacenters", "discover-mailboxes", "discover-sites", "jobs", "last-snapshot-stats", "list", "mailbox", "mailboxes", "retention-period", "sites", "sku", "storage-settings", "subscription-settings"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "identity",
				"description": "Authenticated caller identity",
				"endpoints":   []string{"whoami"},
			},
			{
				"name":        "operations",
				"description": "Async operation tracking and work queue",
				"endpoints":   []string{"status", "workqueue"},
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "Fleet sync", "command": "fleet-sync", "description": "One command pulls every subscription plus per-tenant settings, retention, autodiscover, snapshot stats, mailboxes, sites", "rationale": "The API is strictly per-GUID", "via": "mcp-command-mirror"},
			{"name": "Fleet health posture", "command": "fleet-health", "description": "One cross-tenant protection posture table - every subscription's Exchange/SharePoint enablement, retention, autodiscover", "rationale": "Only a local join across five per-GUID endpoints can answer 'is every customer protected?", "via": "mcp-command-mirror"},
			{"name": "Stale snapshots", "command": "stale-snapshots", "description": "Every mailbox not snapshotted within N hours, fleet-wide.", "rationale": "lastsnapshotstats is per-GUID with no time filter", "via": "mcp-command-mirror"},
			{"name": "Coverage gaps", "command": "coverage-gaps", "description": "Discovered-but-unprotected mailboxes and SharePoint sites per tenant.", "rationale": "The API returns discovery and enablement on separate endpoints and never diffs them", "via": "mcp-command-mirror"},
			{"name": "Fleet alert sweep", "command": "alert-sweep", "description": "One ranked list of open alerts across the whole fleet, with optional bulk mark-complete.", "rationale": "/Alerts/{id} is strictly per-id with no fleet list endpoint", "via": "mcp-command-mirror"},
			{"name": "Retention compliance audit", "command": "retention-audit", "description": "Grades each tenant's retention period against a compliance floor you set.", "rationale": "The API returns a raw retention integer per GUID with no policy concept", "via": "mcp-command-mirror"},
			{"name": "Autodiscover audit", "command": "autodiscover-audit", "description": "Fleet table of autodiscover on/off state per tenant.", "rationale": "Autodiscover state is exposed only per-GUID", "via": "mcp-command-mirror"},
			{"name": "Watch async operation", "command": "watch-operation", "description": "Polls an async operation to a terminal state with backoff in one command.", "rationale": "The API forces a manual GET-poll loop after each discovery POST; this wraps the loop.", "via": "mcp-command-mirror"},
			{"name": "Protection drift", "command": "drift", "description": "Diffs two sync snapshots and reports protection-state changes since last sync.", "rationale": "The API has no history; drift detection requires the timestamped offline store.", "via": "mcp-command-mirror"},
			{"name": "Partner roll-up", "command": "partner-rollup", "description": "Protection posture aggregated by partner for distributor oversight.", "rationale": "GET /Backup/{partnerId} is per-partner only; cross-partner roll-up needs the local join.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "Fleet sync", "insight": "The API is strictly per-GUID; assembling a fleet-wide store requires fanning out across every subscription with rate-limit-aware batching - no wrapper or portal does this."},
			{"topic": "Fleet health posture", "insight": "Only a local join across five per-GUID endpoints can answer 'is every customer protected?' in one offline command - the API never returns it cross-tenant."},
			{"topic": "Stale snapshots", "insight": "lastsnapshotstats is per-GUID with no time filter; finding silently-stale mailboxes across all tenants requires the joined local store."},
			{"topic": "Coverage gaps", "insight": "The API returns discovery and enablement on separate endpoints and never diffs them; the reconciliation is a pure local join."},
			{"topic": "Fleet alert sweep", "insight": "/Alerts/{id} is strictly per-id with no fleet list endpoint; the sweep fans the call across stored ids and collates locally."},
			{"topic": "Retention compliance audit", "insight": "The API returns a raw retention integer per GUID with no policy concept; grading fleet-wide against --floor-days only works against the local store."},
			{"topic": "Autodiscover audit", "insight": "Autodiscover state is exposed only per-GUID; aggregating it to catch tenants where new mailboxes silently won't enroll requires the store."},
			{"topic": "Watch async operation", "insight": "The API forces a manual GET-poll loop after each discovery POST; this wraps the loop."},
			{"topic": "Protection drift", "insight": "The API has no history; drift detection requires the timestamped offline store."},
			{"topic": "Partner roll-up", "insight": "GET /Backup/{partnerId} is per-partner only; cross-partner roll-up needs the local join."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
