// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"superops-pp-cli/internal/cli"
	"superops-pp-cli/internal/client"
	"superops-pp-cli/internal/cliutil"
	"superops-pp-cli/internal/config"
	"superops-pp-cli/internal/mcp/cobratree"
	"superops-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("alerts_list",
			mcplib.WithDescription("List alerts. Optional: first (default: 50). Returns array of Alert."),
			mcplib.WithNumber("first", mcplib.Description("Max alerts per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("assets_get",
			mcplib.WithDescription("Get an asset by ID. Required: id. Returns the Asset."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Asset ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("assets_list",
			mcplib.WithDescription("List assets. Optional: first (default: 50). Returns array of Asset."),
			mcplib.WithNumber("first", mcplib.Description("Max assets per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("clients_get",
			mcplib.WithDescription("Get a client by account ID. Required: id. Returns the Client."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Client account ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("clients_list",
			mcplib.WithDescription("List clients. Optional: first (default: 50). Returns array of Client."),
			mcplib.WithNumber("first", mcplib.Description("Max clients per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("contracts_list",
			mcplib.WithDescription("List client contracts. Optional: first (default: 50). Returns array of ClientContract."),
			mcplib.WithNumber("first", mcplib.Description("Max contracts per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("invoices_get",
			mcplib.WithDescription("Get an invoice by ID. Required: id. Returns the Invoice."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Invoice ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("invoices_list",
			mcplib.WithDescription("List invoices. Optional: first (default: 50). Returns array of Invoice."),
			mcplib.WithNumber("first", mcplib.Description("Max invoices per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("it-docs_list",
			mcplib.WithDescription("List IT documentation. Optional: first (default: 50). Returns array of ItDocumentation."),
			mcplib.WithNumber("first", mcplib.Description("Max IT docs per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("kb_list",
			mcplib.WithDescription("List knowledge base items. Optional: first (default: 50). Returns array of KbItem."),
			mcplib.WithNumber("first", mcplib.Description("Max KB items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("service-items_list",
			mcplib.WithDescription("List service items. Optional: first (default: 50). Returns array of ServiceItem."),
			mcplib.WithNumber("first", mcplib.Description("Max service items per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("sites_list",
			mcplib.WithDescription("List client sites. Optional: first (default: 50). Returns array of ClientSite."),
			mcplib.WithNumber("first", mcplib.Description("Max sites per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tasks_get",
			mcplib.WithDescription("Get a task by ID. Required: id. Returns the Task."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Task ID")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("tasks_list",
			mcplib.WithDescription("List tasks. Optional: first (default: 50). Returns array of Task."),
			mcplib.WithNumber("first", mcplib.Description("Max tasks per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("technicians_list",
			mcplib.WithDescription("List technicians. Optional: first (default: 50). Returns array of Technician."),
			mcplib.WithNumber("first", mcplib.Description("Max technicians per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("tickets_get",
			mcplib.WithDescription("Get a ticket by display ID. Required: id. Returns the Ticket."),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("Ticket ID (ticketId)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "id", WireName: "id", Location: "query"}}, []string{"id"}),
	)
	s.AddTool(
		mcplib.NewTool("tickets_list",
			mcplib.WithDescription("List tickets. Optional: first (default: 50). Returns array of Ticket."),
			mcplib.WithNumber("first", mcplib.Description("Max tickets per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("users_list",
			mcplib.WithDescription("List client users. Optional: first (default: 50). Returns array of ClientUser."),
			mcplib.WithNumber("first", mcplib.Description("Max users per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("worklogs_list",
			mcplib.WithDescription("List worklog entries. Optional: first (default: 50). Returns array of WorklogEntry."),
			mcplib.WithNumber("first", mcplib.Description("Max worklog entries per page")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/graphql", true, false, nil, []mcpParamBinding{{PublicName: "first", WireName: "first", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
		case "PATCH":
			if len(headers) > 0 {
				data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				break
			}
			data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export SUPEROPS_API_TOKEN=<your-key>" +
					"\n      Run 'superops-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your token." +
					"\n      Set it with: export SUPEROPS_API_TOKEN=<your-key>" +
					"\n      Run 'superops-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export SUPEROPS_API_TOKEN=<your-key>" +
					"\n      Run 'superops-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		if binaryResponse {
			out, _ := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      base64.StdEncoding.EncodeToString(data),
				"byte_count":       len(data),
			})
			return mcplib.NewToolResultText(string(out)), nil
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "superops-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 60*time.Second, 0)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "superops-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "superops",
		"description": "Every SuperOps PSA+RMM entity in your terminal",
		"archetype":   "project-management",
		"tool_count":  19,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion superops-cli binary.",
		"auth": map[string]any{
			"type": "bearer_token",
			"env_vars": []map[string]any{
				{
					"name":        "SUPEROPS_API_TOKEN",
					"kind":        "per_call",
					"required":    true,
					"sensitive":   true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name":        "alerts",
				"description": "Manage RMM alerts",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "assets",
				"description": "Manage SuperOps assets and endpoints",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "clients",
				"description": "Manage SuperOps clients (accounts)",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "contracts",
				"description": "Manage client contracts",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "invoices",
				"description": "Manage invoices",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "it-docs",
				"description": "Manage IT documentation",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "kb",
				"description": "Manage knowledge base articles",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "service-items",
				"description": "Manage service catalog items",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "sites",
				"description": "Manage client sites",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "tasks",
				"description": "Manage tasks",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "technicians",
				"description": "Manage technicians",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "tickets",
				"description": "Manage SuperOps tickets",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "users",
				"description": "Manage client users (contacts)",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
			{
				"name":        "worklogs",
				"description": "Manage worklog time entries",
				"endpoints":   []string{"list"},
				"syncable":    true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the first parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "SLA breach board", "command": "sla-watch", "description": "See which open tickets are breaching or about to breach SLA, grouped by technician or client.", "rationale": "Requires a local join of tickets to SLA targets, status", "via": "mcp-command-mirror"},
			{"name": "Unbilled worklog", "command": "unbilled", "description": "Find logged worklog time that never landed on an invoice, totaled in dollars per client.", "rationale": "Reconciling worklog against invoice line items is a cross-domain anti-join SuperOps exposes no endpoint for.", "via": "mcp-command-mirror"},
			{"name": "Patch + ticket risk", "command": "at-risk-assets", "description": "List assets missing a critical patch that also carry an open ticket.", "rationale": "Patch posture and tickets are separate domains with no joined call; the intersection only exists in the local store.", "via": "mcp-command-mirror"},
			{"name": "Alert to ticket coverage", "command": "alert-coverage", "description": "Split active alerts into those that became tickets and orphans that did not.", "rationale": "The API lists alerts and tickets separately; orphan detection is a left-join over SuperOps' shared-DB chain.", "via": "mcp-command-mirror"},
			{"name": "Client 360", "command": "client-360", "description": "One offline bundle of a client plus its sites, users, contracts, open tickets, assets, and open invoices.", "rationale": "Collapses six live SuperOps page loads into a single fan-out join over the local store.", "via": "mcp-command-mirror"},
			{"name": "Stale tickets", "command": "stale-tickets", "description": "Open tickets with no conversation, note, or worklog activity in N days.", "rationale": "Requires scanning child-activity timestamps the API will not aggregate, computed locally.", "via": "mcp-command-mirror"},
			{"name": "Agent context pack", "command": "context-ticket", "description": "Assemble a ticket plus its conversation, notes, asset, client, contract, and SLA into one agent-shaped JSON blob.", "rationale": "Bundles six entities the API can only return across separate calls into one --select-friendly payload.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "SLA breach board", "insight": "Requires a local join of tickets to SLA targets, status, and technician with elapsed-vs-target math that no single SuperOps GraphQL call returns."},
			{"topic": "Unbilled worklog", "insight": "Reconciling worklog against invoice line items is a cross-domain anti-join SuperOps exposes no endpoint for."},
			{"topic": "Patch + ticket risk", "insight": "Patch posture and tickets are separate domains with no joined call; the intersection only exists in the local store."},
			{"topic": "Alert to ticket coverage", "insight": "The API lists alerts and tickets separately; orphan detection is a left-join over SuperOps' shared-DB chain."},
			{"topic": "Client 360", "insight": "Collapses six live SuperOps page loads into a single fan-out join over the local store."},
			{"topic": "Stale tickets", "insight": "Requires scanning child-activity timestamps the API will not aggregate, computed locally."},
			{"topic": "Agent context pack", "insight": "Bundles six entities the API can only return across separate calls into one --select-friendly payload."},
			{"topic": "Finding stale work", "insight": "Use the stale command or sql query to find items not updated recently. More reliable than scanning list results manually."},
			{"topic": "Load analysis", "insight": "When analyzing team workload, filter by assignee and status. Raw counts without status filtering are misleading."},
			{"topic": "Bulk operations", "insight": "For bulk status changes, prefer update endpoints over delete+create. Most PM APIs track history on updates."},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
