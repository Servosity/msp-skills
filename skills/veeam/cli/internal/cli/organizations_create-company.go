// Copyright 2026 Damien Stevens and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newOrganizationsCreateCompanyCmd(flags *rootFlags) *cobra.Command {
	var bodyCompanyServicesHostedServicesIsVbPublicCloudManagementEnabled bool
	var bodyCompanyServicesRemoteServicesIsBackupResourcesEnabled bool
	var bodyCompanyServicesRemoteServicesIsVbPublicCloudManagementEnabled bool
	var bodyIsAlarmDetectEnabled bool
	var bodyIsRestAccessEnabled bool
	var bodyOrganizationInputAlias string
	var bodyOrganizationInputCity string
	var bodyOrganizationInputCompanyId string
	var bodyOrganizationInputCountry int
	var bodyOrganizationInputCountryName string
	var bodyOrganizationInputEmail string
	var bodyOrganizationInputLocationAdmin0Code string
	var bodyOrganizationInputLocationAdmin1Code string
	var bodyOrganizationInputLocationAdmin2Code string
	var bodyOrganizationInputName string
	var bodyOrganizationInputNotes string
	var bodyOrganizationInputPhone string
	var bodyOrganizationInputRegionName string
	var bodyOrganizationInputState int
	var bodyOrganizationInputStreet string
	var bodyOrganizationInputTaxId string
	var bodyOrganizationInputVeeamTenantId string
	var bodyOrganizationInputWebsite string
	var bodyOrganizationInputZipCode string
	var bodyOwnerCredentialsPassword string
	var bodyOwnerCredentialsUserName string
	var bodyResellerUid string
	var bodySubscriptionPlanUid string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-company",
		Short:       "Creates a new company managed in Veeam Service Provider Console.",
		Example:     "  veeam-cli organizations create-company",
		Annotations: map[string]string{"pp:endpoint": "organizations.create-company", "pp:method": "POST", "pp:path": "/organizations/companies"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("organization-input-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "organization-input-name")
				}
				if !cmd.Flags().Changed("owner-credentials-user-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "owner-credentials-user-name")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/organizations/companies"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedCompanyServices := map[string]any{}
					{
						nestedCompanyServicesHostedServices := map[string]any{}
						if cmd.Flags().Changed("company-services-hosted-services-is-vb-public-cloud-management-enabled") {
							nestedCompanyServicesHostedServices["isVbPublicCloudManagementEnabled"] = bodyCompanyServicesHostedServicesIsVbPublicCloudManagementEnabled
						}
						if len(nestedCompanyServicesHostedServices) > 0 {
							nestedCompanyServices["hostedServices"] = nestedCompanyServicesHostedServices
						}
					}
					{
						nestedCompanyServicesRemoteServices := map[string]any{}
						if cmd.Flags().Changed("company-services-remote-services-is-backup-resources-enabled") {
							nestedCompanyServicesRemoteServices["isBackupResourcesEnabled"] = bodyCompanyServicesRemoteServicesIsBackupResourcesEnabled
						}
						if cmd.Flags().Changed("company-services-remote-services-is-vb-public-cloud-management-enabled") {
							nestedCompanyServicesRemoteServices["isVbPublicCloudManagementEnabled"] = bodyCompanyServicesRemoteServicesIsVbPublicCloudManagementEnabled
						}
						if len(nestedCompanyServicesRemoteServices) > 0 {
							nestedCompanyServices["remoteServices"] = nestedCompanyServicesRemoteServices
						}
					}
					if len(nestedCompanyServices) > 0 {
						body["companyServices"] = nestedCompanyServices
					}
				}
				if cmd.Flags().Changed("is-alarm-detect-enabled") {
					body["isAlarmDetectEnabled"] = bodyIsAlarmDetectEnabled
				}
				if cmd.Flags().Changed("is-rest-access-enabled") {
					body["isRestAccessEnabled"] = bodyIsRestAccessEnabled
				}
				{
					nestedOrganizationInput := map[string]any{}
					if bodyOrganizationInputAlias != "" {
						nestedOrganizationInput["alias"] = bodyOrganizationInputAlias
					}
					if bodyOrganizationInputCity != "" {
						nestedOrganizationInput["city"] = bodyOrganizationInputCity
					}
					if bodyOrganizationInputCompanyId != "" {
						nestedOrganizationInput["companyId"] = bodyOrganizationInputCompanyId
					}
					if bodyOrganizationInputCountry != 0 {
						nestedOrganizationInput["country"] = bodyOrganizationInputCountry
					}
					if bodyOrganizationInputCountryName != "" {
						nestedOrganizationInput["countryName"] = bodyOrganizationInputCountryName
					}
					if bodyOrganizationInputEmail != "" {
						nestedOrganizationInput["email"] = bodyOrganizationInputEmail
					}
					if bodyOrganizationInputLocationAdmin0Code != "" {
						nestedOrganizationInput["locationAdmin0Code"] = bodyOrganizationInputLocationAdmin0Code
					}
					if bodyOrganizationInputLocationAdmin1Code != "" {
						nestedOrganizationInput["locationAdmin1Code"] = bodyOrganizationInputLocationAdmin1Code
					}
					if bodyOrganizationInputLocationAdmin2Code != "" {
						nestedOrganizationInput["locationAdmin2Code"] = bodyOrganizationInputLocationAdmin2Code
					}
					if bodyOrganizationInputName != "" {
						nestedOrganizationInput["name"] = bodyOrganizationInputName
					}
					if bodyOrganizationInputNotes != "" {
						nestedOrganizationInput["notes"] = bodyOrganizationInputNotes
					}
					if bodyOrganizationInputPhone != "" {
						nestedOrganizationInput["phone"] = bodyOrganizationInputPhone
					}
					if bodyOrganizationInputRegionName != "" {
						nestedOrganizationInput["regionName"] = bodyOrganizationInputRegionName
					}
					if bodyOrganizationInputState != 0 {
						nestedOrganizationInput["state"] = bodyOrganizationInputState
					}
					if bodyOrganizationInputStreet != "" {
						nestedOrganizationInput["street"] = bodyOrganizationInputStreet
					}
					if bodyOrganizationInputTaxId != "" {
						nestedOrganizationInput["taxId"] = bodyOrganizationInputTaxId
					}
					if bodyOrganizationInputVeeamTenantId != "" {
						nestedOrganizationInput["veeamTenantId"] = bodyOrganizationInputVeeamTenantId
					}
					if bodyOrganizationInputWebsite != "" {
						nestedOrganizationInput["website"] = bodyOrganizationInputWebsite
					}
					if bodyOrganizationInputZipCode != "" {
						nestedOrganizationInput["zipCode"] = bodyOrganizationInputZipCode
					}
					if len(nestedOrganizationInput) > 0 {
						body["organizationInput"] = nestedOrganizationInput
					}
				}
				{
					nestedOwnerCredentials := map[string]any{}
					if bodyOwnerCredentialsPassword != "" {
						nestedOwnerCredentials["password"] = bodyOwnerCredentialsPassword
					}
					if bodyOwnerCredentialsUserName != "" {
						nestedOwnerCredentials["userName"] = bodyOwnerCredentialsUserName
					}
					if len(nestedOwnerCredentials) > 0 {
						body["ownerCredentials"] = nestedOwnerCredentials
					}
				}
				if bodyResellerUid != "" {
					body["resellerUid"] = bodyResellerUid
				}
				if bodySubscriptionPlanUid != "" {
					body["subscriptionPlanUid"] = bodySubscriptionPlanUid
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "organizations", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "organizations", data, "errors")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "organizations",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "organizations", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyCompanyServicesHostedServicesIsVbPublicCloudManagementEnabled, "company-services-hosted-services-is-vb-public-cloud-management-enabled", false, "Indicates whether a company is allowed to manage public cloud appliances.")
	cmd.Flags().BoolVar(&bodyCompanyServicesRemoteServicesIsBackupResourcesEnabled, "company-services-remote-services-is-backup-resources-enabled", false, "Indicates whether cloud backup resources are allocated to a company.")
	cmd.Flags().BoolVar(&bodyCompanyServicesRemoteServicesIsVbPublicCloudManagementEnabled, "company-services-remote-services-is-vb-public-cloud-management-enabled", false, "Indicates whether a company is allowed to manage public cloud appliances.'")
	cmd.Flags().BoolVar(&bodyIsAlarmDetectEnabled, "is-alarm-detect-enabled", false, "Defines whether a company must receive notifications about alarms that were triggered for this company.")
	cmd.Flags().BoolVar(&bodyIsRestAccessEnabled, "is-rest-access-enabled", false, "Defines whether access to REST API is enabled for a reseller.")
	cmd.Flags().StringVar(&bodyOrganizationInputAlias, "organization-input-alias", "", "Alias of an organization.")
	cmd.Flags().StringVar(&bodyOrganizationInputCity, "organization-input-city", "", "City where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputCompanyId, "organization-input-company-id", "", "ID of an organization used for 3rd party applications.")
	cmd.Flags().IntVar(&bodyOrganizationInputCountry, "organization-input-country", 0, "System ID assigned to an organization country of residence.")
	cmd.Flags().StringVar(&bodyOrganizationInputCountryName, "organization-input-country-name", "", "Country name.")
	cmd.Flags().StringVar(&bodyOrganizationInputEmail, "organization-input-email", "", "Contact email address.")
	cmd.Flags().StringVar(&bodyOrganizationInputLocationAdmin0Code, "organization-input-location-admin0-code", "", "Code of a country where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputLocationAdmin1Code, "organization-input-location-admin1-code", "", "Code of a state, region or area where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputLocationAdmin2Code, "organization-input-location-admin2-code", "", "Code of a district or municipality where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputName, "organization-input-name", "", "Name of an organization.")
	cmd.Flags().StringVar(&bodyOrganizationInputNotes, "organization-input-notes", "", "Additional information about an organization.")
	cmd.Flags().StringVar(&bodyOrganizationInputPhone, "organization-input-phone", "", "Telephone number of a primary contact of an organization.")
	cmd.Flags().StringVar(&bodyOrganizationInputRegionName, "organization-input-region-name", "", "Region name.")
	cmd.Flags().IntVar(&bodyOrganizationInputState, "organization-input-state", 0, "System ID assigned to a USA state where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputStreet, "organization-input-street", "", "Street where an organization is located.")
	cmd.Flags().StringVar(&bodyOrganizationInputTaxId, "organization-input-tax-id", "", "Organization Tax ID.")
	cmd.Flags().StringVar(&bodyOrganizationInputVeeamTenantId, "organization-input-veeam-tenant-id", "", "ID of an organization used in Veeam records.")
	cmd.Flags().StringVar(&bodyOrganizationInputWebsite, "organization-input-website", "", "Organization website.")
	cmd.Flags().StringVar(&bodyOrganizationInputZipCode, "organization-input-zip-code", "", "Postal code.")
	cmd.Flags().StringVar(&bodyOwnerCredentialsPassword, "owner-credentials-password", "", "Password.")
	cmd.Flags().StringVar(&bodyOwnerCredentialsUserName, "owner-credentials-user-name", "", "User name.")
	cmd.Flags().StringVar(&bodyResellerUid, "reseller-uid", "", "UID assigned to a reseller that manages the company.")
	cmd.Flags().StringVar(&bodySubscriptionPlanUid, "subscription-plan-uid", "", "UID assigned to a company subscription plan.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
